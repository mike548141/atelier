# Session — the four 2026-07-29 cold passes, taken (2026-08-02 2200 UTC)

**Model:** Fable · **Worktree:** `cold-passes-0729` · **Spawn:** Mike started
this session and pointed it at the review queue ("Please do any review work");
the author session (2026-07-29 1418, Opus 5) neither started nor instructed
it — rule 4's criterion, met and recorded in every brief and verdict.

## What ran

All four queued rule-4 pointers, claimed on `main` first (`d34a424`), each as
a full brief → cold review → verdict cycle, refs-only sourcing. The shared
intent record was opened once, after all four verdicts were durably
committed; reconciliation changed no finding (addenda in each verdict file).

| Delta | Verdict | Majors |
|---|---|---|
| publication-surface (`a9ab2cf`) | PASS-WITH-FINDINGS 1M/2m | PS1: canon's standardise step still instructs the committed allowlist |
| `publishscan` (`8bdcfaa`) | PASS-WITH-FINDINGS 1M/1m/2n | PB1: patterns depth-blind (`fnmatch` `*` spans `/`) |
| deferral (`3acf7d2`) | PASS-WITH-FINDINGS 0M/1m/3n | — |
| recurrence ladder (`4015e06`) | PASS-WITH-FINDINGS 0M/3m/1n | — |

**Nothing was applied** — every finding on these self-authored doctrine
deltas awaits Mike's ruling (rule 3). The two 0-MAJOR cycles close at their
ruling application; the two MAJOR cycles stay open past theirs. One records
fix only: the queue header's "Three" over four pointers (RL4), corrected at
close as a live-document count fix, with the intent record's matching slip
left standing as history.

## Process incidents, said rather than fudged

- **A records-sweeping grep fed deferred material to the reviewer.** Mid
  pass 1, a tree-wide `settings.json` sweep returned the author's
  `SESSIONS.md` index entry — a condensed account of all four deltas —
  pre-findings. The SL2 channel class, second live instance, disclosed in
  every verdict; sweeps in later passes excluded records paths. The
  deferral doctrine's own residual statement (only a context partition is
  structural) is exactly what this demonstrates.
- **Pass 4's brief followed two probes** rather than preceding all of them;
  disclosed in that brief.

## Grounding

Floor green on every commit; `python3 -m unittest discover -s tools` 820 OK;
`node --test instruments/*.test.js` 207 pass; publishscan red/green
re-driven live; reviewscan corpus 79 briefs green; live-proven claims from
the landing commits re-run rather than read (14-test count, 76-brief sweep,
red-then-green legs).

## Owed / next

Mike's rulings on PS1–PS3, PB1–PB4, DF1–DF4, RL1–RL3 (walk-through offered
in-session). Applications follow the rulings: MAJOR-cycle applications
(publish-surface, publishscan) queue their own rule-4 pointers; the other
two are terminal. The five older riding `⏳` items (EVIDENCE §13, RECORD.md
pushed-floor, apex accountability, Zeroth law, PRINCIPLES §1) remain queued —
not taken this session.
