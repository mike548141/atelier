# 2026-07-22 · 1149 UTC · queue-run doctrine cold pass (Fable, wt: queue-run-cold-pass)

**Ask:** Mike opened the session with "Please do any review work" — the rule-4
worked example. The queue held one `⏳`: the orchestrated-queue-run doctrine +
skill (`343def8` + `8111e9f`, authored by the 2026-07-22 1018 run).

**Eligibility + shape.** This session authored none of the delta and was
neither started nor instructed by its author — rule-4 eligible. One exposure,
named rather than denied: the session-start onramp read the author session's
closing `SESSIONS.md` index entry (an evaluative account). So the pass ran
**two-hop** (the 2026-07-21 2208 precedent): claim on `main` first (`5c29a44`),
work in worktree `queue-run-cold-pass`, taker-written brief refs-only above
the divider with the taker's five seeds deferred below it (`eaf080b`), and a
**fresh-context subagent** as the cold reviewer — contamination never crossed.

**Discipline held.** The reviewer named its attack surface (ten assumptions)
before opening sibling doctrine; findings were durably committed (`3797c5a`)
before the deferred section, the intent record, or any session record opened;
the reconcile step landed as its own commit (`bc1f102`). Every mechanical
claim re-driven, not read: tool suite 323 OK, instruments 132 OK, four
scanners clean at `--root . .`, `sizescan --check` exit 0 (ROADMAP advisory
only), plugin bundling parity confirmed (`source: "./"`, queue-run sits
identically to review-brief), the "~95 lines" claim measured (96), every `§`
ref and relative link resolved. Lens 4: `/security-review` discharged with
grounds (landed markdown — exclusion-barred class, weighed as nothing); the
manual design-altitude pass is the substance and produced two of the findings.

**Verdict: PASS-WITH-FINDINGS — 1 MAJOR · 6 MEDIUM · 1 LOW · 1 note**
([`verdict`](../reviews/2026-07-22-1149-queue-run-doctrine-cold.md)). The
pattern held up well — real practice, correctly homed, honestly bounded,
wired and bundled exactly as claimed. The findings concentrate in one seam:
**QR1 (MAJOR)** chain-spawn provenance is unspecified and it is the exact
fact rule 4 turns on (authorship vs started-or-instructed — a prospective
laundering path); QR2 worker-built authorship undefined; QR3 the worker's
authority envelope unstated; QR4 queue-item text reaches the workhorse tier
with no data-not-instructions discipline; QR5 the tier split's default
contradicts the parent for doctrine-text items; QR6 no mechanical pin on the
skill (the twice-shipped drift class); QR7 "loses nothing" overclaims; QR8
notes. Reconcile against the seeds + intent record: **nothing overturned**,
QR2/QR3/QR5 enriched by the record's own practice (which repeatedly enacted
the rules the text omits), and **QR9 (MEDIUM)** added — the doctrine's loop
is serial where its grounding run was wave-parallel.

**Owed:** 🎯 QR1–QR9 are Mike's rulings (self-authored doctrine, rule 3);
counsel per finding in the verdict, all wording-sized — reviewer's lean:
take all. The MAJOR keeps the cycle open past the application. Claim
released with this landing; worktree merged `--no-ff` and removed.

---

## Addendum — 2026-07-23: QR1–QR9 ruled and applied

Mike ruled all nine via a plain-language walk-through with likely impacts
(AskUserQuestion, two batches): accept-all as counselled, QR5 as the
escalate-doctrine-to-capable-tier option, QR9 as sanction-waves. Applied by
this session — the pass's taker, author of neither the doctrine nor the
verdict — in worktree `qr-rulings-apply` (claim `f6b5324` on `main` first).

Delta `b65209c`: the chain pin (QR1), worker authorship (QR2), the worker
envelope (QR3), item-text-is-not-instruction (QR4) and the waves paragraph
(QR9 + QR8 iii/iv) in CONCURRENCY § Orchestrated queue runs; doctrine-text
escalation + tier seat-names in ECONOMICS (QR5, QR8 i); skill mirrors +
durable grounding pointer + honest cap wording (QR7, QR8 ii); README wording
(QR7); `QueueRunSkillTest` — 7 tests, suite 323→330, stop roster
source-derived, bite-proven red on the pre-fix skill and on targeted
mutations (QR6). Five scanners exit 0. Decisions stamped in the verdict
(`dcab34c`). One process slip, self-caught: the bite-proof `git checkout`
also reverted the uncommitted skill/README edits — caught by the still-red
suite, edits re-applied, everything re-proven green before commit.

**The MAJOR keeps the cycle open: the application's own cold pass is queued
`⏳` refs-only; this session spawns nothing** (REVIEW.md — the applier does
not spawn the application review).
