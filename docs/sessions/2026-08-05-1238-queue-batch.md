# 2026-08-05 · 1238 UTC · Queue batch — eleven claims, three worker builds, one honest revert

**Mode.** Mike-spawned ("carry on with any work in queue that's not claimed"),
Fable orchestrating, three Opus workers on disjoint files in one shared
worktree (`queue-batch-0806`) — workers ran no git; the orchestrator verified
every claim independently and owned all commits and records. Three parallel
sessions were live throughout (the five claimed cold passes, the
guard-governance/leakscan build, and a P6 session that claimed, drafted and
landed the publication-posture ADR inside six minutes mid-claim-window); none
of their surfaces was touched, and `tools/floor.py` / `tools/floorfleet.py` /
`tools/secretscan.py` / `tools/leakscan.py` were avoided deliberately while
their passes stay open.

**Delivered, all verified by the orchestrator before commit:**

- **The 2026-08-04 sitting's five small doctrine edits**, one landing with
  one batch rule-4 pointer: the principal-named tier bar + stop clause at
  REVIEW.md rule 4 and the `⏳` legend; the withdrawn-pass general case in
  REVIEW.md's lifecycle; show-first bulk deletion in RECORD.md +
  AUTONOMY.md cross-link; describe-don't-quote in RECORD.md; ZL1 (the
  session-onramp apex block re-taught to the Zeroth form) plus the
  restatement-sweep surface list in PRINCIPLES.md §6 — the widened sweep ran
  and found no other stale restatement on the plugin surfaces.
- **licenscan E1+E2** (Opus worker; 37 → 52 module tests): an unrecognised
  LICENSE is read as a declared custom licence and the per-file header
  checks keep running — the item's own reproduction fixture now reports the
  vendored copyleft file it used to miss; sixteen unambiguous trove
  classifiers resolve; ambiguous families deliberately stay on the warn.
  Unblocks the three children `disabled` on exactly this gap.
- **stampscan D2** (Opus worker; 46 → 65 + 1 template test): all three
  ruled wiring preconditions built (code-context-aware parsing,
  narrow-to-nothing is drift, the ignore-file net), `source=` confined,
  ST7's cleaner fix taken at the parser, the convention's doctrine words in
  PROPAGATION.md, wired advisory in atelier's ci.yml only. The live tree
  that exited 2 on five config errors exits 0; the one live stamped pair
  still reads identical at 52 lines. ST3 (pin-aware child resolution) stays
  the registry bar, recorded as the D2-residue item.
- **pathscan D1 rescope** (Opus worker; 53 → 73 tests): fourth anchor +
  root files in scope, directory-index retry, date-placeholder exemption,
  docstring honesty (PS8's overclaim corrected), and the gate-scope
  residual burnt to zero (one true positive fixed, six reasoned markers —
  markers annotate, records stay verbatim). Registry promotion (PS5)
  deliberately deferred while the floor.py cold pass is in flight; D1 stays
  open on that one leftover.
- **The estate-root repo's token-spec surface** closed in that repo (the
  all-repos grant statement, FS1's last surface), committed and pushed
  there.

**The C5 incident — executed, falsified, reverted, same session.** The
composed ruling was executed on the machine-local term list and
probe-verified both directions. Execution then surfaced three facts the
ruling's premises did not model: forward-only held on the hook plane only —
a local full-tree leakscan run (the ci-plane floor reviewers re-run as a
brief obligation) went red with 86 findings across this repo's frozen
records while three Fable passes were in flight expecting it green; the
zero-ordinary-English measurement was falsified at HEAD (harvestscan's
docstring uses the word as plain English; the 63-occurrence corpus was
records-only and predates it); and the estate-root repo's own records carry
~364 self-references that would hard-block under E7's D1 ruling with no
in-repo hatch. The execution was reverted within roughly an hour, the floor
verified green full-tree, the DONE entry carries the dated correction, and
the re-ruling is queued in Track C with four costed options, none
pre-chosen. **Disclosure:** during that window a local ci-plane floor run in
any worktree on this machine would have shown the 86-finding leakscan red;
one of this session's own workers hit it and correctly classified it as not
its own. The in-flight passes may have seen the same transient.

**Honest notes.** The five-item doctrine batch queues ONE rule-4 pointer for
one landing — a reviewer takes the batch whole. Worker reports claimed a
2026-08-06 date once and the wrong licenscan invocation once; both corrected
at integration (`date -u` is the at-rest rule; root is positional). The
shared-worktree pattern (workers on disjoint files, no per-worker git)
worked, with one measured cost: workers' full-suite baselines drift as
siblings land — each worker isolated its own delta A/B before reporting,
which is the pattern to keep. A scratch probe directory could not be removed
(permission denial on the cleanup command); it is session-scratch under the
harness scratchpad and harmless.

**Decisions carried to Mike:** the C5 re-ruling (Track C, options recorded);
the D3/signscan flip, the Laws removal sitting, the glossary ratify, and the
fleet pin-bump rollout all remain his or unstarted — none was touched.
