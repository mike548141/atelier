- [ ] 🎯 **Does the new naming precedence reach doctrine's own prose?** —
      split out of `320/190` when Mike ruled it 2026-09-19 (*rule 2 wins,
      everywhere*). That ruling settled the surfaces a **filing** creates:
      branch name, PR title, commit subject, PR body. It did not settle the
      narrative in atelier's own doctrine.
      **The live state:** `PROPAGATION.md` names a child repo in § *Pointing
      up* § *The instance* and another in § *Report without harming the
      parent* § *The instance*. At least one of those two repos is private
      (verified at the forge 2026-09-20, name deliberately not written here).
      **Why it is not simply "apply the ruling":** the first naming was
      Mike's own explicit call — **PU-2, 2026-08-22**, reasoning that the
      veil was already defeated because the board item quotes him naming the
      repo, so the honest shape was to name it. Applying the new precedence
      to prose would partly overturn that. Overturning it may still be right:
      what defeated the veil was atelier's own records, which is the
      circularity the estate keeps re-discovering.
      🎯 **The ask, in plain terms:** should a public doctrine file name a
      private child repo at all — even in a worked example, even where an
      earlier ruling allowed it? Options: **(a)** extend the precedence to
      prose and rewrite both instances as *"a private child"*, keeping the
      narrative and losing the name · **(b)** keep PU-2's reasoning and
      leave prose out of scope, on the ground that a worked example is
      history and history is not retro-edited · **(c)** case by case at each
      naming, which is what produced the inconsistency.
      **Not claimable as work:** whichever way it goes, the edit is small.
      The decision is the whole item.
