# Brief — mid-tier standing-executor doctrine, rule-4 cold pass

**Queue ref (refs only):** the mid-tier standing-executor doctrine. Delta:
`docs/method/ECONOMICS.md` § *The orchestrated-run tier split*, the
standing-executor paragraph (landed 2026-08-03, `6b11b11`, merged
`958e59b`). Intent record: `ROADMAP-DONE.md` § *Third-seat executor trial*
(runs 1–4, 2026-07-23) + Mike's trial instruction of 2026-07-23
(`dadde1d`).

**Pass type:** doctrine cold pass (rule 4 — self-authored doctrine by
function: a rule governing which tier future sessions dispatch to).

**Provenance (rule 4):** reviewed by a Fable session Mike spawned onto the
queue; the author sessions neither started nor instructed this session.
Brief written by the taker, cold; intent records opened at reconcile only.

# Verdict — PASS-WITH-FINDINGS · 0 MAJOR / 0 minor / 3 notes

**Read cold and attacked (lens 1):**

- **The paragraph does the four things a tier-promotion rule must do**: it
  names the classes it covers (well-floored builds of *known* patterns;
  prescriptively-reviewed fixes), grounds them in recorded runs with the
  failure counted (four runs, nine dispatched items, one failure), states
  the discriminator (**floor density, not nominal class**) with the worked
  example on both sides — the silent-failure-class scanner change that
  cleared *because* 41 tests plus a prescriptive review floored it, and
  the novel mechanism that failed *because* fixtures cannot cover an unmet
  failure mode — and fences the boundary explicitly (no generalisation to
  first-of-kind builds; a fresh build of a known pattern is in class, a
  novel mechanism keeps capable-tier verify-at-merge).
- **Model-generation survival**: "the mid tier — Sonnet, as this estate
  currently fills it" separates the role from the instance, the same shape
  the principal-named tier bar ruling chose for reviewers.
- **Consistency**: executor ≠ reviewer is preserved — the surrounding
  section keeps orchestration, review verdicts and merge judgement on the
  capable tier, and the promotion touches only the executor slot; the
  extraction-from-practice bar ("kept only when the floor's evidence
  shows…") is the sentence directly above, and the paragraph is that bar
  being met rather than sidestepped.
- **Mechanical refs verified**: the `ROADMAP-DONE.md` § *Third-seat
  executor trial* heading exists; `dadde1d` exists (2026-07-23, the
  billing-state doctrine commit).

## Findings

**MT1 (note, resolved at reconcile) — the promotion's provenance.** The
trial was Mike-instructed; the *promotion to standing* was enacted by the
orchestrated run. Whether the trial instruction pre-authorised standing
adoption on evidence, or the promotion needed its own ruling, is exactly
what the intent records must show — carried as the pass's first reconcile
question rather than assumed either way. (Rule 3 remains satisfied
regardless: this verdict puts the promotion before Mike.)

**MT2 (note) — "well-floored" is a dispatch-time judgement with no
ex-ante test.** The discriminator is right (the failure case proves
nominal class is the wrong axis) but an orchestrator applies it by
judgement, calibrated by one worked example each way. Not a defect —
doctrine extracted from four runs cannot honestly offer a metric — but
worth recording: each future run's dispatch decisions are the data that
either firms the discriminator or breaks it, and the paragraph gives no
instruction to record them. The queue-run records have done so to date.

**MT3 (note) — the landing commit's section ref was corrected in its own
merge.** `6b11b11` cited a `ROADMAP.md` section; the merge that landed it
(`958e59b`) carries the retargeted `ROADMAP-DONE.md` § ref alongside the
harvest, so no pushed state on `main` dangled — verified against both
blobs. Recorded because a ref retargeted inside a merge resolution is
invisible to `git log` on the file and cost this pass a trace; nothing
owed.

## Reconcile (intent records opened after the findings above were committed)
