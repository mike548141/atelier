- [ ] 🛑 **The standard child doctrine block still carries the exact wording
      DA1 overturned — and it is the one spelling that gets COPIED into every
      child** — filed from a private child repo 2026-08-26 via § *Pointing up*,
      found while bumping a pin and reading the moved doctrine.

  - [ ] **The divergence, both sides quoted from this tree at `b38fb43`.**
        `COMMUNICATION.md` § *Asking for a ruling* (reworded in `2b21a56`):
        *"the account must have **reached** the principal before the choice is
        put: in the plain case it goes in the same reply, ahead of the device —
        and where the harness's display mode hides mid-turn text (focus mode
        shows only a turn's final message), it goes in a completed message
        **before** the ask"*, carrying its own note *"Reworded from 'on screen
        while the principal decides' on the principal's ruling, 2026-08-23 — the
        cold pass showed that spelling false in a live display mode."*
        `PROPAGATION.md` § *The standard child doctrine block*, **unchanged**:
        *"When the account will not fit the device, it goes in the session reply
        **first** and the device carries only the choice"*.

  - [ ] 🔎 **Verified as the only surviving instance, not asserted.**
        `grep -n "session reply \*first\*\|on screen while" docs/method/*.md`
        at `b38fb43` returns exactly one hit: `PROPAGATION.md`. The other three
        spellings DA2 named were harmonised in the same round; this fourth one
        was not, and it is the one that leaves the repo.

  - [ ] 🛑 **Why the "lossy summary" disclaimer does not cover it.** § *Pointing
        up* says the block *"is a lossy summary and is not evidence about what
        the house says"*, and that is a fair shield against **incompleteness**.
        This is not incompleteness. The block states a rule that is **false in a
        live display mode**, and false in the direction that produces the
        extracted-approval shape the apex forbids — a device on screen with its
        account nowhere the principal can see. The block is also the layer that
        exists *"so it binds even if atelier is never read"*, so a session
        following it faithfully, and never opening `COMMUNICATION.md`, does the
        wrong thing **because** it obeyed.

  - [ ] 🔑 **The class, which is DA2's, recurring.** DA2 recorded that a duty's
        other live spellings drift when one is corrected, *"a recurrence of the
        class the section's first cold pass flagged and harmonised in 2026-07"*.
        This is that class a third time, with an aggravating property the
        earlier instances did not have: **the stale spelling is the propagating
        one.** The other three sit in atelier and are read by atelier sessions.
        This one is copied verbatim into every child repo's `CLAUDE.md`, so each
        child that bumps its pin takes the corrected *route* bullet and the
        **stale** *asking* bullet in the same commit.
        🎯 **So the ask is not only "fix the sentence".** It is whether a ruling
        that changes doctrine should carry a mechanical check that the block's
        own copy of that rule moved with it — the block being, by design, the
        one place where a lossy copy is sanctioned and therefore the one place
        where a *falsified* copy is hardest to notice.

  - [ ] ⚠️ **Stated honestly: the reporting child did not follow the corrected
        rule either, and that is corroboration rather than confession.** In the
        same session, running under a display mode that shows only a turn's
        final message, it put a ruling ask through the structured device with
        the account carried inside the device's own fields. The principal's
        answer showed the question had already been settled by a standing
        ruling — a briefing failure with its own separate cause, so this is
        **not** offered as proof that the block's wording caused it. What it
        does show is that the corrected rule is easy to miss for exactly the
        session the block is written for.

  - [ ] ✅ **A small clean result from the same sweep, offered because
        `310/030` is still open.** That item lists ten children carrying two
        block defects — *read the staged hunk headers*, and that bullet pointed
        at `CONCURRENCY.md` § *The channel* where the rule is not. The filing
        child **carries neither**: its concurrency bullet already reads *read
        the whole staged index* with plain `git diff --cached`, and already
        points at § *The trigger*. One child off that sweep, dated 2026-08-26,
        offered as evidence toward the hand sweep `310/030` says is what makes
        it closable.
