# 2026-07-12 · 44 — COMMUNICATION.md cold review cleared (Fable)

Mike: "Sounds like we have some reviews to do." One owed: the session-43 cold
review of `method/COMMUNICATION.md`, pre-seeded questions already in the
ROADMAP.

## The review

Brief written to `reviews/2026-07-12-communication.md` (ask on top): the three
lenses instantiated plus five assumptions to attack — A1 leak-by-implication
at the worked example, A2 axes grounded-or-padded, A3 decline-then-revisit
honesty, A4 the practice/instance split structurally, A5 wiring/duplication.
One deliberate harness choice: the reviewer was **barred from `~/.claude/`
and told to set aside any system-injected personal context**, because A1 only
means anything judged as a reader *without* the personal layer.

**VERDICT: PASS-WITH-FINDINGS** (cold fresh-context agent, read-only, claims
re-run per REVIEW.md — wiring verified at HEAD, every cross-reference
resolved, duplication grep clean).

- **A1 cleared, decisively**: the scrubbed example creates an identity ×
  category-existence join with zero specifics, and the scrub note's category
  nouns ("health, workload, household") are ones the repo's own boundary
  statement already publishes — below the harm line, on the sanctioned side
  of RECORD's join rule one layer up (person, not repo). The scrub note is
  itself apex-required: saying *that* material was removed keeps the example
  honest.
- **A2**: grounded, unevenly evidenced — three axes repo-corroborated
  pre-commit, two resting on self-attestation (inherent to the split, stated
  honestly in the bearing).
- **A3**: append-only honesty confirmed — the 1ca394f diff only *adds*; the
  original declined call stands unedited above its own reversal.
- **A4**: TOOLBOX analogy real, not borrowed authority; where COMMUNICATION
  exceeds TOOLBOX (the scrubbed excerpt) it correctly cites ADR 0005.
- **A5**: wiring complete; no duplication; one cross-record tension (→ C2).

## Findings C1–C4, all [fixed] same day

One theme the reviewer named: the doc held the *boundary* rigorously but was
looser on itself than the house is on everything else.

- **C1** — maintenance enforcement unstated, and this is the extreme case of
  RECORD's R4 class: the instance sits where *no* house control reaches — no
  scanner, no CI, and no review sweep either, because reviews are barred from
  the person layer by design (this review's own brief is the proof). Now
  stated: write-time discipline is the only control.
- **C2** — "not even a private one" silently out-narrowed TOOLBOX and the
  portability north star's lighter tier. Resolved the strict way: blanket rule
  kept, divergence surfaced with its why — a calibration travels *with* its
  motivators, so it gets crown-jewel handling whole.
- **C3** — the doc broke its own dating rule twice: the worked example is a
  copy of a living document, undated (EVIDENCE §7/§9/§10), and the
  colour-icons confirmation was dated in the session record but not the doc.
  Both dated; the never-copied bullet now names the snapshot as the one
  sanctioned exception.
- **C4** — "works without the reader knowing it" overclaimed against the
  doc's own serve-don't-recite meta-rule; sharpened to *functions* without it
  — the understanding stays person-local, which is why the instance, not the
  snapshot, is what the agent serves.

## Kept load-bearing

The reviewer's honesty note: it could not unsee system-injected person-level
context, so it answered A1 through repo-citable inference chains only. A
future re-run would be cleaner with a context-stripped reviewer harness —
noted in the disposition, not blocking.

Review-owed queue now empty.
