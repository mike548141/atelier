# atelier — session log (index)

Append-only, newest last; one line per session. Tail-read at session start.
Full detail per session lives in `docs/sessions/<date>-NN-slug.md` — open a
file only when its line needs unpacking (RECORD.md's index/detail split;
restructured to this shape 2026-07-10, entries preserved verbatim in the
detail files).

---

- [2026-07-10 · repo born](sessions/2026-07-10-01-repo-born.md) — atelier extracted from the Mike↔Claude operating model; method/ first slice stood up (00-APEX, AUTONOMY, STORAGE, CONCURRENCY, TOOLBOX); PRINCIPLES + MODEL-ECONOMICS honest stubs; local commit only.
- [2026-07-10 · pushed, reviewed, corrected](sessions/2026-07-10-02-pushed-reviewed-corrected.md) — private `mike548141/atelier` pushed; foundation review (2 Fable + 1 harvest) PASS-WITH-FINDINGS; autonomy-floor gaps closed; canonicality decided + APEX↔ros §0 DRY breach fixed; ROADMAP re-sequenced mechanism-before-content.
- [2026-07-10 · estate-access + data covenant](sessions/2026-07-10-03-estate-access-data-covenant.md) — Mike's data covenant → method/DATA-PROTECTION.md; model-capability who-acts axis in AUTONOMY; estate-access expansion agreed (least-privilege, read-first, widen-in-rings); nas02 two-credential plane split.
- [2026-07-10 · propagation mechanism built + children stamped](sessions/2026-07-10-04-propagation-mechanism-children-stamped.md) — method/PROPAGATION.md (thin anchor, fat pointer; SHA-as-version; layer-override rule; enforcement clause); ros + faves retrofitted with the stamped block at c3676ee; drift check behaviourally proven.
- [2026-07-10 · mechanism, then the harvest extraction (Opus)](sessions/2026-07-10-05-mechanism-then-harvest-extraction.md) — EVIDENCE.md (A1), REVIEW.md (A2), RECORD.md (A3) written grounded-not-invented; PRINCIPLES.md extracted stub → canonical general spine; ros transitional DRY breach flagged + trim deferred to a focused ros session; pins bumped c3676ee→dde4170.
- [2026-07-10 · review brief + leakscan, then seed + hooks (Opus)](sessions/2026-07-10-06-review-brief-first-mechanical-control.md) — method/-layer Fable review briefed (gates further extraction); tools/leakscan.py built (structural + machine-local terms, bit on its own fixtures); term list seeded, hooks installed atelier + ros tiki/-scoped, tiki scrub live-validated; --disable + --staged subtree added.
- [2026-07-10 · parallel-work tooling: worktree.py (Opus)](sessions/2026-07-10-07-parallel-work-tooling-worktree.md) — tools/worktree.py (start/list/land/remove) delivering CONCURRENCY's worktree-per-line with the doctrine baked in as guards (iCloud-base refusal, stale/dirty flags, no-lose-work remove); 12 tests + live round-trip; Mike handed the recipe to run the method/ review as a parallel line.
- [2026-07-10 · the gated method/ review: PASS-WITH-FINDINGS, gate cleared (Fable)](sessions/2026-07-10-08-fable-method-review-gate-cleared.md) — whole method/ layer reviewed at 957fa08; 10 findings [fixed] (trust-surface floor gap, REVIEW reframed to independence-as-core — the "Fable is the cheaper tier" premise was wrong), 3 [backlog] (pin view, ADRs, SESSIONS split); extraction unblocked, ros trim guidance in lens-1 answer 12.
- [2026-07-10 · review follow-through: ros trim + ADRs + SESSIONS split (Fable)](sessions/2026-07-10-09-fable-review-followthrough-trim-adrs-split.md) — the reviewer executing its own verdict: ros PRINCIPLES trimmed to bearings + case-law (ros 73fd50b, DRY breach closed), ADRs 0001–0004 written, SESSIONS.md split to this index + verbatim detail files; remaining P2 pin view + further extraction flagged as Opus work.
- [2026-07-10 · the fleet pin view: tools/pins.py (Opus)](sessions/2026-07-10-10-fleet-pin-view.md) — built the last method-review backlog finding (P2): a read-only roll-up of every child's atelier pin vs HEAD (current/behind/ahead/diverged/unknown/no-pin, --log/--json/--check), the fleet companion to PROPAGATION's per-child drift check; deliberately never bumps (bumping stays per-repo human-in-the-loop); 12 tests, suite 86→98; live-proven faves 9 behind, ros current; PROPAGATION caveat updated to name it observability-not-enforcement.
- [2026-07-10 · MODEL-ECONOMICS.md: stub → canonical (Opus)](sessions/2026-07-10-11-model-economics-extraction.md) — promoted the stub to canonical: kept its shape (match-model-to-job, which-pool self-check, tiered authority, review triggering) and extracted the general session-hygiene mechanics + cache economics from ros (per-model cache, TTL churn, point-don't-paste, one-task, heavy-skills, hot-path-lean); estate-specific numbers stay person-local in ros with a foot-pointer naming the split; README/method-README/ROADMAP/CHANGELOG swept off "stub".
- [2026-07-10 · parallel-worktree reconciliation + E3 salvage + cleanup (Opus)](sessions/2026-07-10-12-worktree-reconciliation-e3-salvage.md) — traced a dangling parallel worktree line (independent method-review verdict draft on atelier-method-review@3bfcbbc, never landed; main had its own reworked verdict); nothing lost (both pushed to origin). Side-by-side of the two verdicts: 12/14 findings common+on-main, 3 main-only (all done), 3 worktree-only — salvaged E3 (two-register EVIDENCE §1) to main (48fa5ff), P2/PR2 consciously dropped. Cleanup way-back-verified: archive tag + kept remote branch, removed worktree, deleted local branches.
- [2026-07-10 · EVIDENCE §13 + §14: A6/A7, extraction section closed (Opus)](sessions/2026-07-10-13-a6-a7-extraction-close.md) — last extraction line: source-acquisition escalation ladder (§13, active counterpart to §3 — climb to the *cost of being wrong*, state the gap when blocked) + honest-instrument doctrine (§14 — an instrument the agent builds is a source; its "ok"/"applied" is a claim the apex governs: verified-not-attempted, silent-success-is-a-defect, "unknown"-is-required, known-failure-test enforces). Grounded in §3/§11 + PRINCIPLES §6; ros diagnose/apply phantom-success named as the estate instance. Review-owed (doctrine text).
- [2026-07-10 · build/ layer opened + secrets doctrine (Opus)](sessions/2026-07-10-14-build-layer-opened-secrets-boundary.md) — three grounded doctrine docs, each pointing up to method/ not copying it: `build/REPO-STANDARD.md` (A10 — the create-repo standard extracted: product-in-subfolder, sizing-to-type, honest-CI, the two processes; instance specifics stay in the skill), `method/SECRETS.md` (reproducible/re-mintable secrets → detect→rotate→burn-cost-is-minutes; closes AUTONOMY's forward-reference; slotted method/README #5), `build/REPO-BOUNDARY.md` (is-this-a-repo by independent-lifecycle discriminators → standalone/component/monorepo-folder; prefer reversible when ambiguous). Skipped the skill-rewire deliberately (don't stack delivery on unreviewed text). All review-owed; ros pin left at 3ba6275.
- [2026-07-10 · access-onboarding doctrine + licence gate (Opus)](sessions/2026-07-10-15-access-doctrine-licence-gate.md) — two non-stacking items: `method/ACCESS.md` (safe-access-onboarding runbook — sequences AUTONOMY/DATA-PROTECTION/SECRETS into grant-recorded→narrowest-cred→store-first→read-only-first-ring→destructive-gate-before-power→widen-in-rings→Zero-Trust; method/README #6) and `tools/licenscan.py` (A11 — pre-publish licence-consistency gate completing the leakscan·secretscan·licenscan triad; 3 checks, copyleft-into-permissive blocks; `--expect <SPDX>`; caught a real line-number bug + its own self-scan in the build; 35 tests, suite 98→133, atelier clean at Apache-2.0). A5 SBOM/signing deferred (tool-install floor + zero-dep break). ros pin left at 3ba6275; **review debt now the standout — next session a strong Fable-sweep candidate.**
- [2026-07-10 · review brief: the post-method-review batch, gate raised (Opus)](sessions/2026-07-10-16-review-brief-post-method-batch.md) — non-stacking Opus move on the flagged review debt: wrote the consolidated Fable brief for the `957fa08..f72031c` batch (`docs/reviews/2026-07-10-post-method-review-batch.md`) — five doctrine docs (EVIDENCE §13/§14, build/REPO-STANDARD, build/REPO-BOUNDARY, method/SECRETS, method/ACCESS) + the scan-triad approach review, 16 grounded assumptions to attack (scan false-negative classes the highest-stakes, `--selftest`-first, ros cross-read as the real-world check). ROADMAP review-gate section supersedes the scattered Review-owed tags; create-repo rewire stays blocked until the verdict. ros pin 3ba6275→f72031c (drift check fired, records-only).
- [2026-07-10 · the batch review: PASS-WITH-FINDINGS, gate cleared (Fable)](sessions/2026-07-10-17-fable-batch-review-gate-cleared.md) — the brief run cold, deep-not-fast: floor green (3 selftests, 133 tests, live runs — licenscan red on its own fixtures), 16 findings B1–B16, every one fixed in-repo + verified same session (2 backlog strands). Sharpest two were the batch's own defect class: B1 "live-proven clean" false at its recording commit (fixed, re-proven exit 0), B14 ACCESS pointing at an access map ros doesn't hold. Tool teeth: B2 `-only` SPDX mis-tier, B4 renamed-file staged hole (ACM→ACMR, proven live), B5 `--require-terms`, B7 stated residuals; suite 133→137. Backlog: ros access map; REVIEW.md "re-run live-proven claims". **Gate cleared — create-repo rewire unblocked.**
- [2026-07-10 · create-repo rewired to inherit; templates moved into build/ (Opus)](sessions/2026-07-10-18-create-repo-rewire.md) — the core Q1 fix, first stack on the cleared gate. `create-repo` was a second source of the standard (re-encoded file set/sizing/processes + a private `templates/` copy); made it a pure *delivery vehicle* over atelier as *source*. Keystone gap closed: the skill had **no CLAUDE.md template at all**, so every scaffolded repo was born with no doctrine block — PROPAGATION bypassed at birth. Templates moved skill→`docs/build/templates/` (18 files, one source), the missing CLAUDE.md template added carrying the stamped block, three instance-residue scrubs (NOTICE holder, "Nova", faves examples) + one live drift caught (MODEL-ECONOMICS template still naming ros canonical). Skill rewritten to point up + stamp the block/pin/scans, hard-depends on atelier, fails honestly if absent; its private templates removed after the atelier copy was pushed. leakscan clean; **not yet exercised on a real scaffold** — that + a Fable sweep are the owed items. ros pin unchanged (f72031c).
- [2026-07-10 · create-repo exercised end-to-end; scan-hook fail-open defect fixed (Opus)](sessions/2026-07-10-19-create-repo-exercise-hook-fix.md) — session 18's owed real-scaffold run, driven not read: scaffolded a real local git repo (seed → 3 renames → stamp → hook → commit). It paid for itself — surfaced a live **§14 silent-success defect** the scratch dry-run couldn't: `tools/pre-commit.sample` pointed at `$repo_root/tools/` and skipped silently when the scanners were absent; a child has none (they live in atelier), so the scaffolded hook committed a real `AKIA…` secret with a green exit. Fixed: resolve atelier's tools (`ATELIER_TOOLS` → `git config hooks.atelierTools` → in-repo fallback) + **fail closed**; step 6 bakes the path + prove-it-once. Re-proven fail-closed / blocks-secret / passes-clean / atelier-unaffected, then the contract **pinned by `tools/test_precommit.py`** (5 real-`git commit` tests; known-failure proven — the pre-fix sample commits the secret with exit 0); suite 137→142 OK. Surfaced-not-fixed: CI templates run no scanner (hook is the only gate) — folds into the deferred supply-chain call. Sweep **briefed not run** (independence): `docs/reviews/2026-07-10-create-repo-rewire.md` — range f72031c..92c0112 + the machine-local skill; 9 assumptions, sharpest three: clone-loses-hook-and-config, template-block drift, prose-stamp as memory-reborn. Gate raised: no keeper repo scaffolded until the verdict. Still owed: the outward `gh repo create` step. ros pin unchanged (f72031c).
- [2026-07-10 · the create-repo sweep: PASS-WITH-FINDINGS, gate cleared (Fable)](sessions/2026-07-10-20-fable-create-repo-sweep.md) — the brief run cold + driven twice: 10 findings C1–C10 all [fixed] + re-driven same session. Proven live: C1 a fresh clone loses hook+config and committed a planted secret green (fixed at the three places a clone looks); C2 the stamped drift check fatals on the unquoted spacey path (block quotes it; skill stamps `../atelier` + mechanical prove-the-stamp). C3 template-block ≡ PROPAGATION canonical pinned by tools/test_templates.py (suite 142→145); templates/LICENSE added; CI-scan gap + ATELIER_TOOLS trust stated; step-7 gate re-anchored to the ask. **Gate cleared — keeper repos may be scaffolded**; owed: the outward `gh repo create` live proof, ros/faves pin bumps.
- [2026-07-10 · first keeper repo (numen) + fleet pin bumps + REVIEW re-run rule (Opus)](sessions/2026-07-10-21-first-keeper-numen-pin-bumps.md) — closed the three owed items off the create-repo sweep. **Pin bumps:** ros f72031c→bbdeece (picked up the C2 quoted drift path) and faves dde4170→bbdeece (30 behind — also adopted the **P1 trust-surface floor clause** it had lagged since before the method review); fleet now all-current. **First keeper repo:** scaffolded **`numen`** (`mike548141/numen`, PRIVATE) — Mike's home-automation vision (an invisible, anticipatory household intelligence; "a great butler + house staff"), driven through the rewired create-repo live end-to-end and **the owed `gh repo create --push` step driven for the first time** (verified `isPrivate: true`). Sized honest as **pre-code**: stack a deliberate open decision, a **stated no-CI-gate** (nothing buildable + child scans can't run in CI yet), the pre-commit scan hook the only automated gate — proven live to block a planted `AKIA…` key and pass the real commit clean; stamp proven mechanically; no `settings.local.json` leaked. **Doctrine touch:** REVIEW.md gained "Re-run every 'live-proven' claim in scope" (a recorded proof can be stale by its own commit — re-run it, don't read it; grounded in B1 + C2), review-owed. B14 ros access map left for its own ros session.
- [2026-07-10 · atelier goes public — ADR 0005, named worked example (Opus)](sessions/2026-07-10-22-atelier-goes-public.md) — the CI-scan-distribution question, reframed by Mike into the floor action it made possible: **publish all of atelier**. A grounded publication-readiness audit first — hard boundary held (health/family/finance/address/pet/med terms all zero; leakscan + secretscan-over-44-commits + licenscan `--expect Apache-2.0` clean); the only exposures identity-not-data (Mike woven in by design; git history name/email). Mike's calls: **named worked example** (adopters *become the principal*; ros/tiki reframed as provenance), accept history, leave numen (in history too → a scrub is cosmetic, real removal is a rewrite he declined). **ADR 0005** supersedes 0003; README "If you're adopting this" + CLAUDE.md visibility PRIVATE→PUBLIC (making-public floor now *spent*, live floor is the next widening). leakscan earned its keep live — blocked the work twice on the author's own literal contact/health terms written into the *records themselves* (email in the ADR prose; enumerated med/family terms in the session file), each reworded to categories (the boundary biting its own author). Flipped act-then-record: local prep commit (hook clean) → `gh repo edit --visibility public` (verified `isPrivate: false`) → push (`22e41f9`). The opening CI-scan gap now *dissolves*: public means child CI can fetch the public scanners — wiring that + atelier's own CI are the unblocked next builds.
- [2026-07-10 · atelier's own CI — the floor, dogfooded (Opus)](sessions/2026-07-10-23-atelier-own-ci.md) — the self-contained half of the now-unblocked CI build: `.github/workflows/ci.yml` (job `floor`, push+PR) runs the floor every review asserted by hand — 3 scanner `--selftest`s, the 145-test tool suite, the scan triad over the whole tree (`licenscan --expect Apache-2.0`). Zero-dep stdlib → a runner needs only Python. One honest scope call, stated in the header not the fine print: **leakscan runs structural-only, deliberately without `--require-terms`**, because its literal term list is machine-local by design (`~/.claude`, never in a repo) — CI can't hold it and must not; full leakscan cover stays on the real-machine pre-commit hook. secretscan/licenscan run at full cover. Least-privilege (`contents: read`), concurrency-cancel. Grounded before claimed — ran the exact command set locally incl. a **simulated bare runner** (no HOME/global/system git config; still 145 OK) — then **watched not assumed** (REVIEW re-run rule): first GitHub run green 11s but flagged the Node-20 deprecation, bumped `checkout@v5`/`setup-python@v6`, re-ran green 7s / 11-of-11 / no annotation. Deliberately not stacked: the child-CI-fetch half and a markdown internal-link check (net-new tooling wanting its own review) left open.
- [2026-07-10 · linkscan — the internal-link integrity check (Opus)](sessions/2026-07-10-24-linkscan.md) — took the second of session 23's two open builds: `tools/linkscan.py`, the machine that guards the "thin anchor, fat pointer" graph the whole doctrine leans on (a relative link that 404s is a silent hole the *reader* hits, not the author). Sixth house tool, scan-triad pattern (zero-dep stdlib, `--selftest`, `linkscan:allow`/`.linkscanignore`, fail-safe `0`/`1`/`2`). Deliberately narrow: internal `[text](path)` links only (external schemes + `//host` skipped — the network is a flakier tool's job); `missing-file` (resolves relative to the linking file / root for `/…`, file *or* dir) + `missing-anchor` (a `#fragment` into a Markdown target whose GitHub slug matches no heading; `#L42` line refs + non-Markdown anchors skipped); fenced/inline code stripped so example links don't false-positive. 26 tests (suite **145→171**). Proven live — selftest OK, whole tree clean (55 files, 36 links); the tree has **zero anchored links today** so the anchor path is covered by selftest + units + a temp probe inside the tree (good/bad cross-file + same-file anchors, code skipped, a planted break flags) — surfaced honestly. Floor mirrored locally then **watched green on GitHub** for the exact SHA (`eedeb14`), not assumed. **Held on purpose:** not wired into `ci.yml`/`pre-commit.sample` — net-new tooling whose worst failure is the false-negative (a real break reported green), so the gate-wiring waits for a review (brief `docs/reviews/2026-07-10-linkscan.md`; third application of don't-stack-a-gate-on-unreviewed-tooling). Residual named in `tools/README.md` (reference-style/HTML links, setext headings, two-line `](…)`, slugger divergence).
- [2026-07-10 · the linkscan review: PASS-WITH-FINDINGS, all five assumptions repaired, gate cleared (Fable)](sessions/2026-07-10-25-fable-linkscan-review.md) — the brief run cold + every proof re-driven: floor reproduced, then all five load-bearing assumptions damaged by probe — L1 a typo'd path arg scanned nothing and exited 0 (the §14 silent-success class, now exit 2), L2 case-mismatched links green on APFS / 404 on GitHub (on-disk casing walked, NFC/NFD-safe), L3 links escaping the repo root (new outside-root kind), L4+L5 anchor matching made exact like GitHub's after fixing the slugger's underscore divergence, L6 paren filenames parse, L7 fence tracker length/info-string-aware (shared tracker), L9 setext anchors minted (was FPing valid links). L8 root-relative /… verified against GitHub docs; L10 indented-code FP stated-not-fixed (the fix costs real FNs). Suite 171→187, tree rescanned clean. **Gate cleared** — ci.yml/pre-commit wiring left open for the next build session (a reviewer doesn't wire its own same-day fixes into the gate; fourth application of don't-stack).
- [2026-07-10 · linkscan wired into the gate — CI + pre-commit, whole-tree (Opus)](sessions/2026-07-10-26-linkscan-wired-into-the-gate.md) — took session 25's held-open wiring. CI: a `--selftest` line + a whole-tree `linkscan --root . .` step. Hook: linkscan added as a **whole-tree integrity** check, *not* `--staged` like the two boundary scanners — a link breaks when a *different* file is renamed/deleted, so the stale file is rarely the one in the diff; `run_scan` generalised to drop the hardcoded `--staged` and let each scanner declare its mode. The design call surfaced not buried: linkscan catches a *recoverable* defect (unlike a burned secret), but atelier pushes to main where CI runs *after* publication, so the hook is the only gate that catches a 404 before a reader does — wired into both, recoverability nuance + child-repo whole-tree-contract friction stated. Contract pinned by 3 new test_precommit tests incl. the whole-tree crux (a rename breaking an *unstaged* link blocks — staged-only would miss it); suite 187→190. Installed hook refreshed so the commit dogfoods it; four selftests + all four whole-tree scans clean locally; GitHub watched green post-push. **Left open:** child-CI scanner fetch (the other CI half).
- [2026-07-10 · child CI scanner floor — the public scanners now gate child repos (Opus)](sessions/2026-07-10-27-child-ci-scanner-floor.md) — took the standing-open half of the CI story (sessions 23 + 26 both left it): **wire the public scanners into child CI**, unblocked by the public flip (ADR 0005). `docs/build/templates/workflows/floor.yml` — a language-agnostic scanner floor any doctrine-inheriting child drops in *beside* its `ci.yml` (correctness vs publish-safety, two files). It sibling-checks-out `mike548141/atelier` and runs its public secretscan/leakscan/linkscan against the child's own tree (`repo/`) on every push + PR — the CI backstop to the hook, which only guards its own clone. Design calls in the header not buried: **floats `atelier@main`** (a security floor wants newest; and avoids a second stamped-SHA drift surface — CLAUDE.md's pin stays the sole doctrine-version truth; `ref:` commented for reproducible-CI wanters), **leakscan structural-only** (machine-local term list — same honest scope as atelier's own `ci.yml`), **licenscan commented** (no-LICENSE hard-fails it → it's a *publish* gate, wrong to default-on for a private child like numen). Scoping to `repo/` is load-bearing (a whole-workspace scan false-positives on atelier's own fake-secret fixtures — proven). Known-failure driven (EVIDENCE §14): clean child 0/0/0, damaged child (real `AKIA…` + broken link) blocks — after a self-caught false start using the whitelisted AWS *example* secret. Wired into create-repo (seed step 3; step 6 "not wired yet" retired), REPO-STANDARD file set, pinned by 5 `test_templates.py` tests (one-source · repo-scoped · structural-only · licenscan-commented · least-priv; suite 190→195). **Left open:** not yet run on a real child (numen predates it); review-owed (net-new CI tooling).
- [2026-07-11 · child-CI floor exercised on numen; a four-session linkscan false-negative found + fixed (Opus)](sessions/2026-07-11-28-child-ci-floor-exercised.md) — closed session 27's owed real-child run by adopting `floor.yml` into **numen** (closing numen's *own* stated no-CI-gate, unblocked by ADR 0005). Drive-not-assume paid off exactly as session 19 predicted: pre-flighting the scanners over numen caught two real link breaks before any push (a disk-only `[tiki](../ros)`; the scaffolded `[0001](0001-slug.md)` placeholder **every child inherits**), and chasing the second upstream exposed a genuine **false negative in linkscan itself** — `SKIP_DIR_NAMES` held `build`, so the whole-tree walk had silently skipped atelier's own `docs/build/` doctrine layer (14 files) since it was built; session 24's "whole tree clean" was clean-except-the-layer-we-never-looked-at, the exact cardinal sin the linkscan review was built to prevent, live for four sessions. **Fix (`d0870a4`):** hardcode-skip now holds only never-authored names (VCS/deps/caches); `build`/`dist` go to `.linkscanignore` (Mike took the recommended full-drop). Unmasking caught the template placeholder → code-spanned in the template + numen; all templates re-scanned as-if-scaffolded clean; pinned by `test_content_dir_named_build_is_walked` (suite 195→196). numen driven on **real GitHub Actions**, not asserted: happy path green (`29092514962` — sibling atelier fetch, `repo/` scanned, all three ✓), fail-closed **red** via a throwaway PR planting a broken link (`29092599385` — linkscan ✗, exit 1; PR closed, branch deleted). Sharp incidental: numen's **pre-commit hook is frozen at scaffold** (pre-session-26, so no linkscan) — on numen the CI floor is the *only* linkscan gate, which the red run proved catches. **Left open:** the review — brief `docs/reviews/2026-07-11-child-ci-floor.md` (9 assumptions, 4 lenses; sharpest: trigger gaps, `atelier@main` supply-chain trust, does broken-link fail-closed generalise to a secret or is the real-infra secret-block still `act`-only-owed). Fifth application of don't-stack: no further children take `floor.yml`, the linkscan change isn't leaned on, until the verdict.
- [2026-07-11 · right-size the ceremony — calibrate review/session gates to risk (Opus)](sessions/2026-07-11-29-right-size-the-ceremony.md) — Mike's observation accepted: "one thing per session" was being over-applied and productivity degraded across the day; the driver was *not* economy (a fresh session is the pricier choice for related work) but an over-generalised rule + a pull to hand back at a safe green boundary. Found the record's actual words are narrower than my use: **"one task per session"** targets an *unrelated pivot* dragging stale tokens, not one-item-then-stop; **"don't-stack" was nowhere in the doctrine** — an un-codified session habit enforced as a rule, over-broadly. Captured proportionately (living the advice): MODEL-ECONOMICS gains **"Match the ceremony to the risk"** (gates/sweeps/session-breaks are *spend*, apply ∝ cost-of-being-wrong; what earns full ceremony vs. what's self-verifying; don't-stack codified with its narrow scope — an *unreviewed dependency*, not mere sequence) + a sharpened hygiene item 1; REVIEW gains a **"whether a change earns a review at all"** bearing pointing up. **Recursive check: this change is doctrine text, so by its own new rule it is review-owed — flagged, not self-certified, not merged by me.** Done in an isolated worktree (`gate-calibration`, off `bafeaa3`) to avoid clobbering a concurrent live session; handed back as a branch + PR for Mike to merge. Template MODEL-ECONOMICS copy deliberately untouched (separate templates pass).
- [2026-07-11 · the child-CI-floor review: PASS-WITH-FINDINGS, the class finally closed (Fable)](sessions/2026-07-11-30-fable-child-ci-floor-review.md) — the brief run cold, floor reproduced, numen logs re-read live, all nine assumptions probed. Headline: the d0870a4 masking fix closed the *instance*, not the class — secretscan+leakscan still hardcode-skipped `build`/`dist` (planted key in `docs/build/` scanned green, proven live), still phantom-succeeded on a nonexistent path (the L1 class), and the child ignore-file hatch was dead under exactly the floor.yml CWD (N1–N3, mirrored out from linkscan's reviewed patterns). floor.yml gains every-push triggers (a never-PR'd branch was scanned by nothing while the header claimed "every push"), a selftests step, and in-file FP-hatch docs (N4–N6). Six findings, all [fixed]+re-driven; suite 196→205. Floating `atelier@main` attacked and held (the fixes floating to every child bump-free is the argument); real-infra secret drive judged not owed (closed by composition). **Gate cleared**; follow-ups: numen re-copies floor.yml, atelier ci.yml shares the N4 trigger gap. Second review same session (related, no unreviewed dependency): the session-29 ceremony-calibration doctrine change (`cb37310`, merged as PR #2) — **PASS, no findings**; grounding probed not read (don't-stack genuinely un-codified pre-change; hygiene item 1's rationale was always pivot-cost), and this very session demonstrates both halves of the calibration.
- [2026-07-11 · N4 trigger follow-ups closed — atelier ci.yml + numen floor.yml (Opus)](sessions/2026-07-11-31-n4-trigger-followups.md) — the two follow-ups session 30 left standing, both the same N4 trigger gap, both proven on real GitHub Actions in a deliberately low-ceremony session (CI-trigger widenings are self-verifying — the first live application of session 29's calibration). **atelier's own `ci.yml`** widened `push:[main]`→every-push + `workflow_dispatch` (`2a4b2fd`): this job *is* the floor so it gates publication not just the merge; gap-closure proven by pushing a throwaway never-PR'd `n4-trigger-proof` branch and watching CI fire green on it (`29131233488`, a run that couldn't have existed before), then torn down local+remote. **numen re-copied `floor.yml`** byte-for-byte from the post-review template (numen `f81f66f`) to pick up the workflow-file fixes that don't float (N4 every-push, N5 selftests step, N6 hatch docs — the scanner fixes N1–N3 float via `atelier@main`); tree re-scanned clean in the exact floor.yml shape first, floor ran green (`29131265209`) with the new N5 selftests step confirmed live in the job log. numen's frozen pre-scaffold hook (no linkscan) stands as flagged — its floor is the only linkscan gate. Also closed the **session-29 templates follow-up** (`53b41db`): the condensed `build/templates/docs/MODEL-ECONOMICS.md` copy shipped the exact "One task per session; start fresh" misread into every scaffold — rewritten to carry the reviewed sharpening (a coherent *line*, not a checkbox) + the ceremony-to-risk bearing; judged self-verifying (applies an already-reviewed decision to its condensed mirror, no live pin on the body), suite 205 OK. Then a **deliberate fleet pin bump** (Mike: keep going): faves/numen/ros had all been 22 behind at `bbdeece` since session 21 — read the delta, ran each child's stamped drift check verbatim (C2's lesson), bumped both occurrences per child to `d45a431` (faves `aaeb785`, numen `86a0f45`, ros `d85bbb9`); every drift check now clean, `tools/pins.py` reads **all 3 current ✓**, numen floor re-ran green. Carries the ceremony-calibration doctrine down to every child's own future sessions. Left open: ros B14 access map; faves/ros may adopt the child-CI `floor.yml` at discretion (available, not owed).
- [2026-07-11 · atelier packaged as an installable Claude Code plugin (Opus)](sessions/2026-07-11-32-plugin-bundle.md) — Mike picked the plugin/skills-bundle as the next widening ("makes the doctrine travel"); built the v1 bundle, **handed back as PR #3** on branch `plugin-bundle` — the marketplace resolves only from the default branch, so **the merge is the go-live act** (widening floor stays the principal's call). Format grounded via a `claude-code-guide` agent (not memory): atelier becomes its own plugin AND marketplace (`.claude-plugin/{plugin,marketplace}.json`, `source: "./"`, Apache-2.0, `version 0.1.0` bumped-deliberately). Two calls made: **root-as-plugin not a subfolder** (forced by one-source — a git-subdir install can't reach `tools/`+`docs/` without copying them; documented REPO-STANDARD exception), and version-set-not-omitted (else records commits ship "releases"). Middle-tier scope (Mike took the recommendation): `/atelier:scan`, `/atelier:install-hook` (resolves `${CLAUDE_PLUGIN_ROOT}/tools` to an absolute path at install time — git has no such var at commit time), `session-onramp` + `review-brief` skills, whole `docs/method`+`docs/build` as bundled reference; de-instanced `create-repo` + worktree/pins deferred to v2. **Verified end-to-end, not assumed**: JSON valid, scans+linkscan clean, suite 205 OK, then the bundle *actually installed* via the real parser (local marketplace → user-scope install → `details` showed all 4 components → uninstalled + marketplace removed, config left clean). leakscan bit its own author (name in the manifests' attribution) → tight file-scoped `.leakscanignore` (JSON can't hold an inline marker; secretscan still covers them). Branch CI green. **The branch is code-only — this record + detail file live on main** (so PR #3 merges without a records conflict). Left open: Mike merges (go-live); a cold `review-brief` pass owed (net-new public-facing tooling, don't-stack — builder doesn't review its own bundle).
- [2026-07-11 · PRINCIPLES §8 — leverage / "productive laziness" (Opus)](sessions/2026-07-11-33-leverage-principle.md) — Mike named a principle mid-session and asked it be written for Claude models going forward: *strategic laziness is leverage — spend now (a work-avoiding design, or a reliable reusable tool) to save repeated work later and get consistent, robust outputs.* Added as **PRINCIPLES.md §8 "Leverage — invest now to stop paying later"** (appended, not renumbered — §5 is cross-referenced): two forms (**design out the work** — thin-anchor-fat-pointer/one-source; **build the reliable tool once** — the scan triad, the codified verb vs recurring hand-surgery, tied to EVIDENCE §14 build-it-honest), plus **the discipline** (payback needs real recurrence, never gold-plating; never a shortcut through §0–§2) and a **"One-off or recurring?"** situation test. Grounded not invented — the repo already *is* this principle. **Review-owed by its own rule** (doctrine text; flagged, not self-certified). Children inherit via the pointer (no edit); fleet reads behind again (real method/ change).
- [2026-07-11 · instruments/ layer — ccrepo & cctranscript move into atelier (Opus)](sessions/2026-07-11-34-instruments-layer.md) — a how-to ("timestamps on VS Code messages?") became a doctrine call. Answer: the chat UI shows none, but every message is timestamped in the session `.jsonl` under `~/.claude/projects/` — so built **`cctranscript`** (timestamped transcript: prompts+replies, elapsed deltas, `--full` for thinking/tools, `--json`), sibling to **`ccrepo`** (per-repo token/cost). Both zero-dep Node, logs read-only. **The decision (ADR 0006):** they'd landed in `homenetwork/bin` by accident of the first one-off; Mike pushed — both have *no value outside working with Claude as a teammate* (`ccrepo` = DevFinOps cost view, `cctranscript` = observability of what happened when), which *is* atelier's purpose, and atelier already ships `tools/`+`create-repo` so the doctrine-only objection is false. Placement fork put to Mike → **new top-level `instruments/` layer**, split from `tools/` by *purpose*: `tools/` **enforces** (Python, hook-wired checks — "these are the checks"), `instruments/` **observes** (Node, user-invoked, cost/observe the collaboration). Membership rule recorded: an instrument earns a place only if its value *is* the teammateship — general infra utilities (macOS/TrueNAS/networking) merely *used* stay with the estate they serve. Shipped `instruments/{ccrepo,cctranscript,install,README}`, ADR 0006 (+ decisions index, which was also missing 0005), README structure row; all three scanners clean on the new files (code carries no personal data — paths runtime-derived, safe to publish); symlinks re-pointed homenetwork→atelier + smoke-tested; `homenetwork/bin/` removed (no stale copy). **Meta-lesson to memory** (`verify-sibling-wiring-before-documenting`): two live corrections both traced to assuming-not-checking wiring (PATH-export vs the sibling's actual `~/.local/bin` symlink; atelier "doctrine-only" when it already ships tools). **Left open:** instruments are untested (unlike `tools/`); Node runtime dep now in that layer — both stated in the ADR.
- [2026-07-11 · instruments/ test floor — ccrepo & cctranscript earn coverage (Opus)](sessions/2026-07-11-35-instruments-test-floor.md) — the `instruments/` layer shipped untested in session 34 (stated, not silent); now floored. **Runner decision, recorded because it's atelier's first Node test surface and so sets the layer's convention: Node's built-in `node:test` + `node:assert`, zero third-party dep** — mirrors `tools/`'s stdlib-only "no pytest" pattern (nothing for CI to install beyond Node). Minimal, behaviour-preserving testability refactor: CLI entrypoints guarded by `require.main === module`, pure functions `module.exports`ed, ccrepo's disk-index build deferred into `buildIndex()` (was running at module load), three colour/tz functions given a defaulted param. **The headline test — a schema-drift `--json` output contract** (`test_cctranscript.js`) drives the real CLI over a checked-in synthetic fixture (`fixtures/session-sample.jsonl`, no personal data) and asserts role classification (prompt vs tool-result carrier), model mapping, timestamp/text extraction, and `--think`/`--tools`/`--full` gating — the test that fails loudly when the Claude Code `.jsonl` schema shifts. Plus pure-function units both sides. **ccrepo integration honestly scoped:** aggregation factored into a pure `aggregate()` and tested over fixture `ccusage` rows; residual (the `execFileSync` shell-out / FX / render) stated in ROADMAP + CHANGELOG, not faked. **One stated fix** a test surfaced: an explicit `.jsonl` path now recovers its repo label via `cwdFromLog` (every other route did; that branch had dropped it). Wired into `ci.yml`'s floor (adds `setup-node`, zero-dep); one leakscan false positive (a bare `HH:MM:SS` clock time as IPv6-structural) annotated with reason. Grounded in **EVIDENCE §14** (an instrument's "ok" is a claim the apex governs). 20 tests green; ROADMAP item ticked, CHANGELOG + this record added.
- [2026-07-11 · parallel builds + three cold reviews cleared (Fable)](sessions/2026-07-11-36-parallel-builds-three-cold-reviews.md) — Mike out ("do all the build work you can"); the open ROADMAP items run in parallel — three cold reviews as fresh-context background agents while the instruments builds ran in the main line. **Reviews, all PASS-WITH-FINDINGS, all dispositioned same day:** PRINCIPLES §8 (3 [fixed]: stale "§1–7" intro — §6's own sweep rule violated above §6 itself; §7 "Numbered last" made position-independent; observed-vs-predicted recurrence bar taken), CONCURRENCY put-away (3 [fixed]: bearing count grounded + sharpened — a *considered* kept-branch still taxed; integration/permanent-branch scoping; date-prefixed archive tags), and the **plugin bundle PR #3, driven live** — install/uninstall at user scope (config verified clean after), scan honest in a foreign repo, hook block/pass/fail-closed re-proven, merge-is-go-live proven directly. **Nothing blocks the merge** (Mike's call); findings 1–3 [fixed] on the branch (`030f185`): the version-pinned-path update trap documented, skills' plugin-root refs made location-relative, all three companions named. **Builds:** ccrepo gains Cache Create/Read + Cache Hit ratio everywhere (driven live: 95–98% per repo); cctranscript gains `N.M` reply refs (text replies only; `ref` in `--json`; `◂ Claude 1.1 (Opus 4.8)` live); create-repo + REPO-STANDARD bake in delete-branch-on-merge at birth; ccrepo actuals-vs-estimate **config designed, code held** (design-before-code honoured — `instruments/README.md` billing-model section; machine-local config, never in a repo); VS Code UI **scoped not built** (official extension has no hooks; statusline is terminal-only; recommended: sideloaded companion extension ~4–6 h, 1 h spike first). One session-limit interruption; the plugin reviewer resumed with context intact. Reviewer worktree/branch put away (zero unique commits).
- [2026-07-11 · the instruments code review's 10 findings, fixed + pinned (Fable)](sessions/2026-07-11-37-instruments-review-findings-fixed.md) — the `8536971` review's report lived only in its session transcript; recovered via `cctranscript --json` (the `ref` scheme addressing its own review), recorded as `reviews/2026-07-11-instruments-test-floor-code-review.md`, and **all ten findings [fixed] + pinned** (suite 20→26, green in UTC/Halifax/Chatham). Floor pair: tz-fragile `--by-day` fixture → midday-UTC stamps; `test_*.js` → `*.test.js` with one honest correction — node:test rejects directory args on 22+, so CI + docs run the shell glob `node --test instruments/*.test.js` (future test files can't skip CI). Correctness: argv help/validation out of module load (require-survival pinned), `shortModel` total, `.session` envelope guard, symlink-proof walk, `main().catch`. Cleanups: `pt`/`paint` dedup, one `sessionRecord()` for all routes (real mtime in list view), `buildIndex()` returns its maps. Three cut-by-cap residuals recorded, deliberately unfixed. Both instruments re-driven live; 205 Python + 26 Node + scan quartet clean.
- [2026-07-11 · ccrepo actuals, plugin go-live, docker-heap standardised (Opus)](sessions/2026-07-11-38-ccrepo-actuals-plugin-merge-docker-heap.md) — three open items cleared in one turn. **ccrepo actuals-vs-estimate built** (design-before-code strand closed): a machine-local `~/.claude/ccrepo-billing.json` adds an **Actual** column beside **Est (API)** — covered-model tokens $0 marginal, the sunk plan fee apportioned per repo by covered-token share, uncovered models keep the API rate; Actual = plan share + uncovered, TOTAL Actual = fee + all uncovered (proven live: **Est US$2,305 vs Actual US$200**). Absent config ⇒ byte-identical estimate-only JSON; malformed ⇒ ignored-with-warning. 8 new tests, Node 26→34. **Plugin bundle PR #3 MERGED** (`a0ef731`) on Mike's go-ahead — resolved a CHANGELOG append-conflict, re-ran the floor green on the merged head, merged; `main` now carries the plugin+marketplace manifest so the doctrine travels as behaviour. The "next widening" live-floor item is spent. **docker-heap standardised** (`atelier@5db645e`): doctrine block + house README + `docs/` (ARCHITECTURE/ROADMAP/SESSIONS) + CONTRIBUTING + infra-scoped `floor.yml` + fail-closed hook; fixed a self-ignoring/untracked `.gitignore` and scoped leakscan to drop network shapes (ros precedent). **No stack config touched** — the scan surfaced findings the repo's own private records already track, with a decided fix, so I deviated from "externalise now" and left it to the owner (specifics stay in that private repo); its floor red by design until the owner acts. **Code-signing** (Mike's steer) captured: SSH commit signing (zero-install, house-ethos) recommended fleet-wide + should become doctrine, blocked on Mike registering a key; artifact/SBOM signing stays deferred.
- [2026-07-11 · the signing doctrine drafted (Fable)](sessions/2026-07-11-39-signing-doctrine.md) — session 38's code-signing capture executed on Mike's ask ("draft the signing doctrine"): drafting was never blocked, only activation is, and the two are now cleanly split. **`method/SIGNING.md`** (meta section, beside RECORD/PROPAGATION — provenance *for* the record): what a signature honestly claims stated first (machine-key custody + tamper-evidence, *not* personal authorship — agent commits sign with the same machine key; the `Co-Authored-By` trailer keeps carrying authorship); layer 1 = SSH-native commit/tag signing fleet-wide (dedicated ed25519 signing key so auth/signing burn independently, machine-global config + create-repo-baked repo-local `commit.gpgsign=true`, one canonical **append-only `allowed_signers` tracked in atelier** as the durable verification plane — GitHub's badge dies if a key deregisters), CI verification from each repo's **adoption boundary** (history never rewritten to sign — ADR 0002's pins), key handling under SECRETS' person-level scope with cheap loss *and* cheap exposure; layer 2 (artifact signing + SBOM) stays deferred behind the first real release. **ADR 0007** accepted, activation pending — rejected GPG / sigstore-gitsign / no-signing / artifact-signing-now, each with why. Doctrine explicitly **decided-but-dormant** (stub-honestly): the activation ladder gates on Mike registering a signing key (trust surface, his act); steps 2–5 (machine config → create-repo → fleet retrofit → floor verify step) are agent-executable the day it lands. ROADMAP restructured accordingly; review-owed with the standing debt.
- [2026-07-11 · the whole fleet adopted (Opus)](sessions/2026-07-11-39-fleet-adoption.md) — "adopt all the other repos I use Claude on." Settled the vocabulary (**child repos** in **the fleet**; the act is **adopting** = stamping the doctrine block + pin, per PROPAGATION), inventoried every repo, and brought **all 11 children current at `1588fda`** (`tools/pins.py` ✓). Newly adopted 7: `rpi`, `nova`, `Baby Brain`, `FoodTracker`, `ec2_builder`, `homenetwork`, `hitchbots_guide` (block + `floor.yml` + fail-closed hook, each proven blocking a planted key). Pin-bumped 4: `faves`/`ros`/`numen`/`docker-heap` — catching real block-wording drift (`record Mike's decision` → `record the principal's decision`; the bump carries wording, not just the SHA). `python-metaname` excluded (third-party, `metaname` org). **The scan floor earned its keep:** surfaced real secret debt in a few repos (tracked in those private repos, not detailed here — atelier is public), and one scary finding verified a false positive *before* reporting (an SSH FIDO algorithm name mistaken for an `sk-` API key). Handled per the session-38 policy: scaffold safely, treat captured-snapshot secrets as the owner's tracked debt, network repos get `--disable ipv4,ipv6,mac-address`. Owed to Mike: the rotations/purges (details private), the two app purpose one-liners, and registering an SSH signing key to activate the signing doctrine.
- [2026-07-12 · the two owed cold reviews cleared (Fable)](sessions/2026-07-12-40-two-cold-reviews-cleared.md) — "do any review work needed": the standing doctrine debt's two items run as parallel cold fresh-context agents (the session-36 pattern), full lifecycle each (brief → cold run → verdict in the brief file → disposition). **RECORD "keep private repos generic": PASS-WITH-FINDINGS, R1–R7 all [fixed]** — the central clause held (re-proven live), but the naming clause was mis-drawn: the harmful class is the **join** (a private repo's name × its sensitive posture), not the name — the rule as written outlawed the repo's own records while the actual join survived the incident scrub in four places. Section redrafted (join regulated; name-only sanctioned behind a load-bearing-name test, e.g. ros/faves/numen; enforcement stated honestly — write-time + review sweeps, no mechanical floor; scrub-of-HEAD-is-not-remediation); the four surviving joins scrubbed at HEAD the strict way so doctrine and record agree. **Signing doctrine (SIGNING.md + ADR 0007): PASS-WITH-FINDINGS, G1–G10 all addressed** — every mechanical claim live-driven in a scratch repo; the core design proved out (bounded retirement genuinely keeps old signatures verifiable). Three blocking, all text-edit-cheap pre-activation: CI verification made **two-plane** (GitHub's web-flow-signed merge commits — two already on `main` — would have red-flagged the repo; `gh api` check closes it spoof-safe), the badge-persistence claim was **inverted** vs current GitHub behaviour (corrected; ADR 0007 addendum per the no-edit rule), quoted `valid-after` timestamps mandated (unquoted man-page syntax fails on the estate's ssh-keygen). Plus trust-list-at-the-child's-pin (never floating `main` — floating distributes fixes, this would distribute *trust*), custody/boundary/backdating honesty. Decision unchanged; activation still gates on Mike registering a key. **The review-owed section is now clear.**
- [2026-07-12 · commit signing activated (Opus)](sessions/2026-07-12-41-signing-activated.md) — "register a signing key — guide me through." The dormant signing doctrine (SIGNING.md + ADR 0007) walked through **ladder steps 1–3, live**. **The principal/agent split held literally:** Mike ran `ssh-keygen` (passphrase set) + `gh ssh-key add --type signing` (after a `gh auth refresh` scope top-up) — the passphrase never touched the agent; key id `1048805`. **Step 2:** canonical `allowed_signers` in atelier root (`namespaces="git",valid-after="20260712"` — quoted, dodging the review's macOS parse trap), global `gpg.format=ssh` + `commit/tag.gpgsign`, **adoption boundary `958b1ea` proven on BOTH planes** — `git verify-commit` good against the trust list *and* `gh api …verification.verified` → `true` (badge lit), boundary set forward (no history rewrite). Two facts surfaced by driving it: passphrase key needs `ssh-add --apple-use-keychain` (unattended signing), and leakscan blocks `allowed_signers` on its principal email → narrow `.leakscanignore` glob (the emails are the data the format holds). **Step 3:** create-repo + REPO-STANDARD bake repo-local `commit.gpgsign`. **shed** (Mike's "shouldn't shed script this?"): *no script* — an SSH signing key is registered once, not minted from a root, so the mint pattern doesn't apply and a `mint_github.py` would be ceremony; *but a registry entry, yes* — a `signing_keys` array under the github provider records id/location/roll (fingerprint omitted — trips validate.py; principal referenced to allowed_signers, one-fact-one-home). Entry flags the open boundary: the key's **secret** is person-level (Apple Passwords) yet the **fleet trusts** it as estate infra. **Then Mike: "do 4 + 5 now", warn-first.** **Step 5** — `tools/signscan.py` (two-plane: machine-key via ssh-keygen; GitHub web-flow commits deferred to the `gh api` plane; a known-signed fixture whose quoted + `Z`-suffixed `valid-after` selftest-guards both parse traps; 11 tests) wired into atelier `ci.yml` + the child `floor.yml` template, trust list read at the child's pin via `git show <pin>:allowed_signers` (never floating main). **The dogfood earned its keep:** atelier's own first CI run flagged every signed commit "not yet valid" — bare `valid-after="20260712"` is read in the verifier's *local* tz, so it passed on UTC+12 and failed in the UTC runner; fixed by UTC-anchoring (`"20260711Z"`), SIGNING.md now mandates it — caught before any child was touched. **Step 4** — the fleet splits three ways: **10 house-floor children retrofit** (pin bump + floor signing steps + `SIGN_BOUNDARY` = pre-signing HEAD; numen piloted green first, then 9 batched), **7 CI-green, 3 red on pre-existing scanner debt** (docker-heap/rpi/homenetwork — fail before signing runs, owner's debt); **faves + ros deferred** (bespoke `ci.yml`, never adopted the house floor — they still sign). shed's boundary `5bdee55`. **Signing fully active, warn-first.** Owed: faves/ros floor adoption, warn→block flip (Mike's call once the pre-existing debt clears), vigilant mode off until green.
- [2026-07-12 · faves + ros adopt the house floor (Fable)](sessions/2026-07-12-42-faves-ros-floor-adoption.md) — "lets go with full adoption": the session-41 follow-up closed. Current `floor.yml` landed beside both bespoke `ci.yml`s, **both green on first run with verification actually verifying** (faves 9/9, ros 2/2, two planes). The load-bearing catches: both pins predated `allowed_signers` (adopted as-was, verification would have *silently skipped* — the pin bump was the fix, not hygiene), and ros's unpushed HEAD was unsigned (amend-signed; boundary stays honest). Green-by-charter, not by scrub: repo-type `--disable` tuning + reasoned ignore hatches for content the repos are chartered to hold, every ros exemption stating it dies at the publish-time scrub pass. licenscan enabled in both; four real link breaks fixed. Warn→block flip still open, Mike's call.
- [2026-07-12 · communication calibration doctrine (Fable)](sessions/2026-07-12-43-communication-doctrine.md) — Mike revisited the same-day "reply style is personal, no atelier artifact" call with a sharper split: the *values* stay in `~/.claude/` (the reframe stands), the *pattern* is shareable doctrine a peer adopter needs. **`method/COMMUNICATION.md`** built — TOOLBOX's practice/instance split applied to communication; axes grounded in the live instance (outcome-first ordering, structure-replaces-length density, visual structure with the colour-icons ✅❌⚠️ specificity confirmed earlier this same day, cognitive-load stance, tone *with dosage* — the overdone-wit case, locale, what-personal-context-is-for); meta-rules (serve-don't-recite, maintain-like-a-record with dated confirmations, person-local-never-copied); Mike's calibration included **scrubbed** as the ADR 0005 named worked example (health/workload/household motivators dropped, stated). Decline-then-revisit recorded honestly in the ROADMAP item, not silently edited. Cold review owed — logged with pre-seeded brief questions (does the worked example leak by implication; axes grounded or padded).
- [2026-07-12 · COMMUNICATION.md cold review cleared (Fable)](sessions/2026-07-12-44-communication-cold-review.md) — the session-43 owed review RAN: **PASS-WITH-FINDINGS**, verdict in `reviews/2026-07-12-communication.md`. The sharpest question cleared decisively — the scrubbed worked example does *not* leak the personal layer by implication (identity × category-existence join, zero specifics, categories the boundary statement already publishes); reviewer barred from the person layer by the brief so the question was judged as a genuine outside reader. 4 findings C1–C4 all [fixed] same day, one theme: the doc held the boundary rigorously but was looser on itself (enforcement now stated as write-time-discipline-only; the not-even-private divergence from the portability north star surfaced, kept strict; the snapshot dated per EVIDENCE; the "works without knowing" overclaim sharpened). Review-owed queue empty.
- [2026-07-12 · browser-fetch adopted; the fetch ladder + credential boundary (Opus→Fable)](sessions/2026-07-12-45-browser-fetch-adopted.md) — session 41's conversation continued: "carry on with the browser fetch work." **The first capability instrument adopted**: `~/.claude/mcp-servers/browser-fetch` (Python MCP server driving Chrome) → `instruments/browser-fetch/`, scrubbed BEFORE entering the public repo (every "Mike" → operator, machine history dropped), pinned deps + reproducible `setup` (venv under `~/.cache`, never committed/iCloud), ADR 0006 addendum (three verbs: enforce · observe · **extend reach**; zero-dep flexes for capability tools, paid honestly). Proven end-to-end twice — driven directly (example.com → 200), then by a fresh parallel session against the re-registered server (`HeadlessChrome/149` UA = real Chrome); old dir + config backup deleted after confirmation. **The fetch escalation ladder** captured in its README, Mike's shape, flat 1–6 (he rejected 4a/4b sub-rungs): WebFetch/WebSearch · curl · standalone headless · persistent dedicated-profile · persistent everyday-session · ask-the-operator; start cheapest, step down only when blocked; Chrome-only today, Safari/Firefox + explicit 4/5 split ROADMAP'd. **The credential boundary sharpened into a purpose-of-storage test** before doctrine drafting: provisioned stores (shed registry keychain items, minted tokens — ros/tiki/shed) are the *intended path*; personal convenience stores (browser logins, Apple Passwords) are off-limits by default — ride an authenticated session, never the stored credentials that mint one; the principal grants across the line per credential. Encoded in the ROADMAP doctrine-elevation item so future `method/` text can't outlaw the estate's intended use. Doctrine drafting deferred to a next session. Signing pre-flight papercut also closed this conversation (session 41's tail). Survival audit after two parallel sessions in the same checkout: everything intact, history linear.
