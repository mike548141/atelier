# 2026-08-05 · 1155 UTC · Five rule-4 Fable cold passes — the whole queue, every cycle closed

**Session shape.** A Mike-spawned Fable taker ("do any review work and any
other work that requires fable"), with parallel sessions declared live.
Claimed all five `⏳` items on `main` from the primary checkout before any
work (`8a394d0`), then worktree `fable-cold-passes-0805`. The tier bar
held: cold passes on Fable, checked at selection.

**Discipline held on every pass.** Findings drafted and committed through
the full floor *before* any intent record or prior verdict was opened;
reconcile written as a second commit. No `--no-verify` anywhere — the one
hook block of the session (datescan reading a figures shorthand as a
slash-date) was fixed by rewording, not exempted. Probes ran in the
session scratchpad, never in the tree. Tree-wide greps excluded the
records store; the one records-adjacent finding (FF4) was verified with
read-only visibility lookups and recorded by reference, names withheld
from the verdict.

**Results — five passes, five verdicts, 0 MAJOR everywhere, every open
cycle CLOSED terminal:**

- **Pointer-grammar build + B4 wiring** (code/design):
  PASS-WITH-FINDINGS 0M/1MOD/1m/5n — both cycles closed. HV1 replay
  figures reproduce as a strict prefix at HEAD; hook-plane firing and the
  index/worktree split proven live in scratch repos. PG1: the docstring
  claims a staged read the hook invocation does not perform — HV4's
  wording class re-imported into the sibling tool of the same build,
  proven with a staged-but-reverted steering pointer committing clean.
- **FS1–FS5 application on floorfleet**: PASS-WITH-FINDINGS 0M/2MOD/0m/2n
  — B2+B3 closed. All five rulings faithful; crash fix and fail-safe
  `--check --status` proven live (16 rows, exit 1). FF1: a missing `gh`
  binary crashes the remote plane against the documented failure contract.
  FF4 (found at reconcile): the prior FS verdict names four private
  children beside their run state on public `main` — the recorded join,
  fourth instance, counsel a classes-only rewrite at HEAD.
- **Mid-tier standing-executor doctrine**: PASS 0M/0m/3n — closed.
  Promotion provenance verified three layers deep (Mike's pre-authorised
  bar, the trial's own prescriptions, a future session's act with rule-4
  queued); every figure verifies against the run records.
- **E6 application (E6a + E6c)**: PASS 0M/0m/3n — the E6 intent cycle
  closed. Six-shape probe 6/6 under credential keys, suppression
  precedence exact, blocking set structurally only-widened, canaries
  pinned. EI1/EI3 honoured by restraint; E6b/E6d stay bring-proposals.
- **Landing-equals-bookkeeping clauses**: PASS 0M/0m/2n — closed. Clause 1
  verified mechanically self-enforcing; the enacting commit practised all
  three rules it joins.

**Bookkeeping in this merge:** the five pointers replaced by one 🎯
rulings-owed item (findings to Mike), harvest section in `ROADMAP-DONE.md`
§ *The 2026-08-05 queue take*, pointerscan/harvestscan clean on the
result. A stale sibling branch was observed and left alone: its secretscan
fix is fully superseded at HEAD (verified by diff), deletion is its
owner's call.

**What is now Mike's:** the ruling round over PG1–PG7, FF1–FF4, MT2–MT3,
EA1–EA3, LB1–LB2 — two MODERATEs lead (FF1, FF4), everything else is
notes; no cycle is held open by any of it.
