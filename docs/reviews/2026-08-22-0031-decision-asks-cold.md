# Cold review — doctrine "Asking for a ruling: the device, the verified basis, the owed recommendation"

**Status: BRIEF ONLY — the review has NOT run.** Written 2026-08-22 by a
Mike-opened `claude-fable-5` session pointed at "any reviews and fable
dependent work"; it did not author the delta (an atelier session,
2026-08-19). Per Mike's rule for such sessions (2026-08-15), *the session
that writes a brief does not run its review* — so this waits for a
**further** cold Fable session that neither the delta's author nor this
brief's writer started or instructed; tier checked at selection
(REVIEW.md rule 4). The reviewer appends its verdict below the `---` and
states its own spawn provenance there.

**Brief-writer's exposure, disclosed:** this writer read the delta commit
(`e6b81e8`) — full message and all three `method/` diffs — to scope this
brief, and had earlier the same session read the **`SESSIONS.md` index
entry** for the author session (encountered while checking the log's tail
format for an unrelated record; it is the author's own dense account, so
treat this brief's framing accordingly). The session record itself — the
intent record proper — was left unopened. Earlier the same session, this
writer also ran the pointing-up cold pass (`310/050`, a different delta);
its verdict does not touch these surfaces.

- **Subject**: the delta of `e6b81e8` (2026-08-19), scoped to paths per
  the queue pointer — **doctrine by function** (REVIEW.md rule 3), so
  rules 3 and 4 bind:
  - `docs/method/00-APEX.md` § *The principal's authority is absolute; his
    rulings are conditioned on being informed* — the two added bullets
    (options with trade-offs; a recommendation), the may→**owes** clause
    change, the verified-or-marked paragraph, the closing delivery
    pointer.
  - `docs/method/COMMUNICATION.md` § *Asking for a ruling — the ask goes
    in the device, the account beside it* (new section, five parts).
  - `docs/method/AUTONOMY.md` § *Always confirm* — the repointed
    `COMMUNICATION.md` sentence.
- **Type**: doctrine cold pass — rules that govern every future ruling
  ask in every repo, at the exact surface where the principal's informed
  authority is exercised.
- **Scope** (point, don't paste):
  - The three delta surfaces above, at HEAD — including whether the two
    homes ("what an ask contains" at the apex; "how it travels" in
    COMMUNICATION) actually partition, or leave a reader of one file
    missing half the duty.
  - The queue pointer
    ([`270-…`](../roadmap/160-doctrine-review-owed/270-rule-4-cold-pass-queued-decision-asks.md))
    as the delta's own account of itself.
  - Intent record: the session record
    [`2026-08-19-0257-…`](../sessions/2026-08-19-0257-asking-for-a-ruling-the-device-and-the-verified-basis.md)
    — under the reviewer's own deferral discipline, and noting the
    exposure disclosure above (this brief-writer read its index summary).
  - Neighbours it must not contradict: the rest of 00-APEX's
    informed-principal section (what the list already bound before the
    extension — floor stops, overrules, grant widenings); AUTONOMY's
    stop semantics for non-interactive contexts; COMMUNICATION's visual
    axis and its calibration / worked example (the "option survey" the
    new section distinguishes itself from); REVIEW.md rule 3's own
    what/why/impact duty (the same account owed at a different moment —
    one duty or two?); `ECONOMICS.md` on spending the principal's
    attention.
  - The live instance's real behaviour: the doctrine names a concrete
    tool (`AskUserQuestion`) and states its capacities. Tool facts are
    checkable facts.
- **Load-bearing claims the record rests on** (extracted from the delta
  text, not evaluated — the reviewer decides which are load-bearing and
  adds its own):
  1. The commission is three separable rules, and the two homes chosen
     (content → apex list; channel → COMMUNICATION) are the right split —
     "extends the existing informed-principal list rather than opening a
     rival section".
  2. The extension binds wherever the list already bound: every account
     the informed-principal duty covers now owes options-with-trade-offs
     and a recommendation.
  3. "May still recommend" → "**owes** one" is Mike's 2026-08-19 ruling
     as recorded, layered beside the 2026-07-14 grounding it kept.
  4. Every fact in an ask is verified or marked; the escape (name what is
     assumed where verifying costs more than the decision is worth) is
     honest rather than a loophole; an unmarked guess is "withholding
     wearing better clothes".
  5. The structured device is the **default** channel for "decisions,
     rulings, clarifications, and anything else the principal is being
     asked to settle" — not a last resort.
  6. The device's stated capacities are the live tool's real ones
     ("four questions, with two to four options apiece"; short labels; a
     line of description each).
  7. When the account outgrows the device, it "goes in the session reply
     *first*, so it is on screen while the principal decides", the device
     carrying only the choice; the reverse is approval extracted by
     withholding.
  8. Where no device exists, the ask is prose "and carries exactly the
     same content" — channel changeable, content not.
  9. The survey-vs-decision distinction: a calibration may bar option
     surveys, and a genuinely-his decision "is an options question by
     nature"; one option with no alternatives is "deciding for him and
     asking him to countersign".
  10. plainscan was measured as a **delta**: 48 findings at HEAD, 48
      after (23/14/11 per file, unchanged), baseline from `git show
      HEAD:` copies.
  11. Nothing watches the channel — plainscan reads committed prose, an
      ask lives in the reply — and the watcher is honestly conjecture,
      queued unfunded at `220/060`.
- **Grounding — what the reviewer can run or read**:
  - Re-measure claim 10 at HEAD: `plainscan` over the three files, and
    the per-file counts against the recorded 23/14/11.
  - Check claim 6 against the live harness's actual `AskUserQuestion`
    contract at review time — a doctrine that states tool specifics has
    accepted a drift liability, and the number is checkable.
  - Exercise claim 7's mechanism, not just its wording: what the
    principal actually has "on screen while he decides" is a harness
    display behaviour, and it varies by his display settings — test the
    claim against the harness's real modes before trusting the rule's
    stated effect.
  - Read the three files whole at HEAD for the partition test in Scope;
    `linkscan`/`pointerscan` are enforced and current.
  - The public commit log around `e6b81e8` for what else landed with it
    (`320/050`, `220/060` — the delta's own account of its limits).
- **Non-goals**: re-ruling Mike's 2026-08-19 commission (the review may
  test the *recorded grounds*); building the channel watcher (`220/060`,
  queued unfunded); the `320/050` guard proposal (a different delta, its
  own item); the 2026-08-17 ruling-round application (separately queued
  at `160/260`).
- **Prior verdicts to open only after your findings are durably written**
  (rule 2): none address this delta. Nearest relatives, pointed at not
  summarised: `2026-07-14-2235-informed-principal-apex-cold.md` (the
  section this delta extends, at its first cold pass),
  `2026-08-17-0622-authority-absolute-cold.md` (the same section's
  authority half), `2026-08-15-1033-communication-floor-cold.md` (the
  file the new section landed in).
- **Deferred material**: the brief-writer's own attack angles are in the
  sibling `2026-08-22-0031-decision-asks-cold.deferred.md`. Open it as a
  deliberate second act, after your own findings are written; then fold
  it below the verdict and delete it (REVIEW.md rule 1).

---

## Verdict — the pass ran 2026-08-22

**PASS-WITH-FINDINGS — 1 MAJOR / 1 MODERATE / 2 minor / 2 note, plus
reconcile-stage additions DA-R1–DA-R5 (2 minor, 3 note). The cycle stays
open on the MAJOR (REVIEW.md: it closes when a pass returns none). Per
rule 3 every finding is Mike's to decide; nothing was applied.**

### Provenance and exposure

Run 2026-08-22 (claimed 0119 UTC, wt: `decision-asks-run-0822`) by a
Mike-opened `claude-fable-5` session ("Do any reviews and fable dependent
work") that authored neither the delta (an atelier session, 2026-08-19)
nor this brief (a different 2026-08-22 session). Hand-run — reviewer
only, no orchestrator; tier and criterion checked at selection.

Exposure, disclosed: at queue triage this reviewer read the board's six
open review pointers, including this item's own account of the brief. At
onramp, before selecting work, it ran the standard `SESSIONS.md` tail
read; the harness truncated the output to a 2KB preview showing only a
2026-07-28 entry, and the saved full output was deliberately left
unopened — the author session's index entry was not read. The
`.deferred.md` sibling and the intent record stayed unopened until the
findings below were committed; the reconcile section says what they
changed. Tree sweeps ran through `coldsweep` (rule-2 barred paths
excluded: 311 files including `docs/reviews/`); no `--include-barred`
sweep was run. The brief's *Grounding* bullets (legitimately read — they
are the brief proper, not the sibling) seeded the display-mode test
direction; DA1 below was formed from the live harness contract before
the sibling was opened.

### Evidence — what was re-run, read, and probed

- **Claim 10 reproduces exactly.** `git log e6b81e8..HEAD` over the three
  files is empty, so HEAD is the landing state; `plainscan --root . --warn
  --limit 0` per file returns 23 (00-APEX) / 14 (COMMUNICATION) /
  11 (AUTONOMY) = 48 — the recorded baseline, unchanged.
- **Claim 6 verified against the live contract.** The harness this session
  runs states `AskUserQuestion` takes 1–4 questions, each with 2–4
  options, headers capped at 12 characters, option labels "concise (1-5
  words)", one description line per option. "Four questions, with two to
  four options apiece", "short labels", "a line of description each" are
  all the tool's real numbers. See DA3 for what sits beside them.
- **Claim 7 probed against a real display mode** — it fails there; DA1.
  The probe is the live harness's own stated contract for this session,
  not an observation of the principal's screen, and is marked as such.
- **Partition test (Scope) holds.** All three files read whole at HEAD:
  the apex closes with the delivery pointer to COMMUNICATION; the new
  section opens by pointing back at the apex for content. A reader of
  either file is told the other half exists. The residue is DA2/DA5:
  the *restatements* of the content list, not the partition itself.
- **Claims 1–5, 8, 9, 11 hold as recorded.** The two-home split matches
  the pre-existing structure (AUTONOMY already pointed at COMMUNICATION
  "for the how" — the delta closed what was until then a promise with no
  section behind it); the owes-clause layering keeps the 2026-07-14
  grounding beside the 2026-08-19 ruling; the survey-vs-decision line is
  consistent with the calibration's anti-survey bullet; `220/060` and
  `320/050` both exist and `220/060` states the watcher uncertainty the
  commit message admits.
- **Floor green** at the claim commit `6c5661c` — every enforced scanner
  passing, `linkscan` and `pointerscan` clean.
- **Security & privacy lens, discharged with grounds**: doctrine prose,
  no code surface. `/security-review` reads the session's pending diff;
  this is a landed-delta review with nothing pending in scope, so the
  scanner cannot be aimed at the work — the doctrine's own
  landed-delta case. Design altitude checked by hand: the delta adds no
  personal data to the public surface; attributions stay inside ADR
  0005's named-worked-example framing.

### Findings

**DA1 (MAJOR) — "on screen while the principal decides" is false in a
live display mode the principal actually uses, and the failure it
produces is the one the same paragraph forbids.**
`COMMUNICATION.md` § *The device is small; the briefing duty is not*
rests on one mechanism: when the account outgrows the device, it "goes
in the session reply *first*, so it is on screen while the principal
decides". Under Claude Code's **focus mode** the harness's contract is
that the principal sees *only the final text message of a turn* — not
tool calls, and not text written before or between them. A ruling-ask
device is a tool call that blocks mid-turn; an account written in the
reply before it is mid-turn text. So in focus mode the device is up and
the account is nowhere: the principal decides on the widget alone, which
is precisely the "account trimmed to fit the widget … approval extracted
by withholding" the paragraph's last sentence forbids — reached by
*following* the rule, not by breaking it. Focus mode is not
hypothetical: it is enabled in the live instance (this session runs
under it; the contract quoted is the harness's own preamble to the
agent). The clause is also a display-behaviour fact stated in a verified
fact's voice — the test the delta's own verified-basis paragraph was
commissioned to impose; the fact varies by a setting the doctrine never
names. *Counsel (the author's position is not represented here; this is
the reviewer's, labelled per rule 3):* the content rule survives
untouched — the defect is one mechanism sentence. A mode-proof spelling
would demand the account have **reached** the principal before the
choice is put (e.g. delivered in a completed prior turn where the
harness suppresses mid-turn text), keeping the current same-reply shape
as the plain case and naming display modes as instance detail.

**DA2 (MODERATE) — the duty grew to five parts; its three other live
spellings still say three, and the landing commit swept none of them.**
Confirmed complete by coldsweep over live surfaces: (a)
`00-APEX.md:104–106` — the withholding clause, twenty lines above the
extended list, still reads "extracted by withholding what/why/impact",
so the section that makes options and a recommendation *owed* arguably
still defines extraction as withholding only the original three; (b)
`AUTONOMY.md:112–117` — the floor-stop sentence says "say what the
action is, why, and what's irreversible …" — three parts, in a sentence
this same delta edited to repoint at COMMUNICATION; (c)
`REVIEW.md:136–139` — rule 3 says the author "owes a plain-language
what / why / likely-impact". Each instance points at the apex section,
so the full duty is one hop away — that is what keeps this MODERATE —
but a reader of any single spelling concludes a three-part duty at
exactly the moments (a floor stop; a rule-3 briefing) the extension was
commissioned to cover. `PRINCIPLES.md` §6 names the obligation the
landing commit missed: when a learning is refined, its stale claims are
swept in the same commit.

**DA3 (minor) — the doctrine names the tool but not the tool's own
rival guidance.** The live `AskUserQuestion` contract carries usage
guidance the agent reads at the moment of use: "Use this tool only when
you are blocked on a decision that is genuinely the user's to make …
Reserve this for decisions where the user's answer changes what you do
next — not for choices with a conventional default or facts you can
verify in the codebase yourself." The doctrine's channel rule is
deliberately broader — "decisions, rulings, clarifications, and anything
else the principal is being asked to settle: not a last resort". The
two are reconcilable (the principal's standing instruction outranks a
tool's default guidance), but the section names the tool without naming
the conflict, so a session meeting both cold resolves it unaided — and
the conservative reading quietly wins by being in the tool's own voice.
One sentence acknowledging and settling the precedence would close it.

**DA4 (minor) — the two added bullets fit only one of the list's two
speech acts.** The list binds "*before* the agent asks the principal to
rule — **or acts on an overrule the principal initiates**". The
pre-existing impacts bullet carries an overrule-specific gloss ("for an
overrule: what the overrule itself trades away"); the added options and
recommendation bullets assume an open decision and carry none. What an
owed options-set means when the principal has already chosen — and
whether recommending against his overrule is owed or merely permitted —
is left to inference at the list's most delicate branch.

**DA5 (note) — a third spelling of the content list now exists.**
`COMMUNICATION.md:168–170` opens the new section by restating the full
list. Accurate today; DA2 is the live demonstration of how restatements
of this exact list drift. A pointer without the enumeration would
remove the liability at the cost of one hop.

**DA6 (note) — the worked example lost its adjacency.** The new section
lands between § *The meta-rules* and § *Worked example (scrubbed)*; the
example illustrates the calibration, and now reads as if it illustrates
ruling asks. Placement, not substance.

### Reconcile — the sibling, the intent record, the prior verdicts

All three opened only after the findings commit (`2cbc72a`), in that
order.

**The deferred sibling.** Its Q1 is DA1's mechanism, independently
converged on — the reviewer formed DA1 from the live harness contract
before opening the sibling, and the convergence of a cold reading and a
seeded angle on the same sentence strengthens the finding rather than
tainting it. Q4 extends DA1's scope and is folded into it: the
verified-basis *marks* live in the reply beside the account, so in the
same display mode they are equally invisible at decision time — one
mechanism, one fix. Q6 was checked and **holds, no finding**: the
calibration bullet does bar surveys flatly, but the new section claims
only that "a calibration **may** bar" them and draws the
work-vs-decision distinction, which is the live instance's actual
position per the 2026-08-19 commission. Q5's deeper half and Q2, Q3, Q7,
Q8 produced the reconcile-stage findings below.

**DA-R1 (reconcile, note — from Q2).** The owed recommendation has no
stated honest-degeneration for decisions whose deciding basis is
exclusively the principal's — his risk appetite, a pure taste call. The
reasoning-shown requirement mostly self-corrects (an honest
recommendation can mark its basis as taste, or as strictly thinner than
the principal's own information), but the clause that says so does not
exist, and "owes" without it can manufacture confident-sounding counsel
exactly where informing-not-steering is most delicate.

**DA-R2 (reconcile, minor — from Q3).** The device's own fields are
content — a mislabelled option is a wrong ask however good the reply
above it — and neither home specifies what they must carry. The live
tool's own convention (the recommended option listed first, labelled
"(Recommended)") is the natural discharge, and it also blunts DA1's
worst case: the one surface guaranteed visible at decision time in
every display mode is the widget itself, so the owed recommendation
marked *inside* it survives the mode that hides the reply.

**DA-R3 (reconcile, note — from Q5).** The recorded plainscan evidence
is a count identity, not a content one: per-file totals (23/14/11) can
hold while a finding is swapped for a different finding. At this HEAD
the distinction is moot (the files are unchanged since landing), but
the method as recorded reads stronger than what it measured; a
per-finding diff (`--json`) is the content-true form for future deltas.

**DA-R4 (reconcile, note — from Q7).** The no-device paragraph
specifies the ask's content and not the protocol around it: what the
session does while the answer is outstanding. It is inferable — a floor
stop halts per `AUTONOMY.md`; elsewhere the ask queues for the ruling
round and recoverable work proceeds — but the paragraph that owns the
no-device case says none of it.

**DA-R5 (reconcile, minor — from Q8).** The two homes cover different
ask classes and the join is unstated. The channel section covers
"decisions, rulings, clarifications, and anything else the principal is
being asked to settle"; the content list was written for accounts of
acts and rulings. Read together, either every clarification formally
owes a five-element account — over-ceremony for "which spelling do you
prefer?" — or the content duty's boundary is undefined. DA4 is the same
re-derivation gap surfacing on the list's overrule branch.

**The intent record** (opened after the sibling) matches the delta as
landed; its judgement calls were checked rather than taken. The
tool-as-instance split holds. The narrowing of the commission's
"unless there are extenuating circumstances" to the one checkable
exception (no device exists) is disclosed with grounds, and "default
channel" keeps the phrase rebuttable — compatible, no finding. The
"nothing was removed" claim verifies: the waiver clause stands at HEAD.

**Prior verdicts** (opened last): the 2026-07-14 informed-principal
pass had already flagged the near-duplicate duty spellings as
un-harmonised (its AS8/F4, then `[fixed]` by aligning them) — **DA2 is
that class recurring**: the list grew again on 2026-08-19 and the
spellings re-split. A second occurrence is the strongest support for
DA5's counsel that restatements of this list should become pointers.
The 2026-08-17 authority pass and the 2026-08-15 communication-floor
pass carry nothing bearing on this delta's surfaces; no prior verdict
anticipates DA1.

### Folded-in deferred sibling (verbatim; file deleted at this landing)

> # Deferred — brief-writer's seeded questions (open only after your findings are written)
>
> Sibling of `2026-08-22-0031-decision-asks-cold.md`. These are the
> brief-writer's own attack angles, written 2026-08-22 by a session that did
> not author the delta. A floor, never a fence: your own findings come
> first, and this file is folded below the verdict and deleted when the pass
> lands.
>
> 1. **"On screen while he decides" is a display assumption, not a
>    guarantee.** The account-in-reply-first rule (claim 7) assumes text
>    emitted before a structured ask is visible to the principal at
>    decision time. The harness has display modes in which **mid-turn text
>    is not shown at all** — only the final message of a turn is — and a
>    structured ask fires mid-turn by construction. If the principal runs
>    such a mode, the rule as written produces exactly the trimmed-account
>    decision it forbids, while the session believes itself compliant. Does
>    the doctrine hold under every display mode the harness actually
>    ships — and if not, is the fix a wording change (send the account,
>    *then* end the turn / confirm receipt) or a rule the harness cannot
>    currently honour?
> 2. **Does the owed recommendation survive the questions where a
>    recommendation is steering?** The apex now says the agent owes a
>    recommendation in every informed-principal account. Run the hard
>    cases: a pure taste/values call (which of two visual designs; a
>    naming choice); a decision where the agent's information is strictly
>    worse than the principal's (his own risk appetite, his family's
>    needs); a floor stop whose options are just yes/no. Does "owes"
>    degenerate honestly (a recommendation with reasoning shown is always
>    possible), or does it manufacture false confidence exactly where the
>    apex's informing-not-steering line needs it least?
> 3. **The two-homes split leaves the widget's own content unspecified.**
>    The apex owes five content elements; the device carries "the choice
>    alone" when the account is big. But the device's option labels and
>    descriptions ARE content — a mis-labelled option is a wrong ask
>    however good the reply text above it was. Neither file says what the
>    *device fields* must carry (e.g. the recommendation marked in the
>    option list, per the tool's own convention). Gap, or correctly left
>    to the tool's guidance?
> 4. **Verified-or-marked meets the size cap.** Claim 4's marks (verified
>    vs assumed) are part of the basis; claim 7 puts the basis in the
>    reply when it outgrows the widget. Combined with question 1, can a
>    principal ever be shown the *marks* at decision time in every mode —
>    and if not, is the verified-basis rule silently reduced to an
>    after-the-fact audit trail?
> 5. **Re-run the plainscan delta, and check what "unchanged" hides.** The
>    record's 48→48 (23/14/11) is a count identity, not a content one: the
>    same totals can hold while individual findings moved between
>    categories or lines. Re-measure at HEAD; if the counts have since
>    moved, the claim was true-at-commit — say so — but check whether the
>    delta method (baseline from `git show HEAD:` copies) would even have
>    caught a finding swapped for a different finding.
> 6. **Byte-honesty of the survey line.** Claim 9 says "a calibration may
>    bar" option surveys and the worked example carries the tension. Does
>    the calibration text at HEAD actually bar surveys in words that
>    support the distinction drawn (surveys-for-work-you-should-do vs
>    options-for-his-decisions), or does the new section characterise a
>    rule that reads differently in place?
> 7. **Non-interactive asks: same content, no receiver.** Claim 8 says the
>    prose fallback "carries exactly the same content". But a
>    non-interactive run cannot receive an answer mid-run: AUTONOMY's stop
>    semantics say what happens to the *work* (stop at the floor; safe
>    default elsewhere?). Do the two files agree on what a session does
>    *after* writing the prose ask nobody will read this run — and is
>    "carries the same content" quietly standing in for an unwritten
>    defer-or-default rule?
> 8. **The list's scope widened without being re-derived.** The
>    informed-principal list was written for accounts of specific acts
>    (floor stops, overrules, widenings). The commission was about ruling
>    *asks*. Grafting the ask duties onto the act-account list binds both
>    ways: every floor-stop account now owes options and a recommendation,
>    and every ruling ask inherits the act-shaped bullets (what/why/likely
>    impacts). Check both directions against the section's own examples —
>    does either produce an obligation that reads wrong in place?
