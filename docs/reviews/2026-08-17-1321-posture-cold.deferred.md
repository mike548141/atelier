# Deferred material — the posture section and the fourth guard requirement (open only after your findings are durably written)

Sibling of `2026-08-17-1321-posture-cold.md` under REVIEW.md rule 1's split.
Fold into the brief below the verdict and delete this file when the verdict
lands.

## Intent records

- `docs/sessions/2026-08-17-0900-posture-recover-cheaply.md` — the authoring
  session's record: how the principal's thinking arrived, how the three
  rulings were put to him and what he answered, and why the section took the
  shape it did. **Not opened by the brief-writer.** Read it first at reconcile:
  the reviewable question is whether the doctrine says what was ruled.
- The board section
  `docs/roadmap/300-posture-recover-cheaply-mike-commissioned/` — its
  `README.md` carries the provenance in the author's words; `010` is the
  posture section and the guard test (the ruling record), `020` the funded
  re-ranking of the guard board against the test, `030` the funded audit of
  the detection precondition ("what would this estate actually notice"). Bodies
  **not opened by the brief-writer**; titles only.
- The `docs/SESSIONS.md` index entry for the 0900 session. **Not read by the
  brief-writer** (the tail read at onramp began after it).

## Prior verdicts on the same surfaces

- `docs/reviews/2026-08-05-1320-f1-guards-allowances-cold.md` — the pass on
  `GUARDS.md` as built (F1). Its findings on the three requirements and the
  registry `why` field are the nearest prior treatment of what a *requirement*
  on a guard is; the new section claims to close a finding "filed against this
  estate's own guard architecture" — check which finding, and whether it is
  closed or restated.
- `docs/reviews/2026-08-09-0821-principles-9-time-dimension-cold.md` — the
  pass on the last principle added (§9), the nearest precedent for how a new
  section of `PRINCIPLES.md` was judged: grounding, placement, situation test,
  cross-references.
- `docs/reviews/2026-08-15-1031-laws-removal-apex-cold.md` — the most recent
  pass touching `PRINCIPLES.md`, if your findings reach the doc's opening
  frame or its precedence order.
- `docs/reviews/2026-08-17-1000-ruling-round-application-cold.md` — the brief
  (verdict may or may not have landed by the time you read this) for the
  delta one commit earlier that added `GUARDS.md` § *A rule with no home is
  not a rule* — the test this brief asks you to apply to "declares".

## Brief-writer's seeded questions (a floor, never a fence)

Generate your own before reading these. Treat a question you did not think of
as a prompt to re-read the surface, not as an agenda — and note that the
brief-writer read the commit message in full, so these questions inherit some
of the author's framing.

1. "Every guard declares which of two things it does." Declares **where**? The
   section says the `why` field lacked a standard; it does not say the
   declaration goes *in* the `why` field, or in a new registry key, or in the
   guard's docstring. Name the surface a future guard author reads this on and
   the slot they write into — or say there is none yet, and whether that makes
   the requirement the class the same page calls *not yet a rule*.
2. The two limits (declare, never unwire; "forbids" is not a failure grade)
   live in `GUARDS.md` only. A reader of `PRINCIPLES.md` §10 and its situation
   test meets *prefer cheap failure where the recovery is buildable* with no
   limit beside it. Is that a rule with two spellings that differ in force?
3. §10 quotes the principal for three paragraphs and then says *four cases,
   one idea*. Count the cases in the quote. Is the fourth (exercise proves the
   recovery) in the quote, or is it the author's — and if the author's, is it
   marked as such?
4. §10's placement paragraph says the individual cases *live where they
   already live* in `SECRETS.md`, `DATA-PROTECTION.md`, `ACCESS.md`. Open each
   and say whether it states the case (rotation-ease; restore *and its
   exercise*; what a device joining means) or whether §10 is now the only
   place the case is stated — a pointer to a home that does not hold it.
5. The precondition paragraph says *claim the freedom that the detection and
   the recovery you have actually exercised will support, and no more*. Is
   that a rule an agent can apply at a decision — what does it do when
   detection is unproven — or a stance? Item `300/030` funds an audit of
   detection; does the section say the freedom is *suspended* until that
   audit reports, or merely that it should be?
6. The commit message frames the section as the principal's thinking "shared
   with no commission attached, then ruled into doctrine three ways". Read
   the ruling record (`300/010`) at reconcile: were three things ruled, and
   is each one applied on the surface the record says? Was anything applied
   that was not ruled — the situation test in particular?
7. The commit that landed this delta also renumbered a board section from
   `290` to `300` under a tie-break the channel doctrine prescribes, and
   queued `010/120` and `160/250` on the way. None of that is in this pass's
   delta. Is the doctrine landing entangled with bookkeeping in a way that
   makes "the landing commit" ambiguous for the pointer's landing = queuing
   rule, and did the pointer land in the commit that completed the doctrine?
