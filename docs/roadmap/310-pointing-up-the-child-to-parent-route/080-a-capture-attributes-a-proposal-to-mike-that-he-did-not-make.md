- [ ] 🔎 **`2cd4730` attributes a proposal to Mike that he did not make, and it
      has been quoted forward ever since** `[XS][docs]` — filed from faves
      2026-08-18 by § *Pointing up*. One line to fix in two places; the reason
      it is worth an item is the class, not the edit.

      **What the board says** (`docs/ROADMAP.md` at `2cd4730`, 2026-07-22, and
      the wording that reached `docs/roadmap/README.md`'s legend):
      > *"**Extend the checkbox grammar to five states?** (Mike, 2026-07-22,
      > mid-build) — proposal: add `[-]` declined and `[^]` superseded beside
      > the tri-state."*

      **What Mike actually wrote** — session `1b189e23`, 2026-07-22 04:31:07
      UTC, one clause at the end of a paragraph about harvest integrity:
      > *"…anything with `[ ]` should not be in roadmap-done I would think,
      > unless perhaps it is supersceeded, or no longer required."*

      He never typed `[-]`, never typed `[^]`, and never enumerated states. The
      two brackets were the **assistant's**, and its close-out the same day
      handed them back as *"Your five-state proposal (`[-]` declined, `[^]`
      superseded)"*, which the capture then recorded as his.

      ⚠️ **Two dates are also conflated.** The capture is 2026-07-22; the
      **ruling** is 2026-07-23 02:16:54 UTC, when it was put to him as a choice
      (*"① Keep the three checkbox states, or extend to five?"*) and answered
      **"Keep three (Recommended)"**, session `64f68104`. **A question captured
      is not a question answered** — and the legend currently dates the rule to
      the capture.

      🔑 **Why this outranks a typo.** A record attributed to the principal is
      quoted forward **carrying his authority**, so nobody re-checks it. A faves
      session did exactly that on 2026-08-17: it found the capture, reported to
      him that he had *"proposed the exact marker already counselled against"*,
      and argued against a position he never took — using his own name as the
      evidence. He corrected it from memory. **The defect is not that the record
      was wrong; it is that being wrong in this direction is undetectable from
      inside the record.**

      🚩 **And the direction is consistent, which is what makes it a class.**
      Three records were found overstating an owner ruling in one day in faves,
      **none understating one**: this attribution; a conditional (*"fine to
      break URLs now, but not once there are lots of users"*) written up as an
      absolute (*"the part that must not be dropped"*); and a user-facing
      changelog line claiming a URL key used to work when it never had. A firm
      sentence is easier to write, easier to quote and easier to act on, so
      every rewrite sheds the qualifier.

      **Options, offered not recommended.** (a) Correct the two places and stop.
      (b) Correct them and add a convention — **quote the principal verbatim
      beside any paraphrase of his words**, one indented line, which makes the
      overstatement visible at a glance. (c) Make it a `RECORD.md` rule with a
      scanner behind it, if a mechanical form exists (attribution to a named
      person without an adjacent quote is greppable, at some false-positive
      cost).
