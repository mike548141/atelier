# Cold pass — the 2026-09-18 hand-up fixes — session-start sync gates, the act-verification clause, and the drift command

**Pass type:** doctrine cold pass, per `docs/method/REVIEW.md` rule 4.
**Tier:** Fable (the principal-named review tier, ruling 2026-08-04), checked at
selection: this batch's reviewer and orchestrator are both `claude-fable-5-1`.
**Status:** BRIEF WRITTEN 2026-09-25 UTC; the review runs in the same batch
under the orchestration below, by a reviewer that is not the brief-writer.
**Queue pointer:**
`docs/roadmap/160-doctrine-review-owed/310-rule-4-cold-pass-queued-the-0918-hand-up-fixes.md`.
**Why it earns a review:** these sentences are the first thing every session in
every repo runs at open (the sync bookends), the sentence that decides whether a
mechanical result counts as done (verify the act), and the command a child uses
to detect doctrine drift — three fleet-wide behaviours changed in one commit.

## Spawn provenance

- **Author of the work under review:** the session(s) that landed the commits
  named under *What the work is*. This brief-writer was not that session, was
  neither started nor instructed by it, and has edited none of the delta's
  paths.
- **Who wrote this brief:** an atelier session Mike opened on 2026-09-20 and
  re-pointed on 2026-09-24 with the prompt "Please deliver all fable dependent
  work, and work that would be best delivered using fable", on the Fable tier
  (`claude-fable-5-1`), orchestrating this batch of twenty rule-4 passes. It
  wrote this brief from the queue pointer, the landing commits' subjects and
  file lists, and the delta paths' names; it did not open the intent record or
  any prior verdict on these surfaces.
- **Who takes the review:** a fresh Fable subagent (`claude-fable-5-1`) spawned
  by the brief-writer with this brief as its only framing. It is not the
  author's session and was not instructed by the author. The reviewer repeats
  its own provenance in the verdict.
- **Orchestration shape, disclosed per rule 4:** reviewer-plus-orchestrator. The
  orchestrator holds the `.deferred.md` sibling outside the worktree, commits
  the reviewer's phase-1 findings unrevised, then releases the sibling's text by
  message; the reviewer appends a reconcile section; the orchestrator folds the
  sibling in and updates the pointer. The orchestrator forms no finding and
  writes no severity. Both seats are Fable, so the off-tier clause is not
  invoked; the shape is stated anyway so the record is auditable.
- ⚠️ **Brief-writer's exposure, disclosed** (rule-2 material it met before
  writing): the `docs/SESSIONS.md` index entries of 2026-09-11 to 2026-09-19
  (the last three summarise the 2026-09-18 and 2026-09-19 runs in the authors'
  words); the first 30 lines and the last ~3,000 characters of
  `docs/sessions/2026-09-20-1053-queue-run-the-loose-ends.md`, read during an
  interrupted-session sweep on 2026-09-20; every queued `⏳` pointer on the board
  in full, including each pointer's own "lens that matters" paragraph — that
  paragraph is the author's framing and has been moved to this pass's sibling;
  the pass file `docs/reviews/2026-08-17-1000-coldsweep-cold.md` in full (brief
  and verdict) and lines 1–80 of
  `docs/reviews/2026-08-21-0820-pointing-up-cold.md`, both as formatting
  templates; the landing commits' subjects and `--stat` file lists (never the
  diffs); and, as doctrine read at onramp, `docs/method/CONCURRENCY.md` §§ *On a
  split board*, *Claiming at a dirty primary checkout* and *Surviving an
  interrupted session*, `docs/method/REVIEW.md` and `docs/method/00-APEX.md` at
  HEAD. Per-pass additions are marked ⚠️ under *Deferred reading*.

## What the work is

Landing commits (diff these; review the paths at HEAD):

- `54201e0` (2026-09-18) — the landing commit for the sync gates, verify-the-act
  and the drift command
- `f1d5b5e` (2026-09-18) — the COMMUNICATION surfaces: the enforceable-split
  bullet and the reply-plane paragraph, edited when `plainscan` was removed
  (`020/360`)

Delta paths:

- `docs/method/CONCURRENCY.md` § *Integration hygiene* — the **Sync bookends**
  bullet's two gates, and the new **Verify the act** bullet
- `docs/method/PROPAGATION.md` § *The mechanism* point 4, and in § *The standard
  child doctrine block* the **Concurrency** and **Source & drift** bullets
- `docs/build/templates/CLAUDE.md` — the same two bullets
- `CLAUDE.md` — onramp step 1
- `docs/method/COMMUNICATION.md` — the enforceable-split bullet and the
  reply-plane paragraph

## Scope

Widest the work admits: intent, decisions, assumptions, design, docs, code,
tests and live behaviour. The lenses organise it; they do not bound it.
Non-goals are the only narrowing and are themselves reviewable.

Whether the two sync gates are the right two, in the right order, and whether a
session obeying them at a dirty checkout with a peer's work in flight ends up
where the wording says it does — drive it in a scratch clone with a planted
dirty peer state. Whether "verify the act" is stated so a session can tell
*which* acts it binds (every command, or the ones whose output does not say they
happened). Whether the drift command reads `origin/main` in a way that works
from a child clone whose remote is not named `origin`. Whether the COMMUNICATION
surfaces still describe an enforcement that exists at HEAD, now that the reply
hook and `plainscan` are gone. **Non-goal:** the children's hand-up findings
that prompted these fixes are not under review — the fixes are.

## The four lenses

1. **Approach & assumptions.** Name the load-bearing assumptions yourself. Each
   fix answers a child's reported defect — test whether the fix answers the
   *class* or only the instance, and whether the new wording creates a new false
   claim (the onramp step 1 sentence about autostash and a peer's dirty work is
   a mechanism claim: verify it against git's actual autostash behaviour).
2. **Correctness & quality.** Diff both commits for the in-scope paths. Run
   every command the delta prescribes, verbatim, in a scratch clone: the two
   gates, the drift command from a child with a differently-named remote, and
   whatever `verify the act` names as its example.
3. **Completeness / harvest.** Search for every other surface prescribing the
   session-start sync or the drift check: `skills/session-onramp/SKILL.md`,
   `session-open-prompt.md`, `skills/queue-run/SKILL.md`,
   `docs/build/REPO-STANDARD.md`. Is the stamped copy inside the floor region
   `stampscan` compares?
4. **Security & privacy** — mandatory. Doctrine prose; the one code-adjacent
   surface is the drift command — check it cannot be made to fetch from or push
   to an unintended remote by a child's config. Discharge the house scanner by
   grounds in one line.

## Re-run obligation

Re-run, do not read. A recorded proof is a claim that can be stale at the commit
that recorded it; a proof you have not re-run is not one you can close on. Lift
floor invocations from `.githooks/pre-commit`, `tools/floor.py` and
`.github/workflows/ci.yml` rather than guessing.

- every prescribed command verbatim, in a scratch clone, states recorded
- `stampscan`, `linkscan`, `pathscan` over the delta's surfaces at HEAD; the
  floor on both planes

## House rules for this run

- You work in the shared review worktree
  `/Users/mike/worktrees/atelier-review-batch-0925` (branch
  `review-batch-0925`), read-only except for THIS brief file. Other reviewers
  are working there at the same time on their own briefs; never open another
  `docs/reviews/2026-09-25-0715-*` file — it is another pass's framing.
- Run **no git command that writes** in the worktree (no add, commit, stash,
  checkout, worktree, reset, clean). Read-only git (`log`, `show`, `diff`,
  `blame`, `branch -r`) is fine. Mutation probes, checkouts of older commits,
  scratch children and scratch linked worktrees go in your own clone: `git clone
  <worktree> <scratchpad>/<PREFIX>/probe` under the session scratchpad, named by
  your prefix so parallel reviewers do not collide.
- One heavy process at a time on this machine: run the full Python suite
  (`python3 -m unittest discover -s tools`, in the foreground, ~minutes) at most
  once, never scan any tree outside the worktree or your scratch clone, and
  never point a scanner at the machine's other repos.
- `/security-review` is **discharged by grounds** for this batch: it reads the
  session's pending diff, which in the shared worktree is other passes' unstaged
  drafts, and this is a landed-delta review. State that line in your lens-4
  answer and deliver the code-altitude read by hand, checked against the OWASP
  catalogue where the work has a code surface.
- Dates in your verdict are absolute ISO-8601 from `date -u` (the hook-plane
  `datescan` reds relative words such as "yesterday" or "next week" tree-wide,
  and would block the orchestrator's commit). Wrap prose at ≤ 100 columns. NZ
  English. Never quote a secret, a placeholder token, an email address or any
  personal detail — this repo is PUBLIC; describe, don't quote.
- Review deep, not fast. A finding needs a probe or a re-driven claim behind it,
  not reasoning alone; a clean lens needs the trail that earned it.

## Deferred reading — do not open before your findings are durably written
<!-- reviewscan:allow:deferral: this section BARS reading and carries no deferred content — the deferred material lives in the sibling .deferred.md, held by the orchestrator outside the worktree under the rule-1 split and released only after the reviewer's phase-1 findings are committed -->

Rule 2 bars until phase 2: `docs/ROADMAP-DONE.md`, `docs/SESSIONS.md`,
`docs/sessions/`, every prior verdict in `docs/reviews/`, the queue pointer
`docs/roadmap/160-doctrine-review-owed/310-rule-4-cold-pass-queued-the-0918-hand-up-fixes.md`
(it carries the author's own lens hints), and:

- `docs/sessions/2026-09-18-0114-queue-run-hand-up-fixes.md`
- the hand-up items `docs/roadmap/320-*/210-*.md`, `320-*/220-*.md`,
  `320-*/260-*.md`, `docs/roadmap/310-*/110-*.md`, `docs/roadmap/020-*/360-*.md`
  (the children's findings, in their words)

Sweep with `python3 tools/coldsweep.py --root
/Users/mike/worktrees/atelier-review-batch-0925 --also-exclude
docs/roadmap/160-doctrine-review-owed/310-rule-4-cold-pass-queued-the-0918-hand-up-fixes.md
<pattern>` — rule 2's default bar, plus `--also-exclude` for the items above;
`--include-barred` only with disclosure in the verdict. Reading the *delta* is
never barred: the code, its tests, the doctrine text and the catalogue entries
are the subject. What is barred is the author's narrative of why, and the
verdicts that recorded it.

## Process

**Phase 1.** Reproduce the floor first; work the lenses and the re-run list.
Then append your verdict below a `---` divider in THIS file: provenance repeated
(how you were spawned, your tier, what you read), per-lens answers, findings
with stable IDs (prefix `HF`: `HF1`, `HF2`, …) and severities (MAJOR / MODERATE
/ minor / note), an overall PASS / PASS-WITH-FINDINGS / FAIL line with counts, a
re-run ledger with the commands and their results, and a follow-up checklist.
Then STOP and report to the orchestrator that phase 1 is written. Do not open
the sibling (it is not in the tree); do not edit the queue pointer, the board,
or any file but this one.

**Phase 2.** On receipt of the sibling's text, append `### Reconcile` beneath
your verdict: per-finding notes against the seeded questions and the intent
records, any finding formed at reconcile marked as such, and the overall line
restated. Never revise phase-1 text. The orchestrator folds the sibling in below
your reconcile.

Findings on doctrine are the principal's to decide (rule 3): record all, apply
nothing; your counsel per finding is welcome, labelled as counsel and kept
beneath the finding.

---

# Verdict — phase 1, written 2026-09-25T07:29Z

## Provenance, repeated

- **Reviewer:** a Fable subagent (`claude-fable-5-1`), spawned by the batch
  orchestrator with this brief as its only framing. Not the author's session;
  neither started nor instructed by any session that landed `54201e0` or
  `f1d5b5e`. Tier checked at claim: Fable, the principal-named tier.
- **Shape:** reviewer-plus-orchestrator, as disclosed above. The orchestrator
  held the `.deferred.md` sibling outside the tree; it was not opened, and it is
  not in the worktree. Phase-2 reconcile follows its release.
- **Worktree:** `/Users/mike/worktrees/atelier-review-batch-0925` at `c4b9cd0`,
  read-only for me except this file. Every mutation probe ran in scratch clones
  under the session scratchpad's `HF/` area (`probe-git.sh`, `probe-p10.sh`,
  outputs in `probe-git.out`); no git command that writes ran in the worktree.
- **What I read before writing findings:** `REVIEW.md` and `00-APEX.md` (the
  rules I run under); the two landing commits' subjects, bodies and in-scope
  diffs; every delta path at HEAD in full; `.githooks/pre-commit`,
  `tools/floor.py --list`, `.github/workflows/ci.yml`, `.atelier-floor.json`,
  `.stampscanignore`, `tools/blockscan_map.json`; the lens-3 surfaces
  (`skills/session-onramp/SKILL.md`, `skills/queue-run/SKILL.md` lines 40–60,
  `skills/create-repo/SKILL.md` lines 125–175, `docs/build/REPO-STANDARD.md`
  grep hits, `docs/method/session-open/session-open-prompt.md`,
  `commands/fleet-pins.md`, `tools/pins.py` lines 280–335 and its grep hits).
- ⚠️ **Exposure disclosed.** The `coldsweep` runs (rule 2's default bar plus
  the pointer) surfaced open board items that are *not* on the brief's barred
  list but carry children's framing on this delta's class, and I opened them:
  `docs/roadmap/030-…/110` and `120` (the `..HEAD` drift defect, filed
  2026-08-16), `docs/roadmap/320-…/150` (the sync command's error text),
  `320-…/180` lines 25–45 (the sibling path from a worktree) and `320-…/230` in
  full (its ninth instance is the drift check). I designed every probe below
  *before* opening them — the probe script was written in the same turn the
  reads were requested — and HF2 is my own finding on the fetch failing, which
  none of those items names. Sweep hit lines only, no file opened: two
  `CHANGELOG.md` lines and four `tools/README.md` lines.

## Per-lens answers

### Lens 1 — approach and assumptions

Load-bearing assumptions, named by me and tested:

1. *`--autostash` puts a peer's dirty work on the shared stash stack, where a
   second session may pop it.* **Partly false.** P1 (planted staged + unstaged
   + untracked peer state, non-conflicting pull): git prints `Created autostash`
   then `Applied autostash`; the stash list and the stash reflog are both empty
   afterwards — the autostash lives in the rebase state, not on `refs/stash`.
   The stack is touched only when the re-apply conflicts (P2), and then the
   entry *is* visible and poppable from a sibling worktree (P4 confirms the
   stack is shared). What the text omits is the harm that actually lands on the
   `320/210` shape: the peer's **staged** files come back **unstaged** (P1:
   `M  s.txt` → ` M s.txt`), and on a collision the peer's file is rewritten
   with conflict markers in the shared tree (P2: `UU a.txt`). The gate is right;
   its reason is inaccurate and understates the damage — HF1. The onramp
   sentence in `CLAUDE.md` step 1 ("autostash shelves dirty work that may be a
   peer's") is accurate as written.
2. *"No remote, no bookend … keeps the stash hazard."* **Not what happens.**
   P3a (no remote) and P3b (remote unreachable): the pull fails before any
   autostash runs; nothing is stashed, the dirty tree is intact. The gate is
   still right — the pull gains nothing and its failure text tells the reader to
   set an upstream, a wrong repair — but the stated reason is not the mechanism
   (HF5). This is also consistent with the `320/210` near-miss being "saved by
   the pull failing first".
3. *The two gates are the right two, in the right order.* Status-first, then
   remote, is the right order (the status read is what discovers a peer; the
   remote check only decides whether the pull is worth running). A third
   condition the evidence exposes is missing: the bookend as written cannot run
   in the worktree gate 1 sends you to, because a fresh worktree branch has no
   upstream (P10) — HF6.
4. *`..origin/main` after a `fetch` measures the published mainline.* **True
   when the fetch succeeds and the remote is named `origin`** (P5b: a checkout
   parked two commits back reports the one new mainline commit; the old `..HEAD`
   form on the same checkout prints nothing). When the fetch fails the chain
   reads as "current" (P6) — HF2. With a differently-named remote the `log`
   fails loudly (exit 128, stderr) with empty stdout (P5a) — noted under HF2.
5. *Each fix answers the class, not the instance.* The sync gates and the drift
   command reached the block, the template and the onramp; they did **not**
   reach `skills/queue-run` (bare bookend, no gate), `skills/create-repo` (runs
   the old `..HEAD` command and calls it the block's check "VERBATIM") or
   `tools/pins.py` (measures every child against the checkout's `HEAD`) — HF3.
   Verify-the-act answers its class in `CONCURRENCY.md` and reaches no
   propagating copy at all — HF8.
6. *"Verify the act" tells a session which acts it binds.* It does not state a
   scope; two readings survive — HF7.
7. *The COMMUNICATION surfaces describe an enforcement that exists at HEAD.*
   The first paragraph and the reply-plane paragraph do; the scoping paragraph
   and "what stays judgement" still speak of a live repo plane and a live gate —
   HF4.

### Lens 2 — correctness and quality

Diffs read for both commits on the in-scope paths; the reviewed sentences are
byte-identical between `54201e0` and HEAD (six later commits touched the files
elsewhere). Every command the delta prescribes was run verbatim in scratch (the
re-run ledger below). The verify-the-act examples all reproduce exactly as the
text says: an `index.lock` collision exits 128 and `git log --oneline -1` is
unchanged (P7a); `git commit <untracked-path>` exits 1 on a pathspec error
(P7b); a backgrounded command reports the wrapper's exit 0 while nothing
committed (P7c). `stampscan` reports the template's stamped block identical to
the canonical region at HEAD and at the landing commit; `blockscan --against
HEAD^` isolated to `54201e0` in the scratch clone is clean, with the concurrency
bullet marked *moved* in both copies. PROPAGATION's "self-removing" →
"removable at the next pin bump — watched by nobody until `310/020`" is honest
and the cited item exists. No overclaim in the doctrine text itself; the
overclaim is in the landing commit's body ("every propagating copy … harmonised
in the same commit"), which is true for the sync gates and drift command and
false for verify-the-act (HF8) and for the three secondary surfaces (HF3).

### Lens 3 — completeness and harvest

Swept with `coldsweep` for `pull --rebase|autostash|..HEAD|..origin/main|log
--oneline <|fetch -q|index.lock|absence of an error` (73 hits, 347 barred
files excluded). Surfaces still prescribing the old form or the ungated form:

| Surface | State at HEAD |
| --- | --- |
| `skills/queue-run/SKILL.md:49` | bare `git pull --rebase --autostash`, no status gate, no remote gate |
| `skills/create-repo/SKILL.md:164–166` | "run the block's own drift check VERBATIM" — `log --oneline <SHA>..HEAD`, "expect: empty" |
| `tools/pins.py:300` | `rev-parse HEAD` of the main checkout; no fetch; no `origin/main` |
| `docs/roadmap/030-…/110` | the exact fix (with `&&`), still `[ ]`, not closed or linked |
| `docs/roadmap/030-…/120` | the fleet sweep the fix triggers, not referenced |
| `docs/decisions/0002-…:16`, `2026-07-21-0748-…:62` | `..HEAD` in two ADRs (records; note only) |
| `session-open-prompt.md` | no sync prescription of its own; points at CONCURRENCY — fine |
| `docs/build/REPO-STANDARD.md` | names "drift check" only by pointer — fine |

The stamped copy is inside the floor region `stampscan` compares: the template's
`stamp:begin … region=floor` block spans both changed bullets, and the scanner
verified it identical (91 lines). `blockscan_map.json` maps the concurrency
bullet to § *Integration hygiene*, so the co-change guard saw this delta; the
map deliberately does not cover the Source & drift bullet, so nothing
mechanical watches that command's copies — which is how the create-repo skill's
copy was missed.

### Lens 4 — security and privacy

`/security-review` is **discharged by grounds** for this batch: it reads the
session's pending diff, which in the shared worktree is other passes' unstaged
drafts, and this is a landed-delta review; its markdown exclusion would make a
pass over doctrine prose definitionally empty in any case. Hand read, checked
against the OWASP Top 10 (2021): the one code-adjacent surface is the drift
command. It runs `git -C "<atelier-path>" fetch -q` — `-C` switches to the
*atelier* checkout, so a child's own config cannot redirect it; `fetch` never
pushes; the remote it reads is the atelier checkout's own `origin`, the same
trust class as that checkout. The placeholder is filled at scaffold time from
the operator's value, not from runtime input, so there is no injection path
(A03). The finding that does map is **A08 software and data integrity
failures**: an integrity check whose failure mode is a silent "current" (HF2)
is a check that can be defeated by an outage. Privacy: the delta names "a
private child" and dates only; no repo, host, person or secret appears, and
`leakscan` on both planes is clean. Nothing in this verdict quotes a secret,
email or personal detail.

## Findings

### HF1 — MODERATE — the sync-bookend paragraph states a mechanism git does not have, and misses the one it does
`docs/method/CONCURRENCY.md` § *Integration hygiene*, **Sync bookends**: "onto
a stash stack shared by the primary checkout and every worktree … never a step
that shelves the peer's work where a second session may pop it." Measured
(P1): a non-conflicting `git pull --rebase --autostash` over a peer's staged,
unstaged and untracked state leaves the stash list and stash reflog empty —
the autostash is created and re-applied without touching `refs/stash`. The
shared-stack exposure exists only on a conflicting re-apply (P2), and then the
tree is *also* left with conflict markers in the peer's file. The harm on the
plain path is that the peer's **staged** files return **unstaged** — the exact
shape of the 2026-09-06 near-miss the paragraph cites (five staged files). A
doctrine sentence that names a rarer harm and omits the common one will be
"checked against git" by the next child and found wanting, which is how
`320/210` arrived in the first place. The gate itself is correct.
*Counsel:* keep the gate; replace the mechanism clause with the measured one:
autostash re-applies whatever was dirty without its index state, so a peer's
staged work comes back unstaged, and on a collision their file is left with
conflict markers and their change on the shared stash stack.

### HF2 — MODERATE — the drift check reads "current" when its fetch fails
The block, the template and PROPAGATION point 4 prescribe `fetch -q` *then*
`log --oneline <SHA>..origin/main` as two commands joined by prose. P6: with the
remote unreachable and the house one commit ahead of the pin, `fetch` exits 128
on stderr and `log` prints nothing with exit 0 — the empty output the bullet
says means "current". The same commit's **Verify the act** bullet names this
exact class ("a grep for failure markers that matches nothing is
indistinguishable from a command that produced nothing"). Board item
`030/110`'s suggested form used `&&`, which P6 shows exits 128 for the chain.
Related, same shape: an atelier checkout whose remote is not named `origin`
makes the `log` fail with exit 128 and empty stdout (P5a); the bullet does not
state the `origin` assumption.
*Counsel:* join with `&&`, add one clause — a fetch error is *no result*, not
*no drift* — and state that the parent checkout's published-mainline remote is
assumed to be `origin`.

### HF3 — MODERATE — the class was fixed in the block and left standing in three house surfaces and one open board item
`skills/create-repo/SKILL.md:164–166` tells the scaffolder to run the block's
drift check "VERBATIM" and then gives `log --oneline <stamped-SHA>..HEAD` with
"expect: empty" — no longer the block's command, and wrong from a parked
checkout (P5b). `skills/queue-run/SKILL.md:49` opens every orchestrated run with
the bare bookend, no status gate and no remote gate — the run pattern is the
one most likely to open onto a dirty shared checkout. `tools/pins.py:300`
classifies every child against `rev-parse HEAD` of the main checkout with no
fetch: the fleet view Mike runs has the `320/260` defect fleet-wide. And
`docs/roadmap/030-…/110`, filed 2026-08-16 with this exact fix, is still open
and unlinked from the landing; `030/120` (the fleet sweep the fix now owes) is
not referenced. The landing commit's "every propagating copy harmonised" was
measured against the block/template/onramp and not against the sweep above.
*Counsel:* one follow-up item that (a) corrects the two skills to the block's
command and gates, (b) gives `pins.py` a `--fetch`/`origin/main` reference with
`HEAD` kept as an explicit option, and (c) closes `030/110` against `320/260`
and points `030/120` at the pin-bump cadence.

### HF4 — MODERATE — COMMUNICATION still describes a live repo plane and a live gate
`docs/method/COMMUNICATION.md` lines 130–142 ("**Each plane is scoped to its
reader** … The repo plane covers only the prose the principal reads … the
scoping is the accepted counter — keep the floor where the human reads, drop it
where none does") and lines 154–159 ("The gate covers the mechanical floor of
readability") are present-tense descriptions of `plainscan`'s repo plane, which
the same file's opening paragraph says was removed on 2026-09-18. The removal
also inverts the paragraph's conclusion: the principal's "opening position" —
remove the repo plane altogether — is what happened, so "the scoping is the
accepted counter" is now false. `tools/README.md` was swept correctly; this
file was swept only at its first paragraph and the reply-plane paragraph.
*Counsel:* recast lines 130–142 as history (the scoping *was* the counter until
the removal) and lines 154–159 to say no gate covers the floor of readability
at HEAD; the calibration instance carries the whole load.

### HF5 — minor — gate 2's stated reason is not the mechanism
"A repo with no upstream … keeps the stash hazard." P3a/P3b: with no upstream
or an unreachable remote the pull fails on tracking/fetch before `--autostash`
runs; nothing is stashed. The gate is still worth having — the pull gains
nothing, and its failure text prompts a wrong repair ("set the upstream to
`origin/<branch>`") — but the doctrine should say that, not a hazard that does
not occur.
*Counsel:* "gains nothing, and fails with a hint that invites a wrong repair."

### HF6 — minor — the bookend cannot run in the worktree gate 1 routes you to
Gate 1 sends a session that finds a peer's dirty work to a worktree. There,
`git pull --rebase --autostash` fails: a fresh worktree branch has no upstream
(P10, exit 1, "There is no tracking information for the current branch"). The
form that works is `git pull --rebase --autostash origin main` (P10, exit 0), or
setting an upstream first. The doctrine hands the relocated session a command
that errors at its first step, and `320/150`'s "spurious failure at open" class
has one more benign member.
*Counsel:* name the worktree form in the bookend bullet, one clause.

### HF7 — minor — verify-the-act does not say which acts it binds
The bullet opens "Reading a check's output covers the check; it says nothing
about whether the act that followed happened" and closes "filtering output is
not reading it". Read one way it binds every state-changing command (confirm
each by effect — heavy, and a workhorse model will do exactly that); read the
other it binds only acts whose output was filtered, backgrounded or blocked.
The examples fit both. Also "for a push, the remote ref" is under-specified:
`git rev-parse origin/main` after a push reads the push's own local
bookkeeping; the remote's truth is `git ls-remote origin main`.
*Counsel:* one scoping sentence — the rule binds any act whose success the
session did not read from that act's own output — and name `ls-remote`.

### HF8 — minor — verify-the-act reached no propagating copy
The child that filed `320/220` reads its block, not `CONCURRENCY.md`. The block
gained the sync gates and the drift command; nothing of verify-the-act. The
Session-rhythm bullet's "evidence-based all-clear" covers the close-out half
only. Whether a clause belongs in the lean block is the principal's call under
the one-bullet-per-concern rule; recorded because the commit body claims every
propagating copy was harmonised.
*Counsel:* either one clause in Session rhythm ("confirm each act by its
effect, never by the absence of an error") or a recorded decision that it
stays behind the pointer.

### HF9 — note — two ADRs still carry `..HEAD`
`docs/decisions/0002-sha-is-the-version.md:16` and
`docs/decisions/2026-07-21-0748-deinstance-create-repo-for-the-plugin.md:62`.
Records are frozen; the RECORD-shaped fix is an addendum line pointing at
`320/260`, if any.

### HF10 — note — a pre-existing pathscan red, outside this delta
`docs/method/session-open/session-open-prompt.md:15` (`../atelier/…`, from
commit `411e96a`) reds warn-only on both planes. It is the `320/180` class and
not this work's; recorded so the floor ledger below is read correctly.

## Overall

**PASS-WITH-FINDINGS** — 0 MAJOR · 4 MODERATE (HF1–HF4) · 4 minor (HF5–HF8) ·
2 notes (HF9–HF10). The four fixes are the right fixes to the right defects;
what fails is the mechanism prose around two of them (HF1, HF5), the
fail-silent shape of the corrected drift command (HF2), the harvest of the
class into the skills and the fleet tool (HF3), and a half-swept COMMUNICATION
(HF4). Nothing here says the landed wording should be reverted.

## Re-run ledger

| # | Command (verbatim or as noted) | Where | Result |
| --- | --- | --- | --- |
| F1 | `python3 tools/floor.py --plane hook --root <wt> --tools <wt>/tools` | worktree | exit 0; 11 ✅, 3 👁️ warn-only; pathscan 1 red (HF10), pointerscan 1 grammar (another item) |
| F2 | `python3 tools/floor.py --plane ci --root <wt> --tools <wt>/tools` | worktree | exit 0; secretscan 22 advisory (design), leakscan clean, same two warn-only reds |
| F3 | `python3 tools/stampscan.py --warn --root <wt> docs/build/templates/CLAUDE.md docs/method/PROPAGATION.md` | worktree | exit 0; template block identical to region `floor` (91 lines) |
| F4 | `python3 tools/stampscan.py --warn --root <wt> <wt>` (CI form) | worktree | clean, 141 files ignored by `.stampscanignore` |
| F5 | stampscan at `54201e0` (scratch clone checkout) | scratch | identical |
| F6 | `python3 tools/linkscan.py --root <wt> <5 delta paths>` | worktree | exit 0 |
| F7 | `python3 tools/pathscan.py --root <wt> <5 delta paths>` | worktree | exit 0 |
| F8 | `python3 tools/blockscan.py --check --warn --root <wt>` | worktree | clean |
| F9 | `blockscan --against HEAD^ --warn` at `54201e0`, `--map` from HEAD | scratch | clean; concurrency bullet *moved* in both copies |
| F10 | `python3 -m unittest discover -s tools -p 'test_*.py'` | worktree | ran once; ⚠️ exit code and footer not captured (zsh `pipestatus`, and a subprocess flushed after the footer) — not re-run per the one-heavy-process rule |
| F11 | `discover -s tools -p test_templates.py` / `test_stampscan.py` / `test_blockscan.py` / `test_pins.py` | worktree | 44 / 90 / 20 / 19 tests, all OK, exit 0 each |
| P1 | `git pull --rebase --autostash` over planted staged+unstaged+untracked peer state, remote ahead | scratch | exit 0; `Created autostash` / `Applied autostash`; stash list 0, stash reflog 0; staged file returned unstaged; untracked untouched |
| P2 | same, peer's dirty line collides with the incoming commit | scratch | exit 0; "Applying autostash resulted in conflicts"; `UU a.txt` with markers; `stash@{0}: autostash` visible from the sibling worktree |
| P3a | same, repo with no remote | scratch | exit 1, "no tracking information"; nothing stashed |
| P3b | same, remote unreachable | scratch | exit 1, fetch fatal; nothing stashed |
| P4 | `git stash push` in worktree, `git stash list` in primary | scratch | entry visible — stack shared |
| P5a | `git -C C fetch -q` ; `git -C C log --oneline <PIN>..origin/main`, remote renamed `upstream` | scratch | fetch 0; log exit 128 "unknown revision", empty stdout |
| P5b | same, checkout parked two commits behind, remote one ahead of pin | scratch | one commit listed; `<PIN>..HEAD` on the same checkout: empty |
| P6 | same, remote unreachable, remote one ahead of pin | scratch | fetch exit 128 (stderr); log empty, exit 0; with `&&` chain exit 128 |
| P7a | `git commit` with a planted `index.lock` | scratch | exit 128; `git log --oneline -1` unchanged |
| P7b | `git commit <untracked-path>` | scratch | exit 1, pathspec error; HEAD unchanged |
| P7c | `( git commit … & ); echo $?` | scratch | wrapper exit 0; HEAD unchanged |
| P8 | up-to-date pull with a peer's staged file | scratch | "Already up to date."; index preserved (no autostash round-trip) |
| P10 | `git pull --rebase --autostash` inside a fresh worktree branch | scratch | exit 1, no tracking information; `… origin main` form exit 0 |

Environment notes: git 2.50.1; `python3` on this machine resolved to 3.14 (the
dispatch said 3.9) — recorded, not material. `date -u` at write: 2026-09-25T07:29Z.

## Follow-up checklist

- [ ] HF1 — reword the sync-bookend mechanism clause to the measured behaviour
      (index lost on re-apply; markers + shared stack only on conflict).
- [ ] HF2 — `fetch -q &&` in PROPAGATION point 4, the block and the template;
      state "a fetch error is no result" and the `origin` assumption.
- [ ] HF3 — one item: fix `skills/create-repo` (drift check) and
      `skills/queue-run` (gated bookend); `pins.py` measures `origin/main` after
      a fetch; close `030/110` against `320/260`; point `030/120` at pin bumps.
- [ ] HF4 — recast COMMUNICATION lines 130–142 and 154–159 as history.
- [ ] HF5 — correct gate 2's reason.
- [ ] HF6 — name the worktree form of the bookend.
- [ ] HF7 — one scoping sentence for verify-the-act; name `ls-remote`.
- [ ] HF8 — principal's call: a clause in the block, or a recorded decision that
      verify-the-act stays behind the pointer.
- [ ] HF9 — optional addendum lines on the two ADRs.
- [ ] HF10 — nothing owed by this pass; the `320/180` item owns it.
- [ ] Any change to the block or template from the above moves the pin the
      children read; `030/120`'s fleet sweep is the delivery cadence.

### Reconcile

Written 2026-09-27 after phase 1 was committed unrevised (`d1ac3e0`, merged in
`1109d49`) and the sibling's text was released by the orchestrator. Opened at
this step, in this order: the sibling; the intent record
`docs/sessions/2026-09-18-0114-queue-run-hand-up-fixes.md`; the hand-up items
`320/210`, `320/220`, `320/260`, `310/110`, `020/360`; and the queue pointer
`160/310`. Phase-1 text above is untouched.

**The seeded question** — *on a dirty tree, does `CLAUDE.md` step 1 say not to
pull, or to pull and expect the stash?* Read as a first-time reader: the step is
a **sequencing** instruction ("`git status` before `git pull …`"), and the only
branch it offers is two sentences later — "uncommitted changes you didn't make
are positive proof another session is live: move to a worktree before touching
anything". A reader who joins those infers *do not pull*; nothing says it. The
block's Concurrency bullet says it outright ("dirty work this session didn't
make means stop and move, never autostash it"), so atelier's own onramp is the
weakest of the three copies of the gate. And the case the question does not
name is answered nowhere: dirty work that **is** yours — a predecessor session
of the same principal cut mid-flight, the exact residue § *Surviving an
interrupted session* expects. P1 shows what the pull then does to it: the
staged half comes back unstaged. Formed at reconcile as HF11 below. On a clean
tree the step's clause is simply inert, which is fine — the question's premise
holds.

**Per finding, against the children's findings and the run's dispositions:**

- **HF1** — *not anticipated; the doctrine inherited the child's mechanism
  claim un-re-driven.* `320/210` asserts "the stash stack is shared … so the
  entry lands somewhere a second session may pop", and its fix sketch point 3
  asks that the stack be "named as shared where the bookend is stated". The
  run's disposition adopted both verbatim; the intent record shows no probe of
  `--autostash` — the "Doctrine inline" paragraph is the whole account. The
  child's own counterfactual ("the same command on a repository where the pull
  succeeds would have moved a peer's work to a shared stack mid-edit") is also
  what P1 falsifies: with five *staged* files it would have re-applied them
  unstaged. The finding stands, and its cause is HF12.
- **HF2** — *not anticipated.* `320/260`'s disposition says "the fetch also
  answers `030/110`'s stale-checkout half"; `030/110` wrote the fix with `&&`,
  the run wrote "then". The fetch was added to answer the stale-ref mode
  (`320/230`'s third failure mode, `030/110`); nobody asked what happens when
  the fetch fails. `320/180` — empty stdout with the error on stderr, already
  filed as the drift command's own silent-negative shape — was on the board and
  was not connected. Stands.
- **HF3** — *partly anticipated, not acted on.* The run knew `030/110` (named
  in `320/260`'s disposition) and left it open. The intent record claims the
  drift command "reads `..origin/main` after a fetch in **all three places it is
  spelled**" — the sweep in lens 3 finds a fourth, `skills/create-repo`, and the
  session-start bookend's fourth copy in `skills/queue-run`; `tools/pins.py`
  was outside the run's sweep entirely. The overclaim is the intent record's,
  consistent with the landing commit's. Stands.
- **HF4** — *not anticipated; a scope-of-item gap.* `020/360`'s delivery line
  is "rewrite the `COMMUNICATION.md` passage that says `plainscan` checks the
  committed prose plane", and that is exactly what was delivered. The
  downstream paragraphs were never in the item's scope, so this is not a
  disposition failure — but the surface at HEAD still describes a live gate,
  so the finding stands on the surface, not on the run.
- **HF5** — *not anticipated; contradicted by the child's own incident.*
  `320/210`'s second condition says a remote-less repo's bookend is "a
  guaranteed no-op that still carries the autostash hazard: all cost, no
  benefit". Its incident says the pull "failed for an unrelated reason before
  the autostash executed" — in a repository with **no remote at all**. P3a
  shows that failure is the no-tracking stop itself, which is why the autostash
  never ran: the near-miss was saved by the very condition gate 2 names, and the
  "still carries the hazard" clause the run adopted is what the incident
  disproves. Stands.
- **HF6** — *not anticipated, and the child's word was the right one.*
  `320/210`'s sketch says "No **upstream**, no bookend"; the doctrine says "no
  **remote**, no bookend". A worktree branch has a remote and no upstream (P10),
  which is the case the child's wording covered and the run's does not. Stands.
- **HF7** — *anticipated by the child, dropped by the run.* `320/220` records
  the child's own widened rule as "any act, not only a destructive one"; the
  house bullet carries the three examples and the filtering clause and drops
  the "any act" scope, which is the ambiguity phase 1 found. "For a push, the
  remote ref" is the child's phrase, adopted without sharpening. Stands.
- **HF8** — *not anticipated.* `320/220`'s sketch put the clause "wherever the
  read-the-output rule is stated"; the run's disposition — "atelier stated no
  read-the-output rule to extend, so the clause landed as its own bullet" — is
  honest and explains why the block was never considered. Stands as filed
  (the principal's call).
- **HF9**, **HF10** — not addressed anywhere in the records; notes stand.

**Findings formed at reconcile:**

### HF11 — minor — the gate is silent on dirty work that is yours, and the onramp copy is the least explicit of three
`CLAUDE.md` step 1 sequences `git status` before the pull and leaves "so do not
pull" to inference; the block says it. Neither, nor `CONCURRENCY.md`, says what
to do when the dirty work **is** the session's own or its predecessor's — the
interrupted-session residue the doctrine elsewhere expects — and P1 shows the
pull round-trips it with its index state lost.
*Counsel:* make the onramp match the block ("dirty and not yours: stop and move;
never autostash it"), and add the one missing branch — dirty and yours: commit
or reverse-edit it first; the bookend runs on a clean tree only.

### HF12 — minor — three mechanism claims went from a hand-up into doctrine without being re-driven
The shared-stack pop hazard (HF1), "keeps the stash hazard" (HF5) and
remote-for-upstream (HF6) were each the child's testimony, adopted by the run
as written; the intent record shows no probe. `CONCURRENCY.md` § *The channel*
already says a peer's claim is a hypothesis to re-run, and `00-APEX.md` says
doctrine changes ride on repeatable proof — this is a countable instance of
those rules not firing at the moment a hand-up becomes house text, which is
the reading § *Pointing up* invites ("consideration … is atelier's"). Recorded
as the class behind two findings, not as a third copy of them; severity minor
because the gate the child asked for is correct and the false clauses are
explanatory.
*Counsel:* one clause in § *Pointing up* → *The duty*: a hand-up's mechanism
claim is re-driven before it is written into a method file, and the record
says so.

**Overall, restated:** **PASS-WITH-FINDINGS** — 0 MAJOR · 4 MODERATE (HF1–HF4)
· 6 minor (HF5–HF8, HF11, HF12) · 2 notes (HF9–HF10). Nothing in the sibling
or the records changes a phase-1 severity; the records explain the causes and
add two minors.

## Deferred material — folded in at reconcile

# Deferred material — 0918-hand-up-fixes (open only after your findings are durably written)

Sibling of `docs/reviews/2026-09-25-0715-0918-hand-up-fixes-cold.md` under
REVIEW.md rule 1's split; held by the orchestrator outside the worktree. Folded
into the brief below the verdict when the verdict lands.

## Intent records

- `docs/sessions/2026-09-18-0114-queue-run-hand-up-fixes.md` — the run's
  account. **Not opened by the brief-writer**; its `docs/SESSIONS.md` index
  entry was read at onramp and names the four doctrine fixes in the author's
  words.
- the hand-up items listed under barred reading — each carries the child's
  finding and the run's disposition.

## Prior verdicts and barred items on the same surfaces

- `docs/sessions/2026-09-18-0114-queue-run-hand-up-fixes.md`
- the hand-up items `docs/roadmap/320-*/210-*.md`, `320-*/220-*.md`,
  `320-*/260-*.md`, `docs/roadmap/310-*/110-*.md`, `docs/roadmap/020-*/360-*.md`
  (the children's findings, in their words)

## The queue pointer's own lens hints — the author's seeded questions, verbatim

The pointer carries no lens paragraph — refs only.

## Brief-writer's seeded questions (a floor, never a fence)

Generate your own before reading these; a question you did not think of is a
prompt to re-read the surface, not an agenda.

1. The brief-writer's own onramp ran `git status` then `git pull --rebase
   --autostash` on a clean tree, as `CLAUDE.md` step 1 now instructs. The step
   says autostash "shelves dirty work that may be a peer's" — on a clean tree
   nothing is shelved; on a dirty one, is the instruction to *not* pull, or to
   pull and expect the stash? Read it as a first-time reader would.
