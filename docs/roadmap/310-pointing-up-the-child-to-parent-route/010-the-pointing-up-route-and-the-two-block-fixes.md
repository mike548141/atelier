- [x] 🎯 **The upstream route, and the two block defects that caused the
      instance** `[M][docs]` — **DELIVERED 2026-08-18** on
      wt: `pointing-up-doctrine-0818`. Mike commissioned it the same day;
      rule-4 cold pass queued at `050`.
      **What landed.** `PROPAGATION.md` **§ *Pointing up* — when a child earns
      a house rule**, in four parts: a **check-the-parent-first** rule that
      bars a session from reasoning about house doctrine out of its own inlined
      block; a **whose-rule-is-it test** (*would this rule be true in a repo
      that shares none of this repo's stack?*); a four-step route — file the
      finding in atelier's board, carry the class and never a private child's
      specifics, keep at most one repo-shaped pending-upstream line, put the
      incident in the child's own record; and the loop's close, where that line
      is replaced by a pointer at the child's next pin bump.
      **The two block fixes, which are the mechanised half.** The canonical
      concurrency bullet said *read the staged hunk headers before every
      commit* and pointed at `CONCURRENCY.md` § *The channel*. The phrase reads
      as covering only what you staged — the exact misreading that produced the
      instance — and the pointer names a section the rule is not in, so a
      session following it correctly found nothing. Both corrected in
      `PROPAGATION.md` § *The standard child doctrine block* and in
      `docs/build/templates/CLAUDE.md`, which stamps new repos.
      **And the rule itself gained the clause it was missing** in
      `CONCURRENCY.md` § *The trigger*: read `git diff --cached` as the **whole
      index, not your own hunks**, and a stale entry needs no live peer — an
      index outlives the ref that fed it, so a path staged before you opened or
      left behind by a peer's `update-ref` landing is still there to be
      committed under your message. Bearing added for the instance.
      **Pointer added** in `GUARDS.md` § *A rule with no home is not a rule* —
      its "queue the homing" clause is what a child session reads at the
      moment this decision is made, and it did not say the home may be in
      another repo.
      **Not done here, deliberately:** the enumerator (`020`), the fleet's pin
      bumps (`030`), and the child-side finding (`040`), which Mike ruled waits
      on this cycle's review.
