# 2026-08-15 · 0809 UTC · The Laws leave the apex (Fable, wt: laws-removal-0815)

**Mike's ruling, verbatim:** *"I have decided that as much as I like
Asimov's 3 laws (+ the zeroth law) they don't belong in the atelier
doctrine and particularly not in the apex. I want you to remove them
completely and cleanly from the doctrine and repo. It is fine that they
will still be in the repo history."*

This executes the sitting the board carried as the policy-as-code
section's `210-…` item (intent stated 2026-08-04 while ruling the ZL
pass; ZL2–ZL5 were ruled then to lapse into this removal). Claim landed
on `main` first (`4ea0a8f`), worktree before the first edit.

## What was removed, and from where

- **`docs/method/00-APEX.md`** — § "Then the Laws" deleted in full
  (Zeroth + the numbered three + the honest-caveats paragraph). Title now
  "honesty, then adaptation"; § "Why this is level 0" reads a duo ("these
  two"), not a trio.
- **Restatement surfaces**, per the ZL1-widened sweep checklist:
  `README.md` (apex line + session-onramp line), `docs/method/README.md`
  (read-order item 1), `GLOSSARY.md` (Apex entry), `PRINCIPLES.md` (§0
  intro + the precedence-ladder clause), `PROPAGATION.md` (the fail-safe
  "inherits" line, the SR2 seven-concerns list — now "the apex", still
  seven bullets — and the inlined child floor block), the byte-identical
  `docs/build/templates/CLAUDE.md` stamp, and the plugin surface
  `skills/session-onramp/SKILL.md` (frontmatter description + the whole
  "Then the Laws" bullet).
- **The Laws-ladder raw note** (`docs/roadmap/270-open-questions/030-…`)
  deleted: its entire subject was the Laws (it quoted them verbatim), so
  it left with them. Flagged to Mike at close — recoverable from git
  history if he wants the world-model thread back under a new frame.

## Judgement calls, said aloud

- **The "surface a genuine dilemma; never silently resolve it" line**
  (floor block + skill) was removed too: it summarised the Laws caveat,
  which no longer exists, and a floor line may not outlive its canonical
  home in the apex. If Mike wants dilemma-surfacing kept as doctrine, it
  needs one new apex line — flagged at close.
- **Records stay.** Session logs, review verdicts (including the
  2026-07-26 Zeroth cold pass), `ROADMAP-DONE.md` and ADR narratives
  keep their Laws mentions: they are the repo's history layer, and the
  ruling blessed history keeping them. Live doctrine, build, skills and
  commands surfaces grep clean of Asimov/Zeroth/Laws references.
- **Children's floor blocks** still carry the old Laws sentence until
  each repo's next pin bump — the standing propagation lane
  (`160-doctrine-review-owed/140-…`, updated to say so). No child was
  edited from this session (work locality).

## Board and review state

- `020-…/210` closed `[x]` with the sweep record; the ZL cycle in
  `160-doctrine-review-owed` is CLOSED (ZL1 applied + reviewed
  2026-08-05; ZL2–ZL5 lapsed, now executed).
- **Rule-4 cold pass queued** (`020-…/215-…`, ⏳ Fable, cold):
  principal-ruled but agent-applied apex doctrine — queued, not spawned,
  per review independence.
- Floor: scanners green locally through the pre-commit hook; the
  pushed-floor run at head is the all-clear and is reported in-session
  at close.
