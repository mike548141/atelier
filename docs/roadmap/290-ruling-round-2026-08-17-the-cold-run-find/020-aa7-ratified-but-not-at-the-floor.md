- [ ] 🎯 **RULED 2026-08-17 — "Ratify, but not at the floor."** AA7 found that
      the apex's conversion of an unbriefed approval from *not a decision the
      doctrine recognises* to *stands as the principal's word but is open to
      challenge* was the author's derivation: the ruling it claims to record
      never mentions extracted approvals or `REVIEW.md` rule 3. Mike ratified
      the derivation **with one exception** — at the always-confirm floor an
      unbriefed approval must be **re-briefed before any irreversible action**.
      - [ ] Apply to `00-APEX.md` and every surface that restates it
            (`RECORD.md`, `AUTONOMY.md`, `CONCURRENCY.md`, `REVIEW.md` rule 3,
            the `PROPAGATION.md` floor block, the byte-identical
            `docs/build/templates/CLAUDE.md` stamp). Self-authored doctrine —
            queue its own rule-4 `⏳` at landing.
      - [ ] **AA6 closes with it.** AA6 named the gap as the divergence between
            pause-and-re-brief and execute-now at exactly an unbriefed
            irreversible floor approval; this ruling decides that case, so the
            application discharges both. AA13 travels with AA6's fix.
