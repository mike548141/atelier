# 2026-07-22 · 04:35 UTC · ECONOMICS.md rename — FinOps ruled out, decision stamped

**Trigger.** Mike floated `FinOps` as an alternate name for
`MODEL-ECONOMICS.md`, noting the roadmap already carried a rename item (it
does — logged 2026-07-21, recommendation RENAME to `ECONOMICS.md`, do not
split).

## What happened

1. **FinOps assessed and ruled against** (`9f08d91`). Three grounds, now
   recorded on the roadmap item as a considered alternative:
   - FinOps is a branded industry term (FinOps Foundation) for the *practice*
     of cloud cost accounting — billing visibility, showback/chargeback. The
     file is spend *doctrine*: tier choice, pool awareness,
     cost-is-lowest-precedence. Economics (how to reason about spend), not
     FinOps (how to account for it).
   - Plain-over-jargon (COMMUNICATION doctrine, public audience): "Economics"
     needs no glossary; FinOps does outside enterprise-architecture circles.
   - The `method/` shelf is plain concept nouns (`CONCURRENCY`, `EVIDENCE`,
     `PROPAGATION`); `FINOPS` would be its only vendor-flavoured name.
2. **Decision made and stamped** (`687e8c9`). Mike: rename to `ECONOMICS.md`,
   do not split — accepting the standing recommendation. The roadmap item now
   leads with the DECIDED stamp; what remains is execution only.

## Deliberately not done

The rename itself and its PROPAGATION ref sweep (ros `CLAUDE.md`, the ros
private `docs/MODEL-ECONOMICS.md` counterpart, atelier cross-refs, the
`docs/build/templates/docs/MODEL-ECONOMICS.md` template copy) stay **queued
for another session** at Mike's direction. The `[ ]` stays open.
