- [~] 🎯 **The posture section, and the fourth requirement it puts on every
      guard** `[M][docs]` (claimed 2026-08-17-0900, wt: posture-0817) — Mike
      commissioned and ruled 2026-08-17.
      **Scope.** A posture section in `PRINCIPLES.md` carrying his four worked
      cases and the inverse diagnostic; one new situation test beside
      **Gate sizing**, which is the nearest existing line and is deliberately
      weaker (it sizes a control to its threat; the posture asks whether the
      control is needed at all once the failure is cheap); and in `GUARDS.md`, a
      **fourth requirement** beside *narrow, noisy, reasoned* — his own three
      from 2026-08-05 — because ruling 2 makes this a test guard work must pass
      rather than a stance held near it.
      **What the fourth requirement asks**, stated so it can be answered rather
      than admired: does this guard make the failure **cheap**, or does it only
      **forbid the act**? Both are legitimate answers; the defect is not
      declaring which. That joins the finding already filed at
      [`115/130`](../115-guardrail-architecture-mike-commissioned/130-a-guard-reports-whether-its-rule-fired-at-all.md) —
      every registry entry carries a `why` that is printed and compared to
      nothing. The posture is the missing standard to compare it against.
      **Deliberately NOT in this item:** re-ranking the open guard board against
      the new test. That is `020`, and it wants a fresh session — see its own
      reasoning.
