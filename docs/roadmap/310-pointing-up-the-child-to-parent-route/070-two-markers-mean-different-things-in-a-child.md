- [ ] 🚩 **Two board markers mean different things in a child than they do
      here, and the child is acting on its meanings** `[S][docs]` — filed from
      faves 2026-08-18 by § *Pointing up*. Reported, **not repaired there**,
      because repairing it in either direction is a standard change and the
      standard is the house's.

      | Marker | This repo | faves' board |
      |---|---|---|
      | `[~]` | **claimed** by a live session — *"don't start a `[~]` item; take the next open one"* | **part-done OR claimed**, told apart by the item's text; a non-claim `[~]` is *"free to pick up"* |
      | `⏳` | **review queued** for a non-author, on the principal-named review tier (`REVIEW.md` rule 4) | **waiting on someone** — in practice, on the owner |

      🔑 **These are not sloppiness; each is a child solving a real problem the
      house vocabulary does not cover**, which is why they belong in the same
      cycle as `060` rather than as a correction to that repo.
      - **`[~]`** — faves has items that are genuinely *partly delivered* with
        the remainder named inline. The house has no state for that, so `[~]`
        absorbed it. **The cost is measured:** a session reading the parent's
        rule literally skips work this board believes is available, and a
        session reading the child's rule may start an item a peer holds. That is
        the same *in-flight* gap `060`'s option 3 names.
      - **`⏳`** — faves does not run `REVIEW.md` rule 4 at all, so the house's
        meaning has no referent there, and the marker was reused for *waiting on
        the owner*. Three items carry the local sense today. 🚩 **Worth noting
        the shape: a state that is meaningless in a child does not stay unused,
        it gets repurposed** — and a repurposed marker is invisible to anyone
        reading the parent's legend.

      **Options, offered not recommended.** (a) Add the missing states so
      neither reuse is needed — folds into `060`. (b) Declare `⏳` house-only and
      give children a distinct *waiting-on-the-principal* marker. (c) Rule that a
      child may narrow a marker's meaning but must declare it in its own legend —
      which is what faves now does, so this would ratify current practice rather
      than change it. (d) Leave both and accept the ambiguity, recorded.

      📌 **faves holds a dated, self-removing pending-upstream line** telling its
      sessions to read a `[~]`/`⏳` item's own text before acting on the marker,
      naming this item. It removes itself at the pin bump that carries the
      answer.
