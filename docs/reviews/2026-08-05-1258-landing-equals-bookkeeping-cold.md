# Brief — landing-equals-bookkeeping preamble clauses, rule-4 cold pass

**Queue ref (refs only):** the landing-equals-bookkeeping preamble clauses.
Delta: the two new sentences in `ROADMAP.md`'s checkbox-states preamble
paragraph — the `[x]`/harvest single commit, and the inline-claim close
(landed 2026-08-03, `0f36356`). Intent record: `ROADMAP-DONE.md`
§ *Doctrine — the harvest rides the `[x]` commit* + the SESSIONS.md entry
of 2026-08-03 (orchestrated run).

**Pass type:** doctrine cold pass (rule 4 — preamble rules govern every
future session's state-change commits).

**Provenance (rule 4):** reviewed by a Fable session Mike spawned onto the
queue; the author sessions neither started nor instructed this session.
Brief written by the taker. Ceremony proportionate to a two-sentence
delta, per ECONOMICS' match-the-ceremony rule — the lenses ran, briefly.

# Verdict — PASS · 0 MAJOR / 0 minor / 2 notes

**Attacked cold, and what held:**

- **Clause 1 (`[x]` + harvest, one commit) is not just stated — it is
  mechanically self-enforcing on the hook plane**, and the clause is the
  correct reading of why: the cold-content gate reds an `[x]` on the hot
  path at commit time, so the only way through the hook is the harvest
  riding the same commit. The clause documents a forcing function that
  exists, states its grounding (the 2026-07-26 pushed-floor red window,
  both SHAs), and resolves a real tension in the legend — the `[x]` state
  can exist at rest only in the archive, which the tri-state definition
  alone did not say.
- **Clause 2 (inline claim closes in the landing commit) names the right
  mechanism**: the worktree merge was the forcing function everyone else
  had, and the `wt: none` item was missed *because* it lacked one — the
  grounding case (`b89a306`) is argued from structure, not carelessness.
- **The enacting commit practised all three rules it joins**: the clauses,
  their grounding's harvest to `ROADMAP-DONE.md`, and this pass's own
  rule-4 pointer landed as one commit (`0f36356`, verified) — AWA2's
  pointer-at-landing, clause 1's harvest-at-`[x]`, and the orchestrated
  run's inline claim closing per the clause it was landing.
- **Family coherence**: the preamble now binds all three state changes
  (pointer queue, harvest, claim close) under one named rule — a state
  change and the bookkeeping the floor demands of it ship together — and
  the forcing-functions section's account matches.

## Findings

**LB1 (note) — clause 2 has no mechanical form, and the family suggests
one.** The `[x]`/harvest clause is enforced by the cold-content gate; the
inline-claim clause is enforced by nothing — a `[~] (wt: none)` whose work
shipped still reads as owned until a human notices, which is the exact
shape `pointerscan`'s cycle detector mechanises for review states. One
recorded instance is not a funding case (the repo's own bar is three);
recorded so a second stale inline claim finds the ladder already built.

**LB2 (note) — the legend's `[x]` definition and clause 1 read together
imply archive-only-at-rest, but neither says it.** "No more work owed,
with the disposition said in the item's own text" invites leaving a
dispositioned `[x]` in place; the gate forbids exactly that on the hot
path. Clause 1 resolves it operationally; a half-sentence in the legend
("an `[x]` lives on the hot path only inside its harvest commit") would
close the read-alone gap. Mike's call whether the preamble's density can
afford it.

## Reconcile (intent records opened after the findings above were committed)
