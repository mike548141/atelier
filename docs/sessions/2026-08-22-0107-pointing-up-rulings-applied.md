# 2026-08-22 · 0107 UTC · The pointing-up ruling round, applied

**Tier:** Fable (the pass's reviewer, same session).
**Worktree:** `pointing-up-rulings-0822`. **Channel:** the structured ask
device, per the day-old doctrine — six findings walked individually after
Mike bounced the first compound ask (*"Take me through them
individually"*, and on the jargon-dense backlog ask, *"Your question
makes zero sense"* — the re-ask in plain words got his ruling in one).

**Rulings applied here:** PU-1 — § *The trigger* now prescribes plain
`git diff --cached`, correction note in place, shed note aligned. PU-2 —
the veil dropped: `cbom` named at all three doctrine sites with the
ruling's date, class-rule-stands note beside it. PU-3/4/5 boarded at
`310/100`, `310/110`, `210/110`. Item `050` closed as run-and-ruled.
**Cycle stays open** (the pass returned 1 MAJOR): the application's own
rule-4 pointer is queued at `160/280` in this landing commit — the
applier authored the verdict, which REVIEW.md's neutral-hand preference
counts against; Mike directed the application live, and both facts are
in the pointer for the taker.

**Not done, and whose it is:** the `040` cbom-side queueing stays open —
Mike's sequencing gates it on the cycle, which now means `160/280`; the
docker-heap ruling round landed in that repo's records the same hour.
