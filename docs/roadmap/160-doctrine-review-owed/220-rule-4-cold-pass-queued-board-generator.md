- ⏳ **Rule-4 cold pass queued — the board generator's child-facing strings.**
      Delta: the generator commit on wt: board-generator-child-truth
      (`tools/board.py`, `tools/pointerscan.py`, `tools/README.md`,
      `CHANGELOG.md`) plus the board commits `e2551da` and the `080`/`070`
      updates that landed with it. Intent record:
      `docs/sessions/2026-08-17-0530-board-generator-child-truth.md`.
      Tier: Fable. Pass type: code cold pass, per `method/REVIEW.md` rule 4.
