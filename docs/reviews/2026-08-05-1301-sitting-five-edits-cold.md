# Brief — the 2026-08-04 sitting's five small doctrine edits, rule-4 cold pass

**Queue ref (refs only):** the five small doctrine edits ruled at the
2026-08-04 sitting, one landing (`53785b9`, 2026-08-05). Delta:
`docs/method/REVIEW.md` (the tier bar + stop clause at rule 4's spawn
criterion; the withdrawn-pass general-case paragraph); the `⏳` legend
sentence in `ROADMAP.md`'s preamble; `docs/method/RECORD.md` (the
show-first bulk-deletion rule; the describe-don't-quote rule);
`docs/method/AUTONOMY.md` (the show-first cross-link);
`docs/method/PRINCIPLES.md` §6 (the restatement-sweep surface list);
`skills/session-onramp/SKILL.md` (the apex block re-taught to the Zeroth
form). *Intent record:* `ROADMAP-DONE.md` § *The 2026-08-04 sitting's five
small doctrine edits* + the apex Zeroth Law cold pass (ZL1) — opened at
reconcile only.

**Pass type:** doctrine cold pass (rule 4).

**Provenance (rule 4):** reviewed by the same Mike-spawned Fable taker
session as the 2026-08-05 queue take; the authoring session
(`queue-batch-0806`) neither started nor instructed it. Claim `52bb1a6`
on `main` before the work. Brief written by the taker, cold.

# Verdict — PASS · 0 MAJOR / 0 minor / 2 notes

**Each edit verified against its ruling, and attacked:**

- **Tier bar + stop clause** — stated at both ruled points of use (rule
  4's spawn criterion and the `⏳` legend), principal-named so it survives
  model generations, with the stop clause and the rejected-pass grounding
  in the REVIEW.md copy and the ECONOMICS cross-reference tying it to
  tier-is-set-by-risk. The two copies agree.
- **Withdrawn-pass promotion** — the general case covers the three ruled
  rejection shapes (content, how-run, half-finished); the three binding
  consequences (the `⏳` stands, no reading before the redo's own verdict
  is committed, findings die with the pass) match the directory README,
  which stays as worked mechanics and exists at HEAD.
- **Show-first** — the RECORD.md rule demolishes both intuitions
  ("my own mess", "git remembers") with the 2026-07-25 grounding, names
  `harvestscan`'s warn as the mechanical net beneath it and scopes itself
  to what the net cannot see; the AUTONOMY.md cross-link binds it even on
  the agent's own creations and carves it out of the standing grant
  explicitly. The two layers point at each other and neither restates the
  other's grounding.
- **Describe-don't-quote** — recurrence-not-severity stated with all four
  instances; the fixture/canary carve-out is drawn exactly where the SF3
  canary suite needs it (suites quote by design; record stores describe).
- **Restatement-sweep surface list** — the whole-repo list (doctrine,
  plugin surfaces, templates, children via the propagation lane) with the
  add-to-the-list rule for unlisted surfaces. `commands/` exists (four
  files) and carries no apex restatement at HEAD.
- **ZL1 skill fix** — the skill's Laws block now teaches the Zeroth form
  and compresses the apex faithfully (Zeroth read first, standing apart
  from the numbering; the four-step ordering as ethic; dilemma-surfacing).
  **The ZL1-widened sweep, run by this pass at HEAD: zero stale
  restatements on any teaching surface** — `PROPAGATION.md` and the child
  `CLAUDE.md` template already carry the Zeroth-form ordering; what
  remains is children's inlined floor blocks via the propagation lane,
  exactly as the ruling recorded.

## Findings

**SE1 (note) — the sweep's clean result is itself worth keeping.** AW2
counted "at least seven in-repo apex restatements" describing the
superseded shape; at HEAD the teaching-surface sweep finds none. The
seven were evidently cleared across the intervening landings, but no
single record says so — this line is that record, so the planned Laws
removal can start from a verified-clean baseline plus the propagation
lane, not from a re-count.

**SE2 (note) — the tier bar now lives in three surfaces.** REVIEW.md rule
4, the `⏳` legend, and ECONOMICS' capacity-never-picks-the-tier edge are
mutually consistent at HEAD; a future change of the named tier must sweep
all three, and the trio is exactly the class PRINCIPLES §6's new list
exists to catch. Recorded so the list's first user knows the tier bar is
on it.

## Reconcile (intent record opened after the findings above were committed)
