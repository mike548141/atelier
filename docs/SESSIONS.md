# atelier — session log (index)

Append-only, newest last; one line per session. Tail-read at session start.
Full detail per session lives in `docs/sessions/<date>-NN-slug.md` — open a
file only when its line needs unpacking (RECORD.md's index/detail split;
restructured to this shape 2026-07-10, entries preserved verbatim in the
detail files).

---

- [2026-07-10 · repo born](sessions/2026-07-10-01-repo-born.md) — atelier extracted from the Mike↔Claude operating model; method/ first slice stood up (00-APEX, AUTONOMY, STORAGE, CONCURRENCY, TOOLBOX); PRINCIPLES + MODEL-ECONOMICS honest stubs; local commit only.
- [2026-07-10 · pushed, reviewed, corrected](sessions/2026-07-10-02-pushed-reviewed-corrected.md) — private `mike548141/atelier` pushed; foundation review (2 Fable + 1 harvest) PASS-WITH-FINDINGS; autonomy-floor gaps closed; canonicality decided + APEX↔ros §0 DRY breach fixed; ROADMAP re-sequenced mechanism-before-content.
- [2026-07-10 · estate-access + data covenant](sessions/2026-07-10-03-estate-access-data-covenant.md) — Mike's data covenant → method/DATA-PROTECTION.md; model-capability who-acts axis in AUTONOMY; estate-access expansion agreed (least-privilege, read-first, widen-in-rings); nas02 two-credential plane split.
- [2026-07-10 · propagation mechanism built + children stamped](sessions/2026-07-10-04-propagation-mechanism-children-stamped.md) — method/PROPAGATION.md (thin anchor, fat pointer; SHA-as-version; layer-override rule; enforcement clause); ros + faves retrofitted with the stamped block at c3676ee; drift check behaviourally proven.
- [2026-07-10 · mechanism, then the harvest extraction (Opus)](sessions/2026-07-10-05-mechanism-then-harvest-extraction.md) — EVIDENCE.md (A1), REVIEW.md (A2), RECORD.md (A3) written grounded-not-invented; PRINCIPLES.md extracted stub → canonical general spine; ros transitional DRY breach flagged + trim deferred to a focused ros session; pins bumped c3676ee→dde4170.
- [2026-07-10 · review brief + leakscan, then seed + hooks (Opus)](sessions/2026-07-10-06-review-brief-first-mechanical-control.md) — method/-layer Fable review briefed (gates further extraction); tools/leakscan.py built (structural + machine-local terms, bit on its own fixtures); term list seeded, hooks installed atelier + ros tiki/-scoped, tiki scrub live-validated; --disable + --staged subtree added.
- [2026-07-10 · parallel-work tooling: worktree.py (Opus)](sessions/2026-07-10-07-parallel-work-tooling-worktree.md) — tools/worktree.py (start/list/land/remove) delivering CONCURRENCY's worktree-per-line with the doctrine baked in as guards (iCloud-base refusal, stale/dirty flags, no-lose-work remove); 12 tests + live round-trip; Mike handed the recipe to run the method/ review as a parallel line.
- [2026-07-10 · the gated method/ review: PASS-WITH-FINDINGS, gate cleared (Fable)](sessions/2026-07-10-08-fable-method-review-gate-cleared.md) — whole method/ layer reviewed at 957fa08; 10 findings [fixed] (trust-surface floor gap, REVIEW reframed to independence-as-core — the "Fable is the cheaper tier" premise was wrong), 3 [backlog] (pin view, ADRs, SESSIONS split); extraction unblocked, ros trim guidance in lens-1 answer 12.
- [2026-07-10 · review follow-through: ros trim + ADRs + SESSIONS split (Fable)](sessions/2026-07-10-09-fable-review-followthrough-trim-adrs-split.md) — the reviewer executing its own verdict: ros PRINCIPLES trimmed to bearings + case-law (ros 73fd50b, DRY breach closed), ADRs 0001–0004 written, SESSIONS.md split to this index + verbatim detail files; remaining P2 pin view + further extraction flagged as Opus work.
- [2026-07-10 · the fleet pin view: tools/pins.py (Opus)](sessions/2026-07-10-10-fleet-pin-view.md) — built the last method-review backlog finding (P2): a read-only roll-up of every child's atelier pin vs HEAD (current/behind/ahead/diverged/unknown/no-pin, --log/--json/--check), the fleet companion to PROPAGATION's per-child drift check; deliberately never bumps (bumping stays per-repo human-in-the-loop); 12 tests, suite 86→98; live-proven faves 9 behind, ros current; PROPAGATION caveat updated to name it observability-not-enforcement.
- [2026-07-10 · MODEL-ECONOMICS.md: stub → canonical (Opus)](sessions/2026-07-10-11-model-economics-extraction.md) — promoted the stub to canonical: kept its shape (match-model-to-job, which-pool self-check, tiered authority, review triggering) and extracted the general session-hygiene mechanics + cache economics from ros (per-model cache, TTL churn, point-don't-paste, one-task, heavy-skills, hot-path-lean); estate-specific numbers stay person-local in ros with a foot-pointer naming the split; README/method-README/ROADMAP/CHANGELOG swept off "stub".
- [2026-07-10 · parallel-worktree reconciliation + E3 salvage + cleanup (Opus)](sessions/2026-07-10-12-worktree-reconciliation-e3-salvage.md) — traced a dangling parallel worktree line (independent method-review verdict draft on atelier-method-review@3bfcbbc, never landed; main had its own reworked verdict); nothing lost (both pushed to origin). Side-by-side of the two verdicts: 12/14 findings common+on-main, 3 main-only (all done), 3 worktree-only — salvaged E3 (two-register EVIDENCE §1) to main (48fa5ff), P2/PR2 consciously dropped. Cleanup way-back-verified: archive tag + kept remote branch, removed worktree, deleted local branches.
- [2026-07-10 · EVIDENCE §13 + §14: A6/A7, extraction section closed (Opus)](sessions/2026-07-10-13-a6-a7-extraction-close.md) — last extraction line: source-acquisition escalation ladder (§13, active counterpart to §3 — climb to the *cost of being wrong*, state the gap when blocked) + honest-instrument doctrine (§14 — an instrument the agent builds is a source; its "ok"/"applied" is a claim the apex governs: verified-not-attempted, silent-success-is-a-defect, "unknown"-is-required, known-failure-test enforces). Grounded in §3/§11 + PRINCIPLES §6; ros diagnose/apply phantom-success named as the estate instance. Review-owed (doctrine text).
- [2026-07-10 · build/ layer opened + secrets doctrine (Opus)](sessions/2026-07-10-14-build-layer-opened-secrets-boundary.md) — three grounded doctrine docs, each pointing up to method/ not copying it: `build/REPO-STANDARD.md` (A10 — the create-repo standard extracted: product-in-subfolder, sizing-to-type, honest-CI, the two processes; instance specifics stay in the skill), `method/SECRETS.md` (reproducible/re-mintable secrets → detect→rotate→burn-cost-is-minutes; closes AUTONOMY's forward-reference; slotted method/README #5), `build/REPO-BOUNDARY.md` (is-this-a-repo by independent-lifecycle discriminators → standalone/component/monorepo-folder; prefer reversible when ambiguous). Skipped the skill-rewire deliberately (don't stack delivery on unreviewed text). All review-owed; ros pin left at 3ba6275.
- [2026-07-10 · access-onboarding doctrine + licence gate (Opus)](sessions/2026-07-10-15-access-doctrine-licence-gate.md) — two non-stacking items: `method/ACCESS.md` (safe-access-onboarding runbook — sequences AUTONOMY/DATA-PROTECTION/SECRETS into grant-recorded→narrowest-cred→store-first→read-only-first-ring→destructive-gate-before-power→widen-in-rings→Zero-Trust; method/README #6) and `tools/licenscan.py` (A11 — pre-publish licence-consistency gate completing the leakscan·secretscan·licenscan triad; 3 checks, copyleft-into-permissive blocks; `--expect <SPDX>`; caught a real line-number bug + its own self-scan in the build; 35 tests, suite 98→133, atelier clean at Apache-2.0). A5 SBOM/signing deferred (tool-install floor + zero-dep break). ros pin left at 3ba6275; **review debt now the standout — next session a strong Fable-sweep candidate.**
- [2026-07-10 · review brief: the post-method-review batch, gate raised (Opus)](sessions/2026-07-10-16-review-brief-post-method-batch.md) — non-stacking Opus move on the flagged review debt: wrote the consolidated Fable brief for the `957fa08..f72031c` batch (`docs/reviews/2026-07-10-post-method-review-batch.md`) — five doctrine docs (EVIDENCE §13/§14, build/REPO-STANDARD, build/REPO-BOUNDARY, method/SECRETS, method/ACCESS) + the scan-triad approach review, 16 grounded assumptions to attack (scan false-negative classes the highest-stakes, `--selftest`-first, ros cross-read as the real-world check). ROADMAP review-gate section supersedes the scattered Review-owed tags; create-repo rewire stays blocked until the verdict. ros pin 3ba6275→f72031c (drift check fired, records-only).
- [2026-07-10 · the batch review: PASS-WITH-FINDINGS, gate cleared (Fable)](sessions/2026-07-10-17-fable-batch-review-gate-cleared.md) — the brief run cold, deep-not-fast: floor green (3 selftests, 133 tests, live runs — licenscan red on its own fixtures), 16 findings B1–B16, every one fixed in-repo + verified same session (2 backlog strands). Sharpest two were the batch's own defect class: B1 "live-proven clean" false at its recording commit (fixed, re-proven exit 0), B14 ACCESS pointing at an access map ros doesn't hold. Tool teeth: B2 `-only` SPDX mis-tier, B4 renamed-file staged hole (ACM→ACMR, proven live), B5 `--require-terms`, B7 stated residuals; suite 133→137. Backlog: ros access map; REVIEW.md "re-run live-proven claims". **Gate cleared — create-repo rewire unblocked.**
- [2026-07-10 · create-repo rewired to inherit; templates moved into build/ (Opus)](sessions/2026-07-10-18-create-repo-rewire.md) — the core Q1 fix, first stack on the cleared gate. `create-repo` was a second source of the standard (re-encoded file set/sizing/processes + a private `templates/` copy); made it a pure *delivery vehicle* over atelier as *source*. Keystone gap closed: the skill had **no CLAUDE.md template at all**, so every scaffolded repo was born with no doctrine block — PROPAGATION bypassed at birth. Templates moved skill→`docs/build/templates/` (18 files, one source), the missing CLAUDE.md template added carrying the stamped block, three instance-residue scrubs (NOTICE holder, "Nova", faves examples) + one live drift caught (MODEL-ECONOMICS template still naming ros canonical). Skill rewritten to point up + stamp the block/pin/scans, hard-depends on atelier, fails honestly if absent; its private templates removed after the atelier copy was pushed. leakscan clean; **not yet exercised on a real scaffold** — that + a Fable sweep are the owed items. ros pin unchanged (f72031c).
- [2026-07-10 · create-repo exercised end-to-end; scan-hook fail-open defect fixed (Opus)](sessions/2026-07-10-19-create-repo-exercise-hook-fix.md) — session 18's owed real-scaffold run, driven not read: scaffolded a real local git repo (seed → 3 renames → stamp → hook → commit). It paid for itself — surfaced a live **§14 silent-success defect** the scratch dry-run couldn't: `tools/pre-commit.sample` pointed at `$repo_root/tools/` and skipped silently when the scanners were absent; a child has none (they live in atelier), so the scaffolded hook committed a real `AKIA…` secret with a green exit. Fixed: resolve atelier's tools (`ATELIER_TOOLS` → `git config hooks.atelierTools` → in-repo fallback) + **fail closed**; step 6 bakes the path + prove-it-once. Re-proven fail-closed / blocks-secret / passes-clean / atelier-unaffected, then the contract **pinned by `tools/test_precommit.py`** (5 real-`git commit` tests; known-failure proven — the pre-fix sample commits the secret with exit 0); suite 137→142 OK. Surfaced-not-fixed: CI templates run no scanner (hook is the only gate) — folds into the deferred supply-chain call. Sweep **briefed not run** (independence): `docs/reviews/2026-07-10-create-repo-rewire.md` — range f72031c..92c0112 + the machine-local skill; 9 assumptions, sharpest three: clone-loses-hook-and-config, template-block drift, prose-stamp as memory-reborn. Gate raised: no keeper repo scaffolded until the verdict. Still owed: the outward `gh repo create` step. ros pin unchanged (f72031c).
- [2026-07-10 · the create-repo sweep: PASS-WITH-FINDINGS, gate cleared (Fable)](sessions/2026-07-10-20-fable-create-repo-sweep.md) — the brief run cold + driven twice: 10 findings C1–C10 all [fixed] + re-driven same session. Proven live: C1 a fresh clone loses hook+config and committed a planted secret green (fixed at the three places a clone looks); C2 the stamped drift check fatals on the unquoted spacey path (block quotes it; skill stamps `../atelier` + mechanical prove-the-stamp). C3 template-block ≡ PROPAGATION canonical pinned by tools/test_templates.py (suite 142→145); templates/LICENSE added; CI-scan gap + ATELIER_TOOLS trust stated; step-7 gate re-anchored to the ask. **Gate cleared — keeper repos may be scaffolded**; owed: the outward `gh repo create` live proof, ros/faves pin bumps.
- [2026-07-10 · first keeper repo (numen) + fleet pin bumps + REVIEW re-run rule (Opus)](sessions/2026-07-10-21-first-keeper-numen-pin-bumps.md) — closed the three owed items off the create-repo sweep. **Pin bumps:** ros f72031c→bbdeece (picked up the C2 quoted drift path) and faves dde4170→bbdeece (30 behind — also adopted the **P1 trust-surface floor clause** it had lagged since before the method review); fleet now all-current. **First keeper repo:** scaffolded **`numen`** (`mike548141/numen`, PRIVATE) — Mike's home-automation vision (an invisible, anticipatory household intelligence; "a great butler + house staff"), driven through the rewired create-repo live end-to-end and **the owed `gh repo create --push` step driven for the first time** (verified `isPrivate: true`). Sized honest as **pre-code**: stack a deliberate open decision, a **stated no-CI-gate** (nothing buildable + child scans can't run in CI yet), the pre-commit scan hook the only automated gate — proven live to block a planted `AKIA…` key and pass the real commit clean; stamp proven mechanically; no `settings.local.json` leaked. **Doctrine touch:** REVIEW.md gained "Re-run every 'live-proven' claim in scope" (a recorded proof can be stale by its own commit — re-run it, don't read it; grounded in B1 + C2), review-owed. B14 ros access map left for its own ros session.
- [2026-07-10 · atelier goes public — ADR 0005, named worked example (Opus)](sessions/2026-07-10-22-atelier-goes-public.md) — the CI-scan-distribution question, reframed by Mike into the floor action it made possible: **publish all of atelier**. A grounded publication-readiness audit first — hard boundary held (health/family/finance/address/pet/med terms all zero; leakscan + secretscan-over-44-commits + licenscan `--expect Apache-2.0` clean); the only exposures identity-not-data (Mike woven in by design; git history name/email). Mike's calls: **named worked example** (adopters *become the principal*; ros/tiki reframed as provenance), accept history, leave numen (in history too → a scrub is cosmetic, real removal is a rewrite he declined). **ADR 0005** supersedes 0003; README "If you're adopting this" + CLAUDE.md visibility PRIVATE→PUBLIC (making-public floor now *spent*, live floor is the next widening). leakscan earned its keep live — blocked the work twice on the author's own literal contact/health terms written into the *records themselves* (email in the ADR prose; enumerated med/family terms in the session file), each reworded to categories (the boundary biting its own author). Flipped act-then-record: local prep commit (hook clean) → `gh repo edit --visibility public` (verified `isPrivate: false`) → push (`22e41f9`). The opening CI-scan gap now *dissolves*: public means child CI can fetch the public scanners — wiring that + atelier's own CI are the unblocked next builds.
- [2026-07-10 · atelier's own CI — the floor, dogfooded (Opus)](sessions/2026-07-10-23-atelier-own-ci.md) — the self-contained half of the now-unblocked CI build: `.github/workflows/ci.yml` (job `floor`, push+PR) runs the floor every review asserted by hand — 3 scanner `--selftest`s, the 145-test tool suite, the scan triad over the whole tree (`licenscan --expect Apache-2.0`). Zero-dep stdlib → a runner needs only Python. One honest scope call, stated in the header not the fine print: **leakscan runs structural-only, deliberately without `--require-terms`**, because its literal term list is machine-local by design (`~/.claude`, never in a repo) — CI can't hold it and must not; full leakscan cover stays on the real-machine pre-commit hook. secretscan/licenscan run at full cover. Least-privilege (`contents: read`), concurrency-cancel. Grounded before claimed — ran the exact command set locally incl. a **simulated bare runner** (no HOME/global/system git config; still 145 OK) — then **watched not assumed** (REVIEW re-run rule): first GitHub run green 11s but flagged the Node-20 deprecation, bumped `checkout@v5`/`setup-python@v6`, re-ran green 7s / 11-of-11 / no annotation. Deliberately not stacked: the child-CI-fetch half and a markdown internal-link check (net-new tooling wanting its own review) left open.
- [2026-07-10 · linkscan — the internal-link integrity check (Opus)](sessions/2026-07-10-24-linkscan.md) — took the second of session 23's two open builds: `tools/linkscan.py`, the machine that guards the "thin anchor, fat pointer" graph the whole doctrine leans on (a relative link that 404s is a silent hole the *reader* hits, not the author). Sixth house tool, scan-triad pattern (zero-dep stdlib, `--selftest`, `linkscan:allow`/`.linkscanignore`, fail-safe `0`/`1`/`2`). Deliberately narrow: internal `[text](path)` links only (external schemes + `//host` skipped — the network is a flakier tool's job); `missing-file` (resolves relative to the linking file / root for `/…`, file *or* dir) + `missing-anchor` (a `#fragment` into a Markdown target whose GitHub slug matches no heading; `#L42` line refs + non-Markdown anchors skipped); fenced/inline code stripped so example links don't false-positive. 26 tests (suite **145→171**). Proven live — selftest OK, whole tree clean (55 files, 36 links); the tree has **zero anchored links today** so the anchor path is covered by selftest + units + a temp probe inside the tree (good/bad cross-file + same-file anchors, code skipped, a planted break flags) — surfaced honestly. Floor mirrored locally then **watched green on GitHub** for the exact SHA (`eedeb14`), not assumed. **Held on purpose:** not wired into `ci.yml`/`pre-commit.sample` — net-new tooling whose worst failure is the false-negative (a real break reported green), so the gate-wiring waits for a review (brief `docs/reviews/2026-07-10-linkscan.md`; third application of don't-stack-a-gate-on-unreviewed-tooling). Residual named in `tools/README.md` (reference-style/HTML links, setext headings, two-line `](…)`, slugger divergence).
- [2026-07-10 · the linkscan review: PASS-WITH-FINDINGS, all five assumptions repaired, gate cleared (Fable)](sessions/2026-07-10-25-fable-linkscan-review.md) — the brief run cold + every proof re-driven: floor reproduced, then all five load-bearing assumptions damaged by probe — L1 a typo'd path arg scanned nothing and exited 0 (the §14 silent-success class, now exit 2), L2 case-mismatched links green on APFS / 404 on GitHub (on-disk casing walked, NFC/NFD-safe), L3 links escaping the repo root (new outside-root kind), L4+L5 anchor matching made exact like GitHub's after fixing the slugger's underscore divergence, L6 paren filenames parse, L7 fence tracker length/info-string-aware (shared tracker), L9 setext anchors minted (was FPing valid links). L8 root-relative /… verified against GitHub docs; L10 indented-code FP stated-not-fixed (the fix costs real FNs). Suite 171→187, tree rescanned clean. **Gate cleared** — ci.yml/pre-commit wiring left open for the next build session (a reviewer doesn't wire its own same-day fixes into the gate; fourth application of don't-stack).
- [2026-07-10 · linkscan wired into the gate — CI + pre-commit, whole-tree (Opus)](sessions/2026-07-10-26-linkscan-wired-into-the-gate.md) — took session 25's held-open wiring. CI: a `--selftest` line + a whole-tree `linkscan --root . .` step. Hook: linkscan added as a **whole-tree integrity** check, *not* `--staged` like the two boundary scanners — a link breaks when a *different* file is renamed/deleted, so the stale file is rarely the one in the diff; `run_scan` generalised to drop the hardcoded `--staged` and let each scanner declare its mode. The design call surfaced not buried: linkscan catches a *recoverable* defect (unlike a burned secret), but atelier pushes to main where CI runs *after* publication, so the hook is the only gate that catches a 404 before a reader does — wired into both, recoverability nuance + child-repo whole-tree-contract friction stated. Contract pinned by 3 new test_precommit tests incl. the whole-tree crux (a rename breaking an *unstaged* link blocks — staged-only would miss it); suite 187→190. Installed hook refreshed so the commit dogfoods it; four selftests + all four whole-tree scans clean locally; GitHub watched green post-push. **Left open:** child-CI scanner fetch (the other CI half).
- [2026-07-10 · child CI scanner floor — the public scanners now gate child repos (Opus)](sessions/2026-07-10-27-child-ci-scanner-floor.md) — took the standing-open half of the CI story (sessions 23 + 26 both left it): **wire the public scanners into child CI**, unblocked by the public flip (ADR 0005). `docs/build/templates/workflows/floor.yml` — a language-agnostic scanner floor any doctrine-inheriting child drops in *beside* its `ci.yml` (correctness vs publish-safety, two files). It sibling-checks-out `mike548141/atelier` and runs its public secretscan/leakscan/linkscan against the child's own tree (`repo/`) on every push + PR — the CI backstop to the hook, which only guards its own clone. Design calls in the header not buried: **floats `atelier@main`** (a security floor wants newest; and avoids a second stamped-SHA drift surface — CLAUDE.md's pin stays the sole doctrine-version truth; `ref:` commented for reproducible-CI wanters), **leakscan structural-only** (machine-local term list — same honest scope as atelier's own `ci.yml`), **licenscan commented** (no-LICENSE hard-fails it → it's a *publish* gate, wrong to default-on for a private child like numen). Scoping to `repo/` is load-bearing (a whole-workspace scan false-positives on atelier's own fake-secret fixtures — proven). Known-failure driven (EVIDENCE §14): clean child 0/0/0, damaged child (real `AKIA…` + broken link) blocks — after a self-caught false start using the whitelisted AWS *example* secret. Wired into create-repo (seed step 3; step 6 "not wired yet" retired), REPO-STANDARD file set, pinned by 5 `test_templates.py` tests (one-source · repo-scoped · structural-only · licenscan-commented · least-priv; suite 190→195). **Left open:** not yet run on a real child (numen predates it); review-owed (net-new CI tooling).
- [2026-07-11 · child-CI floor exercised on numen; a four-session linkscan false-negative found + fixed (Opus)](sessions/2026-07-11-28-child-ci-floor-exercised.md) — closed session 27's owed real-child run by adopting `floor.yml` into **numen** (closing numen's *own* stated no-CI-gate, unblocked by ADR 0005). Drive-not-assume paid off exactly as session 19 predicted: pre-flighting the scanners over numen caught two real link breaks before any push (a disk-only `[tiki](../ros)`; the scaffolded `[0001](0001-slug.md)` placeholder **every child inherits**), and chasing the second upstream exposed a genuine **false negative in linkscan itself** — `SKIP_DIR_NAMES` held `build`, so the whole-tree walk had silently skipped atelier's own `docs/build/` doctrine layer (14 files) since it was built; session 24's "whole tree clean" was clean-except-the-layer-we-never-looked-at, the exact cardinal sin the linkscan review was built to prevent, live for four sessions. **Fix (`d0870a4`):** hardcode-skip now holds only never-authored names (VCS/deps/caches); `build`/`dist` go to `.linkscanignore` (Mike took the recommended full-drop). Unmasking caught the template placeholder → code-spanned in the template + numen; all templates re-scanned as-if-scaffolded clean; pinned by `test_content_dir_named_build_is_walked` (suite 195→196). numen driven on **real GitHub Actions**, not asserted: happy path green (`29092514962` — sibling atelier fetch, `repo/` scanned, all three ✓), fail-closed **red** via a throwaway PR planting a broken link (`29092599385` — linkscan ✗, exit 1; PR closed, branch deleted). Sharp incidental: numen's **pre-commit hook is frozen at scaffold** (pre-session-26, so no linkscan) — on numen the CI floor is the *only* linkscan gate, which the red run proved catches. **Left open:** the review — brief `docs/reviews/2026-07-11-child-ci-floor.md` (9 assumptions, 4 lenses; sharpest: trigger gaps, `atelier@main` supply-chain trust, does broken-link fail-closed generalise to a secret or is the real-infra secret-block still `act`-only-owed). Fifth application of don't-stack: no further children take `floor.yml`, the linkscan change isn't leaned on, until the verdict.
- [2026-07-11 · right-size the ceremony — calibrate review/session gates to risk (Opus)](sessions/2026-07-11-29-right-size-the-ceremony.md) — Mike's observation accepted: "one thing per session" was being over-applied and productivity degraded across the day; the driver was *not* economy (a fresh session is the pricier choice for related work) but an over-generalised rule + a pull to hand back at a safe green boundary. Found the record's actual words are narrower than my use: **"one task per session"** targets an *unrelated pivot* dragging stale tokens, not one-item-then-stop; **"don't-stack" was nowhere in the doctrine** — an un-codified session habit enforced as a rule, over-broadly. Captured proportionately (living the advice): MODEL-ECONOMICS gains **"Match the ceremony to the risk"** (gates/sweeps/session-breaks are *spend*, apply ∝ cost-of-being-wrong; what earns full ceremony vs. what's self-verifying; don't-stack codified with its narrow scope — an *unreviewed dependency*, not mere sequence) + a sharpened hygiene item 1; REVIEW gains a **"whether a change earns a review at all"** bearing pointing up. **Recursive check: this change is doctrine text, so by its own new rule it is review-owed — flagged, not self-certified, not merged by me.** Done in an isolated worktree (`gate-calibration`, off `bafeaa3`) to avoid clobbering a concurrent live session; handed back as a branch + PR for Mike to merge. Template MODEL-ECONOMICS copy deliberately untouched (separate templates pass).
- [2026-07-11 · the child-CI-floor review: PASS-WITH-FINDINGS, the class finally closed (Fable)](sessions/2026-07-11-30-fable-child-ci-floor-review.md) — the brief run cold, floor reproduced, numen logs re-read live, all nine assumptions probed. Headline: the d0870a4 masking fix closed the *instance*, not the class — secretscan+leakscan still hardcode-skipped `build`/`dist` (planted key in `docs/build/` scanned green, proven live), still phantom-succeeded on a nonexistent path (the L1 class), and the child ignore-file hatch was dead under exactly the floor.yml CWD (N1–N3, mirrored out from linkscan's reviewed patterns). floor.yml gains every-push triggers (a never-PR'd branch was scanned by nothing while the header claimed "every push"), a selftests step, and in-file FP-hatch docs (N4–N6). Six findings, all [fixed]+re-driven; suite 196→205. Floating `atelier@main` attacked and held (the fixes floating to every child bump-free is the argument); real-infra secret drive judged not owed (closed by composition). **Gate cleared**; follow-ups: numen re-copies floor.yml, atelier ci.yml shares the N4 trigger gap. Second review same session (related, no unreviewed dependency): the session-29 ceremony-calibration doctrine change (`cb37310`, merged as PR #2) — **PASS, no findings**; grounding probed not read (don't-stack genuinely un-codified pre-change; hygiene item 1's rationale was always pivot-cost), and this very session demonstrates both halves of the calibration.
- [2026-07-11 · N4 trigger follow-ups closed — atelier ci.yml + numen floor.yml (Opus)](sessions/2026-07-11-31-n4-trigger-followups.md) — the two follow-ups session 30 left standing, both the same N4 trigger gap, both proven on real GitHub Actions in a deliberately low-ceremony session (CI-trigger widenings are self-verifying — the first live application of session 29's calibration). **atelier's own `ci.yml`** widened `push:[main]`→every-push + `workflow_dispatch` (`2a4b2fd`): this job *is* the floor so it gates publication not just the merge; gap-closure proven by pushing a throwaway never-PR'd `n4-trigger-proof` branch and watching CI fire green on it (`29131233488`, a run that couldn't have existed before), then torn down local+remote. **numen re-copied `floor.yml`** byte-for-byte from the post-review template (numen `f81f66f`) to pick up the workflow-file fixes that don't float (N4 every-push, N5 selftests step, N6 hatch docs — the scanner fixes N1–N3 float via `atelier@main`); tree re-scanned clean in the exact floor.yml shape first, floor ran green (`29131265209`) with the new N5 selftests step confirmed live in the job log. numen's frozen pre-scaffold hook (no linkscan) stands as flagged — its floor is the only linkscan gate. Also closed the **session-29 templates follow-up** (`53b41db`): the condensed `build/templates/docs/MODEL-ECONOMICS.md` copy shipped the exact "One task per session; start fresh" misread into every scaffold — rewritten to carry the reviewed sharpening (a coherent *line*, not a checkbox) + the ceremony-to-risk bearing; judged self-verifying (applies an already-reviewed decision to its condensed mirror, no live pin on the body), suite 205 OK. Then a **deliberate fleet pin bump** (Mike: keep going): faves/numen/ros had all been 22 behind at `bbdeece` since session 21 — read the delta, ran each child's stamped drift check verbatim (C2's lesson), bumped both occurrences per child to `d45a431` (faves `aaeb785`, numen `86a0f45`, ros `d85bbb9`); every drift check now clean, `tools/pins.py` reads **all 3 current ✓**, numen floor re-ran green. Carries the ceremony-calibration doctrine down to every child's own future sessions. Left open: ros B14 access map; faves/ros may adopt the child-CI `floor.yml` at discretion (available, not owed).
- [2026-07-11 · atelier packaged as an installable Claude Code plugin (Opus)](sessions/2026-07-11-32-plugin-bundle.md) — Mike picked the plugin/skills-bundle as the next widening ("makes the doctrine travel"); built the v1 bundle, **handed back as PR #3** on branch `plugin-bundle` — the marketplace resolves only from the default branch, so **the merge is the go-live act** (widening floor stays the principal's call). Format grounded via a `claude-code-guide` agent (not memory): atelier becomes its own plugin AND marketplace (`.claude-plugin/{plugin,marketplace}.json`, `source: "./"`, Apache-2.0, `version 0.1.0` bumped-deliberately). Two calls made: **root-as-plugin not a subfolder** (forced by one-source — a git-subdir install can't reach `tools/`+`docs/` without copying them; documented REPO-STANDARD exception), and version-set-not-omitted (else records commits ship "releases"). Middle-tier scope (Mike took the recommendation): `/atelier:scan`, `/atelier:install-hook` (resolves `${CLAUDE_PLUGIN_ROOT}/tools` to an absolute path at install time — git has no such var at commit time), `session-onramp` + `review-brief` skills, whole `docs/method`+`docs/build` as bundled reference; de-instanced `create-repo` + worktree/pins deferred to v2. **Verified end-to-end, not assumed**: JSON valid, scans+linkscan clean, suite 205 OK, then the bundle *actually installed* via the real parser (local marketplace → user-scope install → `details` showed all 4 components → uninstalled + marketplace removed, config left clean). leakscan bit its own author (name in the manifests' attribution) → tight file-scoped `.leakscanignore` (JSON can't hold an inline marker; secretscan still covers them). Branch CI green. **The branch is code-only — this record + detail file live on main** (so PR #3 merges without a records conflict). Left open: Mike merges (go-live); a cold `review-brief` pass owed (net-new public-facing tooling, don't-stack — builder doesn't review its own bundle).
- [2026-07-11 · PRINCIPLES §8 — leverage / "productive laziness" (Opus)](sessions/2026-07-11-33-leverage-principle.md) — Mike named a principle mid-session and asked it be written for Claude models going forward: *strategic laziness is leverage — spend now (a work-avoiding design, or a reliable reusable tool) to save repeated work later and get consistent, robust outputs.* Added as **PRINCIPLES.md §8 "Leverage — invest now to stop paying later"** (appended, not renumbered — §5 is cross-referenced): two forms (**design out the work** — thin-anchor-fat-pointer/one-source; **build the reliable tool once** — the scan triad, the codified verb vs recurring hand-surgery, tied to EVIDENCE §14 build-it-honest), plus **the discipline** (payback needs real recurrence, never gold-plating; never a shortcut through §0–§2) and a **"One-off or recurring?"** situation test. Grounded not invented — the repo already *is* this principle. **Review-owed by its own rule** (doctrine text; flagged, not self-certified). Children inherit via the pointer (no edit); fleet reads behind again (real method/ change).
- [2026-07-11 · instruments/ layer — ccrepo & cctranscript move into atelier (Opus)](sessions/2026-07-11-34-instruments-layer.md) — a how-to ("timestamps on VS Code messages?") became a doctrine call. Answer: the chat UI shows none, but every message is timestamped in the session `.jsonl` under `~/.claude/projects/` — so built **`cctranscript`** (timestamped transcript: prompts+replies, elapsed deltas, `--full` for thinking/tools, `--json`), sibling to **`ccrepo`** (per-repo token/cost). Both zero-dep Node, logs read-only. **The decision (ADR 0006):** they'd landed in `homenetwork/bin` by accident of the first one-off; Mike pushed — both have *no value outside working with Claude as a teammate* (`ccrepo` = DevFinOps cost view, `cctranscript` = observability of what happened when), which *is* atelier's purpose, and atelier already ships `tools/`+`create-repo` so the doctrine-only objection is false. Placement fork put to Mike → **new top-level `instruments/` layer**, split from `tools/` by *purpose*: `tools/` **enforces** (Python, hook-wired checks — "these are the checks"), `instruments/` **observes** (Node, user-invoked, cost/observe the collaboration). Membership rule recorded: an instrument earns a place only if its value *is* the teammateship — general infra utilities (macOS/TrueNAS/networking) merely *used* stay with the estate they serve. Shipped `instruments/{ccrepo,cctranscript,install,README}`, ADR 0006 (+ decisions index, which was also missing 0005), README structure row; all three scanners clean on the new files (code carries no personal data — paths runtime-derived, safe to publish); symlinks re-pointed homenetwork→atelier + smoke-tested; `homenetwork/bin/` removed (no stale copy). **Meta-lesson to memory** (`verify-sibling-wiring-before-documenting`): two live corrections both traced to assuming-not-checking wiring (PATH-export vs the sibling's actual `~/.local/bin` symlink; atelier "doctrine-only" when it already ships tools). **Left open:** instruments are untested (unlike `tools/`); Node runtime dep now in that layer — both stated in the ADR.
- [2026-07-11 · instruments/ test floor — ccrepo & cctranscript earn coverage (Opus)](sessions/2026-07-11-35-instruments-test-floor.md) — the `instruments/` layer shipped untested in session 34 (stated, not silent); now floored. **Runner decision, recorded because it's atelier's first Node test surface and so sets the layer's convention: Node's built-in `node:test` + `node:assert`, zero third-party dep** — mirrors `tools/`'s stdlib-only "no pytest" pattern (nothing for CI to install beyond Node). Minimal, behaviour-preserving testability refactor: CLI entrypoints guarded by `require.main === module`, pure functions `module.exports`ed, ccrepo's disk-index build deferred into `buildIndex()` (was running at module load), three colour/tz functions given a defaulted param. **The headline test — a schema-drift `--json` output contract** (`test_cctranscript.js`) drives the real CLI over a checked-in synthetic fixture (`fixtures/session-sample.jsonl`, no personal data) and asserts role classification (prompt vs tool-result carrier), model mapping, timestamp/text extraction, and `--think`/`--tools`/`--full` gating — the test that fails loudly when the Claude Code `.jsonl` schema shifts. Plus pure-function units both sides. **ccrepo integration honestly scoped:** aggregation factored into a pure `aggregate()` and tested over fixture `ccusage` rows; residual (the `execFileSync` shell-out / FX / render) stated in ROADMAP + CHANGELOG, not faked. **One stated fix** a test surfaced: an explicit `.jsonl` path now recovers its repo label via `cwdFromLog` (every other route did; that branch had dropped it). Wired into `ci.yml`'s floor (adds `setup-node`, zero-dep); one leakscan false positive (a bare `HH:MM:SS` clock time as IPv6-structural) annotated with reason. Grounded in **EVIDENCE §14** (an instrument's "ok" is a claim the apex governs). 20 tests green; ROADMAP item ticked, CHANGELOG + this record added.
- [2026-07-11 · parallel builds + three cold reviews cleared (Fable)](sessions/2026-07-11-36-parallel-builds-three-cold-reviews.md) — Mike out ("do all the build work you can"); the open ROADMAP items run in parallel — three cold reviews as fresh-context background agents while the instruments builds ran in the main line. **Reviews, all PASS-WITH-FINDINGS, all dispositioned same day:** PRINCIPLES §8 (3 [fixed]: stale "§1–7" intro — §6's own sweep rule violated above §6 itself; §7 "Numbered last" made position-independent; observed-vs-predicted recurrence bar taken), CONCURRENCY put-away (3 [fixed]: bearing count grounded + sharpened — a *considered* kept-branch still taxed; integration/permanent-branch scoping; date-prefixed archive tags), and the **plugin bundle PR #3, driven live** — install/uninstall at user scope (config verified clean after), scan honest in a foreign repo, hook block/pass/fail-closed re-proven, merge-is-go-live proven directly. **Nothing blocks the merge** (Mike's call); findings 1–3 [fixed] on the branch (`030f185`): the version-pinned-path update trap documented, skills' plugin-root refs made location-relative, all three companions named. **Builds:** ccrepo gains Cache Create/Read + Cache Hit ratio everywhere (driven live: 95–98% per repo); cctranscript gains `N.M` reply refs (text replies only; `ref` in `--json`; `◂ Claude 1.1 (Opus 4.8)` live); create-repo + REPO-STANDARD bake in delete-branch-on-merge at birth; ccrepo actuals-vs-estimate **config designed, code held** (design-before-code honoured — `instruments/README.md` billing-model section; machine-local config, never in a repo); VS Code UI **scoped not built** (official extension has no hooks; statusline is terminal-only; recommended: sideloaded companion extension ~4–6 h, 1 h spike first). One session-limit interruption; the plugin reviewer resumed with context intact. Reviewer worktree/branch put away (zero unique commits).
- [2026-07-11 · the instruments code review's 10 findings, fixed + pinned (Fable)](sessions/2026-07-11-37-instruments-review-findings-fixed.md) — the `8536971` review's report lived only in its session transcript; recovered via `cctranscript --json` (the `ref` scheme addressing its own review), recorded as `reviews/2026-07-11-instruments-test-floor-code-review.md`, and **all ten findings [fixed] + pinned** (suite 20→26, green in UTC/Halifax/Chatham). Floor pair: tz-fragile `--by-day` fixture → midday-UTC stamps; `test_*.js` → `*.test.js` with one honest correction — node:test rejects directory args on 22+, so CI + docs run the shell glob `node --test instruments/*.test.js` (future test files can't skip CI). Correctness: argv help/validation out of module load (require-survival pinned), `shortModel` total, `.session` envelope guard, symlink-proof walk, `main().catch`. Cleanups: `pt`/`paint` dedup, one `sessionRecord()` for all routes (real mtime in list view), `buildIndex()` returns its maps. Three cut-by-cap residuals recorded, deliberately unfixed. Both instruments re-driven live; 205 Python + 26 Node + scan quartet clean.
- [2026-07-11 · ccrepo actuals, plugin go-live, docker-heap standardised (Opus)](sessions/2026-07-11-38-ccrepo-actuals-plugin-merge-docker-heap.md) — three open items cleared in one turn. **ccrepo actuals-vs-estimate built** (design-before-code strand closed): a machine-local `~/.claude/ccrepo-billing.json` adds an **Actual** column beside **Est (API)** — covered-model tokens $0 marginal, the sunk plan fee apportioned per repo by covered-token share, uncovered models keep the API rate; Actual = plan share + uncovered, TOTAL Actual = fee + all uncovered (proven live: **Est US$2,305 vs Actual US$200**). Absent config ⇒ byte-identical estimate-only JSON; malformed ⇒ ignored-with-warning. 8 new tests, Node 26→34. **Plugin bundle PR #3 MERGED** (`a0ef731`) on Mike's go-ahead — resolved a CHANGELOG append-conflict, re-ran the floor green on the merged head, merged; `main` now carries the plugin+marketplace manifest so the doctrine travels as behaviour. The "next widening" live-floor item is spent. **docker-heap standardised** (`atelier@5db645e`): doctrine block + house README + `docs/` (ARCHITECTURE/ROADMAP/SESSIONS) + CONTRIBUTING + infra-scoped `floor.yml` + fail-closed hook; fixed a self-ignoring/untracked `.gitignore` and scoped leakscan to drop network shapes (ros precedent). **No stack config touched** — what the scans reported is the owner's, in that repo's own private records; the pass deviated from "externalise now" after finding the repo's own roadmap already held a considered direction, leaving action with the owner on his own timeline *(reworded 2026-07-13 on the principal's decision — the original joined the repo's name to its scan posture; old wording in git history)*. **Code-signing** (Mike's steer) captured: SSH commit signing (zero-install, house-ethos) recommended fleet-wide + should become doctrine, blocked on Mike registering a key; artifact/SBOM signing stays deferred.
- [2026-07-11 · the signing doctrine drafted (Fable)](sessions/2026-07-11-39-signing-doctrine.md) — session 38's code-signing capture executed on Mike's ask ("draft the signing doctrine"): drafting was never blocked, only activation is, and the two are now cleanly split. **`method/SIGNING.md`** (meta section, beside RECORD/PROPAGATION — provenance *for* the record): what a signature honestly claims stated first (machine-key custody + tamper-evidence, *not* personal authorship — agent commits sign with the same machine key; the `Co-Authored-By` trailer keeps carrying authorship); layer 1 = SSH-native commit/tag signing fleet-wide (dedicated ed25519 signing key so auth/signing burn independently, machine-global config + create-repo-baked repo-local `commit.gpgsign=true`, one canonical **append-only `allowed_signers` tracked in atelier** as the durable verification plane — GitHub's badge dies if a key deregisters), CI verification from each repo's **adoption boundary** (history never rewritten to sign — ADR 0002's pins), key handling under SECRETS' person-level scope with cheap loss *and* cheap exposure; layer 2 (artifact signing + SBOM) stays deferred behind the first real release. **ADR 0007** accepted, activation pending — rejected GPG / sigstore-gitsign / no-signing / artifact-signing-now, each with why. Doctrine explicitly **decided-but-dormant** (stub-honestly): the activation ladder gates on Mike registering a signing key (trust surface, his act); steps 2–5 (machine config → create-repo → fleet retrofit → floor verify step) are agent-executable the day it lands. ROADMAP restructured accordingly; review-owed with the standing debt.
- [2026-07-11 · the whole fleet adopted (Opus)](sessions/2026-07-11-39-fleet-adoption.md) — "adopt all the other repos I use Claude on." Settled the vocabulary (**child repos** in **the fleet**; the act is **adopting** = stamping the doctrine block + pin, per PROPAGATION), inventoried every repo, and brought **all 11 children current at `1588fda`** (`tools/pins.py` ✓). Newly adopted 7: `rpi`, `nova`, `Baby Brain`, `FoodTracker`, `ec2_builder`, `homenetwork`, `hitchbots_guide` (block + `floor.yml` + fail-closed hook, each proven blocking a planted key). Pin-bumped 4: `faves`/`ros`/`numen`/`docker-heap` — catching real block-wording drift (`record Mike's decision` → `record the principal's decision`; the bump carries wording, not just the SHA). `python-metaname` excluded (third-party, `metaname` org). **The scan floor earned its keep:** surfaced real secret debt in a few repos (tracked in those private repos, not detailed here — atelier is public), and one scary finding verified a false positive *before* reporting (an SSH FIDO algorithm name mistaken for an `sk-` API key). Handled per the session-38 policy: scaffold safely, treat captured-snapshot secrets as the owner's tracked debt, network repos get `--disable ipv4,ipv6,mac-address`. Owed to Mike: the rotations/purges (details private), the two app purpose one-liners, and registering an SSH signing key to activate the signing doctrine.
- [2026-07-12 · the two owed cold reviews cleared (Fable)](sessions/2026-07-12-40-two-cold-reviews-cleared.md) — "do any review work needed": the standing doctrine debt's two items run as parallel cold fresh-context agents (the session-36 pattern), full lifecycle each (brief → cold run → verdict in the brief file → disposition). **RECORD "keep private repos generic": PASS-WITH-FINDINGS, R1–R7 all [fixed]** — the central clause held (re-proven live), but the naming clause was mis-drawn: the harmful class is the **join** (a private repo's name × its sensitive posture), not the name — the rule as written outlawed the repo's own records while the actual join survived the incident scrub in four places. Section redrafted (join regulated; name-only sanctioned behind a load-bearing-name test, e.g. ros/faves/numen; enforcement stated honestly — write-time + review sweeps, no mechanical floor; scrub-of-HEAD-is-not-remediation); the four surviving joins scrubbed at HEAD the strict way so doctrine and record agree. **Signing doctrine (SIGNING.md + ADR 0007): PASS-WITH-FINDINGS, G1–G10 all addressed** — every mechanical claim live-driven in a scratch repo; the core design proved out (bounded retirement genuinely keeps old signatures verifiable). Three blocking, all text-edit-cheap pre-activation: CI verification made **two-plane** (GitHub's web-flow-signed merge commits — two already on `main` — would have red-flagged the repo; `gh api` check closes it spoof-safe), the badge-persistence claim was **inverted** vs current GitHub behaviour (corrected; ADR 0007 addendum per the no-edit rule), quoted `valid-after` timestamps mandated (unquoted man-page syntax fails on the estate's ssh-keygen). Plus trust-list-at-the-child's-pin (never floating `main` — floating distributes fixes, this would distribute *trust*), custody/boundary/backdating honesty. Decision unchanged; activation still gates on Mike registering a key. **The review-owed section is now clear.**
- [2026-07-12 · commit signing activated (Opus)](sessions/2026-07-12-41-signing-activated.md) — "register a signing key — guide me through." The dormant signing doctrine (SIGNING.md + ADR 0007) walked through **ladder steps 1–3, live**. **The principal/agent split held literally:** Mike ran `ssh-keygen` (passphrase set) + `gh ssh-key add --type signing` (after a `gh auth refresh` scope top-up) — the passphrase never touched the agent; key id `1048805`. **Step 2:** canonical `allowed_signers` in atelier root (`namespaces="git",valid-after="20260712"` — quoted, dodging the review's macOS parse trap), global `gpg.format=ssh` + `commit/tag.gpgsign`, **adoption boundary `958b1ea` proven on BOTH planes** — `git verify-commit` good against the trust list *and* `gh api …verification.verified` → `true` (badge lit), boundary set forward (no history rewrite). Two facts surfaced by driving it: passphrase key needs `ssh-add --apple-use-keychain` (unattended signing), and leakscan blocks `allowed_signers` on its principal email → narrow `.leakscanignore` glob (the emails are the data the format holds). **Step 3:** create-repo + REPO-STANDARD bake repo-local `commit.gpgsign`. **shed** (Mike's "shouldn't shed script this?"): *no script* — an SSH signing key is registered once, not minted from a root, so the mint pattern doesn't apply and a `mint_github.py` would be ceremony; *but a registry entry, yes* — a `signing_keys` array under the github provider records id/location/roll (fingerprint omitted — trips validate.py; principal referenced to allowed_signers, one-fact-one-home). Entry flags the open boundary: the key's **secret** is person-level (Apple Passwords) yet the **fleet trusts** it as estate infra. **Then Mike: "do 4 + 5 now", warn-first.** **Step 5** — `tools/signscan.py` (two-plane: machine-key via ssh-keygen; GitHub web-flow commits deferred to the `gh api` plane; a known-signed fixture whose quoted + `Z`-suffixed `valid-after` selftest-guards both parse traps; 11 tests) wired into atelier `ci.yml` + the child `floor.yml` template, trust list read at the child's pin via `git show <pin>:allowed_signers` (never floating main). **The dogfood earned its keep:** atelier's own first CI run flagged every signed commit "not yet valid" — bare `valid-after="20260712"` is read in the verifier's *local* tz, so it passed on UTC+12 and failed in the UTC runner; fixed by UTC-anchoring (`"20260711Z"`), SIGNING.md now mandates it — caught before any child was touched. **Step 4** — the fleet splits three ways: **10 house-floor children retrofit** (pin bump + floor signing steps + `SIGN_BOUNDARY` = pre-signing HEAD; numen piloted green first, then 9 batched), **7 CI-green, 3 red on pre-existing scanner debt** (fail before signing runs, owner's debt; which children, in their own records); **faves + ros deferred** (bespoke `ci.yml`, never adopted the house floor — they still sign). shed's boundary `5bdee55`. **Signing fully active, warn-first.** Owed: faves/ros floor adoption, warn→block flip (Mike's call once the pre-existing debt clears), vigilant mode off until green.
- [2026-07-12 · faves + ros adopt the house floor (Fable)](sessions/2026-07-12-42-faves-ros-floor-adoption.md) — "lets go with full adoption": the session-41 follow-up closed. Current `floor.yml` landed beside both bespoke `ci.yml`s, **both green on first run with verification actually verifying** (faves 9/9, ros 2/2, two planes). The load-bearing catches: both pins predated `allowed_signers` (adopted as-was, verification would have *silently skipped* — the pin bump was the fix, not hygiene), and ros's unpushed HEAD was unsigned (amend-signed; boundary stays honest). Green-by-charter, not by scrub: repo-type `--disable` tuning + reasoned ignore hatches for content the repos are chartered to hold, every ros exemption stating it dies at the publish-time scrub pass. licenscan enabled in both; four real link breaks fixed. Warn→block flip still open, Mike's call.
- [2026-07-12 · communication calibration doctrine (Fable)](sessions/2026-07-12-43-communication-doctrine.md) — Mike revisited the same-day "reply style is personal, no atelier artifact" call with a sharper split: the *values* stay in `~/.claude/` (the reframe stands), the *pattern* is shareable doctrine a peer adopter needs. **`method/COMMUNICATION.md`** built — TOOLBOX's practice/instance split applied to communication; axes grounded in the live instance (outcome-first ordering, structure-replaces-length density, visual structure with the colour-icons ✅❌⚠️ specificity confirmed earlier this same day, cognitive-load stance, tone *with dosage* — the overdone-wit case, locale, what-personal-context-is-for); meta-rules (serve-don't-recite, maintain-like-a-record with dated confirmations, person-local-never-copied); Mike's calibration included **scrubbed** as the ADR 0005 named worked example (health/workload/household motivators dropped, stated). Decline-then-revisit recorded honestly in the ROADMAP item, not silently edited. Cold review owed — logged with pre-seeded brief questions (does the worked example leak by implication; axes grounded or padded).
- [2026-07-12 · COMMUNICATION.md cold review cleared (Fable)](sessions/2026-07-12-44-communication-cold-review.md) — the session-43 owed review RAN: **PASS-WITH-FINDINGS**, verdict in `reviews/2026-07-12-communication.md`. The sharpest question cleared decisively — the scrubbed worked example does *not* leak the personal layer by implication (identity × category-existence join, zero specifics, categories the boundary statement already publishes); reviewer barred from the person layer by the brief so the question was judged as a genuine outside reader. 4 findings C1–C4 all [fixed] same day, one theme: the doc held the boundary rigorously but was looser on itself (enforcement now stated as write-time-discipline-only; the not-even-private divergence from the portability north star surfaced, kept strict; the snapshot dated per EVIDENCE; the "works without knowing" overclaim sharpened). Review-owed queue empty.
- [2026-07-12 · browser-fetch adopted; the fetch ladder + credential boundary (Opus→Fable)](sessions/2026-07-12-45-browser-fetch-adopted.md) — session 41's conversation continued: "carry on with the browser fetch work." **The first capability instrument adopted**: `~/.claude/mcp-servers/browser-fetch` (Python MCP server driving Chrome) → `instruments/browser-fetch/`, scrubbed BEFORE entering the public repo (every "Mike" → operator, machine history dropped), pinned deps + reproducible `setup` (venv under `~/.cache`, never committed/iCloud), ADR 0006 addendum (three verbs: enforce · observe · **extend reach**; zero-dep flexes for capability tools, paid honestly). Proven end-to-end twice — driven directly (example.com → 200), then by a fresh parallel session against the re-registered server (`HeadlessChrome/149` UA = real Chrome); old dir + config backup deleted after confirmation. **The fetch escalation ladder** captured in its README, Mike's shape, flat 1–6 (he rejected 4a/4b sub-rungs): WebFetch/WebSearch · curl · standalone headless · persistent dedicated-profile · persistent everyday-session · ask-the-operator; start cheapest, step down only when blocked; Chrome-only today, Safari/Firefox + explicit 4/5 split ROADMAP'd. **The credential boundary sharpened into a purpose-of-storage test** before doctrine drafting: provisioned stores (shed registry keychain items, minted tokens — ros/tiki/shed) are the *intended path*; personal convenience stores (browser logins, Apple Passwords) are off-limits by default — ride an authenticated session, never the stored credentials that mint one; the principal grants across the line per credential. Encoded in the ROADMAP doctrine-elevation item so future `method/` text can't outlaw the estate's intended use. Doctrine drafting deferred to a next session. Signing pre-flight papercut also closed this conversation (session 41's tail). Survival audit after two parallel sessions in the same checkout: everything intact, history linear.
- [2026-07-12 · concurrency triggers and sync bookends (Fable)](sessions/2026-07-12-46-concurrency-triggers.md) — Mike named his real working style (parallel sessions across repos, sometimes several on one repo, atelier/shed flowing down so nothing is fully independent) and asked if it could be managed better — sketching branch/worktree-per-session while honestly unsure it helps. Assessment first: the only real hazard is same-repo-multi-session, and the danger is the **shared uncommitted working tree** (silent clobber/absorb), not concurrent commits; the doctrine already existed (CONCURRENCY worktrees, PROPAGATION pin) but had **no firing condition** — session 45's survival audit was good habits standing in for a trigger. Codified on "yes codify it": CONCURRENCY gains *the trigger* (say-so-at-open + dirty-tree backstop), *the solo default* (no branch-per-session ceremony — Mike's sketch assessed as costing more than it buys for lone sessions), and *sync bookends* (pull `--rebase --autostash` at start, push per commit, append-tail conflicts expected-and-trivial); the child doctrine block gains a **Concurrency** line reaching the fleet on normal pin bumps; CLAUDE.md read order now opens with the bookend (lived this session — pulled before editing).
- [2026-07-12 · REACH doctrine, fetch build, signing gate, B14 (Opus)](sessions/2026-07-12-47-reach-doctrine-fetch-build-b14.md) — "proceed on all 4" (fetch→doctrine, fetch→build, signing warn→block, B14 ros map), orchestrated as parallel streams (main-line doctrine+signing, three background agents). **Red main fixed first:** session 46 added the Concurrency bullet to PROPAGATION's canonical block but not the stamped template copy — the drift test caught it; synced (canonical wins), green-before-content. **Fetch→doctrine:** `method/REACH.md` — the escalation ladder (engine-agnostic, cheapest-first) + the credential boundary as a purpose-of-storage test (provisioned = intended path; personal convenience off by default; ride a session, never mint from saved creds; principal grants across the line), named for the instruments' third verb *extend reach*, indexed in the SECRETS/ACCESS family. Cold review PASS-WITH-FINDINGS, R1–R5 all [fixed] same day (grounded ladder, argued join, purpose test that doesn't outlaw provisioned use, no leak). **Fetch→build:** `browser_fetch` gains `engine` (chromium/firefox/webkit) — **Firefox+WebKit live-verified** end-to-end this session (the gate the agent couldn't run); `browser_fetch_persistent` gains explicit `rung` 4/5 on distinct ports `:9222`/`:9223`. Honest limit: rungs 4/5 **Chrome-only by protocol** (CDP), not faked; rung-5 live fetch owed-to-operator. Taken as files not merged (base predated the REACH review round); worktree put away. **Signing flip HELD** — gate not met and not agent-clearable: the 3 red repos fail for 3 different reasons, none a signing failure (two on owner-tracked secret debt = Mike's rotations; one on bespoke-CI lint/test plus floor leakscan — which children, in their own records). **B14:** agent scoped inside private ros created `docs/ACCESS-MAP.md` (4 axes, honest per-domain onboarding status), signed on worktree branch `access/estate-access-map` `c3bc612`, not pushed (concurrent ros session live). Owed to Mike: the two secret rotations, the third child's cleanup nod, rung-5 operator check, ros map merge (since landed, `ros@82db55c`). **Post-session self-review (Mike's ask) found and fixed two record defects the same day:** this session had published the private-child name × secret-debt join (counts included) in public atelier hours after the session-40 review scrubbed that class — scrubbed at HEAD here (git history retains it, acknowledged, not silently edited; session 41's milder instance scrubbed too); and it had accused session 41 of mis-filing one child's redness on a `--limit 1` query — the child's floor is red too, session 41 was right, accusation retracted. Adversarial un-briefed REACH re-review commissioned (the first was author-briefed — self-certification named) and RAN before close: **PASS-WITH-FINDINGS, A1–A8, two MAJOR on the credential boundary itself, zero overlap with R1–R5** — its verdict on the first review: "cold context, warm questions is self-certification at one remove". Recorded verbatim, no author disposition — **A1–A8 await Mike** (ROADMAP'd with author's counsel); the session-38 borderline join and a REVIEW.md reviewer-independence rule also logged to ROADMAP. Full findings in the session file's addendum.
- [2026-07-12 · communication UX + the close-ready signal (Opus)](sessions/2026-07-12-48-communication-ux-and-close-signal.md) — Mike gave two calibration threads and one drift question. **Verdict UX:** widen the icon vocabulary past ✅❌⚠️ (🎉 🎯 🚩) under the guardrail he endorsed — *apt icon = signal, overused = noise*; surface act-on items (decisions/actions/cautions) in an eye-catching device *apart from the reasoning*; stop running text-heavy and jargon-laden. **Close-ready signal:** after a run of work, do the tidy-up unprompted and *say it's ready to close* — don't make him ask. He noticed this slipped "in the last day or so"; **honest diagnosis** — `RECORD.md` mandated the session-log *artifact* but never the *signal*, so it ran on soft habit and drifted after the opus[1m] switch; no single cause invented, encoding the habit is the fix. **Placed both layers** per COMMUNICATION's practice/instance split: `method/COMMUNICATION.md` gains the widened visual axis + a new accessibility-of-language axis (jargon/prose-wall costs a reread like a missing glyph), worked example updated; `method/RECORD.md` session-log gains "declare the close"; global `~/.claude/` carries the personal instance (plain-over-jargon, signal-when-wrapped). Maintenance the COMMUNICATION doctrine itself prescribes — no cold review owed.
- [2026-07-12 · the reviewer-independence rule, drafted + cold-reviewed (Opus)](sessions/2026-07-12-49-review-independence-rule.md) — resumed after a session limit; the one agent-actionable open item was **REVIEW.md — encode reviewer independence** (grounded in the session-47 REACH case: a cold-context review can still be *warm-questioned* — the author seeded the brief's attack questions, all aimed where it was already looking; the un-briefed re-run found eight findings, zero overlap, two MAJOR). Encoded (`21b1524`) as a new section *Independence is more than fresh context* — three rules when the work's author commissions its own review (reviewer chooses its own attack surface, no author-seeded questions · barred from prior reviews until its verdict drafts · self-authored *doctrine* findings are the principal's disposition, not the author's) — plus two lifecycle carve-outs undoing the steps that institutionalised the trap (step 1 no longer has the author seed questions on its own work; step 4 denies the author the `[rejected: grounds]` escape on doctrine it wrote). **Dogfooded:** the change is itself review-owed doctrine, so it was reviewed *under the new rule* — an author-commissioned brief that seeds no questions, a cold reviewer that chose its own surface, a verdict routed to Mike not the author. **PASS-WITH-FINDINGS, 7 findings (3 MAJOR):** I1 the rule *overshoots its grounding* (REACH proposed the milder author-questions-as-a-floor remedy; REVIEW.md took the maximal cut without arguing it — and the brief itself smuggled a verdict, "Grounded in the REACH case," crossing the steering line the rule forbids); I2 the *author≠principal* split the rule rests on is undefined and collapses for a solo adopter and under atelier's own every-commit-authored-by-Mike convention; I3 the *doctrine-vs-code* line is undefined and dodgeable by encoding doctrine as a validator (which AUTONOMY tells the agent to do). I4–I7 milder. **The rule fired on its first use:** self-authored doctrine, so the author applied nothing — **I1–I7 await Mike's disposition** (ROADMAP review-owed; author's counsel: take I1–I5+I7, I6 a judgement call). No content applied unilaterally.
- [2026-07-13 · REVIEW.md challenged; both batches dispositioned + applied (Fable)](sessions/2026-07-13-50-independence-batch-dispositioned-applied.md) — Mike commissioned an un-briefed second challenge of `method/REVIEW.md`, run under the doctrine's own rules (own attack surface, prior verdict deferred until findings drafted): **PASS-WITH-FINDINGS**, F1–F7 in `reviews/2026-07-13-review-doctrine-second-pass.md` — led by F1 ("finds nothing ⇒ suspect" is a Goodhart trap; the failed REACH pass had *five* findings), F2 (inline-spawn review = warmest-brief shape, unbound), F3 (grounding n=1 *uncontrolled*, no strengthening trigger), F4 (PROPAGATION restated the superseded most-capable-model rule). **Mike dispositioned I1–I7 + F1–F7 as one batch, all [fixed]**, deciding the one fork himself — author questions are a *floor, never a fence* — and strengthening it: seeded questions influence by their very existence, so the reviewer picks its own surface before weighing the list. Applied (`4c7ea9c`) by a session that authored neither the doctrine nor either verdict; dispositions stamped in both verdict files. The application, itself doctrine, got its owed cold pass before close: **PASS-WITH-FINDINGS, G1–G9** in `reviews/2026-07-13-independence-batch-applied.md` — fidelity 14/14 confirmed; sharpest: G1 the ordering rule is behavioural-not-structural (defer seeded questions below the brief divider), G3 the standing test re-keys on the count metric F1 evicted, G4 the regress needs a stopping rule + the neutral-applier pattern encoding. Mike then ruled the loop question live (converging, not looping — MAJORs 3 → 0 → 0) with the no-MAJOR stopping rule + a non-convergence escape valve (MAJORs not falling ⇒ stop, ask him), endorsed G1's structural deferral in his own words, and dispositioned **all nine [fixed]** — applied same day, cycle closed per the rule it encodes; addendum in the session file.
- [2026-07-13 · "decision" replaces "disposition"; four principal decisions land (Fable)](sessions/2026-07-13-51-vocabulary-and-four-decisions.md) — Mike retired "disposition" ("it came out of your own deliberations") for **decision** across the living docs (`5bca7fa`; dated records keep their wording), then a plain-language walkthrough of every item owing him a ruling produced four: **REACH A1–A8 all [fixed]** — applied same session to REACH.md + AUTONOMY.md (A1's carve-out makes the two docs state one credential rule; A2 scopes session-riding to retrieval), decisions stamped, the application's own cold pass briefed **and RUN before close — PASS-WITH-FINDINGS, no MAJOR, cycle CLOSED; fidelity 8/8, H1–H8 backlogged** (`reviews/2026-07-13-reach-batch-applied.md`); **session-38 name × debt join scrubbed** from all three public spots, lesson kept, history noted as keeping the old wording; **next widening = v2 plugin** (de-instanced create-repo + worktree/fleet-pins commands — ROADMAP item, scoping first); **credential-registry home resolved** — it already exists as the private estate-root repo (verified; unnamed here by RECORD's own rule). The warn→block flip stayed parked as not-decidable-today. Incidental: restored ROADMAP's decapitated first-REACH-review bullet header.
- [2026-07-13 · numbered records under concurrency (Fable)](sessions/2026-07-13-52-numbered-records-rule.md) — a child repo's parallel-session number collision (two sessions both allocated 03 from stale views; renumbered 03→05 on human read-through) named the gap CONCURRENCY's append-tail bullet didn't cover: a next-N counter collides *silently* — differently named files, same number, no merge conflict. Codified (`e93e731`) as the append-tail bullet's sibling, three rules and still no locking: **allocate late** (N picked at landing, fresh pull immediately before, pushed at once — the allocate-to-push gap is the entire window), **provisional until pushed** (a loss costs one rename, not a reference cascade), **first landed wins** (the loser renumbers, mechanically). New record series should prefer coordination-free identifiers; counters stay only where the running number carries meaning. Fleet delivery via the child block's Concurrency line (canonical + template together, drift test green); RECORD.md points at the rule from the `<date>-NN` spec. This very entry allocated 52 the new way.
- [2026-07-13 · record identifiers go coordination-free (Fable)](sessions/2026-07-13-1130-coordination-free-record-ids.md) — Mike challenged session 52's counter-keeping carve-out within minutes of it landing: the running number means nothing to him beyond chronology (the date already gives that), and mid-turn extended the ruling to ADRs and every similar series. The reframe: session 52 disciplined the shared counter; this removes it — identifiers are built from facts the session already owns (**date + slug**, plus `HHMM` start time where same-day order matters), so the silent-collision class is structurally gone: worst case is two sessions wanting the same *filename*, a visible trivial conflict. Reviews had already drifted to date+slug in practice, zero incidents. Codified (`93e3e85`): ADR `2026-07-13-coordination-free-record-identifiers` (first record named under the scheme it decides); CONCURRENCY's bullet rewritten (counter rules demoted to interim discipline for legacy repos); RECORD's session spec now `<date>-<HHMM>-<slug>`; child block + template together; REPO-STANDARD + decisions/reviews READMEs + ADR template (renamed off `0000-`) all recast. History never renumbered — ADR 0001–0007 and sessions through 52 keep their names. Children adopt at their next pin bump.
- [2026-07-13 · addendum: HHMM everywhere, interim counter rules retired (Fable)](sessions/2026-07-13-1130-coordination-free-record-ids.md) — the same conversation, two refinements from Mike (`11ef17b`, addendum in the session file and on the ADR): **one uniform form** — `YYYY-MM-DD-HHMM-slug.md` for every record series, ADRs and reviews included, dropping the per-series judgement clause; and the **interim counter rules retired** — hours old, zero adopters, and undeliverable by construction (the pin bump that would carry the interim discipline carries the coordination-free rule in the same block), so keeping them bought no coverage and invited two-scheme confusion. The drift test fired for real along the way: angle brackets in the child block are reserved for create-repo fill placeholders, so the pattern is spelled literally.
- [2026-07-13 · the ADR lifecycle encoded (Fable)](sessions/2026-07-13-1218-adr-lifecycle-verbs.md) — Mike turned the twice-used "0007 precedent" into a rule (`d9924f0`): an ADR is **immutable once decided**; everything after acceptance happens by *appending*, in one of three verbs — **Addendum** (dated section when the same decision matures), **Revoked** (`revoked <date>` + addendum saying why and from when; the file stays — a revoked decision still explains the era it governed), **Superseded** (`superseded by <file>`, the new ADR points back). The gap was real: immutability + supersession were encoded, but the addendum verb ran on precedent alone and revocation didn't exist in the vocabulary. RECORD.md is the home; both decisions READMEs carry the mechanics and point up.
- [2026-07-13 · addendum: the draft state (Fable)](sessions/2026-07-13-1218-adr-lifecycle-verbs.md) — Mike anchored the ADR lifecycle to the IEEE/IETF standards shape (draft → active → superseded or withdrawn), exposing the state the three verbs skipped: **draft**, the one *mutable* state — deliberation written down, binding on nothing; acceptance is the principal's decision, never the author's, and is what freezes the substance. Gives the recurring "await Mike's decision" pattern a first-class home: author the deliberation as a draft ADR, the ruling flips it to accepted. Status vocabulary now draft / accepted / revoked `<date>` / superseded by `<file>`; RECORD.md, both READMEs, and both template copies moved together.
- [2026-07-13 · CI compute as a third spend pool; the estate-root pointer (Opus)](sessions/2026-07-13-1544-compute-pool-and-estate-root-pointer.md) — Mike's GitHub-Actions cost fact (private repos meter minutes, 2000/mo free; public free) surfaced a live gap: `MODEL-ECONOMICS` named two spend pools but not the one GitHub bills, and the floor CI comments' "costs seconds, right side of the trade" reasoning was written on public/free atelier yet is the reference **private** children copy. Drafted a third-pool section; an independent un-briefed review (PASS-WITH-FINDINGS) caught real defects — the fix had patched `ci.yml` but not the `floor.yml` **template** children actually copy; the "scope a private floor to PR+default-branch" prescription reopened the never-PR'd-branch hole and undersold private CI's full-cover scan of history that publishes wholesale at go-public; per-minute-rounding, fails-open-with-billing, and standard-runner facts were off. Landed principle-only (`e9f01c1`, `4a71b7a`): the section names the pool + visibility coupling + the honest two-sided trade and **stops prescribing** — frequency and the numbers it turns on aren't atelier's to hold; precedence restated (cost never buys down safety, publication never cost-driven). Both CI comments corrected. Child doctrine block gained an **"Estate resources — point up, don't re-derive"** bullet (canonical + template byte-identical, drift green, 216 pass) so every child references the unnamed estate root for estate-wide facts. Estate half in shed: **Fin of DevSecFinOps** — financial inventory is shed's, ADR 0004 (`shed@18c6ac1`), build-out ROADMAP'd with the GitHub seed. **Owed:** shed inventory structure (2nd datum); children adopt the bullet at next pin bump.
- [2026-07-13 · addendum: second review caught a self-inflicted defect (Opus)](sessions/2026-07-13-1544-compute-pool-and-estate-root-pointer.md) — Mike asked for a fresh un-briefed pass over the *final* text before close; it found two real findings (`4eac3d3`). **(1)** The rework's "private CI scans branch history at full cover" was **false** — verified the scanners run `--root` (tree at the pushed tip), never the log; `--staged` (the hook's mode) is the only history-aware path, so per-commit cover is the hook's, not CI's. A claim stronger than its evidence, already propagated into the child template; restated as tip-tree cover (go-public argument holds: main-only leaves feature *tips* unscanned). **(2)** The estate-resources prose told a child to name the root in its onramp, but some children are *public* — a public repo naming the estate's credential/inventory root is the name↔posture reconnaissance join RECORD forbids; now a public child references it by local-path convention, never by name. Also: path-filter advice exempts the floor; "not atelier's to prescribe" narrowed to the trim-down; shed's free/2000 figure verified via `gh api user`. Lesson recorded: a review of a *reworked* draft isn't ceremony — the rework is fresh unreviewed code, and here it carried an error the first reviewer's own phrasing seeded.
- [2026-07-13 · runner class as a CI-cost lever (Opus)](sessions/2026-07-13-1721-runner-class-cost-lever.md) — Mike found the Actions wastage runs deeper: **`ros` ran macOS runners for everything** — one of the dearest classes — when it's multi-platform by design and only two `tiki` pieces are genuinely macOS-specific. The compute-pool doctrine (session 55) named **run count** as the private-repo lever but was silent on **runner class**, the per-minute multiplier that sits above it (GitHub standard: **Linux 1× · Windows 2× · macOS 10×** — a whole suite on macOS burns the allowance ten-fold, almost always for no portability gain). Worse, the build layer *shipped* the anti-pattern: `ci-python.yml` ran lint + type-check + tests across a full `os:[ubuntu,macos]` matrix, manufacturing the ros waste in every repo that copied it. Landed: MODEL-ECONOMICS gains **"Runner class — the multiplier lever"** (multipliers; larger runners bill even on public; rule = *default every job to Linux, escalate only for the OS-specific slice in its own job*; ros the worked multi-platform case; blanket matrix named as anti-pattern); the private-repo lever line now names class as the other, larger lever; `ci-python.yml` rewritten to Linux-default with a commented, slice-scoped macOS/Windows job. **Owed:** fixing ros's own live workflows (its repo's follow-up); children adopt the new template at next pin bump.
- [2026-07-13 · addendum: the "when" lever + ros CI review (Opus)](sessions/2026-07-13-1721-runner-class-cost-lever.md) — Mike asked if *triggering* deserves the same scrutiny as runner class. Verdict: mostly already covered (path filters + `cancel-in-progress` were doctrine; every-push-vs-main is the visibility trade), one real un-named lever — **duplicate triggers** (unfiltered `push` + `pull_request` fires *twice* per push on a branch with an open PR). Added one cost-hygiene line to MODEL-ECONOMICS: scope `push` to the branches that need it unless the second run earns its minutes (merge-preview scan a tip-push can't see; fork-PR coverage). Skip-ci/debounce/gating the floor named as the over-engineering to avoid. atelier's own `floor.yml` template carries the overlap — two-sided, touches the deliberate N4 every-push call, so **logged under ROADMAP *Open questions*** for a future per-repo decision, not auto-fixed. **ros review** (Mike: queue, don't implement): its new CI is already good (Linux matrix, macOS gated off PRs, `push` scoped to main → no overlap); two minor trims — the 10× macOS job still runs lint+mypy+full-suite when only the Keychain test needs it, and `ci.yml` lacks `cancel-in-progress` — **queued in ros's own ROADMAP**, untouched otherwise.
- [2026-07-13 · claiming work — making selection collide like naming does (Opus)](sessions/2026-07-13-2226-claiming-work-mechanism.md) — Mike's fourth concurrency snag, hit repeatedly across parallel evening sessions: several sessions each told only "the next thing in the queue" self-selected the **same** roadmap item and duplicated the work, silently — nothing in tree or record ever conflicted to warn them. Selection was the one coordination point CONCURRENCY named a substrate for (worktrees) and a trigger for (say-so / dirty-tree) but never a *claim* on. Fix generalises the record-ID bearing the doc already carries: **force the collision onto one shared line.** A session claims an item by editing *its own roadmap line in place* (`[~]` + `(claimed <date>-<HHMM>, wt: <branch>)`) and pushing **before** working — same-item double-claim lands as a trivial same-line rebase conflict, first-to-push wins, loser takes the next open item; different items touch different lines and both proceed. Worktree-mode only (zero cost to the solo default); grain is the leaf item not the theme (a themed instruction fans out); release = branch put-away; **orphan claims** judged like stale branches (timestamp bounds staleness, no lease/lock — the unhappy path Mike singled out). Landed `method/CONCURRENCY.md` new section + ROADMAP `[~]` legend + review-owed item + CHANGELOG. **Owed:** cold un-briefed review (no seeded brief authored per REVIEW.md independence; self-authored doctrine → Mike decides); children inherit by pointer, `[~]` legend rides the next pin bump.
- [2026-07-13 · addendum: the claiming section reviewed + all seven applied (Opus)](sessions/2026-07-13-2226-claiming-work-mechanism.md) — the cold un-briefed pass earned its keep: **PASS-WITH-FINDINGS, 1 MAJOR · 4 MEDIUM · 2 LOW**, and the MAJOR was a real hole — claiming was gated to worktree-mode, whose only reliable trigger is the principal's say-so, i.e. exactly the condition the grounding incident broke, so the mechanism wouldn't have fired in the case it was built for. Mike ruled **all seven [fixed]**; applied same day. The reframe (option A, decouple): claiming now keys on **selection from the shared queue** — solo or parallel — and the claim commit lands on `main` *before* branching (a feature-branch claim is invisible; separate branches' pushes never collide). MEDIUMs folded in: bundled/themed lines are claimed as a unit and *serialise* (fan-out needs per-leaf lines); **adjacent** one-line claims raise a trivial keep-both rebase conflict (reviewer live-verified — "silent everywhere else" was false), so `[~]` goes on the checkbox line and the guarantee is softened; put-away's abandonment gained the `[~]`→`[ ]` reversion; tracker-based adopters pointed at the assignee/in-progress primitive. LOW: timestamp demoted to a staleness tiebreak behind branch-existence. Decisions stamped in the verdict file. **Applied-batch cold pass owed** per the cycle rule (first pass carried a MAJOR).
- [2026-07-13 · addendum: applied-batch cold pass — cycle closed, two residuals fixed, shed spillover (Opus)](sessions/2026-07-13-2226-claiming-work-mechanism.md) — the cycle rule's owed applied-batch pass RAN un-briefed: **PASS-WITH-FINDINGS, no MAJOR → cycle closes**, all seven confirmed in-text and live-reproduced. Two residuals, Mike ruled **both [fixed]**: a **MEDIUM** the fix-2 reframe opened one layer beneath — "claim on `main`" is right, but git checks `main` out in exactly one place, so a parallel session makes the claim *from the primary checkout before `git worktree add`* (named as the sync-bookend integration landing, with a trigger-section back-reference; separate-clone adopters skip it) — and a **LOW** inherited imprecision (the "three-line diff context" parenthetical was wrong; adjacency = no unchanged line between, a one-line gap rebases clean). Cycle was already closed so no further pass owed. **Shed spillover, same session:** a shed session hit its `validate.py` token-shape guard tripping on a 46-char coordination-free ADR id; diagnosed as **shed's to fix** (its own tool), verified **atelier's own secretscan/leakscan pass these ids clean** (low entropy), and — Mike's call — added a RECORD.md note that ids are long/hyphenated by design so downstream validators/scanners must allow the shape. Claiming-work doctrine now fully reviewed, applied, and closed.
- [2026-07-14 · lean current-truth files — sizescan + the harvest trigger (Opus)](sessions/2026-07-14-1948-lean-current-truth-files.md) — Mike: key files (the ROADMAP in each repo especially) keep getting too big, the same class the SESSIONS file hit before; review the fleet and devise a fix that doesn't drop quality. Diagnosis: one root cause, the fix already half-written in RECORD.md but neither lived nor enforced — a finished roadmap *item accretes the story of how it got done* (valuable case-law, but *history* in a *current-truth* file), and nothing ever *triggered* the index/detail split RECORD prescribes (SESSIONS was split once by hand; ros ROADMAP then reached 3197 lines, atelier's 1091, faves SESSIONS 1157). Decisions (Mike): doctrine **+** a tool; backfill **atelier only** (dogfood). Built **`tools/sizescan.py`** (7th scanner — budgets only the stay-lean files, ignores the append-only *destinations* by design; root-only README/CLAUDE; **advisory exit 0**, `--check` opt-in gate; 24 tests; fleet-proven sharp-on-offenders/silent-on-healthy), sharpened **RECORD.md** (growth dynamic + *current-truth stays lean, history relocates* + harvest-at-close trigger), documented it. Dogfood: **atelier ROADMAP 1091→~180**, ~800 lines of case-law moved **verbatim** to new **`ROADMAP-DONE.md`** (byte-exact slice + item accounting, zero lost). Floor green (240 tests · triad · sizescan · linkscan · full leakscan). **Owed:** cold review before wiring sizescan into any gate (don't-stack); ros/faves harvests logged as backlog. Handed back as branch + PR.
- [2026-07-14 · addendum: sizescan reviewed, fixed, wired automatic (Opus)](sessions/2026-07-14-1948-lean-current-truth-files.md) — Mike merged PR #4 then asked to make sizescan **automatic**; the cold review had run in a separate session meanwhile (**PASS-WITH-FINDINGS, 1 MAJOR · 2 MEDIUM · 1 LOW**, un-briefed). Resolved all findings: **F1 (MAJOR fail-open)** — skip-dir names matched the *absolute* path, so a repo under an ancestor named `archive`/`sessions`/… had every file skipped and reported clean; fixed to match relative to the scan base, **live-reproven**. **F2** — markers matched anywhere, so a prose mention self-exempted the file; now header-only (first 15 lines). **F4** — overlapping paths deduped. **F3 (doctrine, Mike's call → index rotation)** — RECORD.md sharpened: append-only is about *content*, not a fixed home; an over-budget index rotates older entries verbatim to `SESSIONS-ARCHIVE.md` (same split as ROADMAP→ROADMAP-DONE). **Wired** `sizescan --check` into atelier `ci.yml` + child `floor.yml` template — a repo adopting the floor while over-budget reds (the harvest trigger); existing children pick it up on re-adopt. Suite 240→247, floor green. The automation makes the *reminder* automatic (CI red) even where the harvest *work* stays a deliberate per-repo job.
- [2026-07-14 · cold review: lean-files doctrine + sizescan (Fable)](reviews/2026-07-14-2048-lean-files-sizescan-cold.md) — the owed un-briefed cold pass RAN (reviewer not the author; own attack surface committed before the seeded four were reconciled — their unavoidable read-at-selection named, not denied): **PASS-WITH-FINDINGS — 1 MAJOR · 2 MEDIUM · 1 LOW bundle**, all decisions Mike's (rule 3: doctrine + policy-as-code, author absent). Every recorded proof reproduced: 240 tests, selftests, sizescan+linkscan clean, **zero-loss harvest verified mechanically** (94/94 checkbox lines, ROADMAP-DONE byte-verbatim), fleet claim re-run (ros ROADMAP now 3317). **MAJOR (reproduced):** the skip-dir check runs on the *absolute* path, so a repo under an ancestor named like a growth store (`~/archive/<repo>`) silently scans nothing and reports clean — fail-open in the default and fleet invocations, violating the tool's own "read nothing ≠ pass" contract. **MEDIUMs:** a prose *mention* of the allow/budget markers exempts the whole file (reproduced — the review's own ROADMAP paragraph then walked into it same-day); the SESSIONS budget collides with RECORD's append-only index rule with no sanctioned move (atelier's index trips in ~2 weeks at cadence; shed's is over today — reviewer's counsel: index rotation to a SESSIONS-ARCHIVE store). Seeded four reconciled: budgets defensible, root-only right, proxy honest, advisory-alone toothless-but-not-load-bearing. **Wiring sizescan into any gate stays blocked until Mike rules + the F1 fix is live-reproven.** Claim mechanism lived end-to-end (claim on main → work → release).
- [2026-07-14 · two tool principles codified; economics + review alignment confirmed (Fable)](sessions/2026-07-14-2142-two-tool-principles-alignment.md) — Mike's two practices land as hard rules in PRINCIPLES §2: **human-readable output carries a machine-readable twin** (`--json` + exit codes on any tool that prints for a human; web UIs exempt, their services still earn an API) and **a commodity sub-feature sits behind a swappable seam** (in-house PKI CA/secret store/web server is a default implementation of an interface, never a load-bearing wall; case: tiki's planned PKI CA, labelled a direction not a shipped seam). Alignment confirmed, no edits needed: model-economics framing matches MODEL-ECONOMICS + precedence rule 6 (one nuance — quality trades only within low-risk work, cost never buys down correctness); review framing matches REVIEW near-verbatim (lens 1, independence rules, principal decides doctrine findings). Owed: cold pass on the two new bullets (ROADMAP, review-owed).
- [2026-07-14 · addendum: API first + mobile-first responsive join §2 (Fable)](sessions/2026-07-14-2142-two-tool-principles-alignment.md) — same family, two more bullets beside the first pair: **API first** (capabilities land behind an API before any surface; every client rides the same contract — grounding stated honestly as consumption-side today, tiki driving RouterOS via REST) and **one responsive web app, mobile-first** (a separate mobile edition is DRY violated at the presentation layer; labelled adopted-practice-no-shipped-case-yet). ROADMAP cold-pass item widened two → four bullets.
- [2026-07-14 · cold review: the four §2 PRINCIPLES bullets (Fable)](reviews/2026-07-14-2154-principles-s2-four-bullets-cold.md) — the owed un-briefed cold pass RAN (reviewer not the author; attack surface A1–A9 committed before the author's session detail or the seeded ROADMAP pointers were reconciled — the read-at-selection exposure named, not denied): **PASS-WITH-FINDINGS — 1 MAJOR · 3 MEDIUM · 1 LOW bundle**, all decisions Mike's (rule 3: agent-authored doctrine wording; the principles themselves are Mike's and untouched). Floor re-run green (247 tests · sizescan · linkscan · CI ×3 HEADs). **F1 MAJOR (reproduced):** "tiki drives RouterOS *exclusively* through its REST API" is false — tiki's own rescue path names its SFTP step ("the one non-REST step"), ros ACCESS-MAP names two authenticated management planes (REST + SSH); a flat falsity in the file whose own rule 2 forbids claims stronger than evidence. **F2 (reproduced, conservative direction):** "the seam is not yet built" was false at its own recording commit — ros shipped the pluggable CaBackend seam's first slice twelve hours *before* the bullet was committed (direction set 2026-07-12, written spec names the external-CA plug) — the grounding is *stronger* than claimed; REVIEW.md's stale-at-recording class, third instance. **F3** API-first lacks its sibling's scope sentence (literally read, every zero-dep house CLI breaches it — "product" undefined does all the scoping). **F4** "minor"/"legitimate" under-warns on security-critical commodities (no §5 pointer; no adopt-the-product-outright exit; the real tiki spec is *stricter* than the doctrine generalising it). **F5 LOW bundle:** exemption edge one-example-no-test; native-app edge unpicked; named-tiki cases vs the file's anonymised style; §6 cross-link missing. Reconciled clean: mobile-first's no-shipped-case verified true fleet-wide; caselessness honestly stubbed; "specified from the outset as swappable" true. MAJOR on first pass ⇒ applied batch earns its own cold pass. Claim mechanism lived (claim → attack-surface commit → verdict → release).
- [2026-07-14 · addendum: the four §2 findings applied — Mike ruled all five fixed (Fable)](reviews/2026-07-14-2154-principles-s2-four-bullets-cold.md) — Mike ruled as principal ("address any review findings") → **all five [fixed]**, applied to `PRINCIPLES.md` §2 same session. **F1** the "exclusively through REST" overclaim dropped, the SFTP rescue step named at true strength; **F2** the swappable-seam case restated — pluggable backend seam's first slice shipped 2026-07-14, direction set 2026-07-12 (the "not yet built" and misleading "decided 2026-07-14" both gone); **F3** API-first gained the Scope sentence its sibling carries (binds multi-surface/service capabilities; a single-surface CLI satisfies it through the machine twin); **F4** "minor"→"commodity", added the adopt-the-mature-product-outright exit (KISS/precedence 5–6) + the §5 by-design hold for security-critical commodities; **F5 bundle** — machine-twin gained the read-vs-operate exemption test + a §6 cross-link, mobile-first gained the native-app clause (a native app is a client riding the API-first contract, not a banned edition), named-`tiki` case style kept (consistent with sibling method docs). Floor green after applying (247 tests · sizescan · linkscan · leakscan). **Applier-independence exposure named, not denied:** this session authored both the verdict and the edits — REVIEW.md prefers a neutral applier — so the cycle-owed **applied-batch cold pass must run in a different session**; MAJOR on the first pass means the cycle does not close until that pass returns no MAJOR. ROADMAP item now reads *applied-batch cold pass owed*.
- [2026-07-14 · the "informed principal" apex rule — a decision isn't valid uninformed (Fable)](sessions/2026-07-14-1948-lean-current-truth-files.md) — Mike, having approved the four-bullet findings on trust and then asking whether the doctrine text had actually changed or just been marked `[fixed]`, ruled the gap into doctrine: **he cannot make or override a final decision without first being informed properly, in plain language.** Landed as a new `00-APEX.md` section *The principal's authority is conditioned on being informed* — the authority is real but **not exercisable uninformed**; before asking the principal to rule the agent owes, unprompted, the **what** (concrete before→after, not a label), the **why**, and the **likely impacts**, in plain-over-jargon; "an approval given on trust alone is obedience extracted, not a decision made, and the doctrine does not recognise it as one". Framed as the *positive face* of the honesty absolute (honesty forbids the false claim; this requires the true one to be comprehensible to the person acting on it). Pointers stamped at every decision site: `REVIEW.md` rule 3 (self-authored-doctrine findings), `RECORD.md` (ADR acceptance), `PROPAGATION.md` floor bullet — the floor sits inside the canonical child-stamped block, so it was **re-stamped into `build/templates/CLAUDE.md`** (test_templates caught the drift; fixed) and propagates fleet-wide on the next pin bump. Floor green (247 tests · sizescan · linkscan · leakscan). **Owed:** un-briefed cold pass on the wording (doctrine text; the principle is Mike's) — ROADMAP *Doctrine — review-owed*; attack surface seeded there (is "not a decision the doctrine recognises" too absolute; does the floor-block addition over-weight the child stamp; is the AUTONOMY "just proceed" seam clean).
- [2026-07-14 · addendum: informed-principal rule reviewed by a fresh agent, F1–F5 applied, cycle closed (Fable)](reviews/2026-07-14-2235-informed-principal-apex-cold.md) — the owed cold pass RAN via an independent spawned reviewer (not the author; seeded questions deferred below the divider, reviewer committed its own 10-assumption attack surface first — it named honestly that a whole-file Read seeded its context and compensated with breadth): **PASS-WITH-FINDINGS — 0 MAJOR · 4 MEDIUM · 3 LOW**. The reviewer earned its keep on Mike's *kept* wording (seeded q1): **F2** showed the teeth ("approval on trust alone is not a decision") keyed on the *principal's* absorption not the *agent's* discharge — so a fully-informed Mike waving it through was still "void", a Second-Law bind. Mike ruled **F1–F5 [fixed]**, applied same session: F1 apex list unified (governance ruling **or** always-confirm floor stop — child stamp no longer out-scopes parent); **F2** teeth kept but re-pointed at the agent *extracting approval by withholding*, plus the principal's **right to waive** once informed ("the agent informs; it does not police the principal's diligence"); F3 trigger runs both directions (incl. principal-initiated overrule); F4 AUTONOMY's pre-existing what/why/irreversible duty stamped as the reciprocal reference + COMMUNICATION cross-link (asymmetry gone); F5 the section **demoted to a `###` under *Honesty is absolute*** so "honesty and the Laws" stays literally true and the face-of-honesty framing is structural (F6 folded in; F7 no-action, lockstep noted). **0 MAJOR ⇒ cycle CLOSED** without a further ceremony (close rule). Floor green (247 tests · sizescan · linkscan · leakscan). The whole exchange dogfooded the new rule: reviewer informed Mike in plain what/why/impact, Mike ruled, nothing applied on the author's own authority.
- [2026-07-14 · 23:10 · Fable, the estate leaves iCloud — STORAGE.md rewritten](sessions/2026-07-14-2310-estate-leaves-icloud.md) — the owner-decided estate move (shed roadmap, 2026-07-14) executed: every repo copied iCloud → `~/.pets/` (byte-diff verified, fsck clean, all pushed), sibling-relative wiring survived intact, tiki venv now in-repo. This repo's edit: **STORAGE.md rewritten to the new topology** — three locations carry the four roles; the continuous-backup role iCloud filled splits across *push discipline* (offsite leg, new rule bullet: push early, push often) and *Time Machine → NAS*; the trade-off named (between push and TM snapshot, new work exists in one place); the churny-state gotcha generalised to *never let a sync engine hold the working copy*, estate-specific workarounds marked moot. `is_icloud()` tool checks + fixtures untouched — they detect a hazard that still exists for adopters.
- [2026-07-14 · cold review: PRINCIPLES §2 applied batch — escape valve triggered (Fable)](reviews/2026-07-14-2333-principles-s2-applied-batch-cold.md) — the owed applied-batch cold pass RAN by a different session (commissioning session neither author nor applier and read neither §2 nor the prior verdict before writing the un-briefed brief; the spawned reviewer committed its findings before opening the prior verdict, its one exposure — a subject line seen in `git log --oneline` — named, not denied): **PASS-WITH-FINDINGS — 1 MAJOR · 1 MEDIUM · 2 LOW**. Reconciliation: the batch was applied exactly as Mike ruled, and four of five fixes verify clean at the primary sources (F2 seam facts, F3 Scope sentence, F4 commodity + exits, F5 bundle). **F1 MAJOR:** the prior F1 fix traded a large overclaim for a smaller one — "its one non-REST path is a single SFTP upload on the rescue route" is false at ros HEAD and was false when written: ros `0df23f7` (2026-07-13, before both passes) snapshots over SSH before every mutating apply, `tiki pki` writes over SSH by design, ACCESS-MAP names two management planes; the false source is tiki's own stale `rescue.py:21` comment, which propagated through the prior counsel into the fix (REVIEW.md's stale-at-recording class, now a *laundered-through-counsel* instance). F2 MEDIUM: mobile-first's DRY rationale doesn't discriminate the banned web fork from the permitted native client. F3/F4 LOW: ungrounded vendor-UI-seam claim; one 80-word doubly-nested sentence. **MAJOR count not falling (1 → 1) ⇒ the escape valve, not a fourth ceremony: Mike rules on F1–F4 directly** (reviewer's counsel: a one-clause F1 ruling + fixing the stale ros comment, then fix-verification only). Nothing applied — rule 3, decisions are the principal's. ROADMAP re-pointed at the ruling.
- [2026-07-15 · addendum: Mike ruled F1–F4 in person — §2 re-scoped, cycle CLOSED (Fable)](reviews/2026-07-14-2333-principles-s2-applied-batch-cold.md) — the escape valve did its job: Mike ruled directly, supplying the doctrine's *intent* rather than accepting the counselled patch. **F1 [fixed] by re-scope, deeper than the counsel:** API first binds **where we design the service** — contract designed first as the single source of truth, data + business logic behind the API, a web front end holds presentation only; **consuming a third-party system is out of scope** (the provider made the design decision; we use whichever of its interfaces the job requires). tiki/RouterOS re-grounded as the consumption case stated honestly — REST for steady-state convergence, SSH/SFTP where REST doesn't serve (pre-apply snapshot, on-device PKI signing, rescue upload), each a stated choice, not drift — every fact re-verified at ros HEAD before writing. **F3 [fixed]** subsumed — the ungrounded vendor-UI-seam clause is gone with the re-scope. **F2 [fixed]** on Mike's mobile-first definition: the discriminator now stated — one responsive artifact (smallest screen first, CSS scales up, forces core content) already serves every form factor, so a web fork adds no capability; a native app is a different client *medium* (offline, push, sensors) riding the API-first contract — ban on forking the web surface, never on adding clients. **F4 [fixed]** in the same rewrite — short sentences, the carve-out on its own line. The false source killed at origin: ros `rescue.py`'s stale "the one non-REST step" comment (false since ros `0df23f7`) fixed + pushed as ros `261fca2`, per the reviewer's counsel. Applier independence held — this session authored neither the doctrine nor either verdict. **Cycle CLOSED on fix-verification** (principal ruled under the escape valve; no fourth ceremony); floor green (247 tests · sizescan · linkscan). Doctrine — review-owed is now empty of open cycles; the REACH/AUTONOMY H1–H8 backlog remains parked as backlog.
- [2026-07-15 · MODEL-ECONOMICS triple delta — sub-agents, tier selection, reset-by-record (Fable)](sessions/2026-07-15-0954-model-economics-triple-delta.md) — Mike's three economics questions answered and codified: sub-agents earn a real section (isolation-not-savings — a sub-agent re-pays its own overhead and often costs *more* total, buying a lean main context; when/when-not; the report is all that survives), model-tier selection made explicit (the runner-class rule applied to models — cheapest model that genuinely does the work, "genuinely" = a verifiability test, plus rework pricing), and hygiene item 4 rewritten as reset-by-record (no numeric context thresholds; the session record *is* this method's compaction; /compact = mid-task fallback, /clear only after the record). Cold pass ran inline same session: **PASS-WITH-FINDINGS — 0 MAJOR · 3 MEDIUM · 3 LOW** (F1 independence bind dropped, F2 flip-axis mis-specified as depth not work-remaining, F3 pool-blindness, F4 tier×sub-agent uncomposed, F5 cache-warmth inverts past TTL, F6 stale child template); every pricing claim re-ran clean against the current API reference. Mike ruled same day: **F1–F6 [fixed], cycle CLOSED** — F1 applied as the independence *bind* on warm spawns (ban held in reserve, distinction flagged), and Mike supplied a further nuance applied with the batch: **price the job, not the token** (a dearer model finishing in fewer turns/retries is often the cheaper job — only cost-to-done compares outcomes). Child template swept (F6); floor green; propagates on the next pin bump. Coda, same day: Mike reopened F1 — the conditional agreement hadn't matched his intent — and after a plain-language walk-through ruled **tiered independence, now REVIEW rule 4**: routine work may warm-spawn under the deferral bind; **self-authored doctrine is banned from warm spawns** (non-author spawn only — principal, scheduled batch, or neutral session), highest-stakes category stated explicitly in the rule. Rule 4's own cold pass queued for Mike to trigger (can't be warm-spawned by its author). Sentence-shape feedback also codified into COMMUNICATION.md on Mike's yes.
- [2026-07-15 · cold review: REVIEW rule 4 — the queued pass ran, 1 MAJOR, Mike's ruling owed (Fable)](reviews/2026-07-15-1202-review-rule4-cold.md) — the ⏳ pass triggered exactly per rule 4's own terms: Mike opened a fresh session and said "do any review work queued"; that neutral session (non-author — the author session's last act was 9fef4d4) wrote the brief and spawned the cold reviewer, which committed its own 10-point attack surface before opening the deferred questions or the ruling record (one disclosure: the whole-file brief read exposed Q1–Q4 early; overlap claimed as convergence, not independence). **PASS-WITH-FINDINGS — 1 MAJOR · 4 MEDIUM · 3 LOW.** The fidelity-to-intent check earned its place in the brief: Mike's recorded ruling is a single universal criterion ("a session the author neither started nor instructed"), but rule 4 drafts it as a three-path enumeration whose qualifier grammatically reaches only the third path — **F1 MAJOR: an author-written, author-queued, author-scheduled *batch* satisfies the rule's letter while the author keeps framing, timing and composition** — the residual-compliance risk the rule exists to remove, re-entering through its own example list. F2 the author's post-work handoff unstated (9fef4d4 improvised the right shape; doctrine doesn't require it) · F3 companion passages unswept (MODEL-ECONOMICS §Triggering reviews still sanctions inline spawn unqualified — the same single-doc-reader failure its own F1 flagged a cycle ago; "the building model's call" heading; the applier seam) · F4 doctrine-by-function now defined twice, divergently (rule 4 extends the list while citing "rule 3's definition") · F5 no provenance hook ("neither started nor instructed" has no test; this brief's commissioning note is the candidate mechanism) · F6 commission/spawn verb drift · F7 wording · F8 the ⏳ convention and the rule don't name each other. MAJOR ⇒ cycle stays open; **self-authored doctrine ⇒ F1–F8 are Mike's to decide, nothing applied** (rule 3). Floor green (247 tests · sizescan · linkscan).
- [2026-07-15 · addendum: Mike ruled F1–F8 as counselled — applied, application pass queued (Fable)](reviews/2026-07-15-1202-review-rule4-cold.md) — Mike accepted the reviewer's direction on all eight; the commissioning session (authored neither rule 4 nor the verdict, did write the brief — named, not hidden) applied same day. The MAJOR's fix is the load-bearing one: **rule 4 now states the ruling's single criterion as the test** — the review comes from a session the author neither started nor instructed — **with principal / scheduled batch / neutral session recast as examples that must each pass it**, killing the author-scheduled-batch laundering path (the batched-queue bullet carries the matching carve-out). Rule 4 also gains the author's handoff (queue the `⏳` pointer naming delta + intent record, stop; the taker writes the brief), the provenance requirement (brief states who spawned + author's non-involvement, verdict repeats it — no trail ⇒ unauditable ⇒ non-compliant), and the ⏳ licence (any criterion-passing spawner may take a queued item). Companions swept: MODEL-ECONOMICS §Triggering reviews + REVIEW §When to review drop "the building model's call" and gain the rule-4 exception; the applier seam closed — **an application of self-authored doctrine is itself self-authored, so the applier queues, never spawns, its application review**. Doctrine-by-function single-sourced in rule 3 (list gains behaviour-stamping template/skill, per the recorded ruling); verbs aligned on spawn; wording cleaned. **1 MAJOR at the pass ⇒ cycle stays open: the applied batch's cold pass is queued ⏳** (this applier can't spawn it, by the rule it just applied); its delta is the application commit. Floor green (247 tests · sizescan · linkscan).
- [2026-07-15 · cold review: rule 4 applied batch — 0 MAJOR, ceremony closed, Mike's ruling owed on F1–F7 (Fable)](reviews/2026-07-15-1244-review-rule4-applied-batch-cold.md) — the queued ⏳ pass taken exactly per the rule it reviews: Mike opened a fresh session ("do any review work queued"); that taking session (neither author of rule 4, nor of the verdict, nor the applier) wrote the brief and spawned the cold reviewer — and, having read the prior verdict to scope the brief, named itself ineligible to review. The reviewer committed an 8-point attack surface before opening the deferred questions or any prior verdict (two exposures disclosed: a 5-line brief-read overshoot grazed Q1; a `⏳` grep surfaced SESSIONS summaries early — overlaps claimed as convergence). **PASS-WITH-FINDINGS — 0 MAJOR · 2 MEDIUM · 5 LOW.** The fidelity check verified all eight of Mike's rulings encoded faithfully — nothing added, nothing dropped — and every claimed proof reproduces (247 tests · 34 instrument tests · 6 selftests · 5 tree scans). The MEDIUMs are seam debts the application created or inherited, not defects in what was ruled: **F1** the applier-seam sentence queues a pointer unconditionally while the close rule cancels ceremony for a no-MAJOR pass — the terminal application's obligation is ambiguous both ways; **F2** the `⏳` pointer is an undeferred framing channel — rule 4 specs it as *naming* delta + intent record, but the live pointer carried a 17-line evaluative summary in the applier's framing, read by the taker before writing the brief (REACH's mechanism, one channel over; counsel: make the spec a ceiling too). F3 MODEL-ECONOMICS §Sub-agents paraphrase drops "nor instructed" · F4 "commissioning provenance" vs the spawn verb · F5 ROADMAP legend lacks `⏳` · F6 whose non-involvement an application review tests (chain of authors) · F7 comma. **0 MAJOR ⇒ the close rule closes the ceremony; self-authored doctrine ⇒ F1–F7 are Mike's to decide** (rule 3) — queued 🎯 on the ROADMAP, nothing applied. Floor green.
- [2026-07-15 · addendum: Mike ruled F1–F7 as counselled — applied, CYCLE CLOSED (Fable)](reviews/2026-07-15-1244-review-rule4-applied-batch-cold.md) — Mike accepted the reviewer's counsel on all seven; applied by the taking session (wrote the brief + spawned the reviewer; authored neither rule 4, the prior verdict, nor the applied batch — named, not hidden). **F1**: the applier seam is now conditioned on the cycle being open, and the terminal application — applying the rulings of a no-MAJOR pass — closes without a queued pointer; this very application is that fix's first exercise (0 MAJOR ⇒ no pointer queued). **F2**: rule 4's handoff spec is a ceiling as well as a floor — the `⏳` pointer carries refs only (delta + intent record), the author's account lives in the intent record where deferral discipline governs when it's read. F3 MODEL-ECONOMICS §Sub-agents now states the full criterion (started **nor instructed**) · F4 "spawn provenance" (commission family gone) · F5 the ROADMAP legend defines `⏳` (non-author takes; taker writes the brief) · F6 an application review tests the *delta's* author at minimum, prior authors where practical · F7 the comma. **Ceremony closed on the ruled application; no further pass owed.** The whole cycle is now rule 4's own case study: drafted from ruling → 1-MAJOR cold pass → ruled + applied → 0-MAJOR applied-batch pass → ruled + terminal application. Floor green (247 tests · sizescan · linkscan). Propagates on the next pin bump.
- [2026-07-15 · 1327 UTC · CONVENTIONS.md + UTC-at-rest ADR — the default-frame doctrine (Opus)](sessions/2026-07-15-1327-conventions-default-frame.md) — Mike: "do we say anywhere what timezone we use?" We didn't — timezone was stated only in SIGNING's `Z`-anchoring, and only for machine stamps; every human-facing identifier was implicitly local NZ, un-anchorable to a reader elsewhere. Sharpened over four Mike refinements into a **general rule, not a timezone patch**: `method/CONVENTIONS.md` names the anti-pattern (an unlabelled frame — a time needs a zone, a price a currency, text an encoding) and states three clauses — **declare a default once at the boundary and it carries silently · label only a deviation or a collision** (~99% silent, risk-based not absolute; over-labelling is its own failure — no NZT on every line) **· preservation outranks normalisation** (foreign data we don't own kept as-is, its frame stored as metadata — ELT not ETL, a precedence not an exception). Declares the estate defaults (UTC at rest · NZD · ISO 8601 · UTF-8 · NZ English + te reo), roles-not-instances. ADR `2026-07-15-1327` is time's worked instance: UTC at rest, local+labelled on presentation, **record identifiers UTC-forward** (a key belongs in the canonical zone; existing files keep their names, boundary at the ADR) — its own `1327` UTC stamp against a local 07-16 clock is the rule dogfooding itself. Word settled on **convention** (not practice). Written across a session-limit pause + macOS-update reboot; recovered clean (all staged, no other session, floor green). Reboot cleared the ssh-agent → signed commit blocked on Mike's passphrase; after `ssh-add`, `67e8582` signed + pushed. **Config drift caught in passing:** `~/.gitconfig` still pointed `allowed_signers` at the dead iCloud path (2026-07-14 STORAGE move) → local `verify-commit` couldn't reach the trust list; repointed to `~/.pets/atelier/`, HEAD now verifies good. **Cold review ⏳ queued** — self-authored doctrine, non-author takes it (rule 4).
- [2026-07-16 · 1013 UTC · ccrepo — currency to a footnote, not a per-figure prefix (Opus)](sessions/2026-07-16-1013-ccrepo-currency-footnote.md) — Mike: ccrepo stamped the currency on every dollar figure (`NZ$412.14`); move it to a footnote near the `Rate:` line. CONVENTIONS' declare-once rule made literal on the instrument that inspired its currency analogy two sessions back — the currency is now named exactly twice (column header `Cost (NZD)` + footnote) and never per figure. `SYMBOLS` collapses the disambiguating dollar prefixes (`NZ$`/`US$`/`A$`/`C$`) to a plain `$`; `£`/`€`/`¥` unchanged; unknown codes drop to no marker (`symbolFor` → `''`). New footnote `All amounts shown in <currency>.` prints **always** (so `--fx usd`, which has no `Rate:` line, gets it too), directly above the rate line. Verified: 34 instrument tests green (`symbolFor` test updated to the new contract), both render paths driven live (NZD `$412.14` + footnote above rate; USD footnote present, no rate line). No review owed — instrument display change, self-verifying (ceremony ∝ risk).
- [2026-07-17 · 0810 UTC · ccarchive — durable transcript preservation (Opus)](sessions/2026-07-17-0810-ccarchive-transcript-preservation.md) — Mike: "keep all my raw transcripts word-for-word, timestamps + model metadata, for reference" — both levers, compressed archive to iCloud. The raw `.jsonl` already *is* the complete record; the risk is `cleanupPeriodDays` (default 30) deleting it. New instrument **`ccarchive`** (a fourth `instruments/` verb, **preserve**, and its first *writing* one): incremental per-session gzip mirror `<rel>/<name>.jsonl.gz` — not a monolithic tarball (which would re-upload the whole blob to iCloud each run) — append-only (a source pruned by cleanup stays kept), byte-preserving so schema-drift-immune, dest runtime-derived from `$HOME` (no personal path in code). Found+fixed a re-archives-every-run bug (`Date` ms-truncation of the stamped mtime; fix: numeric-seconds stamp + 1 ms freshness tolerance). 12 new tests (46 total green); driven live (428 transcripts, 396→143 MB, idempotent, byte-identity, launchd RunAtLoad). Machine-local: launchd daily agent; `cleanupPeriodDays` raised then reverted at Mike's call (archive is the durable copy, keep the live dir lean). Reconciled with `ccrepo.design.md` §8: ccarchive subsumes the deferred retention-ledger's *survival* rationale (lossless raw beats lossy rollups), leaving it a precompute option. Moved to a worktree mid-session on Mike's parallel-session flag. Owed: ⏳ cold review of the ADR 0006 addendum (self-authored doctrine, rule 4); §8 cross-pointer left for the ccrepo session.
- [2026-07-17 · 0908 UTC · ccarchive — self-installing schedule (Opus)](sessions/2026-07-17-0908-ccarchive-self-scheduling.md) — Mike: how does the schedule survive a new laptop — a feature in ccarchive? It was a hand-written launchd plist, reproducible by nothing (a new machine restores the tool via `install` but not the schedule). Made scheduling built-in: **`--install-schedule`** (writes + loads `com.ccarchive.archive` launchd agent, daily + RunAtLoad, idempotent, pointing at `~/.local/bin/ccarchive` + `process.execPath`, logging to `~/Library/Logs/ccarchive.log`), **`--schedule-status`** (read-only), **`--uninstall-schedule`**; non-macOS prints the cron line. New-machine recovery is now two documented commands (`./instruments/install` → `ccarchive --install-schedule`). +6 tests (18 total; pure builders incl. XML-escaping + read-only status spawn, never spawning install/uninstall to avoid mutating a dev's launchd). Driven live: status→install→status→RunAtLoad archived; migrated off the old hand-made `nz.cxi.ccarchive` agent (`launchctl list` now shows only the tool-managed one). No ADR change (doctrine unchanged); README updated. No new review owed.
- [2026-07-17 · 0918 UTC · ccarchive — integrity manifest + --verify (Opus)](sessions/2026-07-17-0918-ccarchive-integrity-verify.md) — Mike: is there a verify mechanism — how do we know restored files haven't been mutated, sha256? Gap: gzip's CRC-32 only proves a `.gz` is self-consistent, nothing binds archived content to what was captured. Added a **sha256 manifest** (`<dest>/manifest.json`, `{sha256,rawBytes,archivedAt}` per transcript's raw bytes, atomic write, sorted keys) and **`--verify`** (re-hash every `.gz` vs manifest → mismatch/missing/unmanifested, non-zero exit on failure). Manifest tracks the *archive* not live sources (a pruned source keeps its hash; first run backfills existing files from source or by decompressing the `.gz`). Honest limit documented: it's the trust anchor (defends accidental corruption; keep a copy separate for tamper-resistance). +7 tests (25 total; known-answer sha256, tamper→exit 1, missing→exit 1, pruned-entry survives). Driven live: built a 428-entry manifest (~1.5 s) → `--verify` = 428 verified, 0 mismatched, 0 missing. Also answered: **no man pages** for the cc tools (—h/README only; whether to add is Mike's convention call, queued). No new review owed.
- [2026-07-17 · 0921 UTC · ccrepo v2 — flexible grouping, filters, message-grain cost engine (Opus)](sessions/2026-07-17-0921-ccrepo-v2-grouping-cost-engine.md) — Mike: "build all the ccrepo related stuff we have talked about now." Built the design (`instruments/ccrepo.design.md`) in an isolated worktree alongside the live ccarchive session; PR #8. The pivot: **stop trusting ccusage for cost — compute it here, per message, reconcile against ccusage.** Message-grain engine reads the raw logs and prices each assistant message across five token classes (incl. the 5m/1h cache-write split) from a local table whose bases were validated against ccusage as clean round numbers (opus-4-8 $5 · fable-5 $10 · sonnet-5 $2 · sonnet-4-6 $3 · haiku-4-5 $1 /MTok in; ×5 out, ×0.1 read, ×1.25/×2 write) — **published, not fitted, so the drift check stays independent** and lands at **−0.05%**. Ordered `-g` grouping over repo/model/branch/kind/entrypoint/cc-version/agent/year–hour, any order (`--by-model`/`--by-day` dropped); filters mirror the vocabulary (OR within · `!` excludes · `*` globs · session UUID prefix); `--sort`; tree default + `--flat`/tidy `--json`/`--csv`; Sessions a distinct count at every level. Two build discoveries: subagent logs (`<session>/subagents/*.jsonl`) had to be walked recursively and folded into the parent session (~$167, closed the drift from −4% and powers the `kind` dim); and a truthy `'—'` repo default that blocked label re-resolution (fixed + regression test). Dedup keeps the **last** streamed line per `(message.id, requestId)` to match ccusage. 40 ccrepo tests (was 21), 58 instrument total; floor green (247 tool tests, all scans). ccarchive is design §8's retention-ledger — the two land together.
- [2026-07-17 · 0946 UTC · man pages + the CLI-docs standard (Opus)](sessions/2026-07-17-0946-ccarchive-man-cli-docs-standard.md) — Mike: I want both man pages and `--help` for CLI tools (man fuller/plain-language + examples; `--help` far more concise), and **capture the roles of each in atelier as doctrine children inherit**. Added the convention to `build/REPO-STANDARD.md` (§ Repo-craft conventions): `--help` = one-screen digest pointing at the manual; `man <tool>(1)` = the full plain-language reference (FILES/EXAMPLES/NOTES); the page is the superset so detail lives in one place and can't drift; ship roff in `man/`, installer publishes to MANPATH. Worked example landed with it: **`instruments/man/ccarchive.1`** (full manual, lints clean under `mandoc`), `ccarchive --help` trimmed 40→18 lines, and `instruments/install` now publishes man pages to `~/.local/share/man/man1` (auto-found via PATH) — fixing a latent bug where the bin loop symlinked *directories* (`man/`/`fixtures/`/`browser-fetch/`) into `~/.local/bin`. Also roadmapped Mike's four future ccarchive features (local-store audit; full/delta restore; **iCloud dataless/`SF_DATALESS` awareness**; **sign the manifest** → tamper-evident) + man rollout to cctranscript/ccrepo (ccrepo after v2). +2 tests (67 total). Self-authored doctrine ⇒ ⏳ cold review queued (rule 4). ccrepo left untouched (parallel session).
- [2026-07-17 · 0958 UTC · the three queued cold reviews taken (Fable)](sessions/2026-07-17-0958-three-queued-cold-reviews-taken.md) — Mike: worktree (parallel session live) + "do any review work". The three `⏳` *Doctrine — review-owed* items all taken by this rule-4-eligible session (the Opus author sessions neither started nor instructed it): claimed on `main` before working (`325011b`), taker-written briefs with the authors' seeds deferred below the divider, three cold reviewers run in parallel, each committing its own attack surface first and re-running every recorded proof (all green: 247 tool tests · 67 instrument tests · full scan set · mandoc · installer drive · the UTC dogfood verified behaviourally). **All three PASS-WITH-FINDINGS, nothing applied (rule 3):** CLI-docs standard **1 MAJOR · 4 MEDIUM · 2 LOW** (F1: the scope predicate is unbounded — it binds atelier's own seven `tools/` scanners, which have no man pages and no stated exception; the parent ships a letter it doesn't meet); ccarchive ADR 0006 addendum **0 MAJOR · 1 MEDIUM · 3 LOW** (F1: "append-only" is delete-proof not overwrite-proof — a corrupt newer-mtime source silently overwrites the only durable copy and the manifest blesses the damage; the seeded questions all cleared — preserve is a real verb, the public-repo boundary holds, §8 holds); CONVENTIONS + UTC-at-rest ADR **1 MAJOR · 0 MEDIUM · 4 LOW** (F1: the ADR changed what `HHMM` means but the six identifier-minting docs, child templates included, still describe the old regime — stale in the delivering commit by RECORD's own lockstep rule; no person-local leak). **Owed: Mike's rulings** on the three batches — 🎯 ROADMAP items point at the verdicts with per-finding counsel. Claims released with this landing; worktree put away.
- [2026-07-17 · 1003 UTC · ccrepo — a Messages column + two roadmap items (Opus)](sessions/2026-07-17-1003-ccrepo-messages-column.md) — Mike: since we deal in messages now, add a message count "the same way we report the number of sessions." Each message-grain event is one deduped assistant message, so `messages` threads through `zeroAgg`/`addTo` (=1 per event) and shows as a **Messages** column after **Sessions** in tree/`--flat`/`--json`/`--csv`. The two are different-on-purpose: Sessions is a **distinct** count, Messages **sums** (24,312 main + 1,423 subagent = 25,735; the 287 distinct sessions don't sum across kinds) — footnote says so. Also captured two Mike roadmap items under instruments/ccrepo: **tighten the ccusage reconciliation drift** (~0.05% → chase per-model residual, name causes) and **actual plan/usage spend vs the API estimate** (money-side analog of the ccusage cross-check; real-spend source stays in `~/.claude`). Man-page rollout note flipped to unblocked (v2 landed). 67 instrument tests; sizescan + linkscan clean.
- [2026-07-17 · addendum: all three batches ruled + applied same session (Fable)](sessions/2026-07-17-0958-three-queued-cold-reviews-taken.md) — Mike ruled the three finding batches as counselled (CLI-docs F1–F7, ccarchive F1–F4, CONVENTIONS F1–F5 + his own principal-raised **F6**: the "ISO 8601" table row declared a standard the estate deliberately profiles — strict ISO wants `T`, zone-less means local; row rewritten as the declared three-shape house profile). Applied by the taking session (authored none of the doctrines nor verdicts), per-finding decisions stamped in the verdict files. **ccarchive CYCLE CLOSED** (0 MAJOR; shrink guard, repo-dest guard, layout-drift alarm, strict `--verify` — tests 27→35, live dry-run clean); **CLI-docs + CONVENTIONS cycles stay open** (MAJOR at first pass) with applied-batch ⏳ passes queued refs-only for non-author takers. The UTC rule now reaches the six minting sites incl. the child templates (canonical block + template together, drift test green — propagates at next pin bump); installer cleanup proven and the live `~/.local/bin` residue removed. Floor green: 247 tool · 75 instrument · mandoc · sizescan · linkscan · scans.
- [2026-07-17 · 1340 UTC · the two queued applied-batch cold passes run (Fable)](sessions/2026-07-17-1340-two-applied-batch-cold-passes-run.md) — the rule-4 taker Mike pointed at the queue claimed both `⏳` applied-batch items (`c10f73e`, solo on `main`), wrote refs-only briefs with the application-review sequencing, and spawned two cold reviewers; both were cut by the session limit mid-run and resumed with context intact (interruption + disclosures in each verdict). **Both PASS-WITH-FINDINGS with 0 MAJOR** — CLI-docs 2 MEDIUM · 3 LOW (sharpest: the man page's `EXIT STATUS` predates all four new non-zero exit paths), CONVENTIONS/UTC 1 MEDIUM · 5 LOW (sharpest: a seventh minting site `REVIEW.md:157` unannotated; plus one reconcile-stage drift — prior-F6's foreign-formats line silently dropped). All seven and six prior rulings respectively reconciled as implemented. Every recorded proof re-ran green (247 + 75 tests, drift tests, mandoc, five scans, installer drive, MANPATH empirics). Nothing applied — **both rulings are Mike's; 0 MAJOR ⇒ each cycle closes on his ruling** (🎯 ROADMAP). Stale `fable-review` worktree removed.
- [2026-07-17 · 2234 UTC · ccarchive — `--audit`, the live-store drift check (Opus)](sessions/2026-07-17-2234-ccarchive-audit.md) — instruments backlog (Mike's theme); first of ccarchive's four open features. `--verify` checks the archive against its own manifest; **`--audit` checks the *live* store against the archive** — hashes every live `.jsonl` and buckets it: **synced** · **grown** (archived bytes a strict prefix — a plain append the next run captures; the classification that keeps the audit *honest not noisy*, so an active session between daily runs isn't a false "mutated") · **mutated** (rewritten/truncated) · **renamed** (content matched an archived path now gone from live; a still-present match is a copy, not a move) · **new** · **pruned**. Only mutated+renamed are drift (listed, non-zero exit); the rest counted. Read-only over both trees ⇒ no dest git-guard. Pure core (`auditCategorize` sha-bucketing + `classifyDivergence` prefix test) unit-tested; the runner bounds the gunzip IO to the sha-failed `changed` bucket. +11 tests (ccarchive 35→46, instrument 86; tool suite 247 green; mandoc + sizescan + linkscan clean). **Driven live**: 435 archived · 432 synced · 3 grown · 19 new · 0 drift → exit 0 (active sessions read as grown/new, the design goal observed). man AUDIT section + README + `--help`. No review owed (instrument diagnosis, writes nothing). Worked in `instruments-audit` worktree alongside a live main session (rebased onto its `dd51a14`→`7202b85`, disjoint files); landed `0b783ec`, worktree away. Next ccarchive item = **restore** (the writing cure to this read-only diagnosis; its own session + Mike's clobber-UX steer).
- [2026-07-17 · 2235 UTC · file-size hygiene — ros pivot, then the faves harvest (Opus)](sessions/2026-07-17-2235-file-size-hygiene-ros-pivot-faves-harvest.md) — Mike's theme *File-size hygiene (own focused sessions)*. Claimed the flagship **ros ROADMAP harvest** (now **4933 lines**, grown from the 3197 on file) and set up a worktree — but caught a **live ros session** actively claiming roadmap items (`495def8`, mid-`radius-home`), i.e. editing the very `docs/ROADMAP.md` a wholesale reorg would rewrite. **Released the claim, recorded a liveness-check hazard on the item** (a wholesale reorg can't run concurrent with an active ros session — "its own focused ros session" is literal), and pivoted to the quiet **faves** harvest. faves brought fully under budget, `sizescan` clean: `SESSIONS.md` **1157→234** (adopted the index **rotation** the ros/tiki convention implies but faves never made — recent tail stays, older entries verbatim → new `SESSIONS-ARCHIVE.md`); `ROADMAP.md` **766→299** (current-truth/history split — *resolved* items incl. decided-against ✗ and owner-parked → new `ROADMAP-DONE.md` behind lean pointers; relocation done by a **scripted line-range partition** with a coverage check + byte-identical verbatim assertion per block, heading set proven unchanged); `ARCHITECTURE.md` **276→250** (trimmed without losing facts — decided-history condensed, stale hosting row fixed, prose de-duplicated, the rot-prone `js/` module list condensed). Three faves commits `dba7658..ab6a12d`, ff-merged + pushed, worktree away. atelier ROADMAP: faves `[x]`, ros released with the hazard; kept atelier's own ROADMAP ≤300 (dogfood). No review owed (mechanical relocation, verified verbatim; no doctrine change).
- [2026-07-18 · 1602 UTC · the signing warn→block gate, re-probed (Opus)](sessions/2026-07-18-1602-signing-flip-gate-reprobed.md) — Mike challenged the roadmap's "not agent-actionable yet" on the code-signing flip ("I think we are ready … agreed?"). **No** — and the standing 2026-07-12 claim that the flip "wouldn't newly-red them" was itself unsupportable: under `--warn` no floor has ever *failed* on signing, and on the scanner-red children the signature steps never execute at all (earlier scanner fails, later steps skip), so greens and reds were both silent. A blocking-mode probe across all 12 children at **their own pins and boundaries** returned **10 pass / 2 fail — both failures currently green**. Seven commits dated 2026-07-12 carried no signature header. The committer-name drift (a short display name vs the machine's full git identity) read as a second unsigned machine; **Mike corrected that — one laptop** — and the ordering (signed → unsigned → signed) plus one shared committer timestamp, spread author times, the reflog, and **pre-merge originals surviving as dangling objects still correctly signed** pinned the real cause: a **rebase-merge** (`gh pr merge --rebase` — merges here are agent-run via CLI, not the web UI), which GitHub performs server-side, strips local signatures, and stamps the committer with the merging account's *display name*. The sharp finding is that it **recurs**: squash/merge-commit are web-flow-signed and already deferred to the gh plane, rebase-merge is not. Both boundaries corrected on the principal's per-case agreement (shed costs 0 already-signed commits, ros costs 8 of 459 — put to Mike explicitly, not absorbed); probe now **12/12 PASS**. Two probe errors self-caught and recorded (a `sed`/redirect bug that faked a fleet-wide "pin predates signing", and a miscounted range briefly blamed on a concurrent session). **Rebase-merge then disabled on all 13 repos on Mike's call** (verified by independent re-read, not the write response) — the cause shut server-side rather than left to each session to remember; merge-commit + squash remain, local `git rebase` unaffected and still signs, reversible per repo. Left open: the unchanged scanner debt, the still-unverified "every active machine signs", and ros's floor red on 17 pre-existing broken links (unrelated, but it blocks CI from confirming ros's signing fix — the local probe is the only proof there). Roadmap correction had to be tightened after it pushed the file over its own sizescan budget. **The probe was then promoted to `tools/signfleet.py`** (reuses `pins.discover` + `signscan.scan` by import; per-child pin/boundary resolution is why it can't be a loop over signscan; local-only like `pins`, so the suite proves it — 17 tests, 247→264, the load-bearing one asserting it goes RED on genuinely unsigned commits). Documented in `method/SIGNING.md`.
- [2026-07-18 · 1655 UTC · fleet file-size sweep — ros harvested, shed rotated (Opus)](sessions/2026-07-18-1655-fleet-file-size-sweep-ros-shed.md) — Mike: "try doing ros repo again", then "and any other repos that should be done as well". **Liveness checked first** (the lesson from the 07-17 release): ros had no worktrees, clean tree, last commit ~1h old, previous session logged closed — claimed on main and pushed *before* touching ros, and re-checked immediately before the merge (main unmoved, no collision). Scoped the **whole fleet up front** — `sizescan` over all 14 repos found only two over budget: ros (4 files) and shed (1). **ros ROADMAP 7123→4755**: 116 completed blocks moved verbatim to `ROADMAP-DONE.md` behind one-line pointers by a deliberately conservative harvester (only `[x]`, >4 lines, and **no nested open sub-item** — so open work can't be swept along), verified byte-identical with the **open-item census unchanged** (`[ ]` 101, `[~]` 24). Caught a real defect mid-run: `textwrap` split a markdown link across lines; fixed to keep links atomic and re-ran from pristine (116 well-formed, 0 split). Also ros `SESSIONS.md` 269→174, `CLAUDE.md` 257→135 (its self-declared legacy half → `docs/LEGACY-INIT.md`), `tiki ARCHITECTURE` 329→319. **shed SESSIONS 318→204** (verbatim → new `SESSIONS-ARCHIVE.md`, all 14 entries verified present once) — plus a **worktree hazard fixed**: shed's `hooks.atelierTools` was *relative*, resolving only from the main checkout, so the fail-closed scanner blocked every worktree commit (set absolute; worth checking other children). **Where it honestly stops — 🎯 two calls for Mike:** ros's ROADMAP can't reach 300 by harvesting (the `[x]` residue is only 431 lines; 3733 are *genuinely open* items) — it needs a SPECS.md migration or a subsystem split; and `tiki ARCHITECTURE` now **declares a `sizescan` budget of 320** with in-file reasoning rather than sitting red, because ~100 of its lines are Purpose doctrine ratified 07-18 (the clean fix is splitting Purpose out). Fleet now sizescan-clean but for that one file; atelier's own ROADMAP held at 300 by moving its two oldest done harvest items to `ROADMAP-DONE.md`. No review owed (mechanical, verified).
- [2026-07-18 · 1830 UTC · addendum: both structural calls ruled and applied (Opus)](sessions/2026-07-18-1655-fleet-file-size-sweep-ros-shed.md) — Mike ruled both questions the harvest raised. **(a) SPECS migration chosen** → ros ROADMAP **4756→982** (7123 at session start, **86% down**) in three verified passes (open-item detail 66 · feature prose 10 · review-surfaced design work 7; genuine review-queue items stay — SPECS is for feature specs, not queue bookkeeping), done pointers collapsed per section. Verified each pass — byte-identical in SPECS, open census unchanged (101 `[ ]` + 24 `[~]`), 125 titles reachable, no broken links. Left **+682 over budget deliberately** — 125 open items is a structural floor, and silencing a *true* signal is the very error (b) is about. **(b) Mike challenged the a declared `sizescan` budget of 320 and was right** — I'd reverse-engineered it from the file's own 319-line length, so the ceiling was "whatever the file happens to be": circular, ungrounded, and precisely the *"raise the budget"* remedy the 2026-07-14 cold review already ruled **"defers the collision, doesn't resolve it"** (its standard: budgets are defensible only as **grounded** heuristics). Withdrew it; applied the real fix — **Purpose split verbatim to `tiki/docs/PURPOSE.md`** (~100 lines of product doctrine that had been the longest section of a doc about layout/transport), cross-refs updated in lockstep, **ARCHITECTURE 319→218** under its real budget. **Gap closed at the point of use**: the rule lived in a review verdict but not beside the hatch it governs, which is why it was missed — `tools/sizescan.py` now states it (ground it in the file's *class*, never its current length; raising defers; else leave it red). 264 tests green.
- [2026-07-19 · 0111 UTC · the review trigger is commitment, not artefact (Opus)](sessions/2026-07-19-0111-review-trigger-commitment-not-artefact.md) — Mike's steer: *"fix the source of the problem upstream rather than adding more and more mitigations downstream."* Also asked whether the `⏳` cold review on `30c9cd9` had run — **it had not** (no verdict file; pointer still open, 8 commits since). The 2026-07-18 change diagnosed the framing defect correctly — every scope-rule formulation phrased around *a change*, grammar presupposing the work exists — then wrote the diagnosis into `method/REVIEW.md` and stopped. A sweep found what that left: **`build/templates/docs/reviews/README.md`, the file `create-repo` stamps into every child and the one an agent actually reads when deciding to queue a review, still carried a 3-for-3 diff-shaped trigger and still listed *"a doc line"* as not-review-worthy** — the very template `REVIEW.md` cites as one of three places "the correct rule already sat" when `ros` broke it. It was the broken rule, propagating fleet-wide. **The upstream defect: that file was an unmarked fork.** `templates/CLAUDE.md` says *"stamped copy, not a second source"*; the reviews template said nothing, pointing up for the *lifecycle* while restating the *trigger* and *format* as its own — the N-copies shape `PROPAGATION.md` rejects, inside the doctrine forbidding it. The tell that yesterday's fix was downstream: the grammar needed patching in five places, and the next correction would need five more. **Fixed at source instead:** `REVIEW.md`'s trigger re-keyed on **commitment, not artefact** (*what will come to rest on this once it is trusted* — parses the same holding a paragraph, a plan, or a patch); the "why the framing was the trap" paragraph shrank to hand the correction upstream and carry the transferable rule (framing before enforcement); the reviews template converted fork→pointer with a stamped-copy header naming the 07-18 drift as its worked example, its brief format made fillable by unbuilt work (`Build`→`Subject`, `Real-world check`→`Grounding`), *"a doc line"* replaced by its opposite; `MODEL-ECONOMICS.md` + `templates/CONTRIBUTING.md` re-nouned to point rather than imply. **Honest measure: +89/−47, net larger** — what shrank is independent statements of the trigger, **4→1** plus three pointers; a line-count win would be the wrong claim. **Concurrency caught mid-turn**: `sizescan` read `ROADMAP.md` at 297 having read 310 minutes earlier on a file this session never touched — another session live with *staged* changes; diff captured as a patch, `main` restored to theirs alone, work moved to worktree `atelier-review-trigger-commitment` per `CONCURRENCY.md`. **Owed:** the `⏳` delta needs extending to name these files (queue is edited on `main`, which was mid-commit); and the 07-18 structural remedy still has **no artefact** — no ADR/decisions/ROADMAP template carries a `review:` field, so templates keep manufacturing the blank the rule calls a bug (this session's decision record carries it by hand; template change queued, not made, to keep the delta reviewable as one thing). No review spawned — self-authored doctrine, author is the prior delta's applier ⇒ rule 4 in full.
- [2026-07-19 · addendum · Fable retrospective on the 0111 session (Fable)](sessions/2026-07-19-0111-review-trigger-commitment-not-artefact.md) — Mike switched the session to Fable and asked for a review. Boundary first: **model switch ≠ cold spawn** — rule 4 tests the session's provenance, not the tier, so this is a retrospective + author's counsel, not the queued review (which **still hasn't run**, now the critical path). Findings, fixed same turn: a **records overclaim** ("months after the parent said otherwise" — the repo is nine days old) corrected in CHANGELOG + the template header; the harvest **verified post-hoc, clean** (26 moved, 0 open lost — outcome fine, process below the verify-before-commit bar); residual build grammar the "4→1" claim missed aligned (`REVIEW.md` intro, `PROPAGATION.md` enforcement clause) with `PRINCIPLES.md`'s header queued as substantive; create-repo placeholder wiring **verified not assumed** — and the old template's relative link turned out broken in every stamped child, strengthening the fork→pointer case. ⏳ entry notes the delta widened; fleet re-stamp of the reviews template deliberately gated **behind** the cold pass.
- [2026-07-19 · ~0330 UTC · tripwire ruling applied — sizescan gate split + fleet hook sweep (Opus)](sessions/2026-07-18-1655-fleet-file-size-sweep-ros-shed.md) — Mike accepted all four review recommendations; applied in one pass. **R1+R2**: `--check` now gates only the lossless-remedy files (`ROADMAP.md`/`SESSIONS.md` — a red demands a *move*); judgement docs report `[advisory]` but never fail the build; one-sidedness (no thinness floor) stated as deliberate. `GATED` set pinned by test; suite **267 green**; both classes live-proven (atelier gated red → exit 1, judgement-doc red → exit 0); `floor.yml` template comment rewritten (children adopt at pin bump). **R4 widened**: the `hooks.atelierTools` relative-path bug was **fleet-wide** — 9 more children born with it; all 10 set absolute AND the source fixed (`create-repo` step 6 now absolutises what it stamps — grammar-before-enforcement applied at the stamping site). **R3**: sizescan doctrine deltas folded into the queued ⏳ pass. ⚠️ **Silent-absorb occurred and is recorded**: a parallel session's records commit (`4fb09a7`) swept this session's uncommitted gate-split code out of the shared tree under a message that doesn't describe it — content landed in its tested state (verified: 267 green post-absorb), the ⏳ item carries the provenance note for the taker, and the two sessions' interleaved ROADMAP edits were reconciled rather than clobbered. Also moved the ccarchive `--audit` done-detail to `ROADMAP-DONE.md` (verbatim-verified) — the lossless move the gate demanded, at 300 exactly.
- [2026-07-19 · 0407 UTC · the combined cold pass taken (Fable)](sessions/2026-07-19-0407-combined-cold-pass-taken.md) — "Do any reviews, use a worktree": the three-delta `⏳` item (design-review section + commitment re-key + sizescan doctrine) taken by a rule-4-eligible session — claimed on `main` first, taker-wrote the brief (authors' accounts all below the divider; they seeded no questions), one cold reviewer with an auditable attack-surface→findings→reconcile commit trail, every recorded proof re-run. **PASS-WITH-FINDINGS, 3 MAJOR · 3 MEDIUM · 3 LOW, nothing applied** (`reviews/2026-07-19-0407-review-trigger-sizescan-combined-cold.md`). The doctrine content held; the **verification record** didn't: F1 the stamped `<atelier-path>` pointer is never filled in a create-repo child and the prove-the-stamp grep misses it (🚩 fleet re-stamp stays gated); F2 the "old link broken in every stamped child" claim is false on re-run — the conversion swapped a resolving link for a non-resolving placeholder while the record asserts the reverse; F3 `skills/review-brief` still carries the old artefact trigger (sweep never covered `skills/`). F8 rules the PRINCIPLES.md header question: genuine defect, re-key with the application. 🎯 **Mike's rulings owed on F1–F9** — counsel: take F1–F8, accept F9. Cycle open (3 MAJOR).
- [2026-07-19 · addendum · all nine findings ruled + applied (Fable)](sessions/2026-07-19-0407-combined-cold-pass-taken.md) — Mike: "I will take your recommendations and I want all of them fixed" — F1–F8 [fixed], F9 [accepted], applied same session by the taker (authored neither deltas nor verdict). create-repo fills + greps the whole stamped tree (F1, live-proven red/green in scratch); F2/F4's false claims corrected by dated addenda, originals standing; `skills/review-brief` re-keyed + marked stamped copy (F3); the gate wording's honest all-current edge stated (F5); "enforcement is structural" qualified until the `review:` templates land (F6); both stamped review surfaces pinned by drift tests, the old artefact phrases test-banned (F7, suite 267→274); PRINCIPLES.md header re-keyed (F8). Decisions stamped in the verdict. Application is doctrine ⇒ **⏳ applied-batch pass queued** (non-author taker); fleet re-stamp unblocked by F1 but held behind it.
- [2026-07-19 · 0544 UTC · the applied-batch cold pass taken (Fable)](sessions/2026-07-19-0544-applied-batch-cold-pass-taken.md) — "Do any review work": the queued `⏳` applied-batch item taken by a rule-4-eligible session (authored none of the deltas, the 0407 verdict, or the application) — claimed on `main`, taker-wrote the brief (the 0407 verdict's hunks + applier's log below the divider, the application-review rule-2 residual named), one cold reviewer with the auditable attack-surface→findings→reconcile trail (`04ae013`→`b556392`→`52550ef`), every recorded proof re-run (suite 274, four scans, eight F7 bite-mutations red in scratch). **PASS-WITH-FINDINGS, 1 MAJOR · 0 MEDIUM · 1 LOW + 1 note, nothing applied** (`reviews/2026-07-19-0544-combined-applied-batch-cold.md`). The application verified clean — all nine rulings applied exactly as ruled, F2/F4 addenda independently fact-checked. The MAJOR is F1's *proof*: **G1** the whole-tree prove-the-stamp grep can never go green on a full scaffold (floor.yml's `<SHA>` pin slot is a standing hit) — the "live-proven both ways" green only held on a partial tree, and 🚩 the fleet re-stamp would ship a check that is red on every healthy child (ROADMAP flipped to HELD). G2 (LOW) the missing set-wide placeholder-inventory test; G3 withdrawn as a defect. 🎯 **Mike's rulings owed on G1–G3** — counsel: take all three (small fixes); next pass likely terminal. Cycle open (1 MAJOR).
- [2026-07-19 · addendum · G1–G3 ruled + applied (Fable)](sessions/2026-07-19-0544-applied-batch-cold-pass-taken.md) — Mike: "yes take all three" — G1–G3 [fixed], applied same session by the taker (authored neither doctrine, batch, nor findings). floor.yml's pin slot reworded out of placeholder vocabulary and the prove-the-stamp grep **re-proven both legs on a full scaffold** (G1 — the green leg now exists); the false "live-proven both ways" claim corrected by dated addenda, originals standing; set-wide placeholder-inventory test added, suite 274→275, bite-proven (G2); one-liner on the 0820 record (G3). Fleet re-stamp's blocker cleared, still held behind the open cycle. Application is doctrine ⇒ **⏳ pass queued** (non-author taker); next pass likely terminal.
- [2026-07-19 · 0629 UTC · the G1–G3 application cold pass taken (Fable)](sessions/2026-07-19-0629-g1g3-application-cold-pass-taken.md) — "Do any review work": the queued `⏳` G1–G3-application item taken by a rule-4-eligible session (authored none of the chain) — claimed on `main`, taker-wrote the brief (verdict-file hunks + applier's log below the divider, pre-claim exposure named), auditable attack-surface→findings→reconcile trail (`bf5a5b9`→`4cbdb18`), every recorded proof re-run: suite 275 OK, G1's red leg 11 hits / green leg exit 1 on a full scaffold copy **with floor.yml present**, G2 bite-proven red/green, G3's three claims verified. **PASS — 0 MAJOR · 0 MEDIUM · 0 LOW + 1 note (N1: create-repo's machine-local vocabulary copy, prose-synced — unpinnable in-repo)**; reconcile found no discrepancy. First no-MAJOR pass ⇒ **terminal** per the close rule. 🎯 **Mike's ruling owed**: close the cycle (counsel: yes), accept N1 as named; on close the fleet re-stamp hold lifts.
- [2026-07-19 · addendum · cycle closed on Mike's ruling (Fable)](sessions/2026-07-19-0629-g1g3-application-cold-pass-taken.md) — Mike: "accepted" — the review-trigger/sizescan combined cycle **[closed]** on the terminal no-MAJOR pass (0407 → F1–F9 → 0544 → G1–G3 → 0629); N1 [accepted] as named. Decisions stamped in the verdict; ROADMAP item retired to the completed list; **fleet re-stamp unblocked** — the next open item there. Nothing to apply ⇒ no new ⏳, per the close rule.
- [2026-07-20 · 1830 UTC · recovery sweep after cmd+Q (Opus)](sessions/2026-07-20-1830-cmdq-recovery-sweep.md) — Mike cmd+Q'd VS Code with several sessions live, re-prompted each, then hit the session limit mid-action — asked each to verify nothing was lost and tidy up. **atelier: nothing lost, no mess** — working tree clean, `origin/main` 0/0, no stashes, no orphaned worktrees, and the reflog shows no work after the clean close at `dc7f67d` (the review-trigger/sizescan cycle closed terminal before the interrupt, addendum already logged). The interrupted session had committed+pushed its close, so nothing was in flight. **Boundary respected:** a sibling session is live in `ros` right now (its own recovery `45ec39c` at 18:20, `ros-ipv6` worktree already cleared, mid-work on `tiki/docs/GLOSSARY.md`) — tiki recovery + tiki's learnings-capture are that session's job per CONCURRENCY; this session read `ros` for the picture but changed nothing there. A stray `~/worktrees/.pytest_cache` noted, left as found (harmless, ros session's lane). Open unchanged: fleet re-stamp (deliberate item, not crash residue).
- [2026-07-20 · ~1945 UTC · Documentation steer + CONCURRENCY "assume you are not alone" (Opus, wt: atelier-concurrency-assume-parallel)](ROADMAP.md) — Mike surfaced a parallel session's DOCUMENTATION thinking for cross-check; the two independent lines converged hard (point-of-need · altitude · matrix-of-cells · fact-lives-once · same-commit currency · claims-carry-their-rung · errors-are-docs · contract-is-docs-for-software · vendor seam). **Decisions recorded on the DOCUMENTATION ROADMAP item (not authored — the parallel session owns the draft):** anchor on **Diátaxis** + a **consumer axis** (human · AI · software) as the extension; fold three deltas — the **developer** cell (Diátaxis *explanation*, easy to conflate with expert-user), **atelier's record doctrine already IS the AI-reading standard** (so that cell is extraction not invention), and **version-pin the vendor pointer**. Conceded the parallel line's "ship the harvester not the harvest" framing + pool-prefix-length exemplar are sharper. **CONCURRENCY posture flip (Mike's instruction):** `CONCURRENCY.md` § The trigger now leads with a *concurrent* prior — a clean tree is not evidence of solitude, because the doctrine's own commit-small-push-fast hygiene leaves a disciplined parallel session's tree clean between commits (the dirty-tree backstop's blind spot). Precaution scales with the write (read=none · light=sync+claim · write-heavy=worktree by default); solo default reframed as a conclusion earned from evidence, not assumed from silence; `CLAUDE.md` onramp rule 1 restated to match. Dogfooded — done in a worktree despite a clean tree. ⏳ **queued for independent review** (doctrine change reversing a decided prior; author does not self-review). 🚩 **Boundary:** tiki's application half + its learnings-capture are the ros session's lane per CONCURRENCY — not touched from here.
- [2026-07-20 · ~2015 UTC · DOCUMENTATION doctrine — first draft authored (Opus, wt: atelier-documentation-doctrine)](ROADMAP.md) — Mike ruled "start writing … then cold-review my work against any competing drafts to reconcile". Authored `method/DOCUMENTATION.md`: one purpose (transfer intent/capability at the reader's altitude) → two axes (**Diátaxis** what/who × a **consumer** axis human·AI·software that Diátaxis lacks) → the per-cell matrix, artefact inventory (error messages + the `--json`/exit-code machine contract named as documentation), five principles (one-fact-one-home · same-commit currency · claims-carry-their-rung · consumer-sets-form-fact-lives-once · point-of-need), the AI cell as *extraction* from the record doctrine, and the vendor seam (point-never-mirror, version-pin the pointer, "ship the harvester not the harvest"). Grounded read-only against **ros @ 806eb10**: the consumer axis generalises `ros PRINCIPLES §6` ("the principal may be a machine"; tier-1 AI / tier-2 orchestrator split); the vendor-seam exemplar is tiki.1's real CAVEATS (flash-floor RAM-only silent-revert) + its MACHINE OUTPUT section. **Honest gaps stated in-doc:** tiki.1 SEE ALSO does *not* yet point to vendor docs — that (and the whole tiki application) is ros's half, owed, and is the review's evidence base. Registered in `method/README.md` (15). ROADMAP item flipped `[ ]`→`⏳`; **cold review queued** against competing drafts — author does not self-review (REVIEW.md rule 4 / review-brief-independence). Worktree-isolated throughout (parallel session live on `atelier-session-onramp-review`).
- [2026-07-20 · 1302 UTC · Session-onramp operating-rhythm doctrine (Opus, wt: atelier-session-onramp-review)](sessions/2026-07-20-1302-session-onramp-operating-rhythm.md) — Mike asked to review atelier + children + past sessions for how he works with Claude, offering ~7 candidate standing per-session instructions. **Reframe:** 6 of 7 are already grounded doctrine in `method/*`; the gap is **reach** — the only always-loaded surface (the standard child doctrine block, `PROPAGATION.md`) inlines the *safety floor* only, so claim/economics/close never reach a session. Paused mid-task at Mike's word while a sibling session (`atelier-concurrency-assume-parallel`) held the same docs; resumed after it landed `295d94a` + a second landed `62fe96a`, rebased, re-validated. **Authored (home docs, applied):** (A) `CONCURRENCY.md` — a live `[~]` claim **outranks a standing instruction** to take that item; (B) `MODEL-ECONOMICS.md` item 7 — **surface the session boundary** + **stop safely under overload**; (C) `RECORD.md` — the declare-the-close all-clear **carries its evidence**, not a bare "done" (Mike's session-close communication point). **Dropped** "think step-by-step" (ungrounded); **folded** "focus on given work" → "stay in your lane". **Applied:** the child-block **Session rhythm** cue now lives in `PROPAGATION.md` + `build/templates/CLAUDE.md` (Mike ruled *yes*, decoupled from the concurrency-flip catch-up, which stays the concurrency cycle's job). **Follow-on (Mike commissioned, fresh session):** *rebalance the size signal* — meter the hot path, gate only on relocatable cold content, never on live fulsomeness (reverses the 07-19 budget-gate ruling); captured on ROADMAP with full design direction; `main`'s floor left **deliberately RED** until it lands (the red is the false signal being fixed, not a defect). ⏳ **queued for independent review** (rule 4; author does not self-review).
- [2026-07-20 · 1355 UTC · triple cold pass taken — concurrency flip, onramp rhythm, DOCUMENTATION draft (Fable, wt: atelier-review-triple-take)](ROADMAP.md) — "do any reviews waiting": all three `⏳` items claimed on `main` (`69b8de0`) by one rule-4-eligible session (Mike-spawned, authored none of the chain); three taker-written briefs, attack surface committed before deferred material each time, provenance stated in brief and verdict. **Verdicts — all PASS, 0 MAJOR:** concurrency flip **0M·4M·3L** (CF1 "truly-alone pays nothing" overclaims — alone-but-unevidenced sessions do pay; CF2 the cue-relax sentence reads backwards and re-admits inference-from-silence; CF3 no rule for claiming at a dirty primary checkout; CF4 the child-block catch-up is homeless — un-lined work can't be claimed); onramp rhythm **0M·2M·2L** (SR1 "stay in your lane" has no home doc — the cue is its only copy, breaking its own points-up design; SR2 the block runs 48 lines against a "~15 lines of substance" spec — dead text); DOCUMENTATION draft **0M·2M·2L** (DD1 the great-per-cell table drops the developer cell the doc itself warns is "the one most often dropped"; DD3 inventory misses tests-as-docs, commit messages, diagrams; DD4 the competing draft never existed — reconcile clause vacuous, Mike decides whether the cold pass discharges it). **Every recorded proof re-run:** ros grounding all true at `806eb10` (MACHINE OUTPUT · CAVEATS verbatim · SEE ALSO gap · PRINCIPLES §6 · harvester ratification), cue parity mechanically enforced (suite 20 OK), sizescan red confirmed deliberate (ROADMAP 411/300), flipped claim→worktree procedure executed live by this session. 🎯 **All rulings are Mike's** (self-authored doctrine, rule 3); each pass is no-MAJOR ⇒ each cycle closes terminal on his ruling, application batch after.
- [2026-07-20 · 1423 UTC · triple cycle CLOSED — Mike's rulings applied, DOCUMENTATION ratified (Fable, wt: atelier-rulings-apply)](ROADMAP.md) — Mike: *"1–3 I rule that I accept your recommendations"* — all counsel on CF1–CF7, SR1–SR4, DD1–DD4 accepted; the taking session applied as a **terminal application** (`87af9f9` — rulings of no-MAJOR passes close without a further pointer). Landed: **CONCURRENCY.md** solo-default owns the trade + evidence-is-affirmative (CF1/CF5), cues discovery-only (CF2), dirty-primary claiming rule (CF3), "(primary)" dropped (CF6), unsure⇒worktree (CF7), and a new § **Stay in your lane** giving the cue's clause its home (SR1); **child block** (PROPAGATION + template, parity re-proven, suite 20 OK + all selftests green) caught up to the flipped prior (CF4 folded in), cue scoped to the shared queue (SR3), "final verdict"→"declare the work wrapped" (SR4), dead "~15 lines" spec → class-grounded lean rule with the numeric re-grounding folded into the size-signal item (SR2); **DOCUMENTATION.md ratified** — developer column (DD1), centre-of-gravity qualifier (DD2), tests/commit-messages/diagrams rows (DD3), and the cold pass stands as the competing-draft counterweight (DD4). Decisions stamped in all three verdicts; the three items retired verbatim to ROADMAP-DONE behind one [x] pointer. Children adopt at pin bump; tiki application half of DOCUMENTATION is ros's lane. sizescan: ROADMAP red stands as the known deliberate one (rebalance item, scope now includes SR2).
- [2026-07-20 · 2025 UTC · size-signal rebalance — line-count budget → cold-content gate (Opus, wt: atelier-size-signal-rebalance)](sessions/2026-07-20-2025-size-signal-rebalance-cold-content-gate.md) — Mike's queued follow-on, applied. **Ruling (2026-07-20):** cost is size × read-frequency, so `sizescan` now gates on relocatable **cold content** — a completed `[x]` item on a checkbox-worklog file (`ROADMAP.md`), lossless to harvest to `ROADMAP-DONE.md` — and **never on line count**, which is demoted to a pure **advisory** (a class reference point that reports but never fails a build). A file long purely from live current-truth (a wholly-open roadmap) is no longer penalised — this **dissolves the all-open-roadmap red** — and the number can't induce line-golf. Prose-shaped cold content + thinness stay caught at review, not measured. The static `GATED` set is gone (gating is content-driven). **Shipped:** `sizescan.py` (suite 267→282, selftest rewritten incl. the large-all-open-roadmap-does-not-gate reversal), `ci.yml`+`floor.yml` retitled, `RECORD.md` module doc, **SR2** — `PROPAGATION.md` child-block spec re-grounded to **~50 lines** (seven-concern shape, replacing the dead "~15"). **`main`'s floor turned green the RIGHT way** — the 4 inline `[x]` items harvested (2 were duplicate stubs already in DONE; 2 moved verbatim), not 15 lines trimmed; `sizescan --check` clean, exit 0. The 07-19 tripwire ruling is *superseded by append* (ROADMAP-DONE annotation + this entry), never by rewriting its immutable records. ⏳ **queued for independent review** (rule 4; SR2 rides the cycle; author does not self-review). At-rest UTC (`date -u`); local NZ date 2026-07-21.
- [2026-07-20 · 2047 UTC · size-rebalance cold pass taken (Fable, wt: atelier-size-rebalance-cold)](reviews/2026-07-20-2040-size-rebalance-cold.md) — "do any review or fable dependent work": the queued rule-4 review of the cold-content rebalance claimed on `main` (`c8a1222`), taker-written brief committed before any deferred material, all live-proven claims re-run (suite 282 OK; floor green at HEAD; date correction complete; harvest content conserved). **Verdict: PASS — 0 MAJOR / 2 MEDIUM / 3 LOW.** SR1 `tools/README.md` § sizescan still documents the dead line-budget model (adopter-facing, same-commit-currency breach); SR2-C the "~50 lines" figure sits at measured-49+1 — circular by the delta's own module-doc standard, derivation asserted not derived (counsel leans: drop the number, keep the structural rule); SR3 `[x]`-in-code-fence false positive + `+`/ordered-bullet misses; SR4 "moved verbatim" overclaims (tripwire item was compressed in the move); SR5 the `⏳` pointer breached rule 4's refs-only ceiling, second consecutive cycle — counsel: put the ceiling in the ROADMAP header at point of use. SESSIONS→advisory-only coverage change endorsed, residual named. Per rule 3 **all rulings are Mike's**; nothing applied.
- [2026-07-21 · size-rebalance rulings applied — cycle CLOSED (Fable, wt: atelier-size-rulings-apply)](ROADMAP-DONE.md) — "do any review work" → the queue held one blocked cycle (the size-rebalance cold pass, PASS 0M/2M/3L, rulings owed). Mike ruled *"I accept your recommendations for those"*; this session applied all five as a **terminal application** (no-MAJOR pass closes on the ruling, no further pointer — same shape as `87af9f9`). **SR1** `tools/README.md` § sizescan rewritten to the cold-content frame (was still documenting the dead line-budget model + "not wired into any gate yet", both false at HEAD). **SR2-C** the child-block "~50 lines" figure **dropped entirely** (option b) — it sat at measured-49+1, circular by the delta's own module-doc standard; the structural rule (one bullet per irreducible concern) is the whole spec and nothing gates on length. **SR3** detector edges: `cold_item_count` now skips fenced code blocks (false positive — docs showing checkbox syntax) and `_COLD_ITEM` matches every bullet marker incl. `+` and ordered (false negatives) — suite **282→284**, `--selftest` extended, all green. **SR5** the rule-4 refs-only pointer ceiling stated in the ROADMAP `⏳` header at the point authors write it (breached two cycles running — a point-of-use framing gap per the rule-grammar principle). **SR4** ("moved verbatim" overclaim in the authoring record) resolved **accept-as-noted**: the record is immutable history, the correction stands in the review verdict; no immutable record rewritten. `⏳` item harvested to ROADMAP-DONE, CHANGELOG closed, floor green (`sizescan --check` exit 0). At-rest UTC (`date -u`); local NZ date 2026-07-21.
- [2026-07-21 · 0736 UTC · the three review-owed strands taken (Fable, wt: worktree-review-owed-triple)](sessions/2026-07-21-0736-review-owed-triple.md) — "Do the review work", all three *Doctrine — review-owed* items claimed on `main` (`8d3fa9f`) then worked in a worktree; a parallel session's mid-session claim (`4bc0b52`, man-pages) yielded to. **① Fleet re-stamp ✅** — nova/numen/shed's drifted `docs/reviews/README.md` replaced from the closed-cycle template, `<atelier-path>` filled, stamp grep + pointer resolution proven per child, pushed (`13f6970`/`d271ae0`/`118fc69`); item → ROADMAP-DONE. **② Review-line artefact ✅ (closes F6)** — ADR/decisions/ROADMAP templates carry the `review:` field; new `tools/reviewscan.py` (presence-only, `docs/decisions/` only, boundary 2026-07-21, roadmap headings deliberately unlinted per 0820) wired into pre-commit + ci.yml + floor.yml; suite 284→293, red/green bite-proven; `REVIEW.md` enforcement re-stated per surface; ADR `2026-07-21-0744` dogfoods the field; doctrine ⇒ **⏳ rule-4 pointer queued** (delta `fa7a90f`). **③ REACH/AUTONOMY backlog ⚠️ split** — agent-grade applied (H8 browser-fetch README realigned to decided doctrine; two REACH.md rewraps); 🎯 **H1–H7 + the AUTONOMY-repurposing residual are Mike's rulings** — counsel with proposed wordings in the session record, counsel take-all, nothing applied before ruling.
- [2026-07-21 · 0746 UTC · man-page rollout — cctranscript + ccrepo (Opus, orchestrated, wt: worktree-manpages)](sessions/2026-07-21-0746-man-page-rollout-cctranscript-ccrepo.md) — Mike: hand a session queued work back-to-back to max plan use; first item **man pages**, Opus orchestrating Opus agents, worktree for parallel-session safety. Rolled the reviewed `--help`/`man` split (`ccarchive` worked example) out to the **two remaining installed CLIs**, closing the rollout — every tool the installer publishes now carries both registers. Approach: claim on `main` → worktree → **two Opus agents in parallel, one per tool, fully disjoint file sets, no-git-in-agents, orchestrator verifies + commits** (clean pattern, no index race). Each tool gains a full plain-language `man/<tool>.1` (mandoc-lint clean, `ccarchive.1` roff style) + a trimmed `--help` digest pointing at it (cctranscript 42→24, ccrepo 67→35; rationale/examples relocated so the page is the superset — can't drift). **EXIT STATUS enumerated against every `process.exit()` path** — the exact drift a prior ccarchive review caught, designed out at authoring (ccrepo now documents its 0/2/1 usage-vs-runtime split). +6 doc-convention tests. Verified independently: 92 instrument tests green · mandoc ×3 clean · installer drive publishes all three pages · leak/secret/link/size scans clean. **No review owed** — application of a reviewed convention, not new doctrine (rule 4). Landed via local `--no-ff` merge (signature-safe); worktree away.
- [2026-07-21 · 0748 UTC · v2 plugin — create-repo de-instanced (Opus, wt: atelier-v2-plugin-deinstance)](decisions/2026-07-21-0748-deinstance-create-repo-for-the-plugin.md) — Mike: "what work is not completed" → picked the **v2 plugin widening** (CHOSEN 2026-07-13). Ran the gated **scoping pass**: the crux is that `create-repo`, unlike the two already-bundled skills (which carry zero instance facts), *stamps identity* — so de-instancing = **externalising** its seven baked facts, not stripping them. Mike accepted three recommendations → ADR (accepted): (1) adopter profile `~/.atelier/instance.yaml`, interactive-fill-on-first-run; (2) two-mode source resolver (live sibling checkout → pin git SHA · bundled plugin dir → pin plugin version); (3) SHA-pin degrades to plugin-version provenance in bundled mode (a named fork of ADR 0002 for plugin-only adopters). **Built** in the branch: skill rewritten slot-driven + relocated to `skills/create-repo/`, `instance.yaml.example`, `/atelier:worktree` + `/atelier:fleet-pins` command wrappers over the bundled tools, manifests 0.1.0→0.2.0, onramp companion-list updated. Floor green (4 scanners + 2 tool selftests); two placeholder-email leakscan hits allow-marked (RFC 2606 `example.com`, not real identity). **Not exercised end-to-end:** interactive fill + bundled-mode scaffold (model-prose, proven at use). ⏳ **rule-4 review owed before merge** (doctrine-substantive, self-authored — non-author taker writes the brief); go-live/merge + retiring the old global skill stay Mike's call. At-rest UTC (`date -u`); local NZ date 2026-07-21.
- [2026-07-21 · addendum · REACH/AUTONOMY backlog CLOSED — Mike's accept-all applied (Fable, wt: worktree-reach-rulings-apply)](sessions/2026-07-21-0736-review-owed-triple.md) — Mike ruled *"I accept your recommendations on all of those"*: **H1–H7 + R1 [fixed]**, applied by the counsel's author as a **terminal application** (rulings of the 2026-07-13 no-MAJOR pass ⇒ no further pointer, `87af9f9` shape; claimed on `main` `4975092` first). Landed: **REACH.md** — "blocked" defined incl. soft blocks (H5), rung-1/2 equivalence hedged to the worked instance (H6), the resource-owner's "no" named as its own judgement with its own floor (H4), riding scoped to **in-place** use with the cookie-export hazard named (H2), standing reach joined to the provisioned path (H7), purpose-governs-stores / mint-vs-ride-governs-acts with autofill-is-a-mint (H3), operator/principal defined + team-adoption clause (H1); **AUTONOMY.md** — secrets floor catches unprovisioned machinery-mediated *use* (R1); **browser-fetch README** — H2 mirror. Decisions stamped in the verdict's dated addendum; item harvested to ROADMAP-DONE; CHANGELOG closed. The parallel rule-4 taker's claim on the review-line cold pass (0913) observed, no file overlap, lane untouched.
- [2026-07-21 · 0913 UTC · review-line artefact cold pass — PASS 0M/1M/5L, rulings to Mike (Fable, wt: review-line-artefact-cold-pass)](sessions/2026-07-21-0913-review-line-artefact-cold-pass.md) — the queued `⏳` taken by a rule-4 taker (Mike-spawned "Please do any review work", authored none of `fa7a90f`); claim on `main` first, brief committed cold, findings committed before deferred material opened, reconcile appended. All proofs reproduced (293 tests, selftest, red/green legs, dogfood). **RS1 (MEDIUM)**: the silent-success hand-run — the decisions dir or a record file as path arg scans nothing, exits 0 (linkscan-L1 class; wired invocations unaffected). RS2 fence false-green · RS3 empty-field pass · RS4 boundary/typography residuals · RS5 REPO-STANDARD's three-scanner-stale enumeration · RS6 the 0820 addendum splice. Queue pointer was refs-only — first cycle to honour rule 4's ceiling. 🎯 **RS1–RS6 await Mike's ruling**; no MAJOR ⇒ the applying session closes the cycle.
- [2026-07-21 · addendum · review-line cycle CLOSED — Mike's accept-all applied terminal (Fable, wt: review-line-rs-apply)](sessions/2026-07-21-0913-review-line-artefact-cold-pass.md) — Mike ruled *"I accept your recommendations on all these"*: **RS1–RS6 [fixed]**, applied by the counsel's author as a terminal application (no-MAJOR ⇒ no further pointer; claim `8815f40` on `main` first, push deferred by a network outage). reviewscan: explicitly-named paths scanned never silently greened (RS1, red legs proven), fenced quotes excluded (RS2), non-empty value required (RS3), backdate residual stated + caps accepted (RS4); REPO-STANDARD scanner roster → non-staling pointer (RS5); 0820 splice unwound, annotated (RS6). Suite 293→298. Item harvested to ROADMAP-DONE; CHANGELOG closed; cycle closed.
- [2026-07-21 · 1233 UTC · roadmap capture stream (Opus 4.8 1M, main)](sessions/2026-07-21-1233-roadmap-capture-stream.md) — CAPTURE-ONLY, no code/no doctrine change. Parked 6 items from Mike's estate note session: **anti-slop invariant registry** (new strand, from thenewstack.io/engineering-ai-slop-registry — registry + >2×-promotion rule + two-layer criteria, positioned as extending floor-scanners/writer≠verifier/review-before-build); **MODEL-ECONOMICS→ECONOMICS** rename recommendation (now spans models+runners; rename-not-split; ref-sweep); **honesty vs truth vs transparency** (Mike's RAW to-do, do-not-interpret, apex-level); **grab-colleague-AI-chat** to-do (REDACTED — public repo; full pointer in operator's private note); **ccarchive metadata** + **cctranscript/ccarchive-merge** raw open questions. Lesson: capture-mode = park the line, don't answer (over-ran twice; → memory `feedback_capture_mode_default`).
- [2026-07-21 · 2147 UTC · reviewer scope + security & privacy lens 4 (Fable, main)](sessions/2026-07-21-2147-review-scope-security-lens.md) — Mike asked two questions of the review doctrine: does it mandate the widest possible reviewer scope (intent, decisions, assumptions, design, docs, test code, real-world behaviour, code), and is security & privacy clearly a must on every review? Audit: scope ⚠️ structurally close but enumerated-never-mandated (test code and work-docs unnamed — the same enumeration that let "no source code" shrink two past verdicts); security & privacy ❌ absent — zero mentions of security/privacy/threat/OWASP anywhere in review doctrine or templates. Mike: "go", caveating real-world testing *where it's possible to do so*. Delta `f9db922`: scope-mandate paragraph above the lenses (whole commitment; non-goals the only legitimate narrowing; lenses organise, never bound) and **security & privacy as lens 4** (design-altitude privacy weakness through injection/XSS at code altitude; likely vectors checked against OWASP Top 10/ASVS, not recalled; surface-free work discharges in one explicit line with grounds — omission is the bug); child review-floor template carries the same sentence, fleet adopts at pin bump; method/README three→four lenses. Self-authored doctrine ⇒ **⏳ rule-4 pointer queued refs-only** (non-author taker; this session spawns nothing). **Addendum (same session, `a059e49`):** Mike granted `/security-review` — lens 4 now runs the harness's security scanner where one ships, the mechanical floor under the lens, never a discharge of it; ⏳ pointer widened to both deltas.
- [2026-07-21 · 2208 UTC · scope-mandate/lens-4 cold pass — PASS-WITH-FINDINGS 2M/3M/2L, rulings to Mike (Fable, wt: review-scope-lens4-cold-pass)](sessions/2026-07-21-2208-scope-lens4-cold-pass.md) — the queued `⏳` on `f9db922`+`a059e49` taken by a rule-4 taker (Mike-spawned "Please do any review work"; claim `3909d76` on `main` first). **Two-hop spawn, a first**: the taker had read the author's intent record, so it named the exposure, kept the brief refs-only above the divider, seeded below it, and spawned a **fresh-context subagent** as the reviewer — contamination never crossed, except via the doctrine's own instruction (⇒ SL2). **SL1 (MAJOR)**: `skills/review-brief/SKILL.md` still stamps three lenses — the narrowing-free point-of-use surface contradicts the parent, fleet-bundled; drift class's second shipping. **SL2 (MAJOR)**: lens 4's `/security-review` mandate misfires live — scans *pending* changes so it analysed the brief not the landed delta, injected the deferred section into the cold reviewer pre-draft, and its markdown exclusion makes the "mechanical floor" definitionally empty for doctrine; the permissive grant was escalated to a mandate by the delta text. SL3/SL4 template drift · SL5 scope-vs-"scoped and short" + warm-hands non-goals · SL6/SL7 LOW. Repo scanner floor re-run green at HEAD. 🎯 **SL1–SL7 await Mike's ruling**; MAJORs ⇒ cycle stays open past the application.
- [2026-07-22 · addendum · SL1–SL7 accept-all applied; application cold pass queued (Fable, wt: scope-lens4-sl-apply)](sessions/2026-07-21-2208-scope-lens4-cold-pass.md) — Mike: *"accept all as recommended"* ⇒ **SL1–SL7 [fixed]**, applied by the taker (authored neither doctrine nor verdict; claim `fe39f04` on `main` first). Delta `d553045`: review-brief skill → scope mandate + **four lenses** + scanner clause, with a **mechanical lens-roster parity floor** (LensRosterParityTest, REVIEW.md's list the one source, red leg proven, suite **298→302**); lens 4's scanner sentence rewritten **reach-per-review-shape** on the restored permissive grant, never-scan-a-brief + exclusion-barred-class cautions stated (SL2); "pre-scoped and short — short in ceremony, never in scope" + non-goals explicitly reviewable (SL5); impossibility claims carry grounds (SL6); CONTRIBUTING/reviews templates swept (SL3/SL4/SL7). Decisions stamped in the verdict addendum; CHANGELOG entry added. **MAJORs in the pass ⇒ application inherits rule-4 status: ⏳ queued refs-only, applier spawns nothing.**
- [2026-07-22 · addendum · capture: orchestrated queue runs → doctrine? (Fable, wt: orchestration-doctrine-capture)](../docs/ROADMAP.md) — Mike asked whether his queue-run orchestration prompt (drain the queue, orchestrator + worker agents, chain sessions, per-item close) should become doctrine rather than stay prompt-carried. Parked in ROADMAP with counsel: name the pattern in method/ (selection defaults, per-item durability for session caps, stop conditions + 🎯 end-of-run report, role check at open, tier split) + a `queue-run` skill; synergy noted (chained fresh sessions are natural rule-4 takers); plan-spend directives and the closing litany deliberately stay where they already bind. 🎯 Mike to ratify direction; review WARRANTED when built (rule-4 cycle).
- [2026-07-22 · addendum · queue-run doctrine RATIFIED; session closed (Fable, wt: queue-run-ratify-close)](../docs/ROADMAP.md) — Mike: *"I like everything you have there"* ⇒ the orchestrated-queue-runs capture is ratified as counselled (method-layer pattern + `queue-run` skill), deliberately left unclaimed for a fresh session to build; the build earns the full rule-4 cycle when it lands. Session closes with two open hand-offs: that build, and the ⏳ application cold pass on `d553045` (this session is its applier, so ineligible). Session total: the full scope/lens-4 review cycle (claim → taker brief → two-hop cold pass 2M/3M/2L → Mike's accept-all → SL1–SL7 applied, suite 298→302 → application ⏳ queued) + the queue-run capture and ratification.
- [2026-07-22 · 0244 UTC · SL1–SL7 application cold pass — PASS no MAJOR, cycle CLOSED (Fable, wt: scope-lens4-app-cold)](sessions/2026-07-22-0244-sl-application-cold-pass.md) — the queued `⏳` on `d553045` taken by a rule-4 taker (Mike-spawned "please do any review work"; claim `92a146f` on `main` first; prior verdict/intent record unread until the verdict committed). Every proof reproduced: suite 302 OK, **red leg re-driven** (pre-fix skill → 2 failures), floors green; lens 4's scanner **discharged with grounds per its own new reach-per-shape rule** — the text's first cold execution, clean. Reconcile: all seven [fixed] stamps corroborated, nothing overturned. Two LOW to backlog: AC1 (SL3's edit re-shipped the >80-col class SL7 fixed — 122-col template line) · AC2 (skill's scanner clause omits the exclusion-barred caution). Mid-pass concurrency flag from Mike resolved: two live branches, zero in-scope overlap ⇒ continued on the pinned ref. 🎯 **AC1–AC2 await Mike's ruling**; the scope/lens-4 cycle is closed terminal.
- [2026-07-22 · 0257 UTC · interruption-resilience cold pass — 0M/3M/2L, cycle CLOSED; AC1–AC2 applied (Fable, wt: interruption-resilience-cold)](sessions/2026-07-22-0257-interruption-resilience-cold-pass.md) — the queued `⏳` on `9c11525` taken by the same rule-4 taker that closed the scope/lens-4 cycle (still non-author for this delta; warm problem-statement exposure named). **IR1 (M)**: the onramp tell "no closing entry means died mid-flight" also matches a live sibling — live-proven by this session's own commits. **IR2 (M)**: child template onramp lacks the firing pointer (the author's flagged question; counsel: propagate, IR1-corrected). **IR5 (M, [fixed])**: the authoring session left an un-harvested `[x]` — the shared floor ran RED on main for three pushes; harvested, floor re-proven green. IR3/IR4 (L) wrap + legend grammar. Sweep exercised live; suite 302 OK ×3. Also applied Mike's mid-turn accept of AC1–AC2 (stamped in the 0244 verdict). 🎯 **IR1–IR4 await Mike**; the `⏳` queue is empty.
- [2026-07-22 · addendum · IR1–IR4 applied; harvest-integrity invariant captured + first catch (Fable, wt: ir-rulings-apply)](sessions/2026-07-22-0257-interruption-resilience-cold-pass.md) — Mike: *"I take your recommendations on these"* ⇒ **IR1–IR4 [fixed]** as counselled (onramp tell now died-**or-live**; child template carries the firing pointer; rewraps; legend breadcrumb). Mike also raised: would anything catch a live checkbox accidentally harvested into ROADMAP-DONE? **Answer: no — and the first manual grep found one** (a `[ ]` parent with both children DONE; flipped with a dated note). Captured as the **harvest-integrity invariant** (no `[ ]`/`[~]`/`⏳` in the archive; counsel: extend sizescan; scanner finds → agent investigates → Mike decides; `[-]` dropped state for superseded; NOT delivery-verification). Suite 302 OK; floors green.
- [2026-07-22 · 0435 UTC · ECONOMICS.md rename — FinOps ruled out, decision stamped (Fable)](sessions/2026-07-22-0435-economics-rename-decision.md) — Mike floated `FinOps` for the MODEL-ECONOMICS rename; assessed and ruled against (branded practice term vs spend doctrine; plain-over-jargon for a public audience; only vendor-flavoured name on a shelf of plain concept nouns), recorded on the roadmap item as a considered alternative (`9f08d91`). **Mike then decided: rename to `ECONOMICS.md`, do not split** — stamped DECIDED on the item (`687e8c9`). Execution (rename + PROPAGATION ref sweep) deliberately left queued for another session; the `[ ]` stays open.
- [2026-07-22 · 0634 UTC · harvest-integrity gate built — sizescan checks the archive; tri-state ruled; review queued ⏳ (Fable, wt: harvest-integrity-gate)](sessions/2026-07-22-0634-harvest-integrity-gate.md) — Mike ratified embedding the invariant in CI ("I like this idea"); sizescan now **gates on live state markers (`[ ]`/`[~]`/`⏳` list items) in archive stores** (`*-DONE.md`/`*-ARCHIVE.md`) — state coherence only, never [x]-delivery verification; on a hit the output prescribes investigate→recommend→never-silently-fix. Mike ruled the box grammar a **work-owed tri-state** mid-build ([x] = no more work owed, disposition in the item's dated note — supersedes the `[-]` counsel); his four-situation taxonomy is the test matrix (suite 302→314 green, selftest + live scan green). Five-state extension proposed; builder counselled against, captured as an open 🎯. **Review queued `⏳` for a non-author (rule 4) — the builder spawns nothing.**
- [2026-07-22 · 0819 UTC · Harvest-integrity gate cold pass — PASS-WITH-FINDINGS, 1 MAJOR; HI-F1–F6 to Mike (Fable, main)](sessions/2026-07-22-0819-harvest-integrity-cold-pass.md) — the queued `⏳` on `0bdccf3` taken by a rule-4 taker (Mike-spawned "please do any review work"; intent record unread until the verdict committed `0ce23e1`). Every delta claim reproduced: suite 314 OK, selftest OK, live `--check` green, four-situation taxonomy tested, tri-state coherent on all four surfaces, hatches can't silence the gate. **HI-F1 (MAJOR)**: archive stores inside skipped dirs (`sessions/`, `_archive/`…) are invisible to the gate while the clean banner claims coverage — reproduced, latent (fleet keeps stores at `docs/`, verified ×5). HI-F2 unclosed-fence swallow · HI-F3 RECORD.md account incomplete · HI-F4 template lacks the legend (MINOR) · HI-F5/F6 notes. Lens 4 discharged with grounds + manual pass clean. Reconcile: nothing overturned. 🎯 **HI-F1–F6 await Mike**; the MAJOR keeps the cycle open; queue empty.
- [2026-07-22 · addendum · HI-F1–F6 ruled + applied; application `⏳` queued (Fable, wt: hi-rulings-apply)](sessions/2026-07-22-0819-harvest-integrity-cold-pass.md) — Mike: *"I accept your recommendations on all"* ⇒ **HI-F1–F6 [fixed]** in `30d350c` (stores integrity-checked wherever they live — the dir skip bounds metering only; unclosed fences count their tail fail-safe, both counters in parity; RECORD.md names the gate; child template carries the tri-state legend; HI-F5/F6 documented as decisions). Suite 314→319, selftest + both original fail-open repros re-driven red→green, live scan green; verdict stamped `cfb0ae6`. Cycle stays open per the MAJOR: the application's own cold pass queued `⏳` for a non-author.
- [2026-07-22 · 0943 UTC · HI application cold pass — terminal 0 MAJOR, cycle CLOSED; HA1–HA5 to Mike (Fable, main)](sessions/2026-07-22-0943-hi-application-cold-pass.md) — the queued rule-4 pass on `30d350c` taken by a Mike-spawned "do any review work" session (author of none of the chain): claim → taker brief → proofs re-run (suite 319, selftest, live scan, red leg at `30d350c^`) → verdict committed before the deferred material → reconcile, nothing overturned. **PASS-WITH-FINDINGS 0M/2M/3L**: HA1 the HI-F1 bypass reaches non-content dirs (`node_modules`/`.venv` probed red — the conflation traced to the 0819 counsel, applied faithfully); HA2 the fence fix narrows its fail-open, doesn't close it (count=0 demo; the comment's "never hide one" overclaims); HA3–HA5 CI comments / template legend / RECORD.md wording. Terminal per the close rule — the cycle closes, 🎯 HA1–HA5 decided into the backlog for Mike; the stale interruption-resilience roadmap section harvested with the close.
- [2026-07-22 · addendum · HA1–HA5 ruled + applied; skip classes split, fence recount whole-file (Fable, main)](sessions/2026-07-22-0943-hi-application-cold-pass.md) — Mike: *"accept your recommendation on all of them"* ⇒ **HA1–HA5 [fixed]** in `120b777`: non-content dirs never scanned vs growth stores integrity-checked; unbalanced fences recount the whole file (the stray-delimiter window closed, comment now truthful); CI surfaces, template legend, RECORD.md wording fixed. Suite 319→323 green, red leg reds exactly the four new tests, probes re-driven, floors green. Stamps in the verdict; residue harvested — queue and 🎯 backlog empty.
- [2026-07-22 · 1005 UTC · access-management doctrine — SECRETS.md expansion; gap analysis captured; review queued ⏳ (Fable, wt: secrets-access-mgmt)](sessions/2026-07-22-1005-secrets-access-doctrine.md) — Mike's in-conversation rulings became doctrine (`caa85fe`): **one credential, one entity, one system** (per-system minting; revocation independence — "a shared credential is a shared fate"; identity/authenticator split — correlate by name, never by value; privilege-split within an entity, the `sudo` shape); **asymmetric keys graded, not exempt** (one key across systems acceptable, fleet-wide blast radius named); **replaceability absolute — hesitation is the tell**; **minting at max entropy** (Kerckhoffs on credential shape — a leaked example teaches nothing; imposed caps the only allowed pattern); **exposure: watch → roll on confidence → automated + rehearsed** (cadence doubles as rehearsal); **never scrub immutable history** (rolled = dead text; residue is shape, priced to zero by design). Mid-turn widenings grounded before citing: NIST SP 800-63B rev 4 (final 2025-07-31) + OWASP secrets cheat sheet cited as corroboration, the one cadence divergence owned; OWASP's store audit trail absorbed as the watch leg's third surface; NCSC collection + pasted secure-SDLC checklist deliberately NOT crammed in — captured as the ROADMAP **security gap analysis** (corroboration named, candidate gaps listed). ACCESS.md step 2: minted-fresh-for-this-domain. Scans green. **Rule 4: review queued `⏳`, author spawned nothing.**
- [2026-07-22 · 1018 UTC · Orchestrated queue run — ten items landed per-item; queue drained to the principal (Fable orchestrator, Opus/Fable workers)](sessions/2026-07-22-1018-orchestrated-queue-run.md) — the ratified queue-run pattern run live *and built into doctrine mid-run* (CONCURRENCY § Orchestrated queue runs + ECONOMICS tier split + `skills/queue-run`, `343def8`/`8111e9f` — rule-4 `⏳` queued, the run may not take it). Landed: the SECRETS/ACCESS `⏳` taken as a rule-4 worked example (**0M/4m/4L/1n terminal**, 🎯 SA1–SA8 to Mike); security canon gap map (A/B/E narrow, C reframed to mutable-tag CI actions, D dismissed); ECONOMICS.md rename executed (24 refs/16 files, nothing dangles, children re-point at pin bump); ccarchive `--restore` full+delta, dataless awareness (classifier proven on a real evicted file), and manifest signing (HMAC sidecar, off-archive key, caveat closed live) — instruments 109→132; ccrepo dedup defect found (last-wins kept zeroed tails) → drift 0.00% exact vs ccusage; anti-slop mining of all 47 reviews (330 findings → 5 scanner + 7 verifier candidates, 🎯); both cc-instruments questions answered by measurement (tool-result sidecar hole; keep-separate counselled). Stop condition: everything left is the principal's (🎯 backlog) or rule-4-barred. Honesty ledger in the record: three self-caught false alarms, the estimated-stamp defect twice-corrected, one worker API-drop resumed lossless. 🚩 stray `atelier-v2-plugin-deinstance` worktree left for Mike.
- [2026-07-22 · 1149 UTC · queue-run doctrine cold pass — PASS-WITH-FINDINGS 1M/6M/1L/1n, QR1–QR9 to Mike (Fable, wt: queue-run-cold-pass)](sessions/2026-07-22-1149-queue-run-doctrine-cold-pass.md) — the queued `⏳` on `343def8`+`8111e9f` taken by a rule-4 taker (Mike-spawned "please do any review work"; claim `5c29a44` on `main` first). **Two-hop shape** (2208 precedent): the taker's onramp had read the author's SESSIONS index entry, so the exposure was named, the brief kept refs-only above the divider with seeds deferred, and a fresh-context subagent ran the review — attack surface named first, findings committed (`3797c5a`) before any deferred material opened, reconcile its own commit. Every proof re-driven: suite 323 OK · instruments 132 OK · four scanners clean · sizescan --check exit 0 · bundling parity confirmed · every ref/link/line-count claim measured; lens 4 discharged with grounds (landed markdown). **Verdict:** the pattern is genuinely good, but the chaining promise and rule-4 synergy are welded without pinning spawn provenance — **QR1 (MAJOR)** authorship-vs-started-or-instructed laundering path (prospective, not observed); QR2 worker-built authorship undefined; QR3 worker authority envelope; QR4 queue-item text as instruction (injection shape); QR5 tier-split default vs parent on doctrine text; QR6 no mechanical skill pin (twice-shipped drift class); QR7 "loses nothing" overclaim; QR9 (reconcile) serial loop vs wave-parallel grounding run; nothing overturned. 🎯 **QR1–QR9 await Mike** (rule 3, counsel take-all, wording-sized); the MAJOR keeps the cycle open past the application.
- [2026-07-22 · 1210 UTC · Orchestrated queue run #2 — four items landed per-item; queue drained to the principal (Fable orchestrator, Opus+Fable workers)](sessions/2026-07-22-1210-orchestrated-queue-run.md) — the ratified pattern's second live run, on the principal's maximise-plan-use directive. Landed: the **v2-plugin de-instance ⏳ taken as a rule-4 worked example** (the 1018 run's "stray" re-read as a parked handoff on the evidence; two-hop cold pass, **PASS-WITH-FINDINGS 2M/3M/1L/2n**, 🎯 VP1–VP8 + merge to Mike — both MAJORs sit on the bundled-adopter path); **security-canon gaps A/B/C/E closed** (`85157c3` — every CI action SHA-pinned incl. child templates with live-verified SHAs ×2, root SECURITY.md + child template registered publish-time in REPO-STANDARD, REVIEW.md threat-pass + security-finding lifecycle, PRINCIPLES secure-by-default + supply-chain bearing; rule-4 ⏳ queued, barred to this run); **ccrepo actual-spend reconciliation** (`1711711` — machine-local plan/usage spend vs API estimate with honest degradation, 139 tests; pre-existing USD×rate² footnote defect found by the worker, verified and fixed; 🎯 config residue); **person-context portability design pass** (records-only — C1–C8, 8-threat pass, per-leg recommendations incl. the phone leg as a second system; 🎯 D1–D5). Stop: everything left is blocked — full 🎯 report + honesty ledger in the record; fleet pin adoption (12/12 behind) named out-of-lane, per-child follow-on. Stale `review-trigger-commitment` branch put away; v2 worktree deliberately remains (Mike's merge).
- [2026-07-22 · 2134 UTC · Apex third element — adaptation is continuous (Fable)](sessions/2026-07-22-2134-apex-adaptation.md) — Mike's dictation encoded at the apex: **"Adaptation is continuous"** now sits between "Honesty is absolute" and the Laws (`f52c50f` — the literal reading of "just below honesty"); we actively learn and gather evidence through everything we do and adapt by improving ourselves and our tools (strategy, methods/solutions, process, what we do and say — or don't), because we can always be better and the environment keeps changing. Subordination rationale (adaptation runs on evidence; honesty makes evidence trustworthy) and grounding (EVIDENCE/REVIEW/RECORD/PROPAGATION as the running machinery, PRINCIPLES §6) are agent-authored — named as attackable in the intent record. README apex summary matched. Extended same session (`1da0a3e`, second dictation): **don't fear the hard road** — spending time and effort to learn from real evidence beats the quick route, binding on every session and every model — and **doctrine changes ride on proof** — design/doctrine decisions must be evidence-based, hard-fact proven, and repeatable so they can be challenged and contrasted (heading widened to **doctrine *and design*** on Mike's follow-up). Third exchange grounded both bullets in the **ros SSH worked case** (`8d25fb3`) — verified against the ros record before encoding: the "key dead fleet-wide / blame strict-crypto / swap-or-password" testimony collapsed under challenge; the bench campaign proved agent key-crowding, and the evidence-based fix went the opposite direction (stronger key kept, strict policy kept, password auth off); genericised for the public repo. **⏳ rule-4 review queued** (self-authored; this session barred from spawning); **[ ] apex-floor propagation to children** queued, gated on the review.

- [2026-07-23 · 0001 UTC · Fable-in-plan economics rework — billing states, cap rule, hand-up ladder, third seat (Fable, wt: fable-plan-economics)](ROADMAP.md) — Trigger: provider notice verified in the principal's mail (2026-07-21 — the capable model is now plan-included under a capped share, usage-billed past it, drawing the shared allowance down faster), which falsified ECONOMICS.md's two-pool binary and the child template's "Fable (usage-billed: real money)" line. Three principal rulings shaped the fix (quoted in [`decisions/2026-07-23-0001-billing-state-of-the-marginal-token.md`](decisions/2026-07-23-0001-billing-state-of-the-marginal-token.md)): the tank never decays quality — at a cap the exits are stop/delay or the principal paying, never a down-tier; a model past its depth fails **noisily** and hands up, ladder ending at the principal; and the third seat is adopted (fan-out on the cheapest genuinely-capable tier now; stepped-down executor trialled next queue run — ROADMAP item added). **Delta `dadde1d`**: ECONOMICS.md pools→marginal-token billing states + draw-down rate, marginal-cost self-check (two guards; cap = spend beyond plan ⇒ AUTONOMY confirm floor), two hard edges on the tier ladder, third seat in the orchestrated-run split; child ECONOMICS template re-grounded; renamed-section cross-refs fixed in REVIEW.md + CONCURRENCY.md. Deliberately *not* changed: the capable-reviews/workhorse-builds split (it was always a risk argument — the rulings make that explicit); reviews template (no billing claim, already correct); estate numbers (plan name, cap share, prices) stay person-local — entitlement to be recorded in the estate-root repo next session there. Self-authored doctrine ⇒ rule 4: `⏳` pointer queued, non-author writes the brief; edits untrusted until that closes.
- [2026-07-23 · 0021 UTC · Honesty triad + lived experience + glossary seed (Fable, same session as the apex-adaptation entry)](sessions/2026-07-23-0021-honesty-triad-glossary.md) — Mike's second dictation wave encoded: **truth / honesty / transparency kept distinct** at the apex (`07cc855` — truth is what occurred, provable irrespective of observer; honesty the best and faithful interpretation of it, neither implying the other; transparency the inclusion of all relevant information — a component of honesty when withholding is purposeful, separable from it innocently when relevance hinges on the other party's knowledge), placed directly above the principal's-authority duty it explains; the SSH worked case closes with **lived experience** — the principal's months of daily use tripped the warning bell, and models earn the same bell by accumulating lived experience as recorded evidence. (b) ripple: EVIDENCE.md header now carries the truth bar; COMMUNICATION/REVIEW/AUTONOMY assessed no-change; child floors stale → propagation item widened. (c) **GLOSSARY.md seeded** (`c85712d` — thin anchor, tiki's admission rule, PROPOSED-then-ratify; 🎯 ratify pass queued). ⏳ apex review pointer widened to the five-commit delta + both intent records.
- [2026-07-23 · 0113 UTC · Session close — floor restored green; capture queued (Fable, same session as the two apex entries)] — close audit on Mike's "captured everything, tidy to close?": found the floor **red at head since 00:06 UTC** — two debts left by parallel closes (the billing-state decision record missing its machine-visible **Review** line; the 00:47 spend-config close leaving its `[x]` on the ROADMAP hot path) — both fixed mechanically per precedent (`165c40f`), **floor run verified green** on the fix. The gap itself captured as a ROADMAP doctrine candidate: RECORD.md's close rule demands evidence under the all-clear but never names the pushed floor run's result. Session total: five doctrine commits (apex adaptation element → triad → glossary seed), two intent records, one ⏳ review (five-commit delta), two 🎯 items (glossary ratify; SA1–SA8 unchanged), propagation + close-discipline captures queued. Machine-local memory updated (midnight-rollover stamp hazard). Deliberately open: the ⏳ apex review (non-author), glossary ratify (Mike), apex-floor propagation (gated), v2 worktree (Mike's merge).
- [2026-07-23 · addendum · QR1–QR9 ruled + applied; application `⏳` queued (Fable, wt: qr-rulings-apply)](sessions/2026-07-22-1149-queue-run-doctrine-cold-pass.md) — Mike ruled all nine through a plain-language walk-through with likely impacts: **accept-all** (QR5 = escalate doctrine text to the capable tier; QR9 = sanction waves). Applied by the taker (authored neither doctrine nor verdict; claim `f6b5324` on `main` first) in `b65209c`: the chain pin — **a run never starts or instructs its own successor** — worker authorship, the worker envelope, item-text-never-overrides, waves (claim per item, close per merge, report aggregates), doctrine-text escalation, tier seat-names, honest cap wording, and `QueueRunSkillTest` (suite 323→330, bite-proven both legs). Scanners exit 0 ×5. Decisions stamped in the verdict (`dcab34c`). One self-caught slip in the record: the bite-proof checkout reverted uncommitted edits — caught red, re-applied, re-proven. **MAJOR in the pass ⇒ the application inherits rule-4 status: `⏳` queued refs-only, the applier spawns nothing.**
- [2026-07-23 · 0149 UTC · SA1–SA9 ruled + applied — secrets/access cycle CLOSED (Fable, wt: sa-rulings-apply)](sessions/2026-07-23-0149-sa-rulings-apply.md) — the SA decision set walked through plain-language with likely impacts; Mike ruled **accept-all** (SA4 = name the break-glass class, SA9 = repo-wide artefact sweep). Applied `f8350ee` by a non-author (claim `7f83142` on `main` first) as the **terminal application** of the no-MAJOR 1021 pass: live-session key caveat (no agent forwarding through untrusted hosts), honest watch leg (store-trail "where the store can provide one"), entropy-sufficiency minting bar with truncating-verifier honesty, the break-glass class named, NIST attribution precise, never-scrub scoped, one home for the exposure loop, central-auth pointer, artifact→artefact across living surfaces (immutable records untouched). Mid-run parallel-session flag verified disjoint before continuing (VP/D sets, not SA). Floor at the applied state: 330 + 139 tests OK, five scanners exit 0. Decisions stamped `86bad85`; item harvested to ROADMAP-DONE; **cycle closed, no further pointer.**
- [2026-07-23 · 0222 UTC · Queue take — five rule-4 cold passes in one fan-out (Fable, wt: queue-reviews)](ROADMAP.md) — Mike opened a fresh session and pointed it at the queue ("do any review work") — the rule-4 worked example — so the taker claimed all five ⏳ items on `main` first (`3107ea4`) and fanned out five capable-tier cold reviewers in one worktree: refs only, brief-first deferral, intent records held to reconcile; agents ran no git, the orchestrator verified and committed per verdict. Results: **apex widening 1M/4m/3L/1n** (AW1 MAJOR — the proof-bar bullet binds without a stated scope and read literally is violated by its own enacting dictation; scope ruling Mike's; AW2 — seven-plus in-repo apex restatements still two-element) · **economics 0M/4m/3L/1n** (retired two-pool framing survives in both READMEs and ECONOMICS.md itself; the child template re-hardcodes the mapping its own intent record calls the staleness mechanism) · **QR application 0M/1m/3L/2n — cycle terminal** (all nine rulings faithful, QR6's bite reproduced; QA1 — the item-text-never-overrides guard has no skill mirror or test pin) · **security-canon 1M/1m/3L/1n** (SC1 MAJOR — SECURITY.md's only reporting channel, GitHub PVR, is disabled on the repo: no working private disclosure path; instance fix is Mike's settings switch, recurrence routes into SECURITY.md seeding) · **v2-plugin application 0M/2m/1L/1n — cycle terminal** (all eight VP rulings byte-verified; VP2's signing externalisation unswept in SIGNING/REPO-STANDARD). Every pass re-ran the floor (scanners exit 0; 150 node + 330 python tests green) and discharged `/security-review` with grounds (landed markdown deltas). Five 🎯 ruling items queued (AW/EB/QA/SC/VA — 26 findings, rule 3: all Mike's); nothing applied. The two MAJOR cycles' applications will inherit rule-4 status once ruled; the two terminal cycles close on their ruling application.
- [2026-07-23 · 0252 UTC · Rulings applied — three cycles closed, two rule-4 applications queued (Fable, wt: rulings-apply; same session as the 0222 queue take)](ROADMAP.md) — Mike ruled **accept-all as counselled** across all 26 findings (AW1's scope sentence and SC1's enable-PVR agreed verbatim). Applied by the taker session — authored neither the doctrine nor any verdict — after claiming all five 🎯 items on `main` (`9075619`). **SC1 instance fix first**: PVR enabled on the repo and API-verified (`{"enabled":true}`) before the doc fix landed. Applications, one commit per cycle: **apex `e8d707c`** (proof-bar scope sentence; triad's mandated-withholding boundary; glossary cold-review/doctrine/testimony/session fixes, still PROPOSED for the ratify pass; propagation item now names every stale apex restatement + the precondition-clause constraint; ROADMAP pointer rule — delta lists widen in the same commit) · **security-canon `c27189e`** (enable-PVR-and-verify now part of SECURITY.md seeding in REPO-STANDARD + template; three stale deferred-call notes swept to fetch-at-CI; annotated-tag dereference in the pin recipe; two-speed uptake sentence; the atelier@main trust asymmetry named) · **economics `86f8530`** (terminal — indexes + ECONOMICS.md swept off the two-pool frame; unknown cap distance trips the guard; child template de-hardcoded to seats-by-risk with billing facts read off the plan; EB8 [backlog] rides AUTONOMY's next edit) · **queue-run `5891184`** (terminal — injection guard mirrored into skill step 5 + pinned by `test_item_text_never_overrides_in_both`; eviction covers README; chain-session wording; slot-per-run workhorse definition folding EB6+QA6) · **v2-plugin `bbaec81`** (terminal — profile-gated bake swept into SIGNING/REPO-STANDARD; VA2 [backlog] folded into the e2e exercise item with the update case named; delete-guidance-comments clause; eight facts). Decisions stamped in all five verdicts. **Economics, queue-run and v2-plugin cycles CLOSED** (harvested to ROADMAP-DONE); **apex and security-canon applications inherit rule-4 status** — ⏳ pointers queued refs-only (deltas `e8d707c` / `c27189e`), this session spawns neither. Floor at the applied state verified before merge; template tests 32/32 including the two new pins.
- [2026-07-23 · 0319 UTC · The two queued application cold passes taken — both terminal, 0 MAJOR (Fable, wt: apex-sc-application-reviews)](sessions/2026-07-23-0319-aw-sc-application-cold-passes.md) — Mike opened a fresh session and pointed it at the queue ("Please do any review work") — the rule-4 worked example again — so the taker claimed both `⏳` application items on `main` first (`9df3510`; Mike flagged parallel sessions live, the push race arbitrated clean) and fanned out two capable-tier cold reviewers in one worktree: refs only, brief-first deferral, prior verdicts held to reconcile; agents ran no git, the taker verified per verdict (PVR re-checked live independently, AWA claims re-read) and committed. Results: **AW application 0M/2m/1L/1n — cycle terminal** (all nine AW stamps reproduce, zero overlap with the prior pass; AWA1 the AW2 sweep list omits the child template's byte-identical floor block; AWA2 AW6's "queued delta" trigger leaves the landing→queuing window open and it bit in its own enacting batch; AWA3 the scope sentence sits under the 2026-07-22 attribution — the one application-introduced defect, cosmetic; AWA4 "⏳ queued at landing" ran ahead of the tree) · **SC application 0M/1m/1L/1n — cycle terminal** (all six SC stamps reproduce; PVR live `enabled:true`; the SC3 recipe proven on both tag kinds; SCA1 the residual bullet omits the atelier@main code-execution grant; SCA2 "automatically" is default-config-only; SCA3 the verify command lacks its expected observation). Both passes re-ran the floor (331 py + 150 node tests, five scanners exit 0 each) and discharged `/security-review` with grounds. **No MAJOR in either ⇒ both cycles close on Mike's ruling** (🎯 AWA1–AWA4 + SCA1–SCA3, counsel take-all, wording-sized); each ruling's application is terminal, no further pointer. Nothing applied pre-ruling.
- [2026-07-23 · 0327 UTC · cctranscript reads the ccarchive archive (Fable, wt: cctranscript-archive-read)](sessions/2026-07-23-0327-cctranscript-archive-read.md) — Mike's direct ask: keep live logs the default, add an option to read ccarchive's mirror. Built `--archive` (+ `--dest`/`$CCARCHIVE_DEST`, resolution shared verbatim with ccarchive; `--dest` implies `--archive`): every view — list, render, `--json`, UUID/prefix, `-n`/`--repo` — works over `<dest>/<repo>/<uuid>.jsonl.gz`, one `readLogText` gunzip choke-point keeps parsers byte-format-blind, an explicit `.jsonl.gz` path needs no flag, transcripts carry `source: archive`. Eviction-aware: `--list` never reads an iCloud-dataless mirror (SF_DATALESS ported from ccarchive, same test seam; listed with an `evicted` marker, `--repo`-matched by folder suffix), rendering one chosen session deliberately faults it back. Suite 150→156 + 331 tools OK; man page + `--help` under the drift guard; smoke against the real archive (31-turn render, `source: archive`). README sourcing-seam paragraph half-closed; ccrepo half recorded open in ROADMAP § ccrepo; CHANGELOG + ROADMAP-DONE entries. No doctrine authored ⇒ no review pointer owed.
- [2026-07-23 · 0346 UTC · archive sourcing finished + flag renamed (Opus, wt: archive-sourcing-finish)](sessions/2026-07-23-0346-archive-sourcing-finish.md) — two follow-ups on the 0327 work. **(1)** Renamed `--archive` → `--from-archive` across cctranscript (code, HELP, man roff, tests, README, CHANGELOG, ROADMAP) — the bare form read like an imperative ("archive the transcripts"); the tool never shipped `--archive`, so the living records take the final name. **(2)** Built `ccrepo --from-archive`, closing the observe-side sourcing seam: totals now reach past Claude Code's prune horizon over the same mirror, same shared `--dest`/`$CCARCHIVE_DEST` resolution + one `readLogText` gunzip choke-point + `_external` skip. Key divergence from cctranscript — ccrepo reads *every* file, so an iCloud-evicted mirror is **skipped-by-default and counted** (`⚠` + `meta.evicted`), `--materialise` opts into reading (the ccarchive `--verify`/`--materialise` two-step Mike already ruled on). ccusage cross-check auto-off in archive mode (it reads the live store the pruned sessions have left) with a footnote; actual-spend reconciliation still runs. SF_DATALESS ported with the same sim seam. node 156→160, ccrepo 34→41 (spawn contract over a synthetic `.jsonl.gz`, evicted-skip + materialise round-trip); tools/ 331 OK; both man pages lint clean; drift guards green. Smoke: `-g month` over the archive shows 2026-06/07 rollups. Records: combined CHANGELOG + ROADMAP-DONE entries, README seam **closed on observe side**, only the deferred rollup *precompute* ledger remains. No doctrine authored ⇒ no review pointer owed.
- [2026-07-23 · addendum · AWA1–AWA4 + SCA1–SCA3 ruled + applied — both cycles CLOSED (Fable, wt: awa-sca-rulings-apply; same session as the 0319 take)](sessions/2026-07-23-0319-aw-sc-application-cold-passes.md) — Mike asked for plain-language one-by-one; all seven walked through with impacts (AWA2 took three rounds — his reviewer-independence challenge answered in-session before ruling) and **accepted as counselled**. Applied by the taker (authored neither doctrine nor verdicts; claim `e50d84d` on `main` first) as the terminal applications: **apex `7230025`** (AWA1 child-template floor block onto the propagation sweep list, lockstep note; AWA2 landing = queuing — the ⏳ pointer rides the landing commit, ROADMAP preamble + REVIEW.md rule 4 + applying-decisions; AWA3 attribution split; AWA4 folded into AWA2) · **canon `a97c140`** (SCA1 the atelier@main float stated at strength as a bounded standing grant with the ref: escape; SCA2 "automatically" hedged to the default configuration; SCA3 `→ enabled: true`). Decisions stamped in both verdicts, items harvested to ROADMAP-DONE. **Apex-widening and security-canon cycles CLOSED terminal — the apex-floor propagation sweep (in-repo + fleet) is now ungated**, the next open item there.
- [2026-07-23 · 0441 UTC · Orchestrated queue run — four items, one Opus session (Opus orchestrator; workers in isolated worktrees)](sessions/2026-07-23-0441-orchestrated-queue-run.md) — Mike's standing brief: maximise plan use, drain unclaimed queue, worktrees for parallel safety, Opus orchestrates + my choice of agent tiers. Claimed three on `main` first (`95cd222`, `[~]`), then a three-worker wave + a fourth once its files freed, **each with a full per-item close** (records the orchestrator's alone — no worker touched ROADMAP/CHANGELOG/SESSIONS, so four merges, zero record conflicts). Landed: **cc-tools flag-vocab audit** (Sonnet, `6796f81` — table in instruments/README, zero drift, 🎯 rule-direction ratify → Mike) · **apex three-element floor in-repo sweep** (Opus, `9040b02` — 6 surfaces, floor blocks byte-identical, fleet sweep split out; the closed apex-widening cycle's scheduled application, no new review) · **S3 datescan scanner** (Sonnet, `04103a9` — advisory-only `--warn`, 372 tools tests, ⏳ first-of-kind review queued, 60-finding honest baseline) · **ccrepo rollup precompute ledger** (Opus, `8a31b95` — 3.1× warm speedup, rollup==recompute proven live; endorsed per-file-not-per-period keying deviation on a correctness ground; 🎯 transparent-vs-opt-in → Mike). **Third-seat executor trial run 1: both Sonnet items PASSED** review no-hand-up/no-rework (first positive data point; one run ≠ standing claim, trial stays open). **Economics-judgement stop** — remaining items are heavier/focused-session work or stack review debt; chaining is Mike's. Floor green at head (tools 372, instruments 167, scanners exit 0, 0/0 sync).
- [2026-07-23 · 0618 UTC · Orchestrated queue run — one review + two scanners + a floor rescue (Opus orchestrator; Sonnet build workers in isolated worktrees)](sessions/2026-07-23-0618-orchestrated-queue-run.md) — Mike's standing brief again (maximise plan use, drain queue, worktrees, Opus orchestrates, stop-if-wrong-model — Opus, correct). Claimed three on `main` first (`791b04a`, `[~]`/take), then a wave: one Opus **cold reviewer** + two Sonnet build workers. **Item 1 — datescan (S3) first-of-kind review** (`e0055bb`, rule-4 clear: authored by the 0441 chain, taken by this independent Mike-started session — the fresh-session-takes-a-prior-run's-⏳ worked example): **PASS-WITH-FINDINGS 0M/4m/3L/1n but NOT gate-ready** — the 60-finding baseline is ~75% noise (currently-sense `today`, numeric-triple FPs, multi-word quoted mentions), and the loudest check is aimed slightly off S3's grounding (undated/non-UTC, not relative-word); DSR1–DSR8 + a concrete flip precondition queued. **Items 2–3 — S1 wrapscan + S5 spellscan built** (Sonnet, `72e8ecb`/`760260473`, wiring `4061334`), advisory-only, 40+60 tests (suite 472), each surfacing an honest judgement call (wrapscan: 153/286 findings in the SESSIONS.md one-line log; spellscan: license/practice excluded as untaggable homographs) left to its ⏳ first-of-kind review — **executor-trial run 2, both PASSED no-rework** (two positive runs now; a third still wanted before a standing seat claim). **Floor rescue** (`dc37506`): `sizescan --check` had been red for several sessions on un-harvested `[x]` — harvested all 9 to ROADMAP-DONE (597→518 lines, lossless, linkscan clean), restoring green; the worked case for the "close all-clear should carry the pushed floor result" capture. Cleaned up both worker worktrees (cherry-picked → gitignored leftovers force-removed). **Economics stop** — remaining work is heavier/focused-session or stacks ⏳ debt; budget kept for the wrap. Floor green at head (tools 472, all 10 selftests OK, scanners clean, single worktree, 0/0 sync).
- [2026-07-23 · 0707 UTC · Orchestrated queue run — 1 fix + 2 first-of-kind reviews (Opus orchestrator; 1 Sonnet worker in isolated worktree + 2 Opus cold reviewers)](sessions/2026-07-23-0707-orchestrated-queue-run.md) — Mike's standing brief (maximise plan use, drain queue, worktrees, Opus orchestrates + stop-if-wrong-model — Opus, correct). Chose a **backlog-shrinking** batch: the two ⏳ first-of-kind scanner reviews + the datescan DSR-apply (deferred S2/S4 *builds* — they'd add new ⏳ debt and both touch `ci.yml`). Claimed all three on `main` first (`4a3c737`, `[~]` + rule-4 taken-markers). **Item 1 — datescan DSR1–DSR8 + re-baseline** (Sonnet, `b7b292c`): all eight applied, baseline **60→0**, three extra real bugs caught mid-work, DSR3 `today`-narrowing silent-miss trade declared in-header, un-derivable date left as honest `allow` not guessed; 58+489 tests green, still `--warn`. **Items 2–3 — S1 wrapscan + S5 spellscan first-of-kind reviews** (Opus cold, rule-4 clear: prior-run authors, fresh session takes): **wrapscan PASS-WITH-FINDINGS 1M/3m/2L** — MAJOR is *gate-scope* (154/287 baseline is deliberate SESSIONS index rows), fix the net not the detector; **spellscan PASS-WITH-FINDINGS 0M/2m/1L/1n** — core safety proven (no wrong corrections), real latent bug found (SS1), license/practice exclusion ruled permanent. Orchestrator re-verified every worker's numbers before merge/transcribe (no rubber-stamp). **Executor trial → Run 3** recorded + Run 2 (0618) reconciled into the trial record (was only in its SESSIONS one-liner — a V5 contradiction); three runs agree, ready to promote Sonnet into ECONOMICS for well-floored+prescriptively-reviewed classes (rule-4 doctrine edit, future session). **Three 🎯 walked through plain-language + all RULED same session:** datescan flip → **"flip it"** (applied `d24caec` — datescan now blocking in atelier ci + child floor template); wrapscan gate-scope → **option A** (doctrine-surface scope, `efea6a5`, direction locked for the apply); spellscan `catalog` → **rename to "catalogue"** (`856818e`, live ROADMAP done, frozen-record sweep folded into the spellscan apply). Both scanner flips stay Mike go/no-go on a clean re-baseline (datescan's pattern). Floor green at head.
- [2026-07-23 · 0959 UTC · Orchestrated queue run — 2 applies + 2 scanner builds + a doctrine capture, two floor rescues (Opus orchestrator; 4 Sonnet workers in isolated worktrees)](sessions/2026-07-23-0959-orchestrated-queue-run.md) — Mike's standing brief (maximise plan use, drain queue, worktrees, Opus orchestrates + stop-if-wrong-model — Opus, correct). Two waves, per-item close, orchestrator owned all records (4 merges, 0 conflicts). **Wave 1 (applies):** wrapscan S1 (`ceb3fda`) — option-A doctrine-surface scope + WS1–WS6, gated scope 0, flip precondition met; spellscan S5 (`b910962`/`4872f07`) — SS1–SS4 + `catalogue` rename, baseline 71→40, **worker honestly surfaced ~36 general-sense `artifact` breaches in frozen records** → new 🎯. **Item E inline (Opus):** RECORD.md all-clear now carries the *floor at head* not the local scan (`97b4fd2`, self-authored ⇒ ⏳ queued same commit). **Wave 2 (builds, finishing S1–S5):** pathscan S2 (`b738f21`) — bare-prose path resolution, 53 tests, wired advisory; stampscan S4 (`2fe97f3`) — new stamp-drift mechanism, 46 tests, live pair CLEAN, **built but left UNWIRED**. Combined suite 601. **Two floor rescues, dogfooding item E the same run it was written:** (1) cold-content gate red on un-harvested `[x]` → harvested 3 items to ROADMAP-DONE (`3a829c1`); (2) **load-bearing** — stampscan's marker parser reds the *blocking* floor (exit-2 config error, un-suppressed by `--warn`) when any doc merely *documents* the marker syntax; its own ⏳ pointer proved it → **unwired stampscan** (`4f637b0`), precondition to wire = strip code/fenced spans like siblings; did NOT patch the unreviewed tool (author≠reviewer). **Executor trial → Run 4 (honest, NOT a clean sweep):** 3 clean (2 applies + pathscan, known classes), stampscan needed an orchestrator correction — a *novel mechanism*'s fixtures couldn't anticipate its failure mode; sharpens the discriminator to floor-density-not-class (standing claim = well-floored *known*-pattern builds + prescriptive fixes, NOT any first-of-kind). **Economics stop** — ready well-floored queue drained; rest is 🎯/⏳/fleet/large-design. Floor green at head (601 tools, scanners exit 0, 0/0 sync). Four 🎯 for Mike surfaced, none skipped.
- [2026-07-25 · 0645 UTC · Doctrine + design captures — first-principles, cross-repo learning, solve-once, ccarchive encryption (Opus, conversational)](sessions/2026-07-25-0645-doctrine-design-captures.md) — continuation of the 0959 queue-run conversation; no builds, five ROADMAP-only captures (stub-don't-fabricate, content grounded in Mike's words not invented). **(1) Elevate kāinga's first-principles doctrine to atelier** (`6835ce1`/`e97c377`) — aligned meaning locked via the Musk/SpaceX example; rigour is in "the parts you *know* are true"; kāinga's research frontier is why it matured there first. **(2) Cross-repo learning** (`50fa4b7`) — the up-flow (child→atelier) complementing PROPAGATION's down-flow + lateral (child↔child); engine = deliberate domain diversity at different constraint-walls (faves no-wall, tiki hardware-wall, kāinga research frontier). **(3) Solve once, reuse the building block** (`58776c4`) — in-repo (tiki's one wire-protocol module, many consumers) + cross-repo (knowledge as building blocks); unifying claim reframes atelier as the fleet's shared library for knowledge; twin of one-fact-one-home. **(4) ccarchive encryption-at-rest, secure-by-default** (`b5e8d26`) — a design pass whose direction is encryption on by default, a loud plaintext opt-out, and live-decrypt in ccrepo/cctranscript; raises the bar on the ADP-E2E ruling; key-mgmt as the crux, solve-once reuse of the person-context age-capsule key infra, zero-dep-vs-crypto-dep the one design-pass Mike-decision. The first three + ccarchive form a coherent seam. Floor green throughout; no blocking decisions. NOT claimed: the parallel 2026-07-24 apex/principles commits.
- [2026-07-25 · coda · Apex + principles refinements + two raw notes (Opus, conversational) — the cluster the 0645 entry left unclaimed](sessions/2026-07-25-1541-apex-principles-refinements.md) — six `main` commits (2026-07-24), doc/doctrine only. **economics `preferred`-not-`welcome`** (`6ecfce0`) — cheaper-model-that-passes is *first choice*, short of *required*; lone soft-modal spot, rest already directive. **apex: authority rooted in accountability** (`4af5f3b`, `⏳`) — RASCI *Accountable* names the source the informed-principal section assumed (funds it / attributed to him / liabilities land on him ⇒ decisions his *because consequences are*). **apex: Asimov's Zeroth Law** — numbering ambiguous in the ask, surfaced via decision-prompt previews; Mike ruled **renumber** (`572dddd`) then **changed his mind** to the unnumbered **Zeroth above the Three Laws** (`672e838`, `⏳`); "Three Laws" language + 1–3 numbers/wording kept ⇒ off-by-one cost void; READMEs swept. **principles: "Design the way out before the way in"** (`e29c49a`, `⏳`) — adopt a dependency only once its exit exists; twin of "build the way back before the way forward"; grounded in zero-dep tooling + browser-fetch. **Two raw notes** (`9ff507b`) — Laws-are-a-ladder (verbatim + transcript id, do-not-interpret) + complex-vs-complicated (glossary, later). Naming item (`tools/`→`checks/`) parked low-pri. **Open 🎯:** Zeroth subordination-clause micro-choice (originals kept verbatim; Asimov-faithful clauses a small edit if Mike wants). 3 new `⏳` review-owed, none spawned; child apex-floor propagation still gated. Floor green at head (sizescan `--check` 0, linkscan/leakscan/secretscan clean).
- [2026-07-25 · 1311 UTC · Policy-as-code wasn't propagating — enforcement moved from copy to call (Opus, conversational → build)](sessions/2026-07-25-1311-policy-propagation.md) — Mike: *"policy as code … is not propagating to child repos"*, then *"I want to know 100% that its been done for the current and future repos"*. **Root cause: code was one-source, policy was vendored.** Scanners were always fetched from `atelier@main`, but the *list of which scanners run* was copied per child — 247 lines of `floor.yml` each, plus four `run_scan` lines per clone. Measured: 13/13 children drifted; **12/13 ran none of the five checks added since scaffold** (sizescan/datescan/wrapscan/spellscan/reviewscan); `kainga` current only by accident of date. Two incoming framings corrected: ros *does* have CI (its floor was a stale copy, making this a 13-repo fix not a 1-repo one), and the template *did* run sizescan — the children didn't. Doctrinal hole: `PROPAGATION.md` applied thin-anchor/fat-pointer to prose and warned "do not mistake the anchor for the enforcement", and enforcement was the vendored half. **Delivered** (`40c7a22`, `e64c79a`, `d0aea38`, `78e1d20`, `91e78f4`): `tools/floor.py` one registry two planes · atelier-hosted reusable workflow, child template 247→~30 lines **naming no scanner** · hook becomes a shim over the same registry · per-scanner `scope`/`flags` (the networking-repo case) · tracked `.githooks/` + `core.hooksPath`, **proven by a real refused commit** · `tools/floorfleet.py` conformance board (`--check`, `--remote`) · **ADR 0008** + two new `PROPAGATION.md` sections (enforcement propagates by call; enumeration not assumption). Design floor: **nothing is silently absent** — non-enforcement must be declared `advisory`/`disabled` with a reason and is read out estate-wide; softening is not the child's call (boundary/integrity scanners have no advisory form, mode-changing `flags` refused); **the parent is not special** — atelier now runs the floor it ships. **Honest**: I shipped a fail-open into the tree — `_render` passed *absolute* paths to the staged scanners, which match nothing against git's relative path list, so every boundary check silently passed; only the planted-secret commit tests caught it, both shapes now pinned. Also mis-flagged ros's hook for the same defect and corrected it in-turn before touching anything. **Rolled out in full, on Mike's ruling.** I proposed staged; he asked why not full, and was right — the fact my caution rested on (the two unsoftenable scanners read only the STAGED diff in hook mode, so pre-existing findings cannot block a commit) collapsed the risk to two repos. **All 13 wired, pushed, `floorfleet --remote --check` exit 0**, and proven live in CI: one child passed, another failed on a real leakscan finding, workflow clean in both — one of each being the end-to-end proof. Two children bootstrapped with `--no-verify` (the gate blocked its own installation), documented in full; their reds are their own work. **After the rollout:** `create-repo` was still scaffolding the untracked hook, so every FUTURE repo would have been born machine-local — fixed with the CONTRIBUTING template · `secretscan`/`leakscan` hardened at source to refuse an absolute path in `--staged` (fixed at the class; those two are the only staged-mode scanners) · **licence gate enabled estate-wide** (Mike overruled my deferral — publish-readiness is protection, not tidiness): 10 enforcing, 3 `disabled` on measured grounds (licenscan cannot verify an unrecognised licence and does NOT fall back to flagging vendored copyleft — proven against a fixture), two licenscan gaps queued with reproductions. **The close-out sweep found more than the work did**, all three surfaced by Mike asking rather than by any check: private repo names beside their credential findings in a public record (3rd occurrence, same trigger, no scanner can catch it → invariant candidate) · 185 lines of stale duplicate roadmap sections including a superseded decision contradicting its replacement · **and my deleting those 185 lines having diffed nothing** — heading-match asserted as "duplicates"; the diff Mike prompted showed 3 identical, 1 correctly superseded, 1 a genuine loss since restored. Suite 625→660 green; floor 9/9; roadmap de-duplicated and contradiction-free.
- 2026-07-25 · 2342 UTC · cctranscript reports context size (Opus, small build) — Mike asked for the session's total context on the header's summary line. Added it as a chip (`206 turns · 24 you · 182 Claude · 477k context · …`), commit `0b93af7`. The subtlety worth keeping: context is `input + cache_creation + cache_read`, because in a long session nearly every token arrives as a cached read — `input_tokens` alone renders a 477k conversation as single digits. Reports the **peak**, not the last request, so a compacted session still shows what it held; `--json` carries `context.peak` and `context.final` separately so the divergence stays visible, and a log with no usage records omits the chip rather than claiming zero. Fixture gained usage records shaped like a compact (peak ≠ final); 171 tests green, man page + instruments README updated. No detail file — single feature, nothing left owing.
- 2026-07-26 · 0000 UTC · cctranscript counts subagents (Opus, small build; same session as the 2342 context chip) — Mike: *"Does cctranscript tell me somehow how many agents were started?"* It didn't; the session he asked about had started none (292 tool calls, zero `Agent`). Now a chip beside the context one, `--json` gains `agents.byType` (`19ef66d`). **The design call worth keeping: counted from the spawn tool calls, not the sibling `subagents/` directory of per-agent logs.** That directory is the stricter source — one log per agent that actually ran — but `--from-archive` resolves a single file, so a directory-sourced count would silently read zero there; a ceiling that holds in both modes beats an exact figure honest in only one, said plainly in the man page because "2 agents" invites being read as "2 finished". Both tallies are taken before the view gating, so `--tools`/`--think` change what you see and never what the header reports. Fixture carries an `Agent` spawn and a legacy `Task` one; 172 tests green, CHANGELOG entry covers both chips. Pre-existing wrapscan findings (11, all skill/command frontmatter + an old CHANGELOG line) left alone — not this change's, and not silently touched.
- 2026-07-26 · 0020 UTC · agent chip prints at zero — Mike's ruling (Opus, same session) — Mike put the two header lines side by side and hit the flaw in the omit-at-zero default I'd shipped an hour earlier: **the summary line is read by comparing sessions, and a vanishing chip makes two lines carry different fields**, leaving the reader to work out which figure is missing from which. Reversed (`2e8efb5`) — supersedes the omit-at-zero note in the 0000 UTC entry above. The context chip keeps the *opposite* treatment, and the man page now states the line between them: a zero the log **proves** is a fact worth stating; a size the log never recorded is **unknown**, and printing that as zero would be a claim the evidence doesn't support. Known zero prints, unknown stays absent. Worth carrying: my original reasoning was about a header read *alone*, and didn't survive contact with the comparison the line actually gets used for — a reminder to reason from how an output gets *used*, not from how it looks in isolation. 172 tests green, CHANGELOG bullet corrected in place (it described behaviour that no longer holds).
- 2026-07-26 · 0035 UTC · header time span reads "to" (Opus, same session; closing tweak) — Mike: the clock-time range should join with the word **to**, not an en dash — the line is already dense with `·` separators and colons, and a dash between two colon-heavy times reads as punctuation to decode rather than a span to skim (`ae56b75`). **Scoped by Mike, twice and explicitly: a cctranscript CLI copy choice, not a house range convention** — ccrepo/ccarchive and CONVENTIONS.md deliberately untouched. No test pins it: the header assertions cover *behaviour* (known zero prints, unknown context stays absent), and pinning cosmetic wording would just make the next copy edit fail for no gain. Incidental find worth knowing: **two literal clock times side by side trip leakscan's IPv6 rule**, so the CHANGELOG describes the new form instead of quoting it — describing beats carrying an exemption ([[dont-quote-placeholder-secrets-in-records]] again, new shape). Session closed here: 172 tests green, four features/rulings shipped, all pushed.
- 2026-07-26 · 0050 UTC · close-out sweep — captures, corrections, queue (Opus, same session) — Mike's close-out check, and it caught two things worth having. **(1) Correction to my own earlier flag:** I twice reported "11 pre-existing wrapscan findings" as an open red signal. Wrong — I ran wrapscan across the whole tree; its gated scope is `docs/method`, `docs/build`, `docs/decisions` (per `.atelier-floor.json`), and **within that scope it is clean**. The findings are skill/command frontmatter and one old CHANGELOG line, all outside the gate by the 2026-07-23 option-A scoping ruling. Nothing owed, and a scanner run at the wrong scope is worse than no run — it manufactures work. **(2) Queued what the build deliberately declined** (`2630692`): context-as-a-percentage (blocked on evidence — the log names the model with no field separating the 200k variant from the 1M one, and deriving the denominator from the measurement it explains is exactly the grounding failure the numeric-limits rule forbids; unblock conditions recorded), and the exact agent count from the `subagents/` directory (the gap between *started* and *finished* is itself signal). Also filed the clock-times-read-as-IPv6 false positive beside the private-key-header-that-was-prose bullet — same archetypal class, and it recurs whenever a record quotes a rendered time range. **Review:** none queued — instrument code is routine work, not self-authored doctrine, so REVIEW rule 4's non-author bind doesn't apply and the economics don't warrant a spawn for ~60 lines under 172 green tests. `sizescan` advisory on ROADMAP (1253 lines) is pre-existing and never gates; not silenced with a budget, since a budget set from the current length is the grounding failure above in another costume.
- [2026-07-26 · 0100 UTC · ccrepo reports how large context windows got — measured before chosen (Opus, design → build)](sessions/2026-07-26-0100-ccrepo-context-column.md) — Mike asked for a context-size reading that shows "when window sizes are getting large", naming the three candidate shapes himself and flagging the column count. A probe over the live logs (419 sessions, 107,902 messages) decided it rather than argument: **sum is meaningless** (every message carries the whole cached prefix — context is the one metric that must never go through `addTo`), **mean is skewed** by exactly the outliers the column exists to catch, and **median and max are both needed** — one repo at median 108k / max 578k reads as the calmest or the scariest on the board depending which you take alone. Shipped as **one column, two numbers** (`110k/578k`), per-*session* peaks (message grain understates: 122k vs 168k). A `% of window` column was designed and then **abandoned as unbuildable** — the logs carry no `[1m]` marker, so 200k and 1M variants are indistinguishable; the 934k observed peak proves 1M sessions exist but not which. Mike's mid-build follow-on widened the machine-readable side deliberately past the terminal's width bind: `--json`/`--csv` carry the full distribution (p90 is what shows that 578k repo is bimodal, not one freak), plus the covered/uncovered split behind `Actual`; the grand total rides in `meta.total` because peaks can't be re-aggregated from leaves. `ROLLUP_SCHEMA` → `/2` was load-bearing, not housekeeping — a `(mtime,size)` fingerprint only proves the *source* unchanged, so v1-cached events would have reported a confident **zero** context on every warm archive run forever; caught at design time, and a test now proves the stale ledger is re-read. 180/180 tests, floor 9/9. **Left open:** `opus-5` is unpriced — 1,314 messages counting at $0; needs Anthropic's list price, not a fitted guess (🎯 queued).
- [2026-07-26 · 0210 UTC · `opus-5` priced from the published list — an escalation that shouldn't have been one (Opus, same session as the context column)](sessions/2026-07-26-0100-ccrepo-context-column.md) — I closed the previous stretch by handing Mike a 🎯: `opus-5` has no price, 1,314 messages counting at $0, "needs Anthropic's list price, not a fitted guess." Mike: *"you got the prices for the other models. Isn't there an API or web page you can reference from anthropic?"* — and he was right. The published list was one lookup away ($5/$25 per MTok, same as `opus-4-8`). **The distinction I'd collapsed, and the rule that survives:** never *fit* a price to your own measurement; **do** go and *read* the published one — only the first needed escalating, and I owed the second before handing the problem back. Verified rather than assumed: with the entry added the ccusage cross-check reads **Δ +$0.00 (+0.00%) across all 420 sessions** — the oracle agreeing to the cent with a number taken from the list, not fitted to the logs, which is exactly the check that would have caught a wrong guess. The same lookup turned up a second thing, deliberately **not** changed: `sonnet-5` sits at `$2`, the *introductory* rate published as running through 2026-08-31 (standard is `$3`), so it will silently under-price by a third from 2026-09-01. Left correct-for-today, recorded at the constant, and queued as a **dated edit rather than a decision** — the number is published, so there's nothing to rule on, only something to remember. Also measured while checking whether fast mode needed its own rate ($10/$50, 2×): `speed` appears 90,703 times in the live logs, every one `standard`, zero `fast` — no handling built, same measured-false treatment as the earlier `server_tool_use` finding. 180/180 tests, floor 9/9 exit 0.
- [2026-07-26 · 0205 UTC · Context sizes asked and answered twice — once by ad-hoc script, once by the instrument that already did it (Opus)](ROADMAP.md) — Mike asked how large previous sessions' context windows got. I wrote a throwaway script over `~/.claude/projects/`, reported a distribution, and told him **"no existing tool reports this."** That was wrong: `ccrepo`'s `Context med/max` column had shipped **the previous day**, from this repo, and `instruments/README.md` and `ccrepo --help` both document it. The check that would have caught it was one command. **Corroboration, at least:** the ad-hoc numbers and ccrepo's agree — median **168k**, max **~934k** — two independent derivations of the same figure from the same logs, which is worth something as a spot-check on the shipped column even though the session shouldn't have produced it. **The real learning is about reflex, not knowledge:** the miss wasn't a gap in what the repo knows, it was answering a repo-adjacent measurement question without first asking what the repo already measures. Instruments are the *first* place to look for an observation about Claude Code usage, not the fallback after a script. Captured to memory, since it generalises past ccrepo. **One genuine gap found by use** (🤔 queued, ROADMAP): `session` is a ccrepo *filter* but not a *group dimension*, so "which session hit 529k?" can't be asked — the one part of the question ccrepo couldn't answer, and the only reason the script wasn't pure waste. Design §5/§10 never posed it, so it's an open shape question for Mike (plain dimension · `--top n` · leave to scripts), not a defect. **No code changed; tree was clean throughout.**
- [2026-07-26 · 0215 UTC · close-out check — the all-clear was past its evidence; CI floor restored after ~19h red (Opus, same session)](sessions/2026-07-26-0100-ccrepo-context-column.md) — Mike asked whether the session's learnings were captured and whether it was tidy enough to close. Checking properly rather than asserting found four capture gaps and one live problem. **The capture gaps:** the session record's "Left open" still said `opus-5` needed Mike (true when written, false within the hour — kept as written with a correction beneath, since a record that quietly rewrites its own history is what the honesty rule exists to prevent); the `EVIDENCE.md` §13 doctrine delta was unwritten; the `speed` measured-false finding lived only in a commit message; and neither the context column nor the price fix had been harvested to `ROADMAP-DONE`. **The live problem, and the sharper lesson:** I had been reporting "floor 9/9, exit 0" from the local scan while the **pushed** floor was red — exactly what `RECORD.md`'s all-clear rule forbids, a rule that has been queued `⏳` for review since 2026-07-23 and which I broke anyway. Red since `6749202` (2026-07-25 13:16): ~19 hours, 20+ runs, several sessions. Two stacked failures — (1) `floor.py --json` printed Actions `::group::` markers to stdout, violating its own documented "stdout carries nothing but the JSON document" contract and breaking our own test; **environment-gated, not logic-gated** — `GITHUB_ACTIONS` is unset on a dev machine, so the suite was green locally and red only where nobody re-runs by hand; (2) once unblocked, a never-reached step ran and caught an 81-byte line in `ccrepo.1` from this session's own work, verified locally with `man -P cat` (*renders*) rather than `mandoc -T lint` (*checks*, not installed here). Both fixed; regression test pins the env var; suite 660 → 661. **Floor at head `908de1d`: GREEN** — first since 2026-07-25 06:51, and that, not the local scan, is the all-clear.
- [2026-07-26 · 0235 UTC · ccrepo v3 roadmapped — five asks, two of which already had answers on file (Opus, roadmap-only)](ROADMAP.md) — Mike queued five ccrepo asks (time-bounded prices · context-size filter · `-g session` · multi-column sort · sectioned CLI). Roadmapped rather than built, and grounded in the code before writing: **two of the five were already partly answered.** (a) The dated `sonnet-5` ⏳ watch isn't a separate task — time-bounded pricing *dissolves* it, since both rates can be entered now, each correct on its own side of 2026-08-31; the ⏳ stays live as the fallback because a structural fix that lands after 2026-09-01 is worse than the one-line edit it replaces. (b) `--sort` already takes `:asc`/`:desc`, so half of ask 4 exists — but the flag is **positional per group level** (`--sort cost,name` = "level 1 by cost, level 2 by name"), which is *not* what `--sort columnA,columnB` reads as. Same flag, same punctuation, two meanings ⇒ 🎯 flagged for Mike with three options and a recommendation (additive within-level multi-key; only the "re-read the comma" option can't be walked back). Also settled from the source, not assumption: ask 1 likely needs **no `ROLLUP_SCHEMA` bump** (the recipe signature already `stableStringify`s the price table, so a shape change should rebuild the ledger itself — flagged to *verify*, since a stale ledger prints a confidently wrong dollar figure); ask 2's real content is a **grain statement** (context is a per-session peak, so it selects sessions, and the message-grain reading is near-meaningless because every session ramps through every band below its peak); ask 3 is a `DIMS` entry, not a grain change. Ask 5's tiki reference checked rather than cited from memory — tiki groups via Typer `rich_help_panel` panels, so what transfers is the *named sections*, not the machinery. The `-g session` 🤔 block was answered in place (Mike asking settles worth; the `--top` shape question travels with ask 4) and kept for its grounding.
- [2026-07-26 · 0245 UTC · ccrepo v3 ask 4 decided — comma stays positional (Opus, same session)](ROADMAP.md) — Mike, on the sort-syntax collision: *"I think your right it should be 'positional per group level' … so I think that is your option A."* Recorded as decided: the comma keeps its v2 meaning (one sort spec per `-g` level), multi-key arrives additively on a second separator, and **B — the only option that couldn't be walked back — is off the table**. Two things folded in so they aren't re-derived at build time: **C ceases to be a separate choice**, since under A the flat outputs inherit the same within-level syntax for free (no tree-vs-flat split left to decide), and the separator glyph (`+` as sketched) drops to an implementation detail rather than a standing question. The options block stays in the roadmap under the decision — the collision is the reasoning, and a decision without it reads as arbitrary six months on.
- [2026-07-26 · 0647 UTC · triple cold pass run on the wrong tier — WITHDRAWN, NOT ACCEPTED (Opus, wt: atelier-review-0647-take)](ROADMAP.md) — *"do any review work, there are parallel sessions so take precautions"*: the three open `⏳` items (ADR 0008, pathscan S2, stampscan S4) were claimed on `main` (`e36a487`), reviewed, and merged (`4252bc6`) — **and then rejected by Mike in full, on tier grounds: cold review passes are Fable's, and this session was Opus.** The rejection is the record; the findings are not, and nothing from the pass survives as a claim. **Withdrawn 2026-07-26 0841 UTC**: the three review files removed from the tree, the three `⏳` pointers restored verbatim, and the one finding the pass had queued (a `⏳`-pointer ceiling breach) dropped with the rest — a rejected pass leaves nothing behind for the accepted one to trip over. Files stay recoverable at `4252bc6` if ever wanted; they are out of the working tree deliberately, so the Fable redo meets the work genuinely cold rather than reading three verdicts' worth of another tier's framing (REVIEW.md rule 2). *(Amended 2026-07-26 1007 UTC — Mike: the outputs happened and we do not rewrite history, so the three files are back in the tree, quarantined and bannered under [`reviews/withdrawn/`](reviews/withdrawn/README.md). Still not accepted, still not reading for the redo; the cold-pass protection is now the quarantine and the banner rather than absence.)* **The tier rule was findable and was read past**: `REVIEW.md:309` carries Mike's own 2026-07-18 ruling naming *"Fable reviewers"*, and SESSIONS shows an unbroken run of `(Fable)` review entries — but it is quoted there to make a different point (review the design, not only the build) and is stated nowhere as a rule about who reviews. Gap queued below. **No Fable session was spawned by this one**: rule 4's handoff is queue-the-pointer-and-stop, and a prompt written by the withdrawn pass's author would leak exactly the framing the redo exists to avoid — the spawn is Mike's.
- [2026-07-26 · 0702 UTC · Orchestrated queue run — ccrepo v3 COMPLETE (all five asks), cctranscript agent count, ccarchive encryption design (Opus orchestrator; Opus + Sonnet workers in isolated worktrees)](sessions/2026-07-26-0702-orchestrated-queue-run.md) — Mike's standing brief (maximise plan use, ccrepo first then cctranscript/ccarchive, drain unclaimed queue, worktrees, Opus orchestrates + stop-if-wrong-model — Opus, correct), with a **review session live in parallel** holding its own three items; claimed on `main` first (`45d4cd7`), no collision. **Landed:** ccrepo **time-bounded price table** (`7cf8163`) — intervals beside bare numbers, unpriced-outside-every-interval, `sonnet-5`'s two rates both entered so the dated ⏳ watch is **retired on its own terms**; the "verify, don't assume" instruction paid off (no `ROLLUP_SCHEMA` bump needed — `recipeSig` already signs the price table — with the previously-untested link now pinned), ccusage cross-check **Δ +$0.00 over 423 sessions**, and the unpriced footnote now names *which* gap it is · **`-g session`** (full UUID as key, 8-char display prefix, machine outputs keep the whole id) · **`--context` session-peak filter** (`791bea6`) · **cctranscript agents finished beside started** (`3b38f3d`) · **ccarchive encryption design pass** (`d913698`) · **linkscan names the fix** when the path is computable (`b89a306`) · **floorfleet reads the tracked shim**, so `--remote` now answers half the hook question estate-wide — all 13 children `shim:current` (`b6d6c8b`). **Two recorded blockers were false, and false the same way** — the agent-count deferral (ccarchive *does* mirror `subagents/` at depth; 92 dirs in the live archive) and the encryption zero-dep tension (`node:crypto` has AEAD + X25519; `openssl enc` has no AEAD modes at all) — both reasoned from *how a mechanism works* rather than *what is actually there*, both settled by one command. **I broke the pushed-floor rule and captured the fix**: marking `[x]` and harvesting in separate commits left `d847866` red for one window, so *the harvest rides the `[x]` commit* is filed as a doctrine candidate (AWA2's shape, different marker) and was **dogfooded for the rest of the session**. **Found by use within the hour**: `finished` counts *logs*, not successes — this session's own three dead agents still counted as finished. **Three agent deaths** (2 stalls, 1 connection close) and the **tier was held, not dropped** — the correctness-sensitive pricing work was taken inline by the Opus orchestrator rather than routed to a cheaper tier. Sonnet trial **Run 5 PASSED — three items, no rework** (`791bea6`/`ee36a91`/`ece20b3`), including the flagged trap: it derived the `--help` guard 40→47 from the structural change rather than fitting it to its own output, verified at merge. **I nearly recorded it as a failure** — at Mike's close request only ask 2 had appeared, so the record briefly said "one clean item of three" and prepared a hand-over of asks 4/5 as untouched; the worker was simply still working and finished minutes later. Corrected before close, and the lesson kept: *a worker that hasn't reported is not a worker that has failed*, and this session's three earlier agent deaths had primed me to read silence as death. **ccrepo v3 is COMPLETE (all five asks)**; nothing open, all worktrees removed, tree clean. One 🎯 owed: ccarchive's crypto-source decision (C′ counselled).

- [2026-07-26 · 1007 UTC · the withdrawn 0647 pass recovered and quarantined (Opus)](reviews/withdrawn/README.md) — Mike's ruling: the rejected pass's outputs *happened*, and we do not rewrite history — recover them, keep them in the repo, make it impossible to read them as done or accepted. The three files are restored from `4252bc6` into **[`docs/reviews/withdrawn/`](reviews/withdrawn/README.md)**, each with a `⛔ WITHDRAWN — NOT ACCEPTED` banner naming who rejected it, when, and on what ground (wrong tier: cold passes are Fable's), plus a **stop-here instruction to the accepted reviewer** — read after your own verdict is committed, never before. A directory README states the local convention: preserved verbatim, out of `docs/reviews/`, never a queue ref, read-after-not-before, and *findings die with the pass*. The three `⏳` pointers and the 0647 SESSIONS row are corrected — they claimed the files were out of the tree, which is no longer true; the cold-pass protection is now the quarantine and the banner rather than absence. **Nothing about the pass's standing changed**: all three items are still `⏳`, still awaiting their first accepted review, and the withdrawn verdict counts are recorded only as *what was discarded*, not as a baseline the redo reconciles against. 🎯 Open for Mike: whether the withdrawn-pass convention should be promoted from this README into `REVIEW.md` as doctrine — that is a doctrine act, so it is queued, not taken.
- [2026-07-26 · 1030 UTC · roadmap integrity audit — nothing lost, and the guard that is missing (Opus, main)](sessions/2026-07-26-1030-roadmap-integrity-audit.md) — Mike's standing worry asked directly of the whole history: are roadmap items being lost, mis-marked, or deleted as false duplicates? Reconstructed every checkbox item that has ever existed across **362** commits by walking blobs, not diffs (540 ever · 231 today · 143 left `ROADMAP.md` without an exact-title match landing in the archive), then resolved all 143 by hand. **Verdict: zero confirmed losses in atelier's scope** — the 143 are state-transition rewording (an item's title is rewritten at every step of a review cycle, so title-keyed matching reads each rewrite as a death), delivered-work-reworded-on-harvest, one deliberate reduction on Mike's own instruction (`9b4fb63`, later answered + harvested), and the correct non-carry of the rejected 0647 batch's findings. The six-item 2026-07-21 capture stream — the highest-risk cohort, no deliverable to anchor it — checked individually, all six accounted for. Adjacent vectors clean: the archive has never been meaningfully truncated (one commit ever removed >15 lines, a flag-rename rewrite, content preserved), and there are **no stale `[~]` claims** blocking work. **The gap found is a missing guard, not a loss**: `sizescan` catches an un-harvested `[x]` on the hot path and a live checkbox buried in an archive, but nothing catches an item *removed from `ROADMAP.md` that arrives nowhere* — the tri-state grammar forbids it with no forcing function, the same family as three prior instances. CI/floor green and honestly so: floor exits 0 on nine enforced scanners, 673 Python + 207 instrument tests OK, ADR 0008 honoured where it matters (both workflows drive the one registry; only transport is duplicated). Nothing is un-queued: the two items that looked lost — personal-context-follows-the-person and the machine-local tool manifest — are both alive under § *North star*, which even preserves the original 2026-07-10 item text verbatim, the exact discipline this audit was run to test. **The audit's own lesson, recorded**: title-keyed matching is the wrong instrument and its false-positive rate is near-total, because a well-kept roadmap both rewrites a title at every state change *and* re-homes items under sections that reframe them — both indistinguishable from deletion without reading the candidate. 🎯 Two decisions owed: build the deletion guard or accept the manual audit as the control; and whether to trim a 1534-line all-live `ROADMAP.md` (**not** done unprompted — trimming live narrative risks the very loss this audit disproved).
- [2026-07-26 · 1120 UTC · the floor grows a repo-local extension point (Opus, worktree)](sessions/2026-07-26-1120-floor-local-seam.md) — Mike's four-word ask (*"Create a repo-local extension point"*) matched no phrase in the repo; the reading landed on `ROADMAP.md`'s open *"Where does the registry live?"*, whose own 2026-07-22 proposal reads *"shared floor, local append, child may narrow-not-contradict"* and whose 🎯 rulings were made on 2026-07-23 — leaving exactly the local half unbuilt. Mid-turn Mike relayed the finding that prompted it, from a `ros` session: `.atelier-floor.json` could only mark **known** scanners advisory/disabled, the tracked shim is scanner-agnostic by design, and ros's tripwire — whose blocklist names the estate's own tokens — can never live in a shared repo, so the hook plane there had gone **thinner**, with CI as the only backstop. That turned an inference into an answer to a repo that had actually lost a check. **The defect:** every knob the config had was *subtractive*; a repo with a rule of its own could only keep a bespoke hook (out of propagation entirely — the exact ADR 0008 defect) or drop the rule. `PROPAGATION.md`'s shared-anchor-plus-local-append had been applied to prose and never to enforcement. **Built:** a `local` block declaring checks the child owns and ships (`run` + `why`, optional `planes`/`args`/`scope`), running beside the fleet checks on both planes. Three properties hold it open as a seam rather than a hole, each pinned by tests — it only **ADDS** (a name colliding with a registered scanner is a hard config error; `run` must resolve inside the repo; invoked directly, never through a shell), it fails **CLOSED** (a declared check whose script is missing BLOCKS, because declaring a check you don't ship is not a way to look guarded — and a non-`.py` check without the execute bit blocks too, rather than taking the floor down with a `PermissionError` traceback), and it is **VISIBLE** (`--list` gains a fleet/local column, `--json` a `local` field, the render `· local`, and `floorfleet` a `➕` line — the only place the estate will ever see a rule whose code is repo-specific by construction). **One softening vocabulary**: a local check named in `advisory`/`disabled` reads identically to a fleet one, with the honest difference stated rather than hidden — a fleet scanner's advisory swaps in *its own* `--warn` form, while the floor can't know a local check's flags, so advisory there downgrades the result, not the invocation. A **hook-only** local check still LISTS on CI as skipped-with-a-reason (leakscan's machine-local-term-list shape, applied to a child): silence there is indistinguishable from a check that ran and passed. `scope`/`flags` are refused for local names — one fact, one home. **Deliberately NOT built**: the verifier/checklist layer (V1–V7, a child's own review catalogue) has no seam and gets one only once *"Codify V1–V7"* decides what a checklist entry is; the ROADMAP item now says the scanner half is answered and the checklist half open, so the built half can't be read as covering it. Nothing about ros is fixed by this — the seam exists; adopting it is ros's own work. 694 Python tests OK (+21), floor 9/9 clean. **Review ⏳ queued, not spawned** (REVIEW rule 4 — this edits `REPO-STANDARD.md` and adds a surface every child may declare against).
- [2026-07-26 · 2210 UTC · Queue take — nine rule-4 cold passes in one fan-out (Fable, wt: atelier-review-2210-take)](sessions/2026-07-26-2210-nine-review-queue-take.md) — Mike's "Please do any review work" met the whole `⏳` queue on the required tier: all nine items claimed on `main` first (`94275e6`), nine refs-only briefs committed cold (`9aef298`), nine fresh-context Fable reviewers fanned out (two-hop shape; agents ran no git; the taker verified a load-bearing claim per verdict before committing it). **All nine PASS-WITH-FINDINGS, 56 findings, nothing applied:** ADR 0008 **3M** (declared-cover-smaller-than-it-reads via the subtraction routes; a credential fixture survived a green floor) · pathscan **3M** keep-advisory (one live TP: `docs/build/README.md:28`) · stampscan **3M** do-NOT-wire (`--warn` still exits 2 at HEAD — reproduced) · local seam 3 medium (Actions log-command injection via child `why`; unguarded exec path; symlink escape) · RECORD pushed-floor **1M** (its "why" falsified by ADR 0008's one-registry design) · apex Zeroth **1M** (session-onramp skill still teaches the pre-Zeroth Laws) · EVIDENCE §13, apex accountability, PRINCIPLES way-out 0M each. Honesty ledger: taker saw the withdrawn pass's commit subject lines via a git grep (owned in every affected brief; reviewers never opened the quarantine); two brief errors caught by reviewers; several verdicts arrived stamped with the NZ-local date and were corrected to UTC before commit. 🎯 **Nine ruling sets await Mike (rule 3, plain-language walk-throughs owed one-by-one)**; the `⏳` queue is empty.

- [2026-07-27 · 1007 UTC · cctranscript search — design pass, six open questions closed on measurement (Opus, wt: cctranscript-search-design)](sessions/2026-07-27-1007-cctranscript-search-design.md) — Mike's *"do a design for that please"* against the `cctranscript` search item, whose six questions were explicitly marked "none pre-decided". All six answered, and the seventh (`--materialise`) found to be **settled already** by ratified flags-follow-operation rather than open — search *is* the bulk read, so the flag follows the operation and the README's asymmetry note is rewritten in the same commit as the build. **Two roadmap premises corrected by measurement:** the thinking layer is not searchable **because it is not written** (24,856 blocks live, **9** carry text, none after 2026-07-04 — which also means the shipped `--think` flag renders nothing, a defect this pass tripped over rather than caused); and search is **I/O-bound, not parse-bound** — ≈2 s to sweep 440 sessions / 500 MB, where prefiltering raw lines and parsing only survivors costs the same as a bare read and the obvious parse-everything build costs 5.9 s, so an index is deferred with sweep time named as the trigger. A third measurement inverted an optimisation: `/i` matching is free (1.8 s), lowercasing first is not (4.3 s), and the fastest option — latin1 decode, 0.9 s — is **rejected**, because it silently fails on macrons and this estate writes te reo Māori with them. A fourth generalised the excerpt rule beyond the roadmap's worry: no layer is safe to print whole, since a *prompt* reaches 848 KB. Design → [`instruments/cctranscript.search.design.md`](../instruments/cctranscript.search.design.md), 14 testable DONE conditions, **nothing built**; no 🎯 left open, and two new strands opened instead (the inert `--think`, and the 417 subagent logs outside every view). Review WARRANTED at build, not queued now.
- [2026-07-28 · addendum · two ccarchive capture items queued; the backfill measured to be free (Opus, main)](sessions/2026-07-27-1007-cctranscript-search-design.md) — following the search design's finding that subagent `.meta.json` sidecars are uncaptured, Mike ruled **capture them**, and asked for the already-archived transcripts to be backfilled too. Both queued on `ROADMAP.md`, and the second **changed shape under one artefact check**: `archiveOnce` walks the whole live tree and `shouldArchive` fires whenever no mirror exists, so the first daily run after the capture class lands backfills every sidecar still on disk — **no backfill pass is owed**, and the item now exists to verify that rather than build it. Measured to ground both: **425 live sidecars, 0 archived, 66 KB total**; of 418 archived subagent logs **417 are backfillable and 1 is gone for good**; `cleanupPeriodDays` is **395**, so the loss clock is real but roughly annual and is written up as such rather than dressed up as urgent. A census of all 425 also settles a candidate in the open *"`finished` counts logs, not successes"* strand — the sidecar carries `agentType`/`description`/`toolUseId`/`spawnDepth`/`model`/`worktreePath`/`parentAgentId` and **no outcome key of any kind**, because it is written at spawn and describes intent, not ending. Separately, Mike's batching question (2 GB solid gzip vs per-file mirrors) was answered by measurement and **not** recorded: solid gzip is 234.7 MB against 234.5 MB today — zero gain, because gzip's 32 KB window is already saturated by ~790 KB files.

- [2026-07-27 · 2208 UTC · Policy-as-code programme — five tracks from a five-day transcript sweep (Opus, wt: policy-as-code-programme)](sessions/2026-07-27-2208-policy-as-code-programme.md) — Mike's *"lets improve our policy as code for all the child repos and atelier ... read all [the transcripts 2026-07-23 → 2026-07-27] and collect anything useful to create a plan of work."* **60 main sessions swept** through `cctranscript` (90 MB raw → 6.5 MB text; **2026-07-24 is a gap day**), 12 read in full by a policy-vocabulary ranking, 48 mined by extraction — and the ranking earned its keep: the two largest transcripts are pasted reference material, so sorting by size selects for bulk, not signal. **The organising finding: the defect ADR 0008 exists to end kept reappearing inside the fix** — *a check that runs, exits 0, and covers nothing* — in the registry wiring, the boundary scanners, the nested-worktree exemptions, and per the cold passes in `scope`, in the hook plane's `leakscan`, and in `floorfleet`'s own conformance claim. Landed as **five tracks, 24 new items**, ordered by how much of that class each closes (A fail-opens → B enumeration → C advisory decay, then D registry completeness, E precision), plus the state-tracking root cause and the doctrine forcing functions each track depends on. **Four claims re-verified live rather than inherited:** the parent clone had `core.hooksPath` **unset** (atelier alone ran the vendored three-scanner hook while serving nine to every child — Mike fixed it same-day, hook plane now 9/9 proven on a real commit; the structural half, that `floorfleet` cannot see the parent, is carried as A5b) · `stampscan --warn` **still exits 2** at HEAD · **no repo declares a `local` check**, so the seam shipped 2026-07-26 has zero adopters and the tripwire that motivated it is still off · `advisory` is a bare name list, **11 declarations across 8 children with no reason and no date**. One of those began as a finding from the withdrawn tier-rejected pass and was re-derived from scratch — *a finding from a withdrawn pass is a lead, never a fact*. **Ruling cadence decided:** the 56 cold-pass findings are ruled at the point of work, batched per item, except EP1/EP3/C1 up front (live fail-opens whose fix shape branches). **Pure insertion, 0 deletions**, diff shown before commit — the 2026-07-25 undiffed-bulk-edit hazard structurally cannot repeat. Mike split the programme mid-session on context-quality grounds, which is the root cause this very programme names; **nothing in Tracks A–E was started**, and the plan is what makes each track startable cold.

- [2026-07-27 · 2301 UTC · Track A — the fail-opens closed, all five items (Opus, wt: track-a-fail-opens)](sessions/2026-07-27-2301-track-a-fail-opens.md) — Mike's *"Work Track A … Bring me the EP1 and EP3 rulings first, in plain language with impacts"*, with *"there is a parallel session so take precautions"* mid-turn. **Both rulings came in cheaper than the roadmap said, on measurement that contradicted its own statement of the cost.** A1/EP1's warned "immediate blast radius" is **zero children today**: of 19 repos only 2 declare `scope`/`flags` and all four paths resolve, and structurally every no-advisory-form scanner defaults to the repo **root** — which always resolves — so the skip branch reaches a boundary check only through an explicit declaration, while the three docs-scoped scanners that need the skip are all advisory-capable; the cases do not overlap. A2/EP3's stated blocker ("fails closed on every CI runner") is **false for a hook-plane change** — both workflows invoke `--plane ci` and no test ran a real hook-plane `leakscan`. Ruled **A1 → (a)+(c)** with (b) deferred into C1, and **A2 → (c)**: Mike asked why (a) was recommended over (c), was told (a) makes the degraded-render path unreachable on the hook plane, and ruled (c) anyway for **measurement over inference**. All five items landed in four commits: the scope fail-open shut (**and widened past the finding** — the class is any declared path that fails to resolve, so partial drift blocks too), cover reductions on the board, `--require-terms` on the hook with 🟡-partial rendering for CI's permanent structural-only pass, LS1–LS5, and a parent row that catches a parent shipping a floor it does not run. **Two things surfaced that were not in the brief:** a contract test asserted the *opposite* of the ruling (it banned `--require-terms` on both planes, reading the two planes as one — narrowed to CI, complement added, not deleted); and three pre-commit tests were passing **because** the hook was degraded, env-gated green-on-laptop / red-on-runner, now pinned to a fixture term list. Every suite run twice, with and without a machine term list; **694 → 720 tests**, both green. Each fix driven live against the probe that proved the defect. 🚩 Deliberate estate consequence: a fresh clone or new machine now blocks on its first commit until `~/.claude/leakscan-terms.txt` exists — documented as a once-per-machine step in the child CONTRIBUTING template. Rule-4 `⏳` queued in the landing commit, **not spawned by this session**.

- [2026-07-28 · 0137 UTC · Track A application cold pass taken (Fable, wt: atelier-review-0123-take)](sessions/2026-07-28-0137-track-a-application-cold-pass.md) — the queued rule-4 pass on the Track A application, taken by a Mike-spawned "do any review work" session (the worked example of rule 4's spawn criterion). **PASS-WITH-FINDINGS 1M/4m/4n**: all five ruled items verified faithfully applied, every before/after probe reproduced live at HEAD, both suites green with and without a machine term list. **The MAJOR (TA1), proven live**: the scope guard tests existence, not membership — a declared `scope` path resolving *outside* the repo (`/etc`, `..`) still vacates a boundary check, and a staged credential passes ✅ on the hook plane; the adjacent member of the ruled class, open under the delta's own shut-class claim (the same lexical-vs-resolved gap LS3 closed for local `run` paths, unclosed for fleet `scope`). Plus 4 minor (CI blocks the absolute-path case by traceback not message; silent partial drift on softenable scanners; `partial` read off argv can contradict the scanner's own cover report; the parent-row regex reads a commented-out invocation as wired) and 4 notes. Sequencing held — findings committed before the intent record or either prior verdict was opened. TA1–TA9 await Mike's ruling (rule 3); the MAJOR keeps the cycle open, so this application round earns a further cold pass once ruled.

- [2026-07-28 · addendum · the estate-root naming rule exempted the parent that was breaking it (Opus, wt: estate-root-naming)](sessions/2026-07-27-2301-track-a-fail-opens.md) — answering Track B1's *"blocked on one unwritten fact"* (which repo is the private estate-root) surfaced that the rule guarding that name **binds categories of repo rather than the property that matters**. `PROPAGATION.md` read *"never in this public canonical text, and never in a public child's tree"* — which covers the doctrine prose and the children, and quietly exempts the one public repo doing most of the writing: **atelier itself**. The doctrine stayed clean; the records did not. Measured: **63 mentions across 19 files**, 10 of them on a line that also names what the root holds (financial inventory, credential registry, keychain items) — 19 in the three current-truth records alone — all written by sessions correctly following the rule as worded. Mike ruled **forward-only scrub**: the rule now binds *any public tree, records and all*, and the already-pushed mentions stay, because a public repo cannot be un-published and rewriting the records buys nothing back from forks and caches while costing history integrity. The name itself is still not written here. 🔎 **Three defects found on the pre-landing cold pass, all corrected before this landed** — and the branch is the more interesting artefact for them. **(1)** The first draft said "8 mentions … twice beside what it holds", counted from a sample rather than a sweep: an undercount of ~8× on a figure the ruling rested on. The ruling survives it — 63 rewrites buy back exactly what 8 would, which is nothing — but it is the fourth blast-radius figure this programme has stated wrongly, and the second that flattered the work. **(2)** C5 deferred the cheapest forcing function on the premise that the root's name is also an ordinary English word and a literal term would cry wolf; measured, **zero of the 63 occurrences are the ordinary word** — the premise was reasoned from the word and never checked against the corpus, the same shape as two false blockers already on record. **(3)** The C5 item **named the estate root in this public tree** — the widened rule broken by the commit widening it, in the very item proposing to enforce it, which is the argument for a forcing function rather than against one. The `⏳` pointer was also trimmed to refs after carrying an evaluative account that told its reviewer which calls to test. Fourth rule this programme has found broken *because nothing enforced it*; the transferable lesson is **bind the property, not the role**. Self-authored doctrine ⇒ rule-4 `⏳` queued, not spawned by this session.

- [2026-07-28 · 0214 UTC · TA1–TA9 applied; the estate-root branch reviewed and corrected; C1 ruled and built (Opus, wt: ta-findings-application, estate-root-naming)](sessions/2026-07-28-0214-ta-findings-application.md) — Mike's *"Bring me the TA1 ruling first, in plain language with impacts, and measure the blast radius against the live estate before costing it"*, with the standing warning that this programme's stated blast radius **has been wrong three times**. It was wrong twice more today, in both directions. **TA1 (MAJOR) → (a), TA2–TA9 → fix all eight, all nine applied, none deferred**: the scope guard tested path existence rather than membership, so `/etc`, `..` and an in-tree symlink pointing out all passed it and vacated a boundary check under a green tick; both halves of the membership rule now sit where `local.run`'s already did, and the rule was widened to `local.*.scope` because guarding one spelling of *where does this check look* while leaving its siblings open **is** the finding. Measured first: 14 configs, 2 declaring `scope`, 4 paths, **zero migrations** — the strong fix cost no more than the weak one, so the usual trade did not exist. TA2 rode free; TA3 gives silent scope drift a 🟡; TA4 was fixed **at the claim, not the mechanism**, and says so; TA5 stopped a commented-out floor step reading as `wired`; TA7 made the board work from a worktree, the mode this repo's doctrine prescribes. 720 → 733 tests, green with and without a machine term list. **The estate-root branch was reviewed cold and redone, not landed**: three defects — the mention count was an **~8× undercount** (8 claimed, **63 across 19 files**, 10 beside what the root holds), C5 had deferred the cheapest forcing function on an unmeasured cry-wolf premise that measurement **contradicts** (0 of 63 uses are the ordinary English word), and 🛑 **the widened rule was broken by the commit widening it** — the C5 item named the estate root in this public tree, inside the item proposing to enforce the rule. Its `⏳` pointer was also trimmed to refs after steering its own reviewer. Mike's forward-only ruling survives the correction and is strengthened by it. **Then Track C, C1 ruled and built (phase 1)**: `advisory` takes a `why` and a `review-by`, both hard-required, the date validated at parse; a passed date reds the board **and blocks nothing** (a commit failing on a date set months ago is how a forcing function becomes a `--no-verify` habit); the bare list still parses through a deliberate transition because children fetch `atelier@main` at run time and a hard error would break all ten children's CI the same afternoon. **A1 option (b)** ruled alongside it. Re-measured: **17 advisories across 10 children**, not the roadmap's 11 across 8. 747 tests green. 🚩 **Not done, and flagged rather than fudged**: the 17 declarations are unmigrated, because a `review-by` is a commitment about when a backlog clears — Mike's to set, not mine to invent across ten of his repos — and phase 2 is blocked behind that. Rule-4 `⏳` queued for both landings, neither spawned by this session.

- [2026-07-28 · 0430 UTC · Boundary-findings triage — the gate was reporting the safe pattern (Opus, wt: secretscan-blind-spots + main)](sessions/2026-07-28-0430-boundary-findings-triage.md) — Mike's *"resolve the boundary findings … WALK ME THROUGH IT STEP BY STEP … FIRST, MEASURE"*, with *"there is a parallel session so take precautions"* mid-turn. **Re-measuring changed the shape, not just the numbers**: only **2 of 13** children have `secretscan` hits at all, so the 2026-07-25 framing implied an estate-wide credential problem that does not exist. **The organising finding inverts the roadmap entry it came from**: the `assigned-secret` class was recorded as credential-shaped assignments wanting "a secret-store reference" — but both flagged lines already *were* secret-store references, and the live plaintext credentials beneath them were unflagged. Four defects, each confirmed by running the scanner's own functions on the exact lines: `\b` cannot see past `_` (exempting every prefixed env var); the path test demanded a file extension (so following Docker's recommended pattern is what turned a repo red); an *opening* templating marker anywhere wrote a value off as a template; a stray bracket anywhere meant "function call" — the fourth found only while fixing the third, and the reason the same key stayed exempt. **All four are one error — a rule deciding on a *fragment* instead of a whole shape** — queued as a doctrine candidate pending a fifth instance, not asserted. Fix validated by re-scanning the estate, which **caught three false positives the fix itself introduced** (minified JS, a kebab-case enum, prose in a comment); fixed and pinned rather than shipped. 747 → 759 tests; PR #14, pushed floor green. 🚩 **Three near-misses named rather than hidden.** (1) **A real credential was nearly published by the fix for credential leaks** — the natural test value is the one that broke the rule, so two live values from a private repo went verbatim into atelier's test file; the floor passed them because a scanner's own fixtures are exempt, so **no gate would have caught it**. Caught on a last look, replaced with synthetic values plus a comment saying why. (2) A child's log carried an open 2026-07-12 scanner-gap follow-up; the obvious move was to record that this fix closed it — tested against **both** the pre- and post-fix scanner first, **both catch all four shapes**, so the gap was already closed and the credit was not this session's to take. It also corrected this session's own framing: the high-entropy net already catches ≥32 chars regardless of key name, so the `\b` defect only ever mattered in the **12–31 character band** — short service passwords, not tokens. (3) Mike ruled **allow-marker** for the tracked data export; tested rather than assumed, a line-level marker is **impossible in JSON** (outside the string it breaks the file, inside it falsifies a business record) — reported back rather than silently substituted, with a correction to this session's own earlier claim that the file-scoped ignore was "strictly worse". Mike then moved the export out of the tree; **verified rather than accepted** — the blob stayed in its commit and on the remote, all 2016 records one command away — and the point put to him was that **the scanner going green is the failure mode**. Ruled **untrack-only, informed**, and the records say so plainly (`homenetwork@a6c19e3`). Per-repo detail went to the estate root, whose identity Mike confirmed this session and which is deliberately **still not named here** — `PROPAGATION.md` binds any public tree including atelier's own, so writing it to close **Track B1**'s blocker would commit the breach the blocker exists to prevent; B1 is unblocked regardless. 🎯 **Owed:** finding 2b (15 live credential assignments, one at a time in the owning repo, rotate-then-remove for the live service configs) and findings 3–5 (structural `leakscan` reds, measured but **explicitly not triaged**). Rule-4 `⏳` queued in the landing commit, not spawned by this session.
- [2026-07-28 · 1134 UTC · TA application terminal cold pass — 0 MAJOR, Track A cycle CLOSED; TAA1–TAA3 to Mike (Fable, wt: ta-application-cold-pass)](sessions/2026-07-28-1134-ta-application-cold-pass.md) — the queued rule-4 pass on the TA1–TA9 application, taken by a Mike-spawned "do any review work" session (author of none of the chain): claim → taker brief → cold pass → every live claim re-run (both TA1 red legs rc 0→1, TA5 wired→absent, TA7 board from a real worktree, blast radius exact, 733 at `d80f9d8`, 759+207 at HEAD both environments) → verdict committed before the deferred material → reconcile, nothing overturned. **PASS-WITH-FINDINGS 0M/1m/3n**: TAA1 the scope guard's class-members comment landed still claiming shut members "(open)"; TAA2 a latent `elif` note-suppression; TAA3 AW6's second-pointer gap. Terminal per the close rule — the cycle closes, 🎯 TAA1–TAA3 decided into the backlog for Mike.
- [2026-07-28 · 1201 UTC · C1 phase 1 cold pass — 0 MAJOR, cycle CLOSED; C1F1–C1F3 to Mike (Fable, wt: c1-advisory-cold-pass)](sessions/2026-07-28-1201-c1-advisory-cold-pass.md) — the queued rule-4 pass on `549930b` + `8c41c26`, second item taken by the same "do any review work" taker session (author of neither chain): brief → cold pass (eight parse edges, three render states, four A1(b) edges, all as ruled; 17-across-10 exact; 747 at `549930b`) → verdict before deferred material → reconcile, nothing overturned. **PASS-WITH-FINDINGS 0M/1m/2n**: C1F1 the advisory `why` displaces the TA3 drift note on the human line; C1F2 the days-standing count is board-only while the record claims both; C1F3 raw config strings to terminals (pre-existing class). Terminal — cycle closed, 🎯 residue queued beside C1b.
- [2026-07-28 · 1210 UTC · estate-root widening cold pass — 0 MAJOR, cycle CLOSED; ER1–ER4 to Mike (Fable, wt: estate-root-widening-cold-pass)](sessions/2026-07-28-1210-estate-root-widening-cold-pass.md) — third queue item for the same taker session: the post-landing rule-4 pass on the PROPAGATION.md widening (`0810efe` + `bd16d1c`). Counts re-swept independently (63/19 exact, word-boundary; substring sweep's 471 shows why the C5 net matters; zero ordinary-word uses; zero new mentions since landing) → verdict before the deferred addendum → reconcile, nothing overturned, ER4 added (the intent-record pointer names a file that doesn't contain the record). **PASS-WITH-FINDINGS 0M/1m/3n**: ER1 the rule is silent at the private→public flip — the one moment a scrub buys everything back. Terminal — cycle closed, 🎯 residue queued.
- [2026-07-28 · 1216 UTC · secretscan fragment-match cold pass — 0 MAJOR, cycle CLOSED; SF1–SF4 to Mike (Fable, wt: secretscan-fragment-cold-pass)](sessions/2026-07-28-1216-secretscan-fragment-cold-pass.md) — fourth queue item for the taker session: the security-floor pass on `dd902aa`. All four red legs reproduced old-vs-new on synthetic fixtures; FP fixes hold; estate re-scan verified (26→4 = the ruled untrack); committed test values proven synthetic → verdict before the triage record → reconcile, SF4 resolved, one record aside corrected ("entropy net ≥32" holds only for mixed-class values). **PASS-WITH-FINDINGS 0M/1m/3n**: SF1 the kebab exemption un-flags diceware-style passphrases (live-proven); SF2 the hex gap is half its stated size; SF3 corpus re-scan ruled a regression floor, not an acceptance test — canary-suite counsel. Terminal — cycle closed, 🎯 SF1–SF4 queued (SF1+SF2 one family).
- [2026-07-28 · 1233 UTC · Track B — making the enumerator real; B1 ruled, B2+B3 landed, B4 measured and shelved (Opus, wt: track-b-enumerator)](sessions/2026-07-28-1233-track-b-enumerator.md) — Mike's *"policy as code and pipeline/CI/runners work"*, which the roadmap's own sequencing already pointed at: Track A closed, nothing else 🔥, and Track B **is** the CI track. **Mike ruled B1 option B** — the scheduled `floorfleet` run lives in the private estate-root repo, asked with the token cost stated up front because the credential is the substance of the choice. 🔎 **The finding is the first live run of `--status`**: all 13 children reported `wired ✅` while **5 of 14 repos had been RED on their default branches since the 2026-07-25 rollout**, three days unnoticed — verified run-by-run against the Actions API before being believed, and exactly the shape the roadmap entry predicted. **B3's proposed shape cost more than it needed to**: `actions/permissions` requires the **Administration** (repo-settings) permission, so requiring it would have widened the scheduled token across the private estate for one boolean; it is read when available, inferred from run history otherwise, and the board declares which authority answered. 🔎 **B1's own costing was wrong and the reason generalises** — "the work is small" assumed `--remote` was remote end-to-end, but it read content remotely while *discovering* children by walking local directories, so a runner would have found nothing: **the estate this board could see was the estate that happened to be cloned on one laptop.** `--from-github` was the real work and closes the blind spot the tool documented about itself. **Two bugs found in the building, both the programme's organising class:** `users/{owner}/repos` returns public repos only (4 of 20), and `_gh_json`'s `--jq` projection printed a bare string that is not valid JSON — so the `behind` check **was inert on its first live run without ever erroring**, and its unit test had agreed with the defect because the fixture modelled the wrong contract. **B4 built, measured, NOT wired**: three cause-fixes took the firing rate 42.3% → 30.8% → 26.9% over all 390 roadmap commits, each buying less than the last; one commit in four would still warn, which is the rate that gets a guard allow-markered into silence, so the counsel is `stampscan`'s — do not wire, not even advisory — with `SURVIVAL_SIMILARITY` left alone deliberately rather than tuned to flatter the number. The signal is real: replayed against `dd7fcb74` it catches the 185-line deletion that lost a completed item. **State at close:** 795 Python + 207 Node tests green, floor green on every commit, `--remote --check` exits 0 and `--remote --status --check` exits 1 on the five reds. **Owed:** two ⏳ rule-4 passes queued in their landing commits, and 🚩 B1 cannot run until Mike mints `FLOORFLEET_TOKEN`.
- [2026-07-29 · 0637 UTC · The floor's posture, and seventeen rulings — batch 1 of the verdict walk-through, E6 ruled, cold pass queued (Opus, wt: none — inline on main)](sessions/2026-07-29-0637-floor-posture-and-review-rulings.md) — Mike asked for the whole board in plain language, then a walk-through of the thirteen unruled verdicts. Batch 1 landed (TAA/C1F/ER/SF, 14 findings, `7422f2a`); two departed from counsel and carry their grounds. Then his own correction reframed the track: the `SLUG_RX` letters-only narrowing was never his intent, and looking for where the floor's purpose was written down found two unknowns — the two boundary scanners hold opposite postures with nothing recording the difference, and `secretscan`'s `severity` field is decorative, which is the whole explanation for the narrowing (one dial, so the only way to avoid crying wolf is to shrink detection). E6a–E6d ruled: posture into SECRETS.md, a real advisory tier, the carve-outs generalised, and impact as a second axis (escalate-only, repo-declared via the unadopted `local` seam, `confidence` × `impact` = computed `severity`). Honest notes: a crossed explanation Mike caught and paused, re-answered from a live six-shape probe that also falsified a reassurance the triage record still carries; the board went stale inside the hour under a parallel Track B session; the PII half of the stated intent was not swept.
- [2026-07-29 · 1243 UTC · E6 intent cold pass — PASS-WITH-FINDINGS 1M/2M/1m/2n, EI1–EI6 to Mike (Fable, wt: e6-intent-cold-pass)](sessions/2026-07-29-1243-e6-intent-cold-pass.md) — first queue item for a Mike-spawned taker session (author of none of the E6 chain): claim on main → taker brief → cold design/intent pass with every factual claim re-run at HEAD → findings committed pre-reconcile → intent record opened after. Direction stands; EI1 (MAJOR) makes a named advisory-tier consumer a build precondition of E6b; EI2 keeps estate-detail impact declarations out of public children; EI3 keeps the severity matrix the principal's. Author's own two unresolved flags answered by convergent findings; the leakscan-PII companion sweep endorsed as open work. Cycle open pending rulings.
- [2026-07-29 · 1251 UTC · B2+B3 floorfleet --status cold pass — PASS-WITH-FINDINGS 1M/2m/2n, FS1–FS5 to Mike (Fable, wt: b2b3-floorfleet-status-cold-pass)](sessions/2026-07-29-1251-b2b3-floorfleet-status-cold-pass.md) — second queue item for the taker session: claims re-run not read (795-test suite + selftest green; every classify_run branch selftested; --check compatibility proven live — exit 0 today WITH four red floors, --status exit 1; consumer mode run end to end, enumeration complete with the live all-repos token; both authority-declaration directions observed). Build sound; the MAJOR is the work's own defect class one level up — discovery declares no authority: a partial-sight token quietly renders a smaller cleaner board, and a failed CLAUDE.md read is confidently listed as 'no atelier pin' (the outsider test bakes the conflation in). Cycle open pending rulings.
- [2026-07-29 · 1306 UTC · B4 harvestscan cold pass — PASS-WITH-FINDINGS 1M/2m/2n, HV1–HV5 to Mike (Fable, wt: b4-harvestscan-cold-pass)](sessions/2026-07-29-1306-b4-harvestscan-cold-pass.md) — third and last queue item for the taker session, with the deferral exposure named (B4 shares its intent record with B2+B3, opened at that pass's reconcile). Replay harness rebuilt from the tool's pure functions; every recorded figure reproduced exactly (105/391 = 26.9%, 158 items; dd7fcb74 = 2 incl. the real loss). The MAJOR is on the verdict: the named-but-unmeasured bulk-delete scope measures at 6 in-scope commits ever, 3 warns, all justified, incident caught — and the entry's first-ranked strict delete-only shape would have missed the incident. Wire-scoped vs shelved re-put to Mike. Cycle open pending rulings.
- [2026-07-29 · 1340 UTC · Verdict walk-through: EI1–EI6, FS1–FS5, HV1–HV5 all ruled — harvestscan shelf OVERTURNED, wire scoped advisory (Fable, inline on main)](sessions/2026-07-29-1340-sixteen-rulings-walkthrough.md) — the taker session's own three verdicts walked through one-by-one in plain language with per-option impacts, choices in popups. E6: EI1 build-precondition (advisory consumer), EI3 corrected from pre-ruling to proposals-at-pickup, rest accepted. B2+B3: all five accepted, FS1 both legs. B4: HV1 wires harvestscan scoped to net-bulk-delete (≥50 lines) advisory-only on the pass's own measurement — the builder's do-not-wire verdict overturned by evidence, not opinion; HV5 lapsed. Three applications owed, each queueing its own rule-4 pointer; the E6 application needs a neutral hand (this session authored the verdicts).
- [2026-07-29 · 1418 UTC · The deferral was unfollowable; the parent still had the bug the child fixed (Opus, wt: publish-surface-and-deferral)](sessions/2026-07-29-1418-publish-surface-and-deferral.md) — Mike's three asks in one sitting: *"cold reviewers are seeing what is below the divider… do we need a better mechanism?"*, *"study, learn and adapt from taking rpi public"* → *"this should be policy as code"*, and *"sessions are still running into the issue when it is written down 3 times"*. **All three were right, and each had an answer already written down that nothing enforced.** 🔎 **The deferral**: REVIEW.md rule 1 called the below-the-divider deferral *"structural, not willpower"* and it was neither — **reading a file is atomic**, so a deferred section is consumed by the act of reading the brief it sits in, and its *open only after…* label sits **inside the thing it warns about**; the rule was **unfollowable, not merely unfollowed**, since nobody can know where the divider falls without reading past it. It had already leaked once through a side channel — the 2026-07-21 pass, where a pending-changes scanner swept the dirty brief and fed its deferred section to the reviewer pre-draft — and that incident was on the record as a caution about the *scanner*, never about the *box*. Fixed by moving the bytes, not the wording: deferred material to a sibling `.deferred.md`, folded back below the verdict at close (split for the duration, one file at rest), with `reviewscan` gaining the mechanical half — it fires on the **author** at write time, before any reviewer is exposed. Verified against all 76 briefs and driven live red-then-green. **The doctrine now states what the split does NOT buy**: it makes early exposure a deliberate act that leaves a trace, not an impossibility — only a context partition (an orchestrator holding the bytes) is genuinely structural, and overclaiming twice would have been the same defect the rewrite was fixing. `reviewscan` deliberately does **not** lint the fold-back: a reviewer commits findings *before* opening the deferred file, so such a lint would fire on correct behaviour at exactly the prescribed moment. 🔎 **The rpi harvest**: its post-flip cold pass (F1) found the committed `.claude/settings.json` publishing the exact list of commands a session runs **unprompted**, as going public opened untrusted inbound into those sessions — and rpi's local fix **diverged from atelier doctrine**, which mandated committing that file in four places. The child was right, the parent was wrong, nothing carried it upward; Mike asking is what surfaced it. **Twelve repos tracked it** (swept, not estimated — the roadmap's blast-radius figures have been wrong five times). Mike ruled **ⓑ untrack everywhere, uniform** over public-only: a visibility-conditional rule becomes silently wrong the day a repo flips. 🎯 **The class was wider than the file, and is why `publishscan` exists**: every content scanner passed that allowlist *correctly* — it holds no credential and no personal fact — because **the exposure was the file's presence in the tree, not its contents**, and no check in the floor asked that question. New scanner, registry-wired blocking with an advisory form offered for adoption (eleven children would otherwise red on a file nobody told them about); patterns carry provenance per pattern rather than blurring one finding into a survey; it deliberately **allows** the self-describing guard files (`.atelier-floor.json`, the `.<scanner>ignore` set) — they map where the defences are weak *and* must travel for the floor to run, so that exposure is accepted and mitigated by requiring a stated reason for every exemption, never by hiding them. 🚩 **A design error in it was caught by `floor.py`'s own test suite, not by me** — the first cut hard-failed on a tree with no git, which would have made it unrunnable in every child's fixtures; fixed at the behaviour, not the test. 795 → 820 tests, green. 🔎 **The recurrence question**: PROPAGATION gains *When a rule keeps breaking — climb, never restate* (framing → mechanise at the moment of failure → **remove the situation**) and *One statement, stamped copies — never three originals* — three independent statements is the defect; one canonical plus **stamped** copies at the point of use is the mechanism working, and `stampscan`, built to watch exactly that, is shelved on a parser defect, so the stamp discipline is convention watched by nothing. The third rung is what this session actually did to the deferral. **Owed, and said rather than fudged**: the DRY *survey* was not run (R2) — the rule for handling triplication landed, the corpus was not swept, and guessing which passages are redundant is how a consolidation silently drops two of three real facets; the recurrence count is still unmechanical (R1); F5's public-records ruling (P6), the visibility-aware floor (P3), the GitHub-settings publish gate (P5) and the leakscan `--require-terms` ci-plane gap (P4) are queued, not done; P7's transcript harvest is a session of its own. Four rule-4 `⏳` pointers queued in the landing commits, **none spawned by this session**.
- [2026-07-30 · 0301 UTC · The Context Atlas, and what the instruments could not see (Opus 5, inline on main — analysis only)](sessions/2026-07-30-0301-context-atlas-and-trust-window-analysis.md) — Mike asked for `ccrepo` rendered well with context size cross-referenced against incidents, then for the root causes behind three days that damaged his trust in the work. **The headline is about the instruments, not the days.** Across all 470 priced sessions, context size does **not** degrade the work — tool-failure *rate* is flat from 28k to 934k (ρ = −0.05) and the apparent link is a length effect (ρ = 0.85 against message count); what context genuinely causes, overflow and forced compaction, sits almost entirely in the deepest handful of sessions. Pointed at the named days, that same taxonomy reported them **cleaner than baseline** (tool failures 0.71×, zero limits, zero overflow, zero compaction). 🔎 **A mechanical incident layer cannot see a trust failure**: harness error flags measure whether the machinery ran, and trust is damaged by work that ran perfectly and was wrong, unverified, or not the agent's decision to make — this session's own Atlas would have reported a clean week for the worst week. What carried the signal was the principal's own language, counted in human-typed turns only: overall correction rate flat at 0.94×, but *trust* markers at **5.8×** and frustration at **3.9×** — not "that's wrong" more often, "I don't trust this" more often. Five incidents, one class: **the agent acting unilaterally on ground the doctrine reserves to the principal**. Widening from one repo to the estate is what made it honest — only five repos ran in the window, the damage sat in the two doing governed change-work, and a like-for-like control (build-heavy, established, same new model, same new harness, 342k peak) came through clean, **falsifying model tier, harness version and context size in one stroke**. 🚩 **A fourth falsification was withdrawn**: a research repo's 2,403-message subagent run had been used to kill the orchestration hypothesis until Mike identified it as a week-old, research-dominated repo — re-profiling by tool use confirmed him (50.6% research against a next-highest 24.6%), so it was never a valid control and orchestration went back on the table. What survives is narrower than any single cause and is **explicitly not proof**: *distance without a review step, on work that changes things* — the worst single session had no subagents at all and ran 12 assistant messages per turn. **Three corrections made in flight, all mine and all recorded**: an overflow signal that text-matched a refusal string and counted its own scanning session (4 real events inflated to 12 — an agent analysing its own logs is inside its own corpus); a premise stated in one unit and measured in another (archive "not a superset" on session count, when it holds 1,404 files / 892 transcripts against 902 live); and a tool-use profile whose attribution filed every repo's subagent work into one bucket, reporting 2% research where the true figure was 50.6%. Incidentally established: **message count bounds context, wall-clock does not** (ρ +0.877 vs +0.525; a session cut 84% in time but 58% in messages saw peak context *rise*). Doctrine-load cost measured at `ecc9f49`: `method/` ≈ 81k tokens, the full binding set ≈ 163k, all of `docs/` ≈ 854k, against a median session peak of ~168k. **Owed, and said rather than fudged**: no doctrine was written — the two candidates (instruments-can't-see-trust; evidence hygiene) are queued in ROADMAP rather than half-landed at wrap with a fifth `⏳` behind them; the Atlas stays a first mockup, unrefined; and the archive run Mike asked for did not happen.
- [2026-08-02 · 2200 UTC · The four 2026-07-29 cold passes taken — all PASS-WITH-FINDINGS, 14 findings to Mike (Fable, wt: cold-passes-0729)](sessions/2026-08-02-2200-cold-passes-0729-batch.md) — a qualifying rule-4 taker (Mike-started, author-uninvolved) claimed all four pointers on `main`, ran brief → cold review → verdict per delta, and opened the shared intent record only after the fourth verdict was committed; reconciliation changed nothing. Publish-surface 1M/2m — the MAJOR is canon still instructing the committed allowlist in REPO-STANDARD's standardise step, one sentence from re-creating the exposure estate-wide while the stamped copy was fixed (inverted drift, again); plus the identical-bytes seed template residual unnamed where the cost is named, and the seeded `settings.local.json` template publishing a maximal unattended grant. `publishscan` 1M/1m/2n — the MAJOR is depth-blindness: `fnmatch` `*` is not path-aware, so the listed never-publish files pass green one directory down (nested `.npmrc`, `.env` variants, `.mcp.json`), probed live; the reason-required mitigation on the ignore file is claimed but unenforced. Deferral 0M/1m/3n — diagnosis and honesty discipline verified sound, the guard's prefix-anchored heading regex escapes synonym headings while three surfaces claim unqualified cover. Recurrence ladder 0M/3m/1n — all self-application seams: the stop rule lacks its own fitness test, rung 1 has an unmarked second original in REVIEW.md, two recurrence thresholds (second-occurrence vs >2 promotion) sit unreconciled, and the landing commit wrote "Three" over four pointers (fixed at close, records-only). **Nothing applied — rule 3: all 14 findings await Mike's ruling.** Process incident disclosed in every verdict: a records-sweeping grep fed the author's index entry to the reviewer pre-findings (SL2 class, second instance) — the deferral doctrine's own only-a-partition-is-structural residual, demonstrated live by the session reviewing it.
- [2026-08-02 · addendum · All 13 rulings taken and applied same-session (Fable, wt: cold-passes-0729-apply)](sessions/2026-08-02-2200-cold-passes-0729-batch.md) — Mike ruled every finding as counselled through the per-finding walk-through. Applied one commit per delta: `publishscan` gains any-depth matching (the PB1 probes are now the suite's red leg), an enforced `# reason` on every ignore glob, and top-level `--root` rebasing; the deferral guard matches `deferred`/`seeded` anywhere in a heading with the vocabulary limit named on all three surfaces, and its exemption is scoped (`reviewscan:allow:deferral:`); REPO-STANDARD's standardise step no longer instructs the committed allowlist; the seed-template residual is named in TOOLBOX; the published `settings.local.json` template drops its blanket `Bash` + keeps `acceptEdits`; PROPAGATION's ladder gains its fitness test and the two-thresholds reconciliation; REVIEW.md's rung-1 paragraph is stamped pointing at PROPAGATION as canonical. Decisions stamped in all four verdicts. **Deferral + recurrence-ladder cycles CLOSED** (terminal applications); the two MAJOR-cycle applications each queued a rule-4 `⏳` pointer in their own commit — the applier authored the verdicts (neutral-hand preference not met, said plainly), so those reviews are a non-author's. Grounding at head: python suite 828 OK, node 207 pass, selftests + 80-brief corpus green, floor green throughout.
- [2026-08-02 · 2335 UTC · Trust-failure handling as a skill — idea captured (Fable, roadmap-only)](ROADMAP.md) — Mike's idea, queued verbatim for a later design pass: when a trust failure occurs in-session, handle it **deterministically via a skill (enforced actions, code), not doctrine (policy)**. Roadmapped with grounding rather than bare: the class is defined from three real incidents already on record (the all-clear past its evidence 2026-07-26 · the unlooked "no tool does this" claim 2026-07-26 · the undiffed "duplicates" deletion 2026-07-25), and the gap is confirmed real — `00-APEX.md` names the stakes but nothing prescribes the *recovery*, which today is doctrine the failing agent applies to itself: the same fail-open shape ADR 0008 exists to end, one layer up. Four design questions queued (trigger · estate scope · enforced-vs-recorded · relationship to EVIDENCE/RECORD). **Nothing designed or decided; capture only.** Tree was clean; single commit on main.
- [2026-08-02 · 2340 UTC · Track F — the guard governance frame; E6d put back on the table, cold pass queued (Opus, wt: none — inline on main)](sessions/2026-08-02-2340-guard-governance-frame.md) — Mike returned to E6d's escalate-only ruling unsure it was right, and set the handling himself: keep the ruling, record an open action to rebuild the model from base, run a cold Fable pass on the origin problem *before* any design. Two things are new. His decomposition is finer than E6d's — identification-confidence, then the *probability* of harm and the *impact* of harm as separate terms, where E6d collapses the last two; a correctly-identified credential can carry low probability of harm and that is not the same question as how bad it would be. And the concepts he listed (DRY policy-as-code, cannot-reduce-but-may-except, acceptance vs deferment, false-positive reporting, resolve/scope/soften, side-stepping) are a governance model that **six already-open items turn out to be instances of** — D4's zero adopters, C2's seventeen advisories, E1–E4 found ad hoc, Track A's scope fail-opens, C4's unobserved bypass, the never-expiring advisory. Recorded as Track F, F1. No candidate solution written in, deliberately: he invited a sketch and sketching would steer the queued pass. Honest notes — the frame is his and the six-item mapping is the agent's; his doubt and the E6 pass's own EI3 landed on the same soft spot independently; the leakscan/PII sweep is unswept for the third time.
- [2026-08-02 · 2346 UTC · Per-transcript topic capture — idea captured (Fable, roadmap-only)](ROADMAP.md) — Mike's idea, queued under ccarchive for a later design pass: capture the themes/topics discussed per transcript so a specific one can be found later — the companion to the designed-but-unbuilt search strand (search answers *"which transcripts contain this string"*, topics would answer *"which session was the one about X"*). One grounding check taken before capture rather than assumed: the live store carries **zero** `"type":"summary"` records across every project's logs, so a topic layer must be derived, not lifted from an existing field. Three design questions queued (source: model pass vs free log heuristics, measured before paid · owner/shape: ccarchive sidecar at archive time vs cctranscript on demand · consumption: `--topic` filter / header line). **Nothing designed or decided; capture only.** Tree was clean; single commit on main.
- [2026-08-03 · 0155 UTC · Stale-pointer detector folded into the funded pointer-grammar work (Opus, wt: none — inline on main)](ROADMAP.md) — a `ros` session raised a guard idea and routed it to atelier as shared-floor, correctly: an item claiming a review is owed while a matching `docs/reviews/` verdict exists is a mechanically detectable contradiction. Accepted, with two corrections that are the substance of the entry. **It is not a new item** — it rides the FUNDED `⏳`-pointer-grammar work, because both detectors turn on the same unsettled prior question (*what makes an item a queued-review pointer*) and reuse the same two building blocks; a separate item would have been a third original of that question. **The recurrence claim does not survive a sweep**: four instances on first telling, five on measurement, but all five from one commit (`98cef9e`, the nine-pass queue take) which prepended verdicts to nine items and stripped the stale wording from only four — one occurrence with five residues, so promote-at-three is **not** met and the case for wiring rests on the detection being near-free, not on the ladder. Recorded that way so a later session cannot inherit "past the threshold" on faith. **The residue named a sharper defect than the one detected**: all five said "review queued" when the review had run and what was owed was Mike's *ruling* — existence-of-verdict catches the contradiction, reading the cycle states (owed → reviewed → ruled → applied → closed) says which state the item is in; the build settles which the guard carries. The five stale bodies were cleaned in the same commit, so the acceptance corpus is a before/after git pair rather than a live specimen.
- [2026-08-03 · 0557 UTC · Fourth rung queued for the credential ladder — no credential at rest (Opus, wt: none — inline on main)](ROADMAP.md) — a sibling session's ADR argued that where a platform grants delegation standing-only, the credential can be deleted after each run and rebuilt from a tested automated path; Mike routed the doctrine half here. Captured as an idea item, **not designed or decided**. The gap it names is real and narrow: `SECRETS.md` § *The credential triad* and `PRINCIPLES.md` §5 both end at *standing is allowed as a tracked debt*, so "tracked forever" reads as the terminal state — while SECRETS' own store-the-rule-not-the-value property already implies the value need not persist between uses. Recorded with the open questions that would decide it (rung vs branch, what makes a rebuild admissible, the bootstrap regress, the frequency boundary where it collapses into plain JIT, and whether rotation-on-cadence lapses when nothing persists) and with the grounding stated honestly as **one worked case in a child repo**, owing a second instance or an explicit one-case claim before it can become doctrine. Platform and repo left unnamed here on the public-record boundary. Tree was clean; single commit on main.
- [2026-08-03 · 0649 UTC · Three rule-4 Fable cold passes — both 2026-08-02 applications closed, F1 reviewed (Fable, wt: fable-cold-passes-0803)](sessions/2026-08-03-0649-fable-cold-passes.md) — the whole `⏳` queue taken by a Mike-spawned taker ("do any work that requires Fable, including reviews"; author sessions uninvolved). **publishscan application** 0M/1m/3n, PB1–PB4 faithful, every live claim reproduced plus two fresh probes (PA1: the PB3 notice corrupts `--json` under a subdir `--root`, latent on the live floor; PA2: depth matching reds `config/.env.example` estate-wide) — **cycle CLOSED**, PA1–PA4 to Mike. **publish-surface application** PASS 0M/0m/2n, PS1's mandate-site class swept shut at HEAD — **cycle CLOSED**, PSA1–PSA2 to Mike. **F1 design/intent** 0M/2MOD/1m/3n: the frame matches the canonical confidence·likelihood·impact structure (repo-declared impact = CVSS-environmental); FG1 — the mapping under-counts, C3 is the missing adoption case; FG2 — the split does change the response model, the lawful invariant is provenance-of-lowering (declared, reasoned, expiring), not direction; FG1–FG6 to Mike as rebuild input. Honest notes in the record: intermediate commits bypassed the hook (`--no-verify`, C4's own class — floor re-run clean before merge), and a reconcile-before-reading draft caught pre-commit. PII-half sweep now on its fourth carry.
- [2026-08-03 · addendum · All twelve residue findings ruled + applied same-session (Fable, wt: rulings-apply-0803)](sessions/2026-08-03-0649-fable-cold-passes.md) — Mike ruled PA1–PA4, PSA1–PSA2, FG1–FG6 through the walk-through, all as counselled. publishscan: notice to stderr + always-present `rebased_to`, glued-`#` globs loud (suite 20/20); PA2 a recorded no-change; PA4 into C1F3's scope; TOOLBOX names both templates + the mode-bit disclosure; FG1–FG6 written into the F1 entry as binding rebuild input (C3 mapped in · P3 boundary must be declared · provenance-not-direction as the hypothesis to test, E6d unchanged · granularity axis + acceptance/deferment definitions · prior-art check at pickup · FP route specialises resolved-upward · FG6 specimen to the grammar corpus). Both application-pass items `[x]` + harvested in the same commit; decisions stamped in all three verdicts; no new pointers (terminal applications).
- [2026-08-03 · 2016 UTC · Orchestrated queue run — seven Opus workers, eight claims, twelve-scanner floor (Fable, wt: agent worktrees)](sessions/2026-08-03-2016-orchestrated-queue-run.md) — the ruled backlog cleared in one run: TAA+C1F, ER1–ER4, SF+E6c, FS1–FS5 all applied (terminal where their cycles were closed); E6a's posture into `SECRETS.md`; the mid tier promoted to standing executor off the concluded third-seat trial; the landing-equals-bookkeeping preamble doctrine enacted by its own landing commit; the leakscan PII-half sweep discharged on its fourth carry (partial — G1–G7/D1–D6 to Mike); and the FUNDED `⏳`-grammar build + B4 wiring landed as `tools/pointerscan.py` + a registry-wired `harvestscan` — instance 2 located, the recorded counts corrected in both directions, and the three live specimens the new guard warned on fixed in its own landing merge. Suite 830→933; floor ten→twelve scanners, hook+CI green; four rule-4 `⏳` pointers queued (all refs-only, tier Fable); no bypasses. E6b/E6d deliberately unclaimed (bring-proposals-to-Mike pickup); EP/ZL/LS rulings, E7 rulings and a warn-renders-enforced fork queued for the decision round.
- [2026-08-04 · addendum · The decision sitting — the whole ruling backlog walked through and cleared (Fable, main)](sessions/2026-08-03-2016-orchestrated-queue-run.md) — every queued decision ruled one-by-one in plain-language walk-throughs with per-option impacts. **Ruled as counselled:** E7 (thirteen findings; G3 blocking to keep E6a's no-advisory decision whole), the warn-render third state, E6b's consumer (CI + board count) + E6d's scale (estate/repo/local, undeclared=repo) + paired child adoption, EP4–EP10 + EP1(b), ZL1 fix+widened sweep, the principal-named tier bar with the stop clause, P6 to an ADR sitting, withdrawn-pass promoted, E5+E3, show-first adopted, C5 both-composed. **Ruled against counsel, Mike's call recorded:** C1b horizon one month (2026-09-01), pathscan rescope FUNDED, stampscan fixes FUNDED. **Discovered during the sitting:** EP1(a)/(c)+EP2+EP3 and LS1–LS5 were already ruled and applied by Track A on 2026-07-27 — two "await ruling" states were stale (the cycle-state residue class, in wording too coarse for pointerscan to parse); corrected at source, and EP1(b) confirmed never-ridden and now ruled in. **Recorded intent:** Mike plans to remove the Three Laws + Zeroth from the apex at a later sitting — ZL2–ZL5 lapse into it. What remains Mike's: the glossary ratify sitting, the D3 signscan flip (held, pairs with his rotations), the P6 ADR ruling once drafted, and the Laws removal itself.
- [2026-08-05 · 1155 UTC · Five rule-4 Fable cold passes — the whole queue taken, every cycle closed (Fable, wt: fable-cold-passes-0805)](sessions/2026-08-05-1155-fable-cold-passes.md) — a Mike-spawned taker claimed all five `⏳` items on `main` and ran them in one worktree, findings-before-reconcile on each, no bypasses. **All five returned 0 MAJOR and every open cycle CLOSED terminal**: pointer-grammar + B4 wiring (both cycles; PG1 — the docstring claims a staged read the hook invocation does not perform, HV4's wording class re-imported, proven with a staged-but-reverted steering pointer committing clean), the FS application (B2+B3; FF1 — a missing `gh` crashes the remote plane against its documented failure contract; FF4, found at reconcile — the prior FS verdict carries the private-repo × posture join on public `main`, the invariant item's fourth instance), the mid-tier standing-executor doctrine (provenance verified three layers deep), the E6 application (six-shape probe 6/6, precedence exact, blocking set structurally only-widened), and the landing-equals-bookkeeping clauses (clause 1 verified mechanically self-enforcing). Live legs: HV1 replay reproduces as a strict prefix (8 in scope / 4 fired / 16 items at HEAD), hook-plane bulk-delete firing and the index/worktree split proven in scratch repos, floorfleet fail-safe `--check --status` exits 1 on a 16-row board. Bookkeeping per the clauses under review: pointers → one 🎯 rulings-owed item + harvest, same commit. Two MODERATEs (FF1, FF4) and eleven notes await Mike's ruling; nothing holds any cycle open.
- [2026-08-05 · 1233 UTC · P6 delivered — the publication-posture ADR drafted against a measurement (Opus, wt: p6-publication-posture)](ROADMAP.md) — Mike asked for P6's funded work: one ADR draft arguing and costing both postures on whether estate-internal context in a public record is accepted transparency or a records-convention defect. Opened onto a reported interruption, so the read-first recovery sweep ran before anything else — no P6 residue existed, and **two parallel sessions were found live and locked** (the five queued Fable cold passes; F1/`GUARDS.md` + leakscan allowances), neither touching this item. Claim landed on `main` from the primary checkout before the worktree, per CONCURRENCY § Claiming work. **F5 was re-read at source rather than from the harvest here — and the harvest had dropped its second half**: the finding names published internal *workflow* alongside records residue. The two are not symmetric, because publishing method is this repo's stated purpose (ADR 0005), so a rule treating it as leakage would be a rule against atelier existing; the draft draws that boundary as method-versus-operations and asks for it to be ruled explicitly. **Costing done by measurement, not intuition** (the roadmap-figures-wrong-both-ways trap): ~1,030 sibling mentions across ~140 file-instances, ~270 naming a currently-private sibling, and the split is the result that moves the decision — **~96% records, ~4% doctrine**, so any writing friction lands on session closes, the lowest-deliberation surface. A tightened join probe returns 15 lines, mostly index entries, and on inspection they are name × *operational state*, not name × *sensitive* posture: the narrow rule is broadly holding and the adjacent band is what accumulates, consistent with F5 being graded minor. Retrofit is unavailable — pushed history is permanent, `RECORD.md`'s own words — so every option binds forward only and **no option implies a clean-up programme**; that killed a whole branch of the cost comparison. **Decision and Rejected left deliberately unfilled** — a drafter cannot write them without pre-empting the ruling, and `draft` is the only mutable state; the deviation from the template is stated in the file. A third shape the measurement surfaced (bind by *surface*, not by repo — doctrine names freely, records go class-level for operational state) is recorded as a **marked variant**, not smuggled into a posture. The draft obeys the stricter posture throughout and describes rather than quotes, since it is its own specimen. Floor green on the staged set; the 19 tree-wide wrapscan/spellscan findings are the pre-existing frozen-record baseline in files untouched here. **Also found: the `docs/decisions/` index has drifted — five ADRs unlisted since 2026-07-18** — logged as its own roadmap item rather than fixed inline, because five one-line distillations is a read, not a typo sweep. P6 stays `[ ]`: the drafting is done, **Mike's ruling is what remains owed**.
- [2026-08-05 · 1150 UTC · Guard governance rebuilt (F1), and Mike's three allowance rules enforced across all twelve scanners (Opus, wt: guard-governance-allowances)](sessions/2026-08-05-1150-guard-governance-allowances.md) — Mike gave three rules for granting an exception (**narrow · noisy · reasoned**) and asked whether they belonged in atelier's scanners. Survey first: **(a)** largely met, **(c)** met in 3 of 12 markers and 1 of 8 ignore loaders, **(b)** met in **1 of 12** — the convention lived as twelve copy-pasted headers with no doctrine page, so the weak variant was the one propagating. `floor.py` was already the complete exemplar. I proposed holding rule (a) back behind F1; Mike asked why not all of it now, and he was right — his FG1–FG6 rulings *were* the constraints and this session was the pickup. **`method/GUARDS.md`** discharges all six: P3 declared in scope as rule / out as mechanism, granularity as an explicit axis, acceptance vs deferment split, FP route specialising resolved-upward, and **provenance not direction** — tested rather than inherited, and the test found a lawful downward lane already existing in three places, so escalate-only never described this estate. FG4's prior-art check **moved two claims** (Semgrep carries the full three-way split as distinct fields; CVSS v4.0 quarantines time-varying metrics, independently grounding the expiry rule). Then twelve scanners in five commits — and the tightening caught three real defects: a **prose mention** holding a live exemption (CI-visible), a **dual-marker form** where only the second marker had a reason, and **my own regex** using `\S` so `<!-- datescan:allow: -->` parsed `-->` as its reason — rule (c) failing inside the change enforcing it, caught by datescan's existing DSR8 test. D1 applied and pinned. Ignore globs must now state a reason (stanza comment or trailing), exit 2 if not; all 31 live entries pass unchanged. `stampscan` needed no change — it was built to rule (b) before the rule existed. 993 tests green, hook + CI planes green, rule-4 `⏳` queued refs-only. **Owed to Mike:** E6d(i)'s supersession, and D1's consequence — three lines of the author's own published git identity now report against the term list for the *same* reason their marker states, which is not the case D1 was ruled against.
- [2026-08-05 · addendum · A sixth pass — the sitting's five doctrine edits reviewed, cycle closed (Fable, wt: fable-cold-passes-0805)](sessions/2026-08-05-1155-fable-cold-passes.md) — the parallel `queue-batch-0806` landing queued a new rule-4 pointer mid-take; the same taker claimed it on `main` and ran the pass: PASS 0 MAJOR / 2 notes, every edit verified against its 2026-08-04 ruling at HEAD (tier bar + stop clause on both surfaces; withdrawn-pass general case matching the directory README; show-first with the AUTONOMY cross-link; describe-don't-quote with the fixture carve-out; the ZL1 skill fix). The ZL1-widened sweep ran independently before the account was opened and found zero stale Laws restatements on any teaching surface — SE1 records that verified-clean baseline for the planned Laws removal; SE2 notes the tier bar's three surfaces move together. Six passes this session, six cycles closed, nothing held open.
- [2026-08-05 · addendum · A seventh pass — F1/GUARDS.md + the twelve-scanner allowance rules, cycle closed (Fable, wt: fable-cold-passes-0805)](sessions/2026-08-05-1155-fable-cold-passes.md) — the guard-governance landing queued its rule-4 pointer mid-session; the same taker claimed and ran it: PASS-WITH-FINDINGS 0 MAJOR / 1 minor / 2 notes, the F1 review cycle CLOSED. GUARDS.md discharges FG1–FG6 (provenance-not-direction tested against the estate's three existing lawful downward lanes; the E6d(i) supersession asked of Mike, not assumed); the three allowance rules probed live — a reasonless marker exempts nothing, a reasonless ignore glob is exit 2 citing GUARDS.md, every scanner prints its suppressed counts with known zeros. GA1: the reason-required loader ships as ten per-scanner copies — the exact propagation mechanism the build's own survey diagnosed — decision counselled, not assumed. GA2: with C5's composed term list live, the author's recorded 3 identity findings are 89 at HEAD tree-wide on a terms-bearing machine; forward-only holds, first-blocked-commit note recorded. Seven passes this session, seven cycles closed.
- [2026-08-05 · 1238 UTC · Queue batch — eleven claims, three Opus worker builds, one honest revert (Fable, wt: queue-batch-0806)](sessions/2026-08-05-1238-queue-batch.md) — the 2026-08-04 sitting's five small doctrine edits landed as one reviewed batch (tier bar + stop clause, withdrawn-pass promotion, show-first deletion, describe-don't-quote, ZL1 + the sweep surface list); licenscan E1+E2 (37→52 tests — a custom licence keeps the header checks, trove classifiers resolve); stampscan D2 built to the reviewer's bar and wired advisory in atelier only (46→65, live tree exit 2 → 0); pathscan D1 rescoped to the corpus that motivated it (53→73, gate scope burnt to zero; registry promotion deferred while the floor.py pass is in flight); the estate-root token-spec surface closed in that repo. **C5: executed, falsified on contact (86-finding local full-tree red mid-flight of three Fable passes; an ordinary-English hit; ~364 estate-root self-references), REVERTED same session — re-ruling queued in Track C with four costed options.** Four rule-4 pointers queued; three parallel sessions live throughout, zero surface collisions.
- [2026-08-06 · 0859 UTC · The queue drained again — three code cold passes, three cycles closed (Fable, wt: fable-cold-passes-0806)](sessions/2026-08-06-0859-fable-cold-passes.md) — Mike asked for any Fable-dependent review work; the queue held the 2026-08-05 batch's three rule-4 pointers and this non-author session took all three: licenscan E1+E2 (0M/1MOD/2m/2n — LC1: the unknown-license suppression accepts a reasonless or prose-mention marker, against the same file's same-day rule (c) tightening), the stampscan D2 application (0M/2MOD/2m/2n — SD1: canonical-region extraction is code-context blind, the ST1 fix half-applied; SD2: PROPAGATION.md still calls the discipline "watched by nothing", falsified by the delta itself; ST/S4 cycle CLOSED), and the pathscan D1 application (0M/0MOD/1m/2n — the cleanest; PS/S2 cycle CLOSED). Cross-cut: the 0228793 merge left allowance test classes below `unittest.main()` in two suites, so direct runs silently drop 5 tests and the recorded counts match the broken invocation. Every live-proven claim re-run, including the ignore-file-bypassed "parser fix alone clears the tree" measurement. All findings are counsel awaiting Mike's ruling; pointers harvested; the rule-4 queue is empty.
- [2026-08-06 · addendum · The ruling round — fourteen findings walked through, eleven applied (Fable, wt: ruled-fixes-0806)](sessions/2026-08-06-0859-fable-cold-passes.md) — Mike asked for the questions via AskUserQuestion; two rounds, six decisions, every option carrying its impact. All "fix now": LC1/LC2 (licenscan's last reasonless hatch closed and tallied), SD1 (canonical-side fence blindness — region extraction now reads the code-stripped view), SD2 (PROPAGATION.md's "watched by nothing" paragraph rewritten to the current truth), LC3/PD1 (five merge-stranded tests moved above the `__main__` blocks; true totals 56/75/76), SD3+SD4 folded into the same stampscan touch (the tally's first live run surfaced 98 silently-skipped files; whitespace-only `narrow=` voided) — discharging D2 residue (b), whose loader half was found already landed (the board corrected). Three one-liner notes fixed (LC4 five dead aliases, SD6 UTC date, PD2 denominator = the tally's 10 findings); three accepted as recorded (LC5, SD5, PD3). Decisions stamped in the verdicts; full suites green both invocations; the queue take is fully closed — reviewed, ruled, applied, verified, in one day.
- [2026-08-06 · 1154 UTC · Rotatable vs unrotatable exposure queued — history rewrite as a remedy (Opus, wt: none — inline on main)](ROADMAP.md) — Mike's idea, captured verbatim for a later design pass: the exposure doctrine is calibrated end to end for the **rotatable** class (`SECRETS.md`'s *detect → rotate → the burn cost is minutes*), and needs to differentiate it from **confidential, classified, and private information that cannot be changed once exposed** — where rewriting git history may be the right action. The gap is sharper than the ask implies: SECRETS.md **already names the exception** in a single line ("a leak of personal data is the one exposure no rotation undoes") and uses it only to justify keeping `leakscan` non-advisory — nothing tells a session what to *do* after such a leak. Capture carries the four anchors a design pass must reconcile (SECRETS' cheap-burn model; AUTONOMY's *pushed secret is burned even after a history rewrite* plus its truly-destructive classing of force-push/rewrite; ADR 0005's rejected pre-flip rewrite, which was identity not exposure so it constrains rather than forecloses; ADR 0002 + REVIEW.md's *this repo does not rewrite history*, load-bearing for child pins and verbatim review outputs) and five open questions left unpre-empted — class boundary and whether the scanners can tell the classes apart at block time, the rewrite-vs-accept-and-disclose test, who authorises (Mike-floor, not agent discretion), the pin-contract interaction, and the honest ceiling on containment once a public repo has been cloned. No doctrine edited; `addfbd4`, floor green.
- [2026-08-06 · 1222 UTC · Orchestrated queue run — seven items, four rule-4 pointers (Fable orchestrating, Opus/Sonnet workers, five worktrees)](sessions/2026-08-06-1222-queue-run-scanner-floor-builds.md) — Mike's "carry on with anything unclaimed": claims landed on `main` first (`5d083b3`, with E1/E2's stale open-state corrected from the tree), then three waves — E7's funded leakscan build (key-context layer + canaries, path scanning, financial identifiers, opt-in derived forms; E4 + clock-times closed by the same D2 fix), E6b's secretscan advisory tier with all three ruled consumer legs + E3's counted fingerprint carve-out (first live run: 21 advisory findings, all hashes), the decisions-index catch-up, the EP1(b)+EP4–EP10 application (blast radius measured at zero; the ruling's "verified absent" found half-stale), and the floor-render batch (👁️ warn-only as a third honest board state, PS5's registry promotion reaching every child, C1F3's floorfleet strip proving live escape sequences were reaching the terminal). Suite 1060 → 1200; five merges, each closing its items + harvesting + queuing its pointer in the landing commit; pushed floor green per merge. Self-caught: the orchestrator's harvest text quoted the IPv6 doc prefix the entry described (reworded, describe-don't-quote), and the just-promoted pathscan caught the session's own index distillation naming a nonexistent path. Put-away debt cleared at onramp (merged PR #14 branch residue + a merged remote worktree branch deleted). Four ⏳ pointers queued, none takeable here (QR2); the decision queue for Mike (G3 funding, the E6b board interpretation, the EP1(b) legacy deferral, index generation, D1's identity-lines consequence, the standing 2026-08-05 residue rulings) handed over at close.
- [2026-08-08 · 0744 UTC · PRINCIPLES §9 — data carries its time dimension (Opus, wt: time-dimension-0808)](sessions/2026-08-08-0744-principles-time-dimension.md) — Mike minted a rule and named its *placement* as the open question: all data — application, system, user — must have a time dimension, explicitly about the data an application handles rather than the code, since git already dates code's creation, change and removal. Four homes checked against what each doc actually governs: `CONVENTIONS.md` governs the *frame* a value is read against, not whether it exists; `DATA-PROTECTION.md` owns only the deletion clause; `RECORD.md` already *is* this rule applied to our own record, which is why the record reads cold years later. It landed in `PRINCIPLES.md` as **§9 — Data modelling**, where the hole was genuine: §1–8 never reached the data a system holds about the world, and the gap is structural rather than an oversight, because code gets its time dimension free from git so the habit of asking "when?" never transfers. Five clauses: two clocks kept apart (world vs record time), lifecycle transitions instead of booleans, a hard delete destroying the dimension outright, open intervals as legitimate with "unknown" ≠ "none", and the dimension sized by the questions the domain will ask. No ADR — the house pattern for a new principle is an inline adoption stamp (API-first, mobile-first, both 2026-07-14). Grounded before it was written: a curated venue guide in the fleet carries one nullable `verified` field and no world time at all, so it cannot tell a refit from a closure — written in unnamed (the name is not load-bearing for the lesson) and marked **live and unclosed**. Seams named in both directions so no rule duplicates another. Two consequences handed to Mike, not decided: whether §9 obliges retrofits or binds new designs only, and the child-repo retrofit that follows it. Floor green; self-authored doctrine ⇒ rule-4 `⏳` queued, not spawned.
- [2026-08-09 · 0810 UTC · §9 ruled — it binds retrofits, and a stored result carries its derivation (Opus, addendum to the 2026-08-08 entry)](sessions/2026-08-08-0744-principles-time-dimension.md) — Mike ruled both queued 🎯 in one reply and added a third clause. **Retrofits bind**, no grandfathering, decided on the asymmetry the item argued — deferring is not cost-free waiting but ongoing loss — with the obligation bounded honestly in the doctrine rather than taken maximal: start the clock now, never backfill dates that are gone (fabricating them would breach §0), record the unrecoverable stretch. **Ownership sits with the repo that owns the data**; atelier stops at the principle and the `PROPAGATION.md` pointer, and deliberately keeps *no* per-child ledger, which would be a second source of truth for work this repo does not perform — the residual is recorded instead of pre-solved, since a pin delivers doctrine reliably but a retrofit needs someone to notice it applies to data already shipped, and children missing it is the evidence that would justify a check. **Third clause, wider than time:** generalising from the `verified` field in §9's own case, a field holding a *conclusion* now carries when it was established and by what method — without it an owner-confirmed fact and a scraper's two-year-old guess render identically. Anchored to `EVIDENCE.md` §3, the same rule the record already applies to claims, now stated at the data layer; the payoff is that staleness becomes computable rather than felt. One doctrine cycle, so the existing `⏳` pointer's delta was extended rather than a second pointer raised.
- [2026-08-09 · 0300 UTC · a cancelled floor run is not an all-clear (Opus, second addendum to the 2026-08-08 entry)](sessions/2026-08-08-0744-principles-time-dimension.md) — Mike asked for the session's own floor run to be finished rather than leaning on the superseding commit's green, and the ask surfaced a hole in the rule that governs closes. `RECORD.md` already required the pushed floor run's result over the local scan, but never anticipated a third state: `ci.yml` runs the floor under `cancel-in-progress: true`, so a parallel session's push to the same ref cancels the in-flight run for the earlier commit — it ends `cancelled`, neither pass nor fail, and no run ever reports on the commit that was actually pushed. Observed live here: the ruling commit's run was killed ~90 seconds in by a second session's push, and the first close cited that later commit's green — true, and this tree is its ancestor, but it is evidence for a different commit and the distinction went unmade. Re-run attempt 2 against the session's own SHA returned success. Landed as a sub-bullet at the point of use rather than a new section, since the surrounding rule was right and merely under-specified: read the *conclusion*, never "the run finished"; on `cancelled`, re-run against your own SHA. Deliberately not built — the cancellation is correct behaviour that protects the Actions-minutes pool, so the remedy is reading discipline, and a close-time probe is left for evidence of recurrence: one instance grounds a clause, not a build. Rule-4 `⏳` queued.
- [2026-08-09 · 0352 UTC · C5 re-measured — one premise false, one defect underneath (Opus, wt: c5-remeasure-0809)](sessions/2026-08-09-0352-c5-remeasure.md) — C5 is a ruling owed, so the blocking work was the measurement the reverted 2026-08-04 execution never ran: every plane, every repo on the machine, probed with a scratch term list so the operator's own config was never touched. **(a)** the full-tree red stands but is smaller and differently shaped — 67 findings not 86, and **60 of the 67 are frozen records**, only 7 in live files. **(b)** the ordinary-English premise stands and is now **three shapes**, two of which cannot be reworded at all — a verbatim quotation from an upstream bug report, and a physical building in a property inventory; a guard whose only hatch is "reword it" serves neither. **(c) is FALSE, and it is the premise that killed the ruling** — the estate root does not hard-block, having retired the scanner repo-wide by ADR a month *before* the ruling; `.leakscanignore` filters at the path level *before* the term list runs, on the staged plane as well as the tree plane, so D1 never reaches those trees. Verified empirically, not reasoned: it and two sibling permanently-private repos scan clean, exit 0, with the bare term live. The count on record was wrong too (492, not ~364) — the sixth wrong figure on this programme, again in both directions. **What no premise modelled:** the term also binds **six private children that doctrine positively instructs to name the root**, 58 lines between them — a bare term makes the prescribed act red. **The defect underneath**, surfaced by Mike's restated exception ruling (narrow · why · when): **the term list has no narrow hatch** — D1 removed the Line level and `.leakscanignore` is not rule-scoped ("the widest allowance this scanner grants": a whole path, every rule, indefinitely), so every term exception is forced wider than GUARDS rule (a) permits. The estate already paid for that once, deleting a term outright because the only hatch was too broad. The three repo-wide `*` opt-outs were checked against the bar rather than assumed, and pass as genuine acceptances with inverted premises. **A near-miss recorded as evidence:** drafting the item, this session wrote that deleted place name into this public file joined to why it had been on a personal denylist — caught at self-review, uncatchable by any scanner since the term is de-listed, and now recorded in the item as evidence against option 4's write-time-discipline answer. **Second finding, its own item:** a scanner's verdict has two states and needs three — `✓ leakscan clean` covers both "found nothing" and "found things, forgave them all", while this repo's live run subtracts 35 findings and 7 files and a `*`-scanned repo prints clean with 99 files never opened. Roadmapped not built (G3's blast-radius argument); exit codes do not move. Nothing built, no term list touched — the ruling is Mike's and stays open.
- [2026-08-09 · 0430 UTC · half the C5 finding was closed while it was being written (Opus, addendum to the 0352 entry)](sessions/2026-08-09-0352-c5-remeasure.md) — a parallel session pushed `c827705` minutes after this one's own push: Mike ruled the *D1's-consequence* item, and a marker scope naming `local-term` explicitly — reason required, counted, comma-composable — is now the one deliberate term-list hatch. Verified at the source before rewriting, not inferred from the commit subject. It **contradicts the 0352 session's central claim** ("the term list has no narrow hatch") four commits after it was written, so the item was corrected rather than left standing — a false premise in a live decision item is the exact failure C5 exists to record, and this item has now been wrong about its own preconditions twice. **Discharged:** the two unrewordable ordinary-English cases (an upstream quotation, a physical building) were the sharpest argument that rewording cannot be the only hatch; they have one now. **Answered:** D1's missing Line level is restored. **Survives, and is now the whole of C5:** volume — a per-line marker fits 3 lines, not the 60 frozen-record lines here plus 58 across six private children, which would be 118 edits against the same noise rule a child repo already applied in rejecting ~70 markers, and in the children doctrine *instructs* the naming so a marker would be owed forever. The recommendation stays at option 1 but now rests on **one** ground instead of three, said plainly in the item; option 4 is named as the honest fallback and is stronger than it was, since the hatch makes a later decision reversible. Also fixed the cross-link the 0352 session left on the *D1's-consequence* item, which `c827705` carried forward verbatim into a closed item where it read as work still owed.
- [2026-08-09 · 0411 UTC · the estate duplication + exception audit (Opus, wt: doctrine-adoption-ruling-0809)](sessions/2026-08-09-0411-estate-duplication-exception-audit.md) — Mike commissioned two sweeps across all 16 children — what duplicates or conflicts house doctrine, and whether every guard exception is well-reasoned and recorded — naming a suspicion to test: that a child's model-economics doc repeated atelier doctrine. **The suspicion was right and already half-fixed** (that repo was trimmed to repo-local facts eleven hours earlier on his own DRY ruling), but the class is live in **three other children**, and worse: `ros` (106 lines, zero references up) and `rpi` (69, zero, and **public**) both restate the subject wholesale, and `nova` names `ros` as *"the canonical version"* — a child pointing sideways to a sibling mints a second root against ADR 0001. Two are **conflicts** rather than repeats: a two-pool billing model the parent superseded with billing-state-of-the-marginal-token, and a fixed model-to-role mapping the house replaced with tier-by-risk. The failure is evidenced, not predicted — the fixed repo recorded that its copy drifted 17 days behind a provider change and misled a session into arguing from a falsified fact; the three above carry that same claim today. **The exception half came back strong**, and is recorded as such because an audit that only reports defects misrepresents the estate: every one of 11 ignore-globs and ~120 line markers carries a stated reason, the three repo-wide `leakscan` opt-outs each cite a named ADR (verified to exist), state the inverted premise, and bound what does *not* relax — no unreasoned hatch was found anywhere, and *fail-noisy-then-subtract* is genuinely built (a live run prints its suppression counts per rule). Three governance gaps, none an exposure: two ignore-globs are **deferments filed as acceptances** (they name debt they intend to clear, with no expiry); **the parent is in breach of its own rule** — atelier's `scope` block narrows three checks with no `why` where `ros`'s equivalent has one; and eight bare advisories that are **already in hand** (C1b/C2 claimed, horizon set) so a later sweep does not re-file them. **The finding neither question asked for:** eight children carry a floor block missing four of the seven irreducible concerns — Concurrency, Session rhythm, Estate resources, and an apex predating the adaptation and informed-confirmation clauses — all pinned 818 commits back, and *undeclared* because `stampscan` only compares stamped blocks and only the four newest children stamp theirs: **12 of 16 copies are invisible to the check built to watch them**. Adoption coverage came back clean — all 12 worked repos are children with the floor wired. Mike ruled three things mid-audit, none previously written down (which is how the duplication survived four repos while every one passed every check): adoption is the default absent a ruled exclusion; a child may add freely, never repeat, never conflict absent a ruled exemption; and work lands in the repo it changes, with the finder free to **queue** but never deliver. Landed in the sections that already own those subjects rather than as new ones. Every child-repo finding was queued, never delivered — no child repo written to. Floor green on the pushed SHA; self-authored doctrine ⇒ rule-4 `⏳` queued, not spawned.
- [2026-08-09 · 0510 UTC · the join C5 guards is still being written — roadmapped (Opus, addendum to the 0352 entry)](sessions/2026-08-09-0352-c5-remeasure.md) — closing sweep over what this session surfaced that was not yet on the board. The two decisions were already roadmapped (the C5 re-ruling, corrected after `c827705`; and the scanner's missing third verdict state). The exception-audit half was found to be **already done independently** by a parallel session on 2026-08-09, reaching the same verdict on the three repo-wide opt-outs — corroboration, not duplication, so nothing was re-derived. What was genuinely new is now one item: **the name↔posture gap `PROPAGATION.md` declares but nothing enforces, with two fresh instances.** (1) Seven estate-root mentions sit in *live* atelier files rather than frozen records — 2 in ROADMAP, 4 in an instrument design doc, 1 in a tool docstring — cheap to rewrite and **not blocked on the C5 ruling**. (2) A **new join landed 2026-08-09** in the exception-audit section itself: three permanently-private repos named beside the statement that each is the deliberate home for personal data with the publish-safety scanner retired repo-wide — name + posture + contents, in the public tree. Flagged for Mike rather than unilaterally rewritten, since it is another session's landed work and may have been ruled. With this session's own near-miss that is **three instances on 2026-08-09**, which is the signature of a rule nothing enforces — the finding this programme has now recorded five times.
- [2026-08-09 · 0330 UTC · The ruling walk-through + the estate advisory-clearing pass (Fable orchestrating, six Opus workers in child repos)](sessions/2026-08-09-0330-ruling-walkthrough-estate-pass.md) — six of seven queued questions put to Mike one by one (C5 dropped mid-walk: a parallel session held it) and every ruling stamped at the moment it was made: G3 funded as its own item; the estate-wide advisory-count view funded and homed at the estate root; EP1(b)'s legacy exemption ruled bite-now and APPLIED (suite → 1202); the index question ruled up to the generic estate-wide mechanism, census-first, counsel recorded; D1's consequence ruled scoped-markers and APPLIED (the deliberate `local-term` hatch — full local-cover scan green for the first time since 2026-08-05, suite → 1207); the residue ruling round scheduled as a dedicated sitting. Ruling six launched the estate pass: live measurement first (the recorded 17-across-10 was stale — nine legacy advisories across seven repos), all six workers stopped correctly on the latent untracked-allowlist CI break (ruled 2026-07-29, never executed in children), gate corrected, and the second round retired EIGHT advisories outright + migrated one (review-by 2026-09-01), untracked the allowlist in eight repos (one further remote found archived, reset clean, recorded), with two live GitHub floor runs proving the latent break fixed. One concurrency incident recorded honestly (a broad-staged parallel commit swept a worker's in-flight edits — content correct, attribution muddled). Two self-migrated repos' own later dates surfaced to Mike rather than rewritten.
- 2026-08-09 · 0501 UTC · roadmap: every rule directive first, enforced second (Opus, single item) — Mike, at close: he wants **all** doctrine enforced as policy-as-code, *and* directive — worded so it shapes the thinking before the act the code would otherwise have to catch. Filed as one four-part item in the policy-as-code programme, next to § *Doctrine forcing functions the programme depends on*, because that section is the hand-maintained version of half of it. (a) The census nobody has run — enumerate `docs/method/` rule by rule, per-rule state **enforced · directive-only · unenforceable-and-accepted** (reasoned + dated to the `GUARDS.md` bar); an unenforceable rule is lawful, an unexamined one is the defect. (b) The half with no owner — doctrine that reaches the moment of decision, which is a property of *wording*, and which this repo currently reaches for only as remediation after recorded breakage (`PROPAGATION.md` § *When a rule keeps breaking*, rung 1). **The item deliberately does not adopt the posture change it implies**: that ladder is stop-at-the-first-rung-that-fits, framing **or** mechanism; "enforced *and* directive" is both-always, so whether the ladder gains a floor is flagged as Mike's doctrine ruling rather than taken. Fourth bullet carries the cost honestly — instructing prose is longer than condition-stating prose, and the decay mode is a directive preamble on every rule until readers skim past all of them, the same failure a child repo named when it rejected ~70 allow-markers as noise. No doctrine text changed, so no rule-4 `⏳` is owed. `sizescan` stays advisory on this file (pre-existing, never gates).
- 2026-08-09 · 0520 UTC · roadmap: the aim recorded verbatim, and the paraphrase corrected (Opus, correction to the 0501 entry above) — Mike, reading the filing back: he wants his prose captured **verbatim** with the item, and named the drift — he said *all doctrine*, the heading said *every rule*. Three fixes. (1) His sentence now sits in the item as a blockquote in his words, with a line saying the restatement beneath it is a reading, not a replacement — **where they differ, the quote governs**. (2) The heading is **All doctrine directive first, enforced second**. (3) The drift was not only cosmetic: the census bullet had scoped itself to `docs/method/`, which is narrower than *all doctrine*, so it is now scoped on `REVIEW.md` rule 3's **function-not-file-type** definition — prose, ADRs, schemas, validators, CI gates, and templates or skills that stamp behaviour into other repos — with `docs/method/` named as where the census starts, not where it stops. Worth carrying: a paraphrase of the principal's aim narrowed the work it was commissioning, in the item whose whole subject is rules reaching the moment they are needed.
- [2026-08-09 · 0708 UTC · The C5 cold pass, and the `add -A` sweep that was owed (Opus orchestrating, one Fable reviewer)](sessions/2026-08-09-0708-c5-cold-pass-and-addall-sweep.md) — Mike asked for the full open-work list (112 items, 7 queued reviews, 25 of them blocked on his ruling rather than on capacity), then said he did not trust the session that re-measured C5. The rule-4 Fable pass came back PASS-WITH-FINDINGS, 2 MAJOR / 4 MODERATE / 3 minor / 3 notes: the distrust was misplaced about the measurement — all thirteen re-run claims reproduce exactly at the states the sweep measured, the best-verified figure set the programme has produced — and well placed about the framing, where the "sharpest cost" is mis-composed (one of the six children is public, so its lines are the guard working, and only ~6 of 58 are the prescribed onramp act) and the 2026-08-06 deletion precedent is misdescribed against its own ADR. C5R4 changes procedure rather than a fact: every atelier-side figure was falsified by drift within hours, so the re-ruling rides on classes, never a frozen number. The brief deliberately withheld the authoring session's own framing — the "question shrank after `c827705`" line — entering it as an assumption to attack instead, so it did not travel through the orchestrator. The `add -A` sweep the last close asked for ran across all 24 repos: 29 never-publish files, 27 safe, 2 tracked, zero exposed, so the guard was deliberately not built (recurrence earns a check, and the one instance was remediated in the commit that caused it) — but it found that one of the two remaining red floors is red for a cause nobody had named, its allowlist never untracked at all. Nothing ruled, no other repo touched, the live lines not scrubbed: all three are someone else's call or another session's lane.
