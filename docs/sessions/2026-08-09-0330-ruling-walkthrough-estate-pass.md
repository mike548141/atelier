# 2026-08-09 · 0330 UTC · The ruling walk-through, and the estate pass it launched

- **Model**: Fable orchestrating (continuation of the 2026-08-06 run session);
  Opus workers ×6 across six child repos
- **Worktrees**: none in atelier (records + two small ruled builds inline on
  main); workers in the children's own checkouts

## The walk-through

Mike asked for the queued questions one by one. Seven were prepared; six were
put (C5 was dropped mid-walk — a parallel session was actively re-measuring
it, and a live claim outranks the agenda). Rulings, all stamped at the moment
they were made:

1. **G3 (binary media)** — funded as its own item, soon; shape unchanged.
2. **Advisory-count visibility** — the per-repo board build stands; an
   estate-wide combined view is funded, homed in the private estate-root repo
   (estate state, not shareable method). Landing this answer took three
   rewrites of the question — the lesson recorded honestly: the first two
   framings assumed vocabulary ("E6b", "floor board") the reader had no
   reason to carry; the one that landed led with the actual screens.
3. **EP1(b) legacy exemption** — bite now. Applied in-session: the legacy
   bare-list spelling may no longer narrow a never-softened scanner (config
   error naming the reasoned form); softenable checks keep it until C1
   phase 2. Six test fixtures moved to the reasoned spelling; suite
   1200 → 1202. Blast radius measured: one declaration estate-wide.
4. **Decisions index** — Mike chose the GENERIC estate-wide index-integrity
   mechanism over the counselled single-index check; counsel and the
   one-instance risk recorded with the ruling, census-first constraint
   binding the pickup.
5. **D1's consequence** — scoped markers. Applied in-session: a marker scope
   naming `local-term` explicitly (reason required, counted, comma-composable)
   is the one deliberate term-list hatch; the unscoped marker still never
   reaches the term list. The three published-identity lines carry the scoped
   form; the full local-cover scan is green for the first time since
   2026-08-05. Suite 1202 → 1207.
6. **The 2026-09-01 advisory horizon** — run the clearing now (below).
7. **The residue ruling round** (~19 findings + the P6 ADR) — a dedicated
   sitting, soon.

## The estate pass (ruling 6 executed)

Live measurement first: the recorded "17 declarations across 10 repos" was
stale — nine legacy declarations across seven repos, plus four
already-reasoned in two repos carrying their own later dates. Six workers
dispatched (one skipped repo: dirty tree, possible live session).

**First round: all six workers stopped, correctly** — every repo's floor was
already red on checks outside their remit. The dominant cause was one latent
break: the still-tracked agent allowlist, ruled untracked-everywhere on
2026-07-29 and queued for "each child's next touch", which had not come — so
every one of these repos would have failed CI on its next push whatever that
push contained. Two workers proved the other red class (machine-local
term-list findings) does not exist on a real runner by re-running with an
empty list. The gate was corrected (CI-as-CI-sees-it), the already-ruled
untrack authorised, and the C5-owned term-list class explicitly fenced off.

**Second round: all six landed.** Eight legacy advisories retired outright
(rewraps and one respelling, several verified content-identical
mechanically; zero new exemption debt), one migrated with an honest reason
and review-by 2026-09-01. The allowlist untracked in all six plus two more
repos swept afterwards (one further repo's remote turned out to be archived —
its local commit was reset away and the fact recorded; the latent break is
moot there). Two live GitHub floor runs confirmed the fix: the first pushes
since the publishscan registry change passed *because* the untrack rode
them.

**Incident, recorded not smoothed:** in one child a concurrent session's
broad-staged commit swept a worker's in-flight edits into its own commit —
content correct, attribution muddled, no loss. Both sessions were in the
primary checkout; the worktree substrate exists for exactly this, and the
worker moved to explicit-path staging immediately. The class is already
named in machine memory (never stage broadly in shared checkouts).

**Deliberately untouched, per lane discipline:** the term-list finding class
(C5's, actively worked in parallel); one repo's deeper secretscan/leakscan
triage debt and its config-vs-docs drift (its own queue, now noted in its own
log); root-file prose width outside wrapscan's docs scope (flagged upstream).

## Close state

All rulings stamped in ROADMAP at the moment of ruling (six small pushed
commits); C1b/C2 claims released with the run's results; per-repo session
entries written in all five repos that lacked them; kainga/numen swept;
memory updated (the leakscan-red note was corrected by the parallel session
in coordination — both sessions converged on the same file cleanly).
