- [x] **The route's three shapes — WRITTEN 2026-08-23** (wt: report-up-duty-0823)
      — the route assumed a reachable parent, and the text named only one of
      its three working shapes (PU-3, ruled to the board 2026-08-22) —
      step 1 reads as the child session writing a board item into atelier's
      tree (sanctioned by `CONCURRENCY.md` § *Stay in your lane*'s
      queue-never-deliver carve-out), yet the route's first live exercise
      (section `320`) used a different shape the text never names: the
      child sent findings over the cross-session channel and an atelier
      session did the filing. And a child session with neither a sibling
      atelier checkout nor a live channel — a fresh clone elsewhere, a CI
      context — has no route at all; the text is silent. The fix is three
      sentences in § *The route*: write the item directly (the carve-out),
      hand it over the channel (the worked example), and hold-and-flag in
      the child's own record when neither exists.
      ✅ **Landed** inside § *The route* step 1, as three sentences ordered by
      directness rather than preference: write the item directly (the
      queue-never-deliver carve-out), hand it over the channel (the shape
      section `320`'s nine findings actually used, with the channel's own
      volatility law attached — a hand-up is not *filed* until an atelier
      session lands the item), and hold-and-flag in the reporting repo's own
      record where neither is reachable. The last is the one that mattered: it
      is what stops a session with no route concluding it has no duty.
      ⚖️ **Taken with the reporting duty rather than after it**, because a duty
      that binds every repo is unmeetable for any repo the route cannot reach —
      the item's own text said such a repo "has no route at all". Argued in the
      claim commit, not decided quietly.
