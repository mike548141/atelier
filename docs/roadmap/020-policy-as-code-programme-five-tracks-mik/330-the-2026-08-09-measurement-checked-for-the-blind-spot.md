- [x] ✅ **The 2026-08-09 measurement checked for the three-channel blind spot —
      the headline does not move; a method note is owed.** Mike's direction to a
      `cbom` session, 2026-08-19: *"pass that question to atelier for it to look
      into"*, after that session flagged that `COMMUNICATION.md`'s *"6,704
      replies in 1,094 transcripts"* might rest on a corpus that selected
      messages by `type == "user"` and so under-read the principal by a third.
      **Answer: no impact on the headline.** The corpus is **assistant replies**
      of 200+ characters, and every published metric is computed *inside* a
      reply — bracketed aside, unexpanded acronym, sentence length, bare
      reference code, and the July→August density trend. The one metric that
      needs a boundary, *first use of a reference code in a session*, needs a
      **session** boundary, not a prompt. No metric is conditioned on the
      prompt, so the mis-bucketing the filer described has nothing to bucket.
      ⚠️ **The limit, stated rather than smoothed over:** the measuring script
      was not preserved in the repo, so this is a reconstruction from the four
      published metrics, **not a read of the code that produced them**. It is
      strong enough to answer the question asked and not strong enough to be
      called a re-run.
      **What is still owed, and it is the filer's real point.** The exposure was
      never this number — it is the **method as a template**. An audit of *what
      the principal has asked for*, built the same way, is fatally wrong rather
      than cosmetically so (`320/070`). So the three-channel fact wants a home
      where the next person measuring from transcripts will meet it —
      `EVIDENCE.md` § acquisition method, or `COMMUNICATION.md` beside the
      measurement — which is a doctrine edit and therefore Mike's, queued at
      `320/070`, not taken here.
      **Unrelated and already open:** CMF3 (2026-08-15 cold pass) separately
      finds the figures *as printed in doctrine* unsupportable — the thresholds
      used were not all the shipped ones, and the density trend is confounded by
      a review-heavy August. That finding stands untouched by this check; the two
      are different objections to the same sentence.
