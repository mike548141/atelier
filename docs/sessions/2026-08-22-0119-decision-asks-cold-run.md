# 2026-08-22 · 0119 UTC · The decision-asks cold pass run

**Tier:** Fable (`claude-fable-5`). **Worktree:** `decision-asks-run-0822`.
**Commission:** Mike's standing prompt — *"Do any reviews and fable
dependent work"* — in a fresh session that authored neither the delta
(2026-08-19) nor the brief (a different 2026-08-22 session), so it passed
rule 4's criterion and the brief-writer split at selection. Of the six
open review pointers, `160/270` was the only takeable one: the other five
sit in Mike's ruling rounds.

**What ran.** The queued rule-4 doctrine cold pass on the ruling-ask
delta (`e6b81e8`), from the unrun brief. Verdict: **PASS-WITH-FINDINGS —
1 MAJOR / 1 MODERATE / 2 minor / 2 note, plus reconcile-stage additions
DA-R1–DA-R5 (2 minor, 3 note)** →
`docs/reviews/2026-08-22-0031-decision-asks-cold.md` (sibling folded in
and deleted). DA1 (MAJOR): "on screen while the principal decides" fails
under focus mode — the harness shows only a turn's final message, so the
device is up with no account beside it, the extracted-approval shape the
same paragraph forbids, reached by *following* the rule; probed against
the live harness contract, marked as not an observation of Mike's screen.
DA2 (MODERATE): the duty grew to five parts and its three other live
spellings still say three — the 2026-07-14 pass's un-harmonised-spellings
class recurring.

**Verified rather than trusted.** The plainscan delta reproduced exactly
at HEAD (23/14/11 = 48, files unchanged since landing); the device
capacities checked against the live `AskUserQuestion` contract (all
real); the sweep ran through `coldsweep` (311 rule-2 files barred, no
`--include-barred`); security lens discharged with grounds (landed-delta
review, no pending diff to aim the scanner at). Sequence honoured:
findings committed (`2cbc72a`) before the sibling, the intent record, or
any prior verdict was opened. Near-miss disclosed in the verdict: the
onramp's `SESSIONS.md` tail read was truncated by the harness to a 2KB
preview showing only a 2026-07-28 entry; the saved full output stayed
unopened, so the author's index entry was not read.

**Nothing applied.** Per rule 3 all findings are Mike's to decide;
DA1–DA6 and DA-R1–DA-R5 join his ruling round. The cycle stays open on
the MAJOR.
