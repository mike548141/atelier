# 2026-07-21 · 1233 UTC · Opus 4.8 (1M) · roadmap capture stream (Mike feeding ideas)

Capture-only: Mike fed a stream of ideas for the atelier roadmap during an
estate note-capture session. Each parked in `docs/ROADMAP.md`, committed +
pushed as it landed (scanners clean each time). Worked on `main` — finished doc
captures, committed immediately.

## Captured

- **Anti-slop invariant registry** (new strand) — from
  <https://thenewstack.io/engineering-ai-slop-registry/> (Aviator). Invariant
  catalog = codified always-checked rules for per-codebase conventions a model
  can't know. NEW for atelier: the registry + promotion rule (a review finding
  left >2× → invariant, mined + human-approved) + two-layer criteria. Positioned
  as EXTENDING what atelier already has (floor scanners = proto-invariants,
  REVIEW.md writer≠verifier, review-before-build). URL kept as the Source line.
- **`MODEL-ECONOMICS.md` → `ECONOMICS.md`** — now spans models AND CI compute/
  runners; recommend RENAME not split (cross-cutting precedence doctrine; 308 ln
  within hygiene); cost = a propagation ref-sweep. Naming call is Mike's.
- **honesty vs truth vs transparency** — stored as Mike's RAW to-do with a
  do-NOT-interpret marker (apex-level, he'll frame it himself). First seeded a
  framing, then reduced to raw at his direction.
- **"grab the colleague AI chat" to-do** — stored REDACTED (atelier is public);
  full verbatim pointer (name + local path) held in the operator's private note,
  not published. Do-not-interpret; file left in place.
- **ccarchive: "is there any metadata it misses?"** — raw open question. (I
  over-ran first with a full code investigation + finding; Mike corrected — it
  was a line to PARK, not answer — reduced to the raw question.)
- **"Should cctranscript and ccarchive be one?"** — raw open question.

## Lesson recorded (operator-side memory)

In a capture rhythm, DEFAULT TO PARKING the line — don't investigate, answer, or
interpret, even when it's phrased as a question; offer to dig in instead. Over-ran
twice this session (seeded framing on an apex item; fully answered the ccarchive
question). Captured to estate memory `feedback_capture_mode_default`.

No code, no doctrine changes — all items are captures awaiting Mike's pickup and
their own reviews. Tree clean, all pushed.
