# The residue ruling round runs in a FRESH SESSION (Mike, 2026-08-09)

**Mike's ruling, 2026-08-09, on how to approach the ruling pile:** *"Do it in a
fresh session."* Asked whether to triage first, sit on the list as-is, split by
severity, or delegate a class to the agent — he took none of those and scoped the
whole thing out of the session that raised it.

**What that session inherits, measured 2026-08-09 rather than estimated:**

- **34 open `[]` items carry `🎯`** — reserved to Mike, not blocked on capacity.
  Measured as `grep -c '^\s*- \[ \].*🎯'`; the same expression read 27 at
  `6887118` that morning, so the pile grew by 7 in one day.
- **The seven-pass batch's residue: 1 MAJOR, 12 MODERATE, 48+ findings**, none
  ruled and none applied. The MAJOR is AP1, now ruled and half-applied — its
  machine-check half is still owed.
- **Standouts the review session named for the round:** AB1 (the E3 carve-out
  reaches credential-keyed assignments — exceeds the ruled scope), LK1 (a
  malformed scoped marker fails open to all-structural), FR3 (the C1F3 strip
  passes C1 and bidi spoofing), CM1 (the membership wording annexes third-party
  checkouts).
- **Expect the count to shrink on contact.** Two items were closed on 2026-08-09
  whose rulings had already been made, applied and verified — pure residue
  inflating the count. That class has now been recorded six times.

⚠️ **One thing on the pile is operationally live rather than merely undecided**
and does not need a ruling to stop hurting: `ccarchive` exits 1 on every
scheduled run, and two whole-document files' archived copies are frozen at
pre-condensation bytes. A `--force` run clears the staleness at any time. Only
the *class* question — whether a size-only guard should govern whole documents —
needs the sitting.
