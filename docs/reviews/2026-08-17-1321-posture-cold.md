# Cold pass — the posture section (PRINCIPLES §10) and the fourth guard requirement

**Pass type:** doctrine cold pass (REVIEW.md rule 4 — a new design principle
in `PRINCIPLES.md`, a new situation test beside the existing ones, and a new
*requirement on every guard in the estate* in `GUARDS.md`; the wording is the
applying session's own, rendered from the principal's dictated thinking and a
same-day ruling that made it binding).
**Tier:** Fable (the principal-named review tier, ruling 2026-08-04). Checked
at selection: a session that cannot honour the bar stops rather than takes.
**Status:** BRIEF WRITTEN, REVIEW NOT RUN. The next cold session that passes
rule 4's criterion and the tier bar takes it — see *Spawn provenance*.

## Spawn provenance

- **Author of the work under review:** the session that wrote the section on
  2026-08-17 (wt: `posture-0817`, claim `e4be7f7`, landing commit `16bf719`,
  merged to `main`; Opus 5 1M per the commit trailer). It received the
  principal's thinking as dictation, put the ruling to him, and rendered both.
- **Who wrote this brief:** an atelier session Mike opened 2026-08-17 at 1321
  UTC on the **Fable** tier under his standing cold-session instruction (*do
  any reviews and any Fable-dependent work; write any briefs required*; a
  brief-writer never runs its own brief). It authored no part of this delta,
  was neither started nor instructed by the authoring session, and has edited
  no file this delta touches. It wrote this brief from the diff of the two
  delta paths at `16bf719` and from the queue pointer; it did **not** open the
  intent record, the board section `300-…` beyond the pointer, or the
  `SESSIONS.md` index entry for this delta.
- ⚠️ **Two disclosures, both about the brief-writer.**
  1. **It read the `docs/SESSIONS.md` tail at onramp** (the last two index
     entries, dated after this delta — not the entry for this delta itself,
     which sits earlier in the file and was not printed). Rule-2 barred
     material in general; stated so the trail is complete.
  2. **It read the landing commit's message in full.** The message is the
     author's account of what the section says and why, and of a
     board-section renumbering it performed alongside. Generate your own
     reading of the diff before you weigh anything this brief says about it.
- **Who takes the review:** the next cold session meeting rule 4's single
  criterion — a session the author neither started nor instructed — on the
  Fable tier, checked at selection. The taker repeats its own provenance in the
  verdict: how it was spawned, and its non-involvement with both the authoring
  session and this brief-writing session.
- **Orchestration shape:** the deferred material sits in the sibling
  `2026-08-17-1321-posture-cold.deferred.md` (rule 1's split). Recommended:
  run under an orchestrator that holds the sibling's bytes and releases them
  only after the reviewer's findings are durably written; an off-tier
  orchestrator is permitted only under REVIEW.md rule 4's two conditions
  (forms no finding, disclosed in claim, pointer and verdict). A taker working
  by hand opens the sibling as a deliberate second act after its findings are
  committed, and says so. Fold in and delete when the verdict lands.

## What the work is

One commit, `16bf719`, two doctrine paths (the commit also renumbered a board
section and queued two board items; those are outside the delta). Reviewed at
HEAD.

**`docs/method/PRINCIPLES.md` § *10. Posture — strength is what you are free
to do* (new, ~72 lines).** A tenth principle placed after §9 and before the
*Trade-offs* section. It opens by distinguishing itself from the other nine
(*they ask how a thing should be built; this asks what being strong is for*),
answers *freedom of action*, and then carries the principal's own statement
**verbatim as a block quote** — three paragraphs on network posture, secret
rotation, and restore/DR — on the stated ground that *the four cases are the
principle*. It distils them to one line (*engineer for the failure being
cheap, not for the failure being impossible*) and four bullets: recoverability
licenses action; a control that makes you reluctant to act is failing even
while nothing has gone wrong (named as the diagnostic half); prefer the
exposure that tells them nothing; prove the recovery by exercising it. A
flagged paragraph names the **precondition** — *"we will know if something
goes wrong"* — as the leg with the least mechanism behind it, and bounds the
freedom to what exercised detection and recovery will support. A closing
paragraph places the section against §1, §5, and four other method docs
(`SECRETS.md`, `DATA-PROTECTION.md`, `ACCESS.md`, `GUARDS.md`) as the homes of
the individual cases.

**`PRINCIPLES.md` § *Trade-offs* gains a situation test, *Prevention, or cheap
failure?*** — placed beside *Gate sizing* as "the prior question": whether the
failure can be made cheap enough that the control stops being the interesting
part; both answers legitimate, differently costed; prefer cheap failure where
the recovery is buildable, keep prevention where it is not, and **say which
you chose**. Its precedent line cites the same-day ruling.

**`docs/method/GUARDS.md` § *The fourth requirement — declared* (new, ~38
lines)** under the existing three (*narrow, noisy, reasoned*): every guard
declares whether it *makes the failure cheap* or *forbids the act*, in a
two-row table with costs; both legitimate, **not declaring is the defect**;
framed as the standard the registry's `why` field was missing; two limits — a
test arriving after the work is grounds to declare, never to unwire a working
gate on the author's judgement, and "forbids the act" is not a failure grade —
and a note that the pass over the open guard board is queued, not done. The
doc's *Related* list gains a line pointing at §10 and the situation test.

## Scope

Widest the work admits. This delta's subject is **a principle whose grounding
is the principal's own words, made a binding requirement on every guard the
same day**, so the reviewable question is not only whether each paragraph is
well written but whether a *principle* was written or a *quotation with
commentary*; whether the fourth requirement is a requirement in the sense the
first three are (checkable per guard, at a defined moment, with a defined
answer slot) or a stance; and whether the two surfaces agree on what the
declaration is. In scope: whether "declares" names a **surface** — the
registry entry, the `why` field, a new field, a doc line — or leaves the
declaration homeless (`GUARDS.md` § *A rule with no home is not a rule*, on
the same page); whether the two limits are enough to stop the requirement
being read as licence to weaken guards, or too much to let it find anything;
whether the *precondition* paragraph binds (does anything say what a session
does when detection is unproven — stop, declare, or proceed?); whether the
four case-homes named actually carry the case (open `SECRETS.md`,
`DATA-PROTECTION.md`, `ACCESS.md` and say whether each states its case in a
form §10 can point at, or whether §10 now states it for them); whether §10's
relationship to §1 and §5 is placement or overlap; and whether a verbatim
principal quotation in a **public** method doc is the right grounding
mechanism for a principle every adopter inherits.

**Non-goals, and neither fences the risk:** the principal's thinking and his
three rulings are not under review — only their rendering into doctrine; the
board-section renumbering the same commit performed is not under review; the
re-ranking pass over the guard board (`300/020`) is funded, not done, and is
not under review; and no finding is decided by the reviewer (rule 3), so the
findings go to the principal's next ruling round.

## The four lenses

1. **Approach & assumptions.** Name the load-bearing assumptions yourself
   first. Then: the section quotes the principal at length *because the cases
   are the principle*. Is that a grounding choice the method already sanctions
   (compare how §1–§9 ground themselves — precedent lines, dated cases,
   pointers), or a new form, and what does it cost an adopter who is not the
   principal (the quote speaks in *we* about one estate's network)? Consider
   whether the fourth requirement is at the right altitude — a requirement on
   *every guard*, or a property of the registry, or a review lens — and
   whether "declared" without a slot is the shape `GUARDS.md` itself calls a
   rule with no home. Consider whether *Prevention, or cheap failure?* is a
   situation test (a decision procedure applied at a moment) or a restatement
   of §10 in the trade-offs voice.
2. **Correctness & quality.** Read the diff of both paths, then the whole of
   `PRINCIPLES.md` §1, §5, §9, §10 and *Trade-offs*, and the whole of
   `GUARDS.md` §§ *The intent this serves* through *The fourth requirement*
   and § *A rule with no home is not a rule* — a diff hides what it did not
   touch. Check every cross-reference resolves to a section that says what the
   reference claims (`SECRETS.md` on rotation; `DATA-PROTECTION.md` on
   restore *and the exercise that proves it*; `ACCESS.md` on what a device
   joining means; §1's *build the way back* and *design the way out* by their
   actual wording). Check the two surfaces agree on the requirement's wording
   and on the two limits. Check the `GUARDS.md` table renders (a table row is
   wrapscan-exempt — is anything else in the section over width?). Check the
   *Related* line points at a heading that exists.
3. **Completeness / harvest.** Every surface that states the guard
   requirements or lists the principles: `GUARDS.md` (the three-requirement
   sentence at the top — does it now say four?), the registry schema and
   `tools/floor.py`'s `why` handling (is there a field to declare into?),
   `docs/build/REPO-STANDARD.md`, `docs/build/templates/`, `PROPAGATION.md`,
   `00-APEX.md` (any principle list), `README.md`'s layer description,
   `CHANGELOG.md` (the brief-writer saw no CHANGELOG hunk in the landing
   commit — is there an entry, and if not is that a gap by the repo's own
   convention?), and the board (`300/010`–`030` — read their **titles only**
   in phase 1 to see what is funded; the bodies are the author's framing).
   Does any surface still say "three requirements"?
4. **Security & privacy** — mandatory. atelier is PUBLIC. The delta carries a
   verbatim quotation describing an estate's network posture — home wifi,
   mobile hotspots, BYOD, DR — in the first person. Check it against
   `PROPAGATION.md`'s no-personal-data boundary and this repo's own
   `CLAUDE.md` rule (no personal-estate context): is a *policy stance* about
   an estate personal-estate context, and does the quotation name anything
   (a network, a device class, a specific plan) that a reader could act on?
   The section is itself a security-posture rule; test whether *engineer for
   the failure being cheap* could be read as licence to skip a prevention
   control that the floor currently enforces, and whether the two limits in
   `GUARDS.md` reach `PRINCIPLES.md` too (a reader of §10 alone does not see
   them). The house security scanner reads the session's pending diff
   whatever path it is aimed at; this is a landed-delta review, so state the
   reach case that applied rather than assuming one. If the lens has no
   surface beyond these, discharge it in one explicit line with grounds.

## Re-run obligation

Re-run, do not read:

- The floor on **both** planes at HEAD, and the full Python and node suites.
  Lift the invocations from [`.githooks/pre-commit`](../../.githooks/pre-commit)
  and `.github/workflows/ci.yml` rather than guessing them. The commit claims
  *1,344 tests, OK, exit code read directly* — reproduce the count and read
  the exit code yourself.
- `linkscan` and `wrapscan` over the two paths (the floor runs them; say what
  they report for these files specifically).
- `plainscan` over `PRINCIPLES.md` and `GUARDS.md` (advisory): report the
  count the delta adds, if any, as a measurement rather than a finding — the
  section is long prose with a block quote.
- The grounding claims inside the doctrine text ("Mike's ruling, 2026-08-17";
  "the finding filed against this estate's own guard architecture"; "the pass
  over the open board is queued") point into barred material or the board; in
  phase 1 note them as claims and verify them at reconcile.

## Deferred reading — do not open before your findings are durably written
<!-- reviewscan:allow:deferral: this section BARS reading and carries no deferred content — the deferred material lives in the sibling .deferred.md under the rule-1 split, opened only after the reviewer's findings are durably written -->

Rule 2 bars: `docs/ROADMAP-DONE.md`, `docs/SESSIONS.md`, `docs/sessions/`
(the intent record included), every prior verdict in `docs/reviews/` (the
GUARDS.md build pass and the earlier principles passes especially), and the
board section `docs/roadmap/300-…/` beyond the pointer and the item titles.
Sweep the tree with `python3 tools/coldsweep.py`, whose default exclusion is
the records set, adding the board paths above with `--also-exclude`; if you
use `--include-barred`, disclose it. The sibling `.deferred.md` holds those
references and the brief-writer's seeded questions; open it after your
findings are committed. Reconcile after, never anchor before. A taker whose
own onramp has already read the `SESSIONS.md` tail discloses that in the
verdict, as this brief-writer has.

Reading the *doctrine* is not barred — both paths at HEAD and every method doc
they cross-reference are the delta and its context. What is barred is the
author's narrative of why.

## Process

Append your verdict below a `---` divider in this file: provenance repeated,
per-lens answers, findings with stable IDs (prefix `PT`) and severities
(MAJOR / MODERATE / minor / note), an overall PASS / PASS-WITH-FINDINGS / FAIL
line with counts, and a follow-up checklist. Then open the sibling; append a
reconcile section; fold the sibling in below it and delete the sibling;
finalise. Update the queue pointer
(`docs/roadmap/300-posture-recover-cheaply-mike-commissioned/040-rule-4-cold-pass-queued-the-posture.md`)
and rebuild the index in the same commit. Findings on this delta are the
principal's to decide (rule 3): record them, apply nothing.

---

# Verdict — cold pass run 2026-08-18 (Fable)

## Provenance, repeated

- **Taker:** a session Mike opened 2026-08-18 on the **Fable** tier
  (`claude-fable-5`, checked at claim — on-tier) under his standing
  instruction (*do any reviews and Fable-dependent work*). It authored no
  part of the delta, was neither started nor instructed by the authoring
  session (wt: `posture-0817`, Opus 5 1M per the landing commit's trailer —
  confirmed) or the brief-writing session (1321), and had edited no file
  this delta touches.
- **Shape:** run by hand — reviewer only, no orchestrator. The deferred
  sibling stayed unopened until the findings below were durably committed.
- ⚠️ **Taker disclosures.** (1) Onramp ran `tail -80 docs/SESSIONS.md`; the
  harness displayed only a ~2KB preview showing a 2026-07-27 entry — the
  recent entries were never displayed, and this delta's intent record
  (`sessions/2026-08-17-0900-…`) was not read. (2) The landing commit's
  message was read in full; the diff was read and the surfaces measured
  before weighing it. (3) **This taker ran the BW cold pass immediately
  before this one**, in the same session, and at BW's *reconcile* read
  excerpts of two prior verdicts (the board-store and board-generator
  passes), board items `290/050` and `010/020`, and the 0955 session
  record's second-sitting section — all after BW's findings were committed
  and none touching this delta's surfaces, its authoring session (0900), or
  its narrative; disclosed because rule 2's bar on prior verdicts is
  general. (4) The completeness sweep used `coldsweep.py` defaults plus
  `--also-exclude` on `docs/roadmap/300-…`; `--include-barred` not used;
  board section 300 read at item-title level only.

## Lens 1 — approach & assumptions

Load-bearing assumptions, named first: (i) *the verbatim quotation is a
sanctioned grounding form* — partially: `GUARDS.md` § *The intent this
serves* already grounds three requirements in a verbatim Mike quote, so the
form has same-family precedent; a three-paragraph first-person quote as the
core of a *principle* is a first for `PRINCIPLES.md` (§1–§9 ground in dated
precedent lines and attributed rulings), and its cost to an adopter is the
"we" of one estate — mitigated because the distillation (the one-liner, four
bullets, precondition) is separately usable without the quote (PT9). A
principle was written, not a quotation with commentary: the distillation
does independent work, and the diagnostic half (reluctance as a live
failure) is genuinely §10's own contribution. (ii) *The fourth requirement
is a requirement in the sense the first three are* — **no**: narrow, noisy,
reasoned are checkable per guard against surfaces that exist (an allow
marker's scope, its noise, its recorded reason); "declared" names no surface
to declare into, which is PT1 and the delta's central defect. (iii) *The
situation test is a decision procedure, not a restatement* — mostly: it has
a moment (before gate sizing), a question, a preference rule and a
say-which-you-chose duty, so it earns its slot; but its duty inherits PT1's
homelessness for non-guard designs (where does a design "say" it? — the
`review:` line convention is the obvious home and is not named), and it
carries neither of the two limits (PT5). Altitude: a requirement on every
guard is the right level *given* the ruling made it one; what is missing is
the registry-property half that would let the requirement be administered.

## Lens 2 — correctness & quality

Both diffs read, then the whole of `PRINCIPLES.md` §1, §5, §9, §10 and
*Trade-offs*, and `GUARDS.md` from § *The intent this serves* through the
fourth requirement and § *A rule with no home is not a rule*. Cross-references:
§1's *build the way back before the way forward* and *design the way out
before the way in* exist by that wording ✓; §5 is "Security, privacy, cost —
by design" and carries control-to-threat sizing ✓; `SECRETS.md` states the
rotation case in §10's sense — including the reluctance diagnostic ("the
moment someone hesitates to rotate…") ✓; `DATA-PROTECTION.md` states restore
plus the verified-restore discipline ✓; **`ACCESS.md` does not carry the
case §10 assigns it** — it is an agent-onboarding runbook for new domains
(its one "network" mention is a domain-type list; no BYOD, no
device-joining meaning) (PT4). The two surfaces agree on the requirement's
core (declare which; not declaring is the defect); the two limits appear
**only** in `GUARDS.md` (PT5). The `GUARDS.md` table renders and the whole
section passes `wrapscan` (run over both paths: clean, exit 0); `linkscan`
over both paths: clean, exit 0. The *Related* line points at a §10 heading
and a situation test that exist ✓. The headline sentence directly above the
new section still says **"Three requirements … The rest of this doc is the
model that makes them enforceable"** — contradicted by the section beneath
it, and the model does not in fact reach the fourth (PT2).

**Re-runs, exit codes read directly:** Python suite at HEAD `Ran 1344
tests … OK` (exit 0) — reproduces the landing commit's *1344 tests, OK*
exactly; node suite 235 pass / 0 fail (exit 0); floor green on both planes
at HEAD (hook and CI invocations lifted from `.githooks/pre-commit:82` and
`ci.yml:173`, exit 0 each; the hook floor also ran green on this session's
own commits). `plainscan` over both paths: 75 findings at HEAD vs 75 at
`16bf719^` (control verified — the pre-revision lacks §10), so the delta
adds **zero** plainscan findings despite the block quote.

## Lens 3 — completeness / harvest

Surfaces stating the guard requirements or counting the principles, swept
(`coldsweep.py`, disclosure above): `GUARDS.md`'s own headline still says
three (PT2); `tools/secretscan.py` and `tools/leakscan.py` carry "GOVERNED
BY method/GUARDS.md — narrow, noisy, reasoned" banners and five test files
carry the same three-word docstring — code surfaces that now name a
superseded requirement set (PT3). The registry has no declaration slot: 19
`why=` strings in `floor.py`, none answering cheap-failure-or-forbid, and
`floorfleet` reads `why` as free text (PT1's evidence). No surface
enumerates "nine principles" as a total, so no count went stale ✓.
`REPO-STANDARD.md`, templates and `PROPAGATION.md` restate neither the
requirement set nor the posture ✓. **No CHANGELOG entry exists for this
delta** — against the repo's own convention: §9's addition and its ruling,
and the BS1 wording change the same day, all carry entries, and a new
principle is the largest doctrine unit the file records (PT6). Board
section `300`, titles only: `010` the posture section and the guard test ·
`020` re-rank the guard board against the test · `030` what would this
estate actually notice — `030`'s title appears to fund the detection
precondition's follow-up; whether any body homes the declaration slot is
title-invisible in phase 1 (checked at reconcile).

## Lens 4 — security & privacy (mandatory)

The quotation names no network, device, plan, or system — it is a policy
stance in universal categories (home wifi, hotspots, BYOD, DR), and the
mechanical boundary passed it (floor green at landing). Two judgement-level
residues, both the principal's to weigh: the pairing of a first-person
posture-confidence quote with the flag that its detection leg has "the
least mechanism behind it" publicly annotates an attributable estate's
least-evidenced leg (PT8, low actionability); and §10 read alone — the
surface children read most — states *engineer for the failure being cheap*
without the two limits that stop it being read as licence to weaken
prevention controls the floor currently enforces (PT5; the limits reach
`GUARDS.md` readers only). The delta is itself a security-posture rule;
its informed-principal trail is visible in the text ("with the consequence
stated to him first") ✓. House scanner reach, stated: landed-delta review —
`/security-review` reads the session's pending diff, which held only this
verdict draft (the SL2 class), so it cannot be aimed at the work and was
not run; the lens discharges on those grounds beyond the findings above.

## Findings

- **PT1 (MAJOR)** — **the declaration has no home, by the same page's own
  test.** "Every guard declares which of two things it does" and "not
  declaring is the defect" — but no surface is named to declare into: the
  section implies the `why` field ("the standard the `why` field was
  missing") without saying so, names no format, and wires no check; none of
  the 19 registry `why` strings answers the question today. `GUARDS.md`
  § *A rule with no home is not a rule* — the principal's ruling of the
  **same day**, on the same page — says *name the surface a future session
  reads it on; queue the homing or drop the claim*. A session building a
  guard today cannot comply, and every landed guard is in permanent defect
  with no compliant path. The situation test's "say which you chose"
  inherits the same gap for non-guard designs (the `review:`-line
  convention is the obvious home and is not named). Whether a board item
  funds the homing is title-invisible in phase 1 (300 bodies barred);
  reported at reconcile.
- **PT2 (MODERATE)** — **the sentence directly above the new section still
  says three.** "Three requirements — narrow, noisy, reasoned — and they
  are requirements on every guard in the estate … The rest of this doc is
  the model that makes them enforceable rather than aspirational" — both
  halves now false: the count, and the enforceability claim as applied to
  the fourth (no mechanism reaches it). The contradiction is between two
  adjacent blocks of the same doc.
- **PT3 (minor)** — code surfaces still name the three-requirement set:
  `secretscan.py` and `leakscan.py` "GOVERNED BY … narrow, noisy, reasoned"
  banners and five `test_*.py` docstrings. The class BG4 named one commit
  earlier: corrections that do not reach the sibling surfaces carrying the
  superseded claim.
- **PT4 (MODERATE)** — **`ACCESS.md` is assigned a case it does not
  carry.** §10's closing names it the home of "what a device joining the
  network is allowed to mean"; `ACCESS.md` is an onboarding-a-new-domain
  runbook for the agent's own credentials — no BYOD, no device-joining
  posture, one "network" mention in a domain-type list. §10 currently
  states the case for it, which is the drift-by-pointer shape the closing
  paragraph exists to prevent.
- **PT5 (minor)** — **the two limits reach only `GUARDS.md` readers.** §10
  and the situation test both carry the licence-shaped half ("engineer for
  the failure being cheap"; "an undeclared choice is the defect") without
  the limits that domesticate it (declare-don't-unwire; forbids-is-not-a-
  failure-grade). Children read `PRINCIPLES.md` far more than `GUARDS.md`.
- **PT6 (minor)** — **no CHANGELOG entry**, against the repo's own
  convention for doctrine changes (§9's addition and ruling, and the BS1
  wording delta of the same day, all have entries); a new principle plus a
  new estate-wide requirement is the largest unit the file records.
- **PT7 (note)** — the precondition paragraph binds the *claim* ("claim
  the freedom that exercised detection and recovery will support, and no
  more") but names no move for a session that finds detection unproven —
  stop, declare, or proceed-with-narrowed-claims. `300/030`'s title
  suggests the follow-up is funded; bodies unread in phase 1.
- **PT8 (note, lens 4)** — the public pairing of the first-person
  confidence quote with "the leg with the least mechanism behind it"
  annotates an attributable estate's least-evidenced posture leg. Low
  actionability (nothing named); the boundary judgement (policy stance vs
  personal-estate context) was made silently and is the principal's to
  make visibly.
- **PT9 (note)** — the three-paragraph verbatim quotation as a principle's
  core is a first for `PRINCIPLES.md` (`GUARDS.md`'s intent quote is the
  family precedent); the cost is one estate's "we" in every adopter's
  inheritance, mitigated by the separately-usable distillation. Recorded as
  a form observation for the ruling round, not a defect.

## Overall

**PASS-WITH-FINDINGS — 1 MAJOR / 2 MODERATE / 3 minor / 3 note.** The
section is a real principle, honestly grounded, with the diagnostic half
and the precondition flag as its strongest and most original content; the
suites, floor, and scans reproduce the landing commit's claims exactly, and
the delta adds zero mechanical findings. What fails is administration, not
philosophy: the fourth requirement cannot currently be complied with (PT1),
the sentence above it still says three (PT2), and one of the four case-home
pointers points at a doc that does not carry the case (PT4). Per rule 3
nothing is applied; PT1–PT9 go to the principal's ruling round. The cycle
stays open on PT1.

## Follow-up checklist

- [ ] PT1–PT9 to the principal's ruling round (rule 3; cycle open on PT1).
- [ ] Whatever wording is ruled lands with its own rule-4 pointer in the
      landing commit (landing = queuing).
- [ ] If PT1 is ruled fixed-by-homing: name the slot (registry field, `why`
      grammar, or doc line), its format, and the check that reads it, and
      declare the floor's own guards in the same change.
