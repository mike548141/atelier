- [x] 🔎 **Hand-up: the run-open role check still sends the tier question back
      to the principal, which the 2026-09-16 ECONOMICS ruling says he should
      never get.** Filed from a private child 2026-09-18 via § *Pointing up*,
      by direct write into the parent's tree, with the PR opened before
      stopping. The child is not named; per § *The route* rule 2 that omission
      is deliberate. The branch name carries no repo token either (the
      `320/190` collision is still unruled, so this follows the neutral form
      the other private-child filings use).

      **The two texts, quoted from `origin/main` at `63e27b3`:**
      - `ECONOMICS.md` § *Match the model to the job* (added 2026-09-16):
        *"This is a standing default, not a question. A session picks its own
        tier … without putting it to the principal first. State which tier
        you're on; don't ask which to use."* Its parenthesis gives the reason:
        the question had reached Mike from well over a hundred sessions.
      - `CONCURRENCY.md` § *Orchestrated queue runs*, *Role check at open*:
        *"A session opened on the wrong tier for its role … stops and says so
        instead of proceeding; the fix (switch at the session boundary) is
        free before the work starts."* And `ECONOMICS.md` § *The
        orchestrated-run tier split* defines the capable tier as *"the most
        capable model available"*.

      **What happened (evidence, one session).** A queue run opened on the
      workhorse tier. The role check fired and the session stopped. The
      principal's own run brief also said *"if you're not [on the capable
      tier], stop and say so"*. The session put the choice to him as a
      structured question: restart on the capable tier, per-run override, or
      stop. He chose the override, then asked mid-turn: *"What caused that
      question? Is something implying that only [the capable model] should be
      used for orchestration?"* The answer was a four-link chain (brief → role
      check → "most capable available" → the estate's own records naming
      which model is above which). The last link was an inference, and no
      single text says it.

      **Why this is a contradiction, not just friction.** Under "most capable
      available", any orchestrating session opened below the top model
      reaches the principal with the tier question at open, which is the
      exact per-session question the 2026-09-16 ruling retired. The two rules
      are each reasonable alone. ECONOMICS answers "which tier do I use for
      what?" as a standing default; the role check answers "am I the right
      seat for this role?" by stopping. When a principal deliberately opens a
      run on a cheaper orchestrator, only a per-run override silences it,
      and that is still a per-session answer.

      **Options (the house's to weigh; the child does not pick):**
      - **(a)** Let the role check honour a *standing* principal override, a
        person-local line such as "workhorse may orchestrate, the capable tier
        reviews", so it fires only where no such standing answer exists.
        Keeps the check; retires the repeat question.
      - **(b)** Downgrade the role check from *stop* to *state and proceed*
        when the orchestrator sends every review and structural call to a
        capable-tier sub-agent. The capability that matters most (the
        verdicts) is still paid for.
      - **(c)** Keep it as is, and say in ECONOMICS that the run-open role
        check is the deliberate exception to "not a question". Honest, but it
        leaves the repeat question in place.

      *Unevidenced beyond one session:* whether other runs hit this. The
      child has one live instance and the principal's own surprised question
      as evidence.
      *review: owed on whatever doctrine text the ruling produces (rule 4:
      a session that neither authored nor was instructed by the author).*
      🎯→✅ **RULED 2026-09-19 by Mike, mid-run, after a `ros` session put the
      contradiction to him in these terms:** *"the cheapest model that can do
      a good job is the one that should be used for every job including
      orchestration. Fix everything that is causing this mess"*, and, asked
      whether that reopened the named review tier: *"fable reviews remain
      fable reviews"*. So neither option (a), (b) nor (c) as offered — the
      ruling goes under them: the seat is never reserved for the top model by
      rank, and the run-open check **states the tier and proceeds**.
      ✅ **Applied 2026-09-20, same sitting:** `ECONOMICS.md` (the ruling
      stated at § *Match the model to the job*; the capable seat redefined in
      § *The orchestrated-run tier split*; the role-check sentence) ·
      `CONCURRENCY.md` § *Orchestrated queue runs* (the seat sentence and
      *Role check at open* → *Tier at open — state it, don't ask*) ·
      `skills/queue-run/SKILL.md` step 1 · `docs/method/session-open/
      session-open-prompt.md` (the prompt Mike pastes, which carried the stop
      clause itself) · `AUTONOMY.md` (first-of-kind wording) · `REVIEW.md`
      and `docs/method/README.md` (review reads as the *named* tier, not
      "most capable available"). Two stops survive and neither is about rank:
      work that outruns the model doing it, and a `⏳` pass whose named tier
      the session cannot honour.
      *review: queued as a rule-4 cold pass, `160/330`.*
