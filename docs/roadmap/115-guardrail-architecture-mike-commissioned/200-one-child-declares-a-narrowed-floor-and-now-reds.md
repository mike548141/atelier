- [x] **One child declares a narrowed floor and reds from now on** — the
      consequence of `115/030` landing (2026-09-20, Mike's *"Floor copy
      verbatim"*). Measured read-only across the sibling repos as the change
      landed: **5 carry a floor stamp; 4 are byte-equal already; 1 declares
      `narrow=` on it** and will go red at its next `stampscan` run. The repo
      is not named here — atelier is public, and a private child's name is
      not coupled to its posture (`RECORD.md`, the name↔posture split); the
      measurement sits in this run's session record the same way.
      **What is owed:** the child restores the canonical floor text word for
      word (its narrowing was a *visibility* statement it can make outside
      the stamped region), at its next pin bump or sooner. **Who does it:**
      the child's own session, in its own repo — work is delivered where it
      lives (`PROPAGATION.md` § *Pointing up*, the queue-never-deliver rule),
      so this item closes when that repo reports it done, not when atelier
      decides it is.
      **Why it is not just "let it red":** a red a child cannot clear is how
      a floor stops being read at all — the alarm-fatigue class `320/010`
      documents. One red, one fix, then quiet.
      ---
      ✅ **CLOSED 2026-09-20 on the child's own report**, which is the closure
      condition this item named — *"when that repo reports it done, not when
      atelier decides it is."* The report is `320/360`, filed via § *Pointing
      up* and merged as PR #83, so it is a pushed artefact rather than a
      message: the canonical `floor` region restored **word for word**,
      `narrow=` dropped from the begin marker, the visibility statement kept
      as its own bullet below the stamp. Measured both sides — **before:** one
      bullet omitted, every other line byte-equal; **after:** the four
      declared placeholders only. The narrowing was honest and narrow; the
      child complied rather than worked around the rule.
      🚩 **The reason the narrowing existed is NOT closed by this.** This item
      read the narrowing as *"a visibility statement it can make outside the
      stamped region"*, which is true of the **fact** and not of the
      **verification**. That residue is atelier's and is open at `320/370`:
      `<owner/repo>` is a declared placeholder with no admissible fill for a
      child with no remote. Filed separately on purpose, so closing this
      cannot close that alongside.
